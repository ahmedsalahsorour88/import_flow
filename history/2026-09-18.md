# 2026-09-18 — Daily Development History

---

## 📝 [2026-09-18 03:40] - Completed Task: DC-01 & DC-02 — ACID Registration, Verification & Expiry Radar Tracking

### 📌 Overview

- **Task Codes:** `DC-01` & `DC-02`
- **Stage:** المرحلة 3: التوثيق ونافذة و ACID (Stage 3: Documentation, Nafeza & ACID Operations)
- **Features Covered:**
  1. **DC-01:** `ACID Registration & Verification` — طلب وقيد الرقم التعريفي المبدئي للشحنة (ACID)
  2. **DC-02:** `ACID Expiry Radar Tracking` — رادار تتبع صلاحية ACID وتنبيه الـ 14 يوماً وحظر الإفراج الجمركي النهائي
- **Description:** 
  - Complete backend lifecycle orchestration and two-way data synchronization between Nafeza ACID Registration Sessions (`AcidRegistrationSession`) and active Import Files (`ImportFile`).
  - Automatic stage advancement from `STEP_05_ADVANCE_PAYMENT` to `STEP_06_BOOKING` upon issuance/verification of valid 19-digit ACID numbers.
  - Automatic completion of pending ACID smart tasks and dispatching downstream smart tasks for Logistics Officers (`BK-01 Freight Booking`).
  - Proactive notification triggers (14-day warning, 3-day critical) with automated alert suppression for already released shipments (`is_customs_released == True`).
  - Mandatory legal customs release hard-block in `complete_customs_release_service` preventing final release permits if the ACID number is expired.
  - Reactive Flutter frontend state management invalidation (`importFilesProvider`, `acidTrackerProvider`) across all CRUD mutations and direct deep navigation from Import File details to `NafezaAcidScreen`.

### 📁 Files Changed

- `modules/import_documentation/service.py` — Modified:
  - Implemented `_handle_acid_verification_workflow`:
    - Two-way synchronizes issued 19-digit `acid_number`, `acid_issue_date`, and `acid_expiry_date` into the linked `ImportFile`.
    - Auto-advances lifecycle stage from `STEP_05` to `STEP_06` with minimum progress percent set to 40.0%.
    - Resolves pending ACID smart tasks (`task_type="ACID_REGISTRATION"`) to `Completed`.
    - Dispatches new smart task for `Logistics Officer` to initiate carrier space booking (`BK-01 Freight Booking`).
    - Dispatches high-priority `SystemNotification` alerting the operations team that the ACID has been issued and shipping booking can proceed.
  - Integrated this workflow into both the creation/upsert branches (`create_acid_session_service`) and modification branches (`update_acid_session_service`).
- `modules/customs_clearance/service.py` — Modified:
  - Enforced ACID validity legal hard-block at the entry point of `complete_customs_release_service`: raises `HTTPException(400)` with explanatory Arabic reason if `imp_file.acid_expiry_date < date.today()` and `is_customs_released == False`.
- `modules/notifications/expiry_checker.py` — Modified:
  - Fixed property access bug (`acid.acid_id`).
  - Broadened `_check_acid_expiries` to query active `ImportFile` records approaching expiry (<= 14 days warning, <= 3 days / expired critical) and auto-suppress alerts when `is_customs_released == True`.
- `frontend/lib/features/import_documentation/providers/import_documentation_provider.dart` — Modified:
  - Added `Ref? _ref` parameter to `AcidSessionsNotifier` and wired automatic cache invalidation (`_ref?.invalidate(importFilesProvider)` and `_ref?.invalidate(acidTrackerProvider)`) across create, update, softDelete, and restore operations.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart` — Modified:
  - Added header action button `نافذة و ACID (DC-01)` navigating directly to `NafezaAcidScreen`.
- `tests/unit/test_dc01_acid_registration_verification.py` — NEW:
  - Comprehensive unit test suite (5 test cases) validating 19-digit ACID format validation, two-way ImportFile synchronization, automatic lifecycle progression, smart task auto-resolution & logistics officer dispatch, and Nafeza discrepancy engine.
- `tests/unit/test_dc02_acid_expiry_radar_tracking.py` — NEW:
  - Comprehensive unit test suite (5 test cases) validating ACID Radar Tracker calculation, customs release alert auto-suppression, strict customs release hard-block on expired ACID, customs release clearance on valid ACID, and proactive notification generation.
- `TASKS.md` & `checklist_tasks_dump.json` — Modified:
  - Marked `DC-01` and `DC-02` as `[x] done` (`مكتملة ومختبرة بنسبة 100%`).

### 🧪 Validation / Testing

- **Backend DC-01 & DC-02 Suites:**
  - `python -m pytest tests/unit/test_dc01_acid_registration_verification.py tests/unit/test_dc02_acid_expiry_radar_tracking.py -v` -> **10 passed**, 0 failed (100% green).
- **Backend Full Regression Suite (ACID & Customs & Notifications):**
  - `python -m pytest tests/unit/test_dc01_acid_registration_verification.py tests/unit/test_dc02_acid_expiry_radar_tracking.py tests/unit/test_acid_tracker.py tests/unit/test_acid_import_file_linkage.py tests/unit/test_acid_discrepancy_service.py tests/unit/test_customs_clearance.py tests/unit/test_notifications.py -v` -> **22 passed**, 0 failed (100% green).
- **Frontend Model & Tracker Tests:**
  - `flutter test test/acid_tracker_test.dart` -> **3 passed**, 0 failed (100% green).
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/import_documentation lib/features/import_files` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps

- Proceed to **`DC-03:` `Bank Form 4 / LC Link`** — ربط نموذج 4 البنكي أو الاعتماد المستندي بملف الشحنة (Stage 3 Checklist item #22).

---

## 📝 [2026-09-18 03:46] - Completed Task: DC-03 — Bank Form 4 / LC Link

### 📌 Overview

- **Task Code:** `DC-03`
- **Stage:** المرحلة 3: التوثيق ونافذة و ACID (Stage 3: Documentation, Nafeza & ACID Operations)
- **Features Covered:**
  - `DC-03:` `Bank Form 4 / LC Link` — ربط نموذج 4 البنكي أو الاعتماد المستندي بملف الشحنة
- **Description:**
  - Central backend business engine workflow `_handle_banking_document_verification_workflow` triggered whenever a Banking Document (`Form 4`, `Letter of Credit (L/C)`, or `Form 9`) is created, updated, or received.
  - Automatically synchronizes banking document reference number (`doc_reference_number`), request date, received date, and turnaround execution days into `ImportFile` (`form4_no`, `form4_request_date`, `form4_received_date`, `form4_execution_days`).
  - Advances central lifecycle step from `STEP_12_BANK_DOCUMENTS` to `STEP_13_CUSTOMS_DECLARATION` with minimum progress percent elevated to $\ge 65\%$.
  - Automatically completes prior pending banking SmartTasks on the file.
  - Automatically dispatches downstream SmartTask for the `Customs Broker` / Specialist: `قيد الإقرار الجمركي ونموذج 46 ك.م (CS-03)` with High priority.
  - Emits high-priority `SystemNotification` alerting customs brokers and operations that the bank document is verified and Declaration 46 can begin.
  - Frontend reactive state update: wired `_ref?.invalidate(importFilesProvider)` to all `BankingDocumentsNotifier` mutations (`create`, `receive`, `update`, `delete`).
  - Frontend UI enhancement in `BankForm4Screen`:
    - Added `_bankDocType` dropdown selection (`Form 4`, `Letter of Credit (L/C)`, `Form 9`).
    - Added text input for `_docReferenceNumberCtrl` (رقم المرجع البنكي / رقم الاعتماد).
    - Added table columns for `نوع المعاملة` and `رقم المرجع / الاعتماد`.
    - Added interactive row action button and modal dialog for `توثيق استلام نموذج 4 / الاعتماد (DC-03)` with reactive submission.
  - Added direct header action button in `ImportFileDetailsDialog`: `المستندات البنكية (DC-03)` navigating directly to `BankForm4Screen`.

### 📁 Files Changed

- `modules/import_documentation/service.py` — Modified:
  - Added `_handle_banking_document_verification_workflow`.
  - Integrated into `create_banking_document_service`, `update_banking_document_service`, and `receive_banking_document_service`.
- `frontend/lib/features/import_documentation/providers/import_documentation_provider.dart` — Modified:
  - Added `Ref? _ref` to `BankingDocumentsNotifier` and wired `_ref?.invalidate(importFilesProvider)` across create, receive, update, and delete.
- `frontend/lib/features/import_documentation/screens/bank_form4_screen.dart` — Modified:
  - Added `_docReferenceNumberCtrl` and input controls for document type and reference number.
  - Added table columns and interactive `_openReceiveBankDocDialog` modal dialog.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart` — Modified:
  - Imported `BankForm4Screen` and added header action button `المستندات البنكية (DC-03)`.
- `tests/unit/test_dc03_bank_form4_lc_link.py` — NEW:
  - 5 comprehensive test cases validating Form 4 creation, L/C creation, Form 4 receipt & execution days calculation, L/C receipt & smart task auto-resolution + Declaration 46 dispatch, and API endpoints.
- `frontend/test/dc03_bank_form4_lc_link_test.dart` — NEW:
  - 3 test cases validating model parsing and serialization for Form 4, L/C, and ImportFile linkage.
- `TASKS.md` & `checklist_tasks_dump.json` — Modified:
  - Marked `DC-03` as `[x] done` (`مكتملة ومختبرة بنسبة 100%`).

### 🧪 Validation / Testing

- **Backend DC-03 Suite:**
  - `python -m pytest tests/unit/test_dc03_bank_form4_lc_link.py -v` -> **5 passed**, 0 failed (100% green).
- **Backend Combined Regression:**
  - `python -m pytest tests/unit/test_dc01_acid_registration_verification.py tests/unit/test_dc02_acid_expiry_radar_tracking.py tests/unit/test_dc03_bank_form4_lc_link.py tests/unit/test_banking_form4_workflow.py -v` -> **17 passed**, 0 failed (100% green).
- **Frontend DC-03 Suite:**
  - `flutter test test/dc03_bank_form4_lc_link_test.dart test/acid_tracker_test.dart` -> **6 passed**, 0 failed (100% green).
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/import_documentation lib/features/import_files` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps

- Proceed to **`DC-04:` `Shipping Docs Matrix Verification`** — [COMPLETED]
- Next priority task: **`BK-01:` `Freight Booking Confirmation`** — تأكيد حجز الشحن وتسجيل Booking No ومواعيد الإبحار والوصول (Stage 4 Checklist item #24).

---

## 📝 [2026-09-18 03:55] - Completed Task: DC-04 — Shipping Docs Matrix Verification

### 📌 Overview

- **Task Code:** `DC-04`
- **Stage:** المرحلة 3: التوثيق ونافذة و ACID (Stage 3: Documentation, Nafeza & ACID Operations)
- **Features Covered:**
  - `DC-04:` `Shipping Docs Matrix Verification` — مصفوفة فحص واستكمال مستندات الشحن ورصد النواقص وتتبع شهادة التبخير ونسبة الاستكمال
- **Description:**
  - Full backend enhancement of the Cross-Document Matrix Engine (`run_cross_document_matrix_check_service`) evaluating all 7 mandatory Egyptian shipping documents: Commercial Invoice, Packing List, Bill of Lading, Certificate of Origin / EUR.1, Inspection Certificate, Fumigation Certificate (ISPM 15), and Bank Form 4 / LC.
  - Added official support for `Fumigation Certificate` in `VALID_DOCUMENT_TYPES` and `auto_generate_approvals_for_import_file_service`.
  - Added deficiency radar tracking detecting missing documents, generating explicit Arabic deficiency recommendations (`تنبيه استكمال مستندي (DC-04)...`), and calculating `completeness_percent` (0.0% to 100.0%).
  - Evaluates overall compliance: `Fully Compliant` when 100% complete with no mismatches, or `Discrepancies Found` / `Critical Blocker` when deficient.
  - Frontend integration: updated `CrossDocumentMatrixResultModel` with `missingDocuments` and `completenessPercent`.
  - Enhanced `CustomsDocumentApprovalTab` live matrix banner with interactive completeness badge and deficiency radar alert chip.
  - Enhanced `_getLocalizedDocType` with localized Fumigation Certificate label.
  - Added direct header action button in `ImportFileDetailsDialog`: `مصفوفة المستندات (DC-04)` navigating directly to `ShipmentDraftDocsScreen` with pre-selected import file.

### 📁 Files Changed

- `modules/docs_customs_approval/validators.py` — Modified:
  - Added `"Fumigation Certificate"` to `VALID_DOCUMENT_TYPES`.
- `modules/docs_customs_approval/schemas.py` — Modified:
  - Added `missing_documents: List[str]` and `completeness_percent: float` to `CrossDocumentMatrixCheckResponse`.
- `modules/docs_customs_approval/service.py` — Modified:
  - Added `"Fumigation Certificate"` to `standard_docs` in `auto_generate_approvals_for_import_file_service`.
  - Enhanced `run_cross_document_matrix_check_service` with 7 shipping documents checks, deficiency radar, and completeness calculation.
- `frontend/lib/features/import_documentation/models/docs_customs_approval_model.dart` — Modified:
  - Added `missingDocuments` and `completenessPercent` to `CrossDocumentMatrixResultModel`.
- `frontend/lib/features/import_documentation/widgets/customs_document_approval_tab.dart` — Modified:
  - Added `'Fumigation Certificate'` translation in `_getLocalizedDocType`.
  - Added completeness percent chip and deficiency radar display in live matrix banner.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart` — Modified:
  - Imported `ShipmentDraftDocsScreen` and added header action button `مصفوفة المستندات (DC-04)`.
- `tests/unit/test_dc04_shipping_docs_matrix_verification.py` — NEW:
  - Comprehensive unit test suite (5 test cases) validating Fumigation Certificate creation, auto-generation, deficiency radar, 100% completeness, and REST API endpoint.
- `frontend/test/dc04_shipping_docs_matrix_test.dart` — NEW:
  - 3 test cases validating Flutter model parsing, missing documents, and 100% completeness payload.
- `TASKS.md` & `checklist_tasks_dump.json` — Modified:
  - Marked `DC-04` as `[x] done` (`مكتملة ومختبرة بنسبة 100%`).

### 🧪 Validation / Testing

- **Backend DC-04 Suite:**
  - `python -m pytest tests/unit/test_dc04_shipping_docs_matrix_verification.py -v` -> **5 passed**, 0 failed (100% green).
- **Backend Combined Regression:**
  - `python -m pytest tests/unit/test_dc01_acid_registration_verification.py tests/unit/test_dc02_acid_expiry_radar_tracking.py tests/unit/test_dc03_bank_form4_lc_link.py tests/unit/test_dc04_shipping_docs_matrix_verification.py tests/unit/test_docs_customs_approval.py -v` -> **24 passed**, 0 failed (100% green).
- **Frontend DC-04 Suite:**
  - `flutter test test/dc04_shipping_docs_matrix_test.dart test/dc03_bank_form4_lc_link_test.dart test/acid_tracker_test.dart` -> **9 passed**, 0 failed (100% green).
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/import_documentation lib/features/import_files` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps

- Proceed to **`BK-01:` `Freight Booking Confirmation`** — تأكيد حجز الشحن وتسجيل Booking No ومواعيد الإبحار والوصول (Stage 4 Checklist item #24).

---

## 📝 [2026-09-18 04:05] - Completed Task: BK-01 — Freight Booking Confirmation

### 📌 Overview

- **Task Code:** `BK-01`
- **Stage:** المرحلة 4: حجز الشحن (Stage 4: Freight Booking)
- **Features Covered:**
  - `BK-01:` `Freight Booking Confirmation` — تأكيد حجز الشحن وتسجيل Booking No ومواعيد الإبحار والوصول
- **Description:**
  - Implemented booking confirmation business logic in `modules/freight_booking/service.py`:
    - `confirm_booking_service`: validates confirmation data (`booking_confirmation_no`, `vessel_name`, `voyage_number`, `etd`, `eta`, `free_demurrage_days`).
    - Two-way synchronizes confirmed parameters to linked `ImportFile` (`required_eta`, `target_free_days`, `selected_scenario`).
    - Advances lifecycle stage to `STEP_07_BOOKING_PLACED` with progress percent elevated to $\ge 50\%$.
    - Auto-completes prior pending `BK-01` SmartTasks.
    - Dispatches downstream `BK-02` SmartTask for `Logistics Officer`: "تسجيل فترات السماح المجانية للحاويات لتغذية رادار الغرامات".
    - Dispatches `SystemNotification` alerting the team of booking confirmation.
  - Exposed `POST /api/v1/freight-booking/{booking_id}/confirm`.
  - Frontend reactive integration in `FreightBookingScreen`:
    - Added quick confirm button in table rows and confirmation dialog (`_FreightBookingConfirmDialog`).
    - Supported `initialImportFileId` filter with active dismissible filter chip.
    - Added header action button in `ImportFileDetailsDialog`: `حجز الشحن (BK-01)` navigating to `FreightBookingScreen`.

### 📁 Files Changed

- `modules/freight_booking/schemas.py` — Modified: added `ShipmentBookingConfirm`.
- `modules/freight_booking/service.py` — Modified: added `confirm_booking_service` and `_handle_booking_confirmation_workflow`.
- `modules/freight_booking/router.py` — Modified: added `POST /{booking_id}/confirm`.
- `frontend/lib/features/freight_booking/providers/freight_booking_provider.dart` — Modified: added `confirmBooking` and cache invalidation.
- `frontend/lib/features/freight_booking/screens/freight_booking_screen.dart` — Modified: added confirmation dialog, table row action, filter chip.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart` — Modified: added header action `حجز الشحن (BK-01)`.
- `tests/unit/test_bk01_freight_booking_confirmation.py` — NEW: 2 unit tests passing 100%.
- `frontend/test/bk01_freight_booking_test.dart` — NEW: 3 frontend tests passing 100%.

### 🧪 Validation / Testing

- Backend Pytest: 12 passed.
- Frontend Flutter test: 3 passed.
- Flutter analyze: No issues found!

---

## 📝 [2026-09-18 04:20] - Completed Task: BK-02 — Free Days Agreement Registration

### 📌 Overview

- **Task Code:** `BK-02`
- **Stage:** المرحلة 4: حجز الشحن (Stage 4: Freight Booking)
- **Features Covered:**
  - `BK-02:` `Free Days Agreement Registration` — تسجيل فترات السماح المجانية للحاويات لتغذية رادار الغرامات
- **Description:**
  - Comprehensive demurrage free days agreement workflow connecting negotiated booking addendums with the live demurrage & detention tracking radar engine:
    - `register_free_days_agreement_service`: registers agreed demurrage free days, detention free days, port storage free days, agreement reference, and notes.
    - Two-way sync: updates `target_free_days` on `ImportFile`, updates `free_demurrage_days` on `ShipmentBooking`, and records reference in `shipping_instructions_notes`.
    - Live radar feed: dynamically recalculates all active `DemurrageTracking` sessions linked to the file using newly agreed free days.
    - Calculates cost avoidance compared to standard 14-day tariff ($70/day/container for demurrage).
    - Auto-completes pending `BK-02` SmartTasks (`task_code="TSK-0202"` or containing `BK-02` / `سماح`).
    - Dispatches operational `SystemNotification` alerting the team of updated free days and cost savings.
    - Exposed REST endpoints `POST /api/v1/demurrage-detention/free-days-agreement` and `GET /api/v1/demurrage-detention/free-days-agreement/{import_file_id}`.
  - Frontend reactive integration:
    - Added `FreeDaysAgreementModel` to `demurrage_model.dart`.
    - Added `registerFreeDaysAgreement` and `fetchFreeDaysAgreement` in `DemurrageNotifier` with cache invalidation (`importFilesProvider`, `smartTasksProvider`, `freightBookingProvider`).
    - Created `FreeDaysAgreementDialog`: interactive dialog with demurrage/detention inputs, quick preset chips (+14, +21, +28 days), document reference, live savings card preview, and submission with loading indicator.
    - Updated `DemurrageDetentionScreen` to accept `initialImportFileId` and added top action button `توثيق فترات السماح (BK-02)`.
    - Added header action button in `ImportFileDetailsDialog`: `فترات السماح (BK-02)`.

### 📁 Files Changed

- `modules/demurrage_detention/schemas.py` — Modified: added `FreeDaysAgreementRegister` & `FreeDaysAgreementResponse`.
- `modules/demurrage_detention/service.py` — Modified: added `register_free_days_agreement_service` and `get_free_days_agreement_service`.
- `modules/demurrage_detention/router.py` — Modified: added POST & GET endpoints for free days agreement.
- `frontend/lib/features/demurrage_detention/models/demurrage_model.dart` — Modified: added `FreeDaysAgreementModel`.
- `frontend/lib/features/demurrage_detention/providers/demurrage_provider.dart` — Modified: added `registerFreeDaysAgreement`, `fetchFreeDaysAgreement`, and cache invalidation.
- `frontend/lib/features/demurrage_detention/widgets/free_days_agreement_dialog.dart` — NEW: interactive dialog for free days agreement.
- `frontend/lib/features/demurrage_detention/screens/demurrage_detention_screen.dart` — Modified: added `initialImportFileId` and AppBar action button.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart` — Modified: added header action `فترات السماح (BK-02)`.
- `tests/unit/test_bk02_free_days_agreement.py` — NEW: unit test suite for BK-02 (3 test cases).
- `frontend/test/bk02_free_days_agreement_test.dart` — NEW: unit and widget test suite for BK-02 (3 test cases).
- `TASKS.md` & `checklist_tasks_dump.json` — Modified: marked `BK-02` as done / `مكتملة ومختبرة بنسبة 100%`.

### 🧪 Validation / Testing

- **Backend Pytest Suite:**
  - `uv run pytest -o pythonpath=. tests/unit/test_bk02_free_days_agreement.py tests/unit/test_bk01_freight_booking_confirmation.py tests/unit/test_freight_booking.py tests/unit/test_demurrage_alerts.py tests/unit/test_demurrage_detention_engine.py -v` -> **31 passed**, 0 failed (100% green).
- **Frontend Test Suite:**
  - `flutter test test/bk02_free_days_agreement_test.dart test/bk01_freight_booking_test.dart` -> **6 passed**, 0 failed (100% green).
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/demurrage_detention lib/features/import_files/widgets/import_file_details_dialog.dart test/bk02_free_days_agreement_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps

- Proceed to **Stage 5: Sailing & CargoX (الإبحار و CargoX)**:
  - **`SH-01:` `Departure Confirmation & Bill of Lading`** — تأكيد الإبحار الفعلي وإصدار بوليصة الشحن B/L (Checklist item #26).

---

## 📝 [2026-09-18 04:35] - Completed Task: SH-01 — Departure Confirmation & Bill of Lading

### 📌 Overview

- **Task Code:** `SH-01`
- **Stage:** المرحلة 5: الإبحار و CargoX (Stage 5: Sailing & CargoX)
- **Features Covered:**
  - `SH-01:` `Departure Confirmation & Bill of Lading` — تأكيد الإبحار الفعلي وإصدار بوليصة الشحن B/L (Checklist Item #26).
- **Description:**
  - Complete implementation of the actual departure confirmation and Bill of Lading registration workflow, transitioning the shipment into the operational **"In-Transit / On Water"** state:
    - `confirm_departure_and_bol_service`:
      - Registers Actual Time of Departure (`ATD`), Bill of Lading Number (`bill_of_lading_no`), revised ETA, vessel name, voyage number, shipped on board date, and departure notes.
      - Updates `ShipmentBooking` status to `"Sailed"`.
      - Recalculates delay days (`departure_delay_days`) and expected warehouse arrival date with resilient timezone handling.
      - Two-way synchronizes `ImportFile`: updates `required_eta`, advances `current_module` to `"STEP_08 الإبحار وبوليصة الشحن / In-Transit On Water"`, updates `next_action`, and elevates `progress_percent >= 60.0%`.
      - Two-way synchronizes active `DemurrageTracking` sessions with the new `bill_of_lading_no`.
      - Advances `LifecycleBoard` to `STEP_08_BL` ("Draft B/L Review & Approval").
      - Auto-closes prior pending `SH-01` / departure SmartTasks.
      - Dispatches downstream `SH-02` SmartTask: "مطابقة مسودة بوليصة الشحن آلياً مع أمر الشراء والفاتورة (Draft B/L Dual Review)".
      - Dispatches operational `SystemNotification` alerting the team of vessel departure and B/L issuance.
    - Added REST endpoint `POST /api/v1/freight-booking/{booking_id}/depart`.
  - Frontend reactive desktop integration:
    - Added `billOfLadingNo` to `ShipmentBookingModel` (deserialization, serialization, and constructor).
    - Added `confirmDepartureAndBol` to `FreightBookingNotifier` with automatic invalidation of `importFilesProvider` and `smartTasksProvider`.
    - Created `DepartureConfirmationDialog`: responsive modal with inline form validation, date pickers for ATD and revised ETA, live delay calculation banner, vessel/voyage inputs, live automation details, and loading indicator.
    - Updated `FreightBookingScreen`: added table row actions for departure confirmation and B/L badge display in Confirmation No column, enhanced status pill with intuitive tooltip and tap action.
    - Updated `ImportFileDetailsDialog`: added quick action button `تأكيد الإبحار (SH-01)` in dialog header.

### 📁 Files Changed

- `modules/freight_booking/model.py` — Modified: added `bill_of_lading_no` column.
- `modules/freight_booking/schemas.py` — Modified: added `bill_of_lading_no` to base/update schemas and defined `ShipmentDepartureConfirm`.
- `modules/freight_booking/repository.py` — Modified: handled `bill_of_lading_no` in create, update, and list search.
- `modules/freight_booking/validators.py` — Modified: added timezone normalization for date comparisons.
- `modules/freight_booking/service.py` — Modified: added `confirm_departure_and_bol_service` and `_handle_departure_workflow`.
- `modules/freight_booking/router.py` — Modified: added `POST /{booking_id}/depart`.
- `frontend/lib/features/freight_booking/models/freight_booking_model.dart` — Modified: added `billOfLadingNo`.
- `frontend/lib/features/freight_booking/providers/freight_booking_provider.dart` — Modified: added `confirmDepartureAndBol`.
- `frontend/lib/features/freight_booking/widgets/departure_confirmation_dialog.dart` — NEW: interactive departure confirmation dialog.
- `frontend/lib/features/freight_booking/screens/freight_booking_screen.dart` — Modified: added departure action buttons, B/L badge, and status pill actions.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart` — Modified: added `تأكيد الإبحار (SH-01)` action button.
- `tests/unit/test_sh01_departure_confirmation.py` — NEW: comprehensive unit tests for SH-01 (service, downstream sync, and REST API).
- `frontend/test/sh01_departure_confirmation_test.dart` — NEW: unit test suite for SH-01 (model, JSON serialization, and validation).
- `TASKS.md` & `checklist_tasks_dump.json` — Modified: marked `SH-01` as done / `مكتملة ومختبرة بنسبة 100%`.

### 🧪 Validation / Testing

- **Backend Pytest Suite:**
  - `uv run pytest -o pythonpath=. tests/unit/test_sh01_departure_confirmation.py tests/unit/test_bk01_freight_booking_confirmation.py tests/unit/test_bk02_free_days_agreement.py -v` -> **7 passed**, 0 failed (100% green).
- **Frontend Test Suite:**
  - `flutter test test/sh01_departure_confirmation_test.dart test/bk01_freight_booking_test.dart test/bk02_free_days_agreement_test.dart` -> **9 passed**, 0 failed (100% green).
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/freight_booking lib/features/import_files/widgets/import_file_details_dialog.dart test/sh01_departure_confirmation_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps

- Proceed to **`SH-02:` `Draft B/L Dual Review Tool`** — أداة المراجعة المزدوجة لبوالص الشحن ومطابقتها آلياً مع أمر الشراء والفاتورة (Checklist item #27).

---

## 📝 [2026-09-18 05:25] - Completed Task: SH-02 — Draft B/L Dual Review Tool

### 📌 Overview

- **Task Code:** `SH-02`
- **Stage:** المرحلة 5: الإبحار و CargoX (Stage 5: Sailing & CargoX)
- **Features Covered:**
  - `SH-02:` `Draft B/L Dual Review Tool` — أداة المراجعة المزدوجة لبوالص الشحن ومطابقتها آلياً مع أمر الشراء والفاتورة لمنع أخطاء التعديل الجمركي (Checklist Item #27).
- **Description:**
  - Automated 20-parameter cross-check comparison engine matching Draft B/L fields against linked Purchase Orders, Commercial Invoices, and Booking specifications.
  - Interactive dual approval workflow between Importer Compliance Officer and Customs Broker.
  - Lifecycle automation in `_complete_draft_bl_review_workflow`:
    - Synchronizes verified Draft B/L details (`bl_number`, `booking_no`, `vessel_name`, etc.) directly into `ImportFile`.
    - Updates active `CargoShippingRecord` with confirmed B/L and voyage metadata.
    - Synchronizes `CustomsDocumentApproval` for the B/L document (`commercial_approved=True`, `customs_approved=True`).
    - Advances shipment lifecycle stage to `STEP_08_COO` (شهادة المنشأ / Certificate of Origin) with minimum progress percent elevated to $\ge 65\%$.
    - Auto-closes pending `SH-02` SmartTasks (`task_type="DRAFT_BL_REVIEW"`).
    - Dispatches downstream `SH-03` SmartTask ("منصة النقل الرقمي CargoX واعتماد الملفات") for the Logistics Coordinator.
    - Emits operational `SystemNotification` alerting the operations team that the Draft B/L has achieved certified dual approval.
  - Frontend reactive desktop integration:
    - Added `ref.invalidate(smartTasksProvider)` across review certification and dual approval actions in `DraftBLReviewTab`.
    - Created `DraftBLReviewDialog`: responsive modal dialog wrapper around `DraftBLReviewTab` supporting single-click launching with context file preselected.
    - Updated `ImportFileDetailsDialog`: added quick action button `مراجعة مسودة البوليصة (SH-02)` next to departure confirmation.
    - Updated `FreightBookingScreen`: added `reviewDraftBlRowBtn_${bkg.bookingCode}` action button on sailed booking rows.
    - Built comprehensive unit & widget test suite in `frontend/test/sh02_draft_bl_dual_review_test.dart` and `tests/unit/test_sh02_draft_bl_dual_review.py`.

### 📁 Files Changed

- `modules/import_documentation/service.py` — Modified: added `_complete_draft_bl_review_workflow` and integrated it into `create_draft_bl_review_service`, `approve_draft_bl_service`, and `process_dual_approval_service`.
- `modules/import_files/model.py` — Modified: added first-class database columns `bl_number`, `booking_no`, and `vessel_name` to `ImportFile`.
- `modules/import_files/schemas.py` — Modified: added `bl_number`, `booking_no`, and `vessel_name` to `ImportFileResponse` and update schemas.
- `frontend/lib/features/import_documentation/widgets/draft_bl_review_tab.dart` — Modified: added invalidation for `smartTasksProvider` upon draft saving and dual approval submission.
- `frontend/lib/features/import_documentation/widgets/draft_bl_review_dialog.dart` — NEW: modal dialog wrapper for SH-02 review tool.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart` — Modified: added quick action button for SH-02 review tool in header.
- `frontend/lib/features/freight_booking/screens/freight_booking_screen.dart` — Modified: added table row action button and handler to launch SH-02 for sailed bookings.
- `tests/unit/test_sh02_draft_bl_dual_review.py` — NEW: comprehensive backend pytest suite (comparison, discrepancies, dual approval, smart task auto-closure, and downstream progression).
- `frontend/test/sh02_draft_bl_dual_review_test.dart` — NEW: frontend unit and widget test suite for SH-02.
- `TASKS.md` & `checklist_tasks_dump.json` — Modified: marked `SH-02` as `[x] done` (`مكتملة ومختبرة بنسبة 100%`).

### 🧪 Validation / Testing

- **Backend Pytest Suites:**
  - `python -m pytest tests/unit/test_sh02_draft_bl_dual_review.py tests/unit/test_sh01_departure_confirmation.py tests/unit/test_bk01_freight_booking_confirmation.py -v` -> **7 passed**, 0 failed (100% green).
- **Frontend Test Suite:**
  - `flutter test test/sh02_draft_bl_dual_review_test.dart` -> **3 passed**, 0 failed (100% green).
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/import_documentation/widgets/draft_bl_review_dialog.dart lib/features/import_documentation/widgets/draft_bl_review_tab.dart lib/features/import_files/widgets/import_file_details_dialog.dart lib/features/freight_booking/screens/freight_booking_screen.dart test/sh02_draft_bl_dual_review_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps

- Proceed to **`SH-03:` `CargoX Digital Transfer & Sealing`** — منصة النقل الرقمي CargoX واعتماد الملفات (Checklist item #28) [COMPLETED].

---

## 📅 2026-09-18 — Task SH-03: CargoX Digital Transfer & Sealing (منصة النقل الرقمي CargoX واعتماد الملفات)

### 🎯 Objective
Checklist Item #28 — المرحلة الخامسة: الإبحار و CargoX (Stage 5: Sailing & CargoX).
منصة النقل الرقمي CargoX واعتماد الملفات وتسجيل رقم المظروف (Envelope ID) وتأكيد اعتماد وختم الوثائق إلكترونياً بتقنية البلوك تشين مع التوقيع الرقمي PKI وإرسالها لمصلحة الجمارك المصرية عبر نافذة.

### 🛠️ Key Changes
- **Backend Model & Schemas (`modules/import_files/`):**
  - Added CargoX synchronization columns to `ImportFile` model and Pydantic schemas: `cargox_envelope_id`, `cargox_envelope_code`, `cargox_envelope_status`, `cargox_transferred_at`.
- **CargoX Orchestration Service (`modules/cargox/service.py`):**
  - Implemented `_handle_cargox_seal_transfer_workflow`:
    1. Synchronizes `cargox_envelope_id`, `cargox_envelope_code`, `cargox_envelope_status="ACCEPTED_BY_CUSTOMS"`, and `cargox_transferred_at` directly onto `ImportFile`.
    2. Advances import file progress to $\ge 70\%$ and lifecycle stage from `STEP_10` to `STEP_11`.
    3. Auto-completes prior pending `SH-03` / `CARGOX_TRANSFER` SmartTasks.
    4. Automatically generates and dispatches downstream `SH-04` SmartTask ("استلام وتوثيق أصول المستندات البنكية للشحنة (SH-04)").
    5. Emits `SystemNotification` (category `STAGE_PROGRESSION`, target role `ALL`).
    6. Synchronizes `CustomsDocumentApproval` record for `CARGOX_ENVELOPE` with `overall_status="Approved for Clearance"`, `commercial_status="Approved"`, and `customs_status="Approved"`.
- **Frontend Layer:**
  - `frontend/lib/features/import_files/models/import_file_model.dart`: Added `cargoxEnvelopeId`, `cargoxEnvelopeCode`, `cargoxEnvelopeStatus`, and `cargoxTransferredAt` to model, constructor, `fromJson`, and `toJson`.
  - `frontend/lib/features/cargox/providers/cargox_provider.dart`: Connected `Ref` to `CargoXNotifier` and wired reactive state invalidation (`importFilesProvider` and `smartTasksProvider`) on `createEnvelope`, `updateEnvelope`, and `sealAndTransferToCustoms`.
  - `frontend/lib/features/cargox/widgets/cargox_hub_dialog.dart`: Created responsive modal dialog wrapper around `CargoXHubScreen` with custom header, ACID verification badge, and embedded support.
  - `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart`: Added `مستندات CargoX (SH-03)` action button in the dialog header.
- **Registries:**
  - Updated `checklist_tasks_dump.json` (Item #28 -> `"مكتملة ومختبرة بنسبة 100%"`).
  - Updated `TASKS.md` marking `SH-03` as `[x] done`.

### 🧪 Validation / Testing
- **Backend Unit Tests:**
  - `python -m pytest tests/unit/test_sh03_cargox_digital_transfer.py -v` -> **3 passed**, 100% green.
  - `python -m pytest tests/unit/test_cargox_hub.py tests/unit/test_cargox_standard_invoice.py -v` -> **21 passed**, 100% green.
- **Frontend Automated Tests:**
  - `flutter test test/sh03_cargox_digital_transfer_test.dart` -> **3 passed**, 100% green.
  - `flutter test test/sh02_draft_bl_dual_review_test.dart` -> **3 passed**, 100% green.
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/cargox lib/features/import_files` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- Proceed to **`SH-04:` `Original Bank Documents Receipt`** — استلام وتوثيق أصول المستندات البنكية للتخليص (Checklist item #29) [COMPLETED].

---

## 📅 2026-09-18 — Task SH-04: Original Bank Documents Receipt (استلام وتوثيق أصول المستندات البنكية)

### 🎯 Objective
Checklist Item #29 — المرحلة الخامسة: الإبحار و CargoX (Stage 5: Sailing & CargoX) [الختام الرسمي للمرحلة الخامسة بنسبة 100%].
استلام وتوثيق أصول المستندات البنكية للشحنة، تتبع شحنات الكورير الدولي (DHL, FedEx, Aramex)، مطابقة الأصول الورقية (الفاتورة، كشف العبوة، بوليصة الشحن، شهادة المنشأ)، وتجهيز ملف الإقرار الجمركي 46 ك.م.

### 🛠️ Key Changes
- **Backend Model & Schemas (`modules/import_files/`):**
  - Added dedicated original documents tracking columns to `ImportFile` and Pydantic schemas:
    - `original_documents_status` (String 50)
    - `original_documents_received_at` (DateTime timezone-aware)
    - `original_documents_courier_no` (String 100)
    - `original_documents_session_code` (String 50)
- **Original Documents Service (`modules/original_documents_collection/service.py`):**
  - Implemented `_handle_original_documents_orchestration_workflow`:
    1. Synchronizes `original_documents_status`, `original_documents_received_at`, `original_documents_courier_no`, and `original_documents_session_code` directly onto `ImportFile`.
    2. Elevates file progress percent to $\ge 75\%$ and advances next action to `CS-01` ("تعيين المخلص الجمركي والتفويض الإلكتروني (CS-01)").
    3. Auto-completes prior pending `SH-04` SmartTasks (`ORIGINAL_DOCS_RECEIPT`).
    4. Automatically generates and dispatches downstream `CS-01` SmartTask ("تعيين المخلص الجمركي والتفويض الإلكتروني (CS-01)").
    5. Emits `SystemNotification` (category `STAGE_PROGRESSION`, severity `INFO`, target role `ALL`).
    6. Creates/updates `CustomsDocumentApproval` for `ORIGINAL_DOCUMENTS` with `overall_status="Approved for Clearance"`, `commercial_status="Approved"`, `customs_status="Approved"`.
    7. Advances lifecycle step from `STEP_11` (تحصيل أصول المستندات) to `STEP_12` (نموذج 4 البنكي والإعداد الجمركي).
  - Wired orchestration in both `save_or_upsert_collection_session` and `confirm_courier_receipt`.
- **Frontend Layer:**
  - `frontend/lib/features/import_files/models/import_file_model.dart`: Added `originalDocumentsStatus`, `originalDocumentsReceivedAt`, `originalDocumentsCourierNo`, and `originalDocumentsSessionCode` to model, constructor, `fromJson`, and `toJson`.
  - `frontend/lib/features/import_documentation/providers/original_documents_collection_provider.dart`: Connected `Ref` to `OriginalDocumentsCollectionNotifier` and wired reactive state invalidation (`importFilesProvider` and `smartTasksProvider`) on `saveOrUpsertSession` and `confirmCourierReceipt`.
  - `frontend/lib/features/import_documentation/widgets/original_documents_collection_dialog.dart`: Created desktop modal dialog wrapper around `OriginalDocumentsCollectionTab` with responsive styling, file code badge, ACID indicator, and close button.
  - `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart`: Added `تتبع أصول المستندات (SH-04)` action button with Amber styling in dialog header.
- **Registries:**
  - Updated `checklist_tasks_dump.json` (Item #29 -> `"مكتملة ومختبرة بنسبة 100%"`).
  - Updated `TASKS.md` marking `SH-04` as `[x] done`.

### 🧪 Validation / Testing
- **Backend Unit Tests:**
  - `python -m pytest tests/unit/test_sh04_original_documents_receipt.py tests/unit/test_original_documents_collection.py -v` -> **7 passed**, 100% green.
  - All Stage 5 backend pytest suites (`test_sh01_departure_confirmation.py`, `test_sh02_draft_bl_dual_review.py`, `test_sh03_cargox_digital_transfer.py`, `test_sh04_original_documents_receipt.py`, `test_original_documents_collection.py`) -> **15 passed**, 100% green.
- **Frontend Automated Tests:**
  - `flutter test test/sh04_original_documents_receipt_test.dart` -> **3 passed**, 100% green.
  - All Stage 5 frontend test suites (`sh01`, `sh02`, `sh03`, `sh04`) -> **12 passed**, 100% green.
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/import_documentation/ lib/features/import_files/ test/sh04_original_documents_receipt_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- Stage 5 (المرحلة الخامسة: الإبحار و CargoX) is now **100% COMPLETE**.
- Transition immediately to **Stage 6: Customs Preparation (46) / المرحلة السادسة: الإعداد الجمركي (46)** starting with **`CS-01:` `Customs Broker Electronic Authorization`** — تعيين المخلص الجمركي والتفويض الإلكتروني (Checklist item #30) [COMPLETED].

---

## 🎯 Task: CS-01 — Customs Broker Electronic Authorization (تعيين المخلص الجمركي والتفويض الإلكتروني)

### 📌 Overview & Objectives
- **Checklist Item:** #30 (المرحلة السادسة: الإعداد الجمركي 46 — Stage 6: Customs Preparation).
- **Goal:** تعيين واعتماد المخلص الجمركي رسمياً للشحنة وإصدار التفويض الإلكتروني المعتمد عبر منظومة نافذة (Nafeza MTS Authorization Code)، مع رفع تقدم الشحنة لـ 80%، وإرسال إشعار لحظي لكافة الفرق وتكليف مهمة سداد مصاريف التوكيل الملاحي واستلام إذن التسليم (CS-02)، وتوليد خطاب التفويض الجمركي الرسمي تلقائياً.

### 🛠️ Architecture & Changes Made
- **Database & Models:**
  - `modules/customs_clearance/model.py`: Added `broker_id`, `broker_name`, `delegation_number`, `delegation_date`, `delegation_status`, `authorization_notes`, `mandate_letter_code`, and foreign key relationship to `ExternalServiceProvider`.
  - `modules/import_files/model.py`: Added `customs_broker_delegation_no`, `customs_broker_delegated_at`, and `customs_broker_authorization_status` to `ImportFile`.
- **Schemas:**
  - `modules/customs_clearance/schemas.py`: Created `CustomsBrokerAuthorizationSubmit` and added broker delegation fields to `CustomsClearanceResponse`, `CustomsClearanceCreate`, `CustomsClearanceUpdate`.
  - `modules/import_files/schemas.py`: Added `customs_broker_delegation_no`, `customs_broker_delegated_at`, `customs_broker_authorization_status` to `ImportFileBase` and `ImportFileUpdate`.
- **Service & Business Orchestration (`modules/customs_clearance/service.py`):**
  - Implemented `authorize_customs_broker_service`:
    1. Validates active `ImportFile` and `ExternalServiceProvider` (Customs Broker).
    2. Validates and records electronic delegation number (min length 3).
    3. Auto-generates official `customs_broker_mandate` formal letter via `generate_formal_letter_service`.
    4. Creates or updates `CustomsClearanceRecord` with broker delegation attributes.
    5. Synchronizes `ImportFile`: sets `broker_id`, `broker_name`, `customs_broker_delegation_no`, `customs_broker_delegated_at`, `customs_broker_authorization_status = "Authorized"`, elevates progress to $\ge 80\%$, advances `next_action` to `"سداد إذن التسليم الملاحي واستلام D/O (CS-02)"`.
    6. Auto-completes prior pending `CS-01` SmartTasks (`CUSTOMS_BROKER_ASSIGNMENT`).
    7. Dispatches downstream `CS-02` SmartTask ("سداد إذن التسليم الملاحي واستلام D/O (CS-02)").
    8. Emits `SystemNotification` (category `STAGE_PROGRESSION`, severity `INFO`, target `ALL`).
    9. Advances lifecycle board step: `STEP_12 -> STEP_13` (قيد ومطابقة إقرار 46 ك.م).
- **Router Endpoint (`modules/customs_clearance/router.py`):**
  - Added `POST /api/v1/customs-clearance/authorize-broker` returning `CustomsClearanceResponse`.
- **Frontend Layer:**
  - `frontend/lib/features/customs_clearance/models/customs_clearance_model.dart`: Added broker delegation fields to model, constructor, `fromJson`, and `toJson`.
  - `frontend/lib/features/import_files/models/import_file_model.dart`: Added `customsBrokerDelegationNo`, `customsBrokerDelegatedAt`, `customsBrokerAuthorizationStatus`.
  - `frontend/lib/features/customs_clearance/providers/customs_clearance_provider.dart`: Connected `Ref` to `CustomsClearanceNotifier`, implemented `authorizeBroker(payload)` with reactive cache invalidation of `importFilesProvider` and `smartTasksProvider`.
  - `frontend/lib/features/customs_clearance/widgets/customs_broker_authorization_dialog.dart`: Created desktop modal dialog with `SearchableDropdownField` for broker selection, electronic delegation code generator (`DEL-YYYY-PORT-XXXX`), date picker, customs port selector, mandate letter generator checkbox, and form validation.
  - `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart`: Added `تفويض المخلص (CS-01)` quick action button with purple styling.
  - `frontend/lib/features/customs_clearance/screens/customs_clearance_screen.dart`: Added `تفويض المخلص (CS-01)` button in header actions.
- **Registries:**
  - Updated `checklist_tasks_dump.json` (Item #30 -> `"مكتملة ومختبرة بنسبة 100%"`).
  - Updated `TASKS.md` marking `CS-01` as `[x] done`.

### 🧪 Validation / Testing
- **Backend Unit Tests:**
  - `python -m pytest tests/unit/test_cs01_customs_broker_authorization.py -v` -> **3 passed**, 100% green.
  - Full customs clearance backend test suite (`test_cs01_customs_broker_authorization.py`, `test_customs_clearance.py`, `test_customs_clearance_quotations.py`, `test_customs_clearance_variance_and_extraction.py`, `test_sh04_original_documents_receipt.py`, `test_sh03_cargox_digital_transfer.py`) -> **18 passed**, 100% green.
- **Frontend Automated Tests:**
  - `flutter test test/cs01_customs_broker_authorization_test.dart` -> **3 passed**, 100% green.
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/customs_clearance/ lib/features/import_files/ test/cs01_customs_broker_authorization_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- Proceed to **`CS-02:` `Delivery Order (D/O) Payment`** — سداد مصاريف التوكيل الملاحي واستلام إذن التسليم D/O (Checklist item #31, Stage 6).

---

## 📅 Task: CS-02: Delivery Order (D/O) Payment & Receipt (سداد إذن التسليم الملاحي واستلام D/O)
- **Checklist Item:** #31 — Stage 6: Customs Preparation / المرحلة السادسة: الإعداد الجمركي 46
- **Status:** ✅ مكتملة ومختبرة بنسبة 100%

### 📝 Overview & Rationale
تنفيذ وتفعيل متطلب **CS-02** الخاص بسداد مصاريف التوكيل الملاحي واستلام إذن التسليم (Delivery Order - D/O):
1. تسجيل سداد رسوم التوكيل الملاحي والمناولة الملاحية (THC / Agency Fees) وإثبات رقم وتاريخ إيصال السداد البنكي أو النقدي.
2. استلام وقيد رقم إذن التسليم الملاحي (D/O Number) وتاريخ صلاحيته وأيام السماح المتبقية (Free Time Days).
3. ربط التوكيل الملاحي / الخط الناقل من جدول شركاء الأعمال المعتمدين (`ExternalServiceProvider`).
4. رفع نسبة الإنجاز للشحنة إلى 83% وتحديث الإجراء التالي إلى `"قيد الإقرار الجمركي ونموذج 46 ك.م (CS-03)"`.
5. الإغلاق التلقائي للمهام الذكية المعلقة لـ CS-02 وتوليد المهمة الذكية اللاحقة CS-03 للمخلص الجمركي.
6. بث إشعار نظام مركزي لفرق العمليات والتخليص بجاهزية الشحنة لفتح الإقرار 46 ك.م.

### 🛠️ Changes Implemented
- **Backend Models & DB (`modules/customs_clearance/model.py`, `modules/import_files/model.py`):**
  - `modules/customs_clearance/model.py`: أضيف لجدول `customs_clearance_records` أعمدة: `delivery_order_number`, `delivery_order_date`, `delivery_order_expiry`, `free_days_allowed`, `shipping_agent_id`, `shipping_agent_name`, `delivery_order_fees`, `delivery_order_currency`, `delivery_order_payment_ref`, `delivery_order_paid_at`, `delivery_order_status`, `delivery_order_file_url`, `delivery_order_notes`، مع علاقة `shipping_agent = relationship("ExternalServiceProvider", foreign_keys=[shipping_agent_id])`.
  - `modules/import_files/model.py`: أضيف لجدول `import_files` أعمدة: `delivery_order_no`, `delivery_order_date`, `delivery_order_expiry_date`, `delivery_order_status`.
- **Backend Schemas (`modules/customs_clearance/schemas.py`, `modules/import_files/schemas.py`):**
  - أضيف المخطط `DeliveryOrderPaymentSubmit` مع حقول التحقق الإلزامية.
  - تم تحديث `CustomsClearanceCreate`, `CustomsClearanceUpdate`, `CustomsClearanceResponse`, `ImportFileBase`, `ImportFileUpdate`.
- **Backend Service (`modules/customs_clearance/service.py`):**
  - إنجاز دالة `record_delivery_order_payment_service(db, payload, user)` بأوركسترا تشغيلية كاملة:
    1. التحقق من وجود الشحنة وصلاحيتها.
    2. التحقق من رقم إذن التسليم ومرجع السداد والرسوم الموجبة.
    3. التحقق من التوكيل الملاحي واستخراج بياناته المرجعية.
    4. إنشاء أو تحديث سجل `CustomsClearanceRecord` بحالة `"D/O Received - Ready for 46"`.
    5. مزامنة `ImportFile`: تحديث رقم إذن التسليم، تاريخ الصلاحية، رفع نسبة الإنجاز $\ge 83\%$، وتوجيه الإجراء التالي إلى `"قيد الإقرار الجمركي ونموذج 46 ك.م (CS-03)"`.
    6. الإغلاق التلقائي لكافة المهام الذكية المعلقة الخاصة بـ CS-02.
    7. جدولة وتوليد المهمة الذكية اللاحقة CS-03 للمخلص الجمركي.
    8. بث إشعار نظام مركزي `SystemNotification` (بفئة `STAGE_PROGRESSION`).
    9. مزامنة لوحة دورة الحياة وتثبيت الشحنة في مرحلة `STEP_13` (قيد ومطابقة إقرار 46 ك.م).
- **Backend Router (`modules/customs_clearance/router.py`):**
  - إضافة نقطة النهاية `POST /api/v1/customs-clearance/delivery-order-payment` المستجيبة بـ `CustomsClearanceResponse`.
- **Frontend Layer:**
  - `customs_clearance_model.dart` & `import_file_model.dart`: إضافة حقول إذن التسليم وسداد التوكيل للموديل والـ serialization.
  - `customs_clearance_provider.dart`: إضافة `recordDeliveryOrderPayment(payload)` مع إبطال الكاش التفاعلي لـ `importFilesProvider` و `smartTasksProvider`.
  - `delivery_order_payment_dialog.dart`: إنشاء نافذة حوارية احترافية تتضمن `SearchableDropdownField` لاختيار التوكيل الملاحي، توليد كود الـ D/O التلقائي، إدخال الرسوم والعملة ومرجع السداد، تحديد تاريخ الصلاحية وحساب أيام السماح، وإظهار مؤشر التحميل مع حماية `Form` و `validator`.
  - `import_file_details_dialog.dart`: إضافة زر إجرائي سريع `سداد إذن التسليم (CS-02)` في شريط رأس النافذة.
  - `customs_clearance_screen.dart`: إضافة زر إجرائي مباشر `سداد إذن التسليم (CS-02)` في شريط إجراءات الشاشة.
- **Registries:**
  - تحديث `checklist_tasks_dump.json` (البند #31 -> `"مكتملة ومختبرة بنسبة 100%"`).
  - تحديث `TASKS.md` بتحديد `CS-02` كـ `[x] done`.

### 🧪 Validation / Testing
- **Backend Unit Tests:**
  - `python -m pytest tests/unit/test_cs02_delivery_order_payment.py -v` -> **3 passed**, 100% green.
  - Clearance suite (`test_cs01_customs_broker_authorization.py`, `test_cs02_delivery_order_payment.py`, `test_customs_clearance.py`) -> **9 passed**, 100% green.
- **Frontend Automated Tests:**
  - `flutter test test/cs01_customs_broker_authorization_test.dart test/cs02_delivery_order_payment_test.dart` -> **6 passed**, 100% green.
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/customs_clearance/ lib/features/import_files/ test/cs02_delivery_order_payment_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- الانطلاق إلى **`CS-03:` `Customs Declaration 46 Registration`** — قيد الإقرار الجمركي ونموذج 46 ك.م (البند #32، المرحلة السادسة).

---

## 📅 Task: CS-03: Customs Declaration 46 Registration (قيد الإقرار الجمركي ونموذج 46 ك.م)
- **Checklist Item:** #32 — Stage 6: Customs Preparation / المرحلة السادسة: الإعداد الجمركي 46
- **Status:** ✅ مكتملة ومختبرة بنسبة 100%

### 📝 Overview & Rationale
تنفيذ وتفعيل متطلب **CS-03** الخاص بقيد وتثبيت الإقرار الجمركي ونموذج 46 ك.م الموحد عبر نافذة MTS:
1. تسجيل رقم الإقرار الجمركي 46 ك.م وتاريخ القيد الجمركي ورقم الشهادة الجمركية المبدئية أو مرجع MTS.
2. تحديد المركز والميناء الجمركي المختص عبر قائمة منسدلة ذكية قابلة للبحث الفوري (`SearchableDropdownField`).
3. تخصيص المسار الجمركي للشحنة (Red Channel - كشف كامل وسحب عينات، Yellow Channel - فحص مستندي، Green Channel - إفراج فوري).
4. تحديد جهات العرض والرقابة الفنية المشتركة (هيئة الرقابة على الصادرات والواردات GOEIC، سلامة الغذاء، الكيمياء، الاتصالات، الدواء، الحجر).
5. رفع نسبة إنجاز الشحنة إلى $\ge 86\%$ ونقلها إلى المرحلة السابعة (`Under Customs Clearance (تحت التخليص والكشف 46)`) مع توجيه الإجراء التالي إلى `"تسجيل الكشف والمعاينة وسحب العينات (CL-01)"`.
6. الإغلاق التلقائي للمهام الذكية المعلقة الخاصة بـ CS-03 وجدولة المهمة الذكية اللاحقة CL-01 لفريق المخلص الجمركي.
7. بث إشعار نظام مركزي لجميع المعنيين باكتمال القيد والبدء في أعمال الكشف والمعاينة وسحب العينات.
8. ترقية لوحة دورة الحياة وتمرير الملف من `STEP_13` إلى `STEP_14` (متابعة الكشف والتثمين) و `STEP_15` (سحب العينات).

### 🛠️ Changes Implemented
- **Backend Models & DB (`modules/customs_clearance/model.py`, `modules/import_files/model.py`):**
  - `modules/customs_clearance/model.py`: أضيف لجدول `customs_clearance_records` أعمدة: `declaration_46_date`, `mts_certificate_number`, `customs_tariff_items_count`.
  - `modules/import_files/model.py`: أضيف لجدول `import_files` أعمدة: `form46_date`, `form46_status`.
  - تحديث قاعدة بيانات SQLite `sorour_logistics.db` بإضافة الأعمدة الجديدة.
- **Backend Schemas (`modules/customs_clearance/schemas.py`, `modules/import_files/schemas.py`):**
  - إضافة مخطط `CustomsDeclaration46Submit` مع التحقق من صحة المدخلات.
  - تحديث `CustomsClearanceCreate`, `CustomsClearanceUpdate`, `CustomsClearanceResponse`, `ImportFileBase`, `ImportFileUpdate`.
- **Backend Service (`modules/customs_clearance/service.py`):**
  - إنجاز دالة `register_customs_declaration_46_service(db, payload, user)` بأوركسترا تشغيلية كاملة:
    1. التحقق من وجود الشحنة وسلامتها وصلاحيتها.
    2. التحقق من صحة رقم الإقرار الجمركي 46 ($\ge 3$ أحرف/أرقام).
    3. إنشاء أو تحديث سجل `CustomsClearanceRecord` وتعيين الحالة إلى `"Under Customs Clearance"`.
    4. مزامنة `ImportFile`: تحديث رقم وتاريخ وحالة نموذج 46 (`form46_status = "REGISTERED"`), رفع نسبة الإنجاز $\ge 86\%$, وتوجيه الإجراء التالي إلى `"تسجيل الكشف والمعاينة وسحب العينات (CL-01)"`.
    5. الإغلاق التلقائي لمهام CS-03 الذكية المعلقة.
    6. جدولة وتوليد المهمة الذكية اللاحقة CL-01 للمخلص الجمركي.
    7. بث إشعار نظام مركزي `SystemNotification` (بفئة `STAGE_PROGRESSION`).
    8. ترقية لوحة دورة الحياة من `STEP_13` إلى `STEP_14` و `STEP_15`.
- **Backend Router (`modules/customs_clearance/router.py`):**
  - إضافة نقطة النهاية `POST /api/v1/customs-clearance/register-declaration-46` المستجيبة بـ `CustomsClearanceResponse`.
- **Frontend Layer:**
  - `customs_clearance_model.dart` & `import_file_model.dart`: تحديث النماذج والـ serialization بالحقول الجديدة.
  - `customs_clearance_provider.dart`: إضافة دالة `registerDeclaration46(payload)` مع إبطال الكاش التفاعلي لـ `importFilesProvider` و `smartTasksProvider`.
  - `customs_declaration_46_dialog.dart`: إنشاء نافذة حوارية تفاعلية متكاملة مع `SearchableDropdownField` للموانئ والمراكز الجمركية، محدد المسارات الجمركية (Red/Yellow/Green)، شرائح جهات العرض والرقابة المتعددة، مولد أرقام الإقرارات التلقائي، وحماية النماذج بـ `Form` و `validator` ومؤشر تحميل أثناء الحفظ.
  - `import_file_details_dialog.dart`: إضافة زر إجرائي سريع `قيد إقرار 46 (CS-03)` في شريط رأس النافذة.
  - `customs_clearance_screen.dart`: إضافة زر إجرائي مباشر `قيد إقرار 46 (CS-03)` في شريط أدوات الشاشة.
- **Registries:**
  - تحديث `checklist_tasks_dump.json` (البند #32 -> `"مكتملة ومختبرة بنسبة 100%"`).
  - تحديث `TASKS.md` بتحديد `CS-03` كـ `[x] done`.

### 🧪 Validation / Testing
- **Backend Unit Tests:**
  - `python -m pytest tests/unit/test_cs03_customs_declaration_46.py -v` -> **3 passed**, 100% green.
  - Clearance suite (`test_cs01_customs_broker_authorization.py`, `test_cs02_delivery_order_payment.py`, `test_cs03_customs_declaration_46.py`, `test_customs_clearance.py`) -> **12 passed**, 100% green.
- **Frontend Automated Tests:**
  - `flutter test test/cs01_customs_broker_authorization_test.dart test/cs02_delivery_order_payment_test.dart test/cs03_customs_declaration_46_test.dart` -> **9 passed**, 100% green.
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/customs_clearance/ lib/features/import_files/ test/cs03_customs_declaration_46_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- الانتقال إلى **المرحلة السابعة: التخليص والإفراج (Stage 7: Customs Clearance & Release)**:
  - **`CL-01:` `Inspection & Samples GOIEC Report`** — تسجيل الكشف والمعاينة وسحب العينات ومطابقة الرقابة (البند #33).

---

## 📅 Task: CL-01: Inspection & Samples GOIEC Report (تسجيل الكشف والمعاينة وسحب العينات ومطابقة الرقابة)
- **Checklist Item:** #33 — Stage 7: Customs Clearance & Release / المرحلة السابعة: التخليص والإفراج
- **Status:** ✅ مكتملة ومختبرة بنسبة 100%

### 📝 Overview & Rationale
تنفيذ وتفعيل متطلب **CL-01** الخاص بتسجيل وتوثيق إجراءات الكشف والمعاينة الميدانية بالميناء، وسحب العينات للفحص المعملي، ومطابقة الهيئة العامة للرقابة على الصادرات والواردات (GOEIC) والجهات الرقابية الفنية:
1. تسجيل نوع الكشف والمعاينة (كشف فعلي وسحب عينات، تفريغ كلي 100%، كشف عشوائي، فحص بالأشعة X-Ray، مطابقة مستندية).
2. تحديد ساحة ورصيف الكشف والمعاينة بالميناء عبر قائمة منسدلة ذكية تدعم البحث الفوري (`SearchableDropdownField`).
3. توثيق اسم المفتش أو رئيس لجنة الفحص الجمركي المشترك، وتاريخ المعاينة الفنية.
4. تسجيل نتيجة الكشف والمعاينة (مطابقة تامة Conforming، وجود عجز Shortage، وجود زيادة Surplus، اختلاف في التوصيف أو المنشأ Discrepancy).
5. إدارة سحب العينات المعملية: رقم محضر سحب العينات الرسمي (مع مولد أرقام آلي)، تاريخ السحب، شرائح الجهات الرقابية الفاحصة (GOEIC، سلامة الغذاء، الكيمياء، الوقاية الإشعاعية، الحجر)، رقم شهادة الفحص أو إشعار المعاينة الرقابية، وحالة فحص العينات (Samples Under Testing, Approved, Rejected)، ومصاريف التحليل المعملي.
6. رفع نسبة إنجاز الشحنة إلى $\ge 88\%$ ونقلها إلى مرحلة `"Customs Inspection & Sampling Completed (تم الكشف وسحب العينات)"` مع توجيه الإجراء التالي إلى `"احتساب الرسوم والضرائب الجمركية النهائية (CL-02)"`.
7. الإغلاق التلقائي للمهام الذكية المعلقة الخاصة بـ CL-01 وجدولة المهمة اللاحقة CL-02 لفريق المخلص الجمركي.
8. بث إشعار نظام مركزي لجميع المعنيين باكتمال الكشف وسحب العينات.
9. ترقية لوحة دورة الحياة وإتمام `STEP_14` (متابعة الكشف والتثمين) و `STEP_15` (سحب العينات) وتوجيه المسار نحو `STEP_17` (الحساب والسداد الجمركي النهائي).

### 🛠️ Changes Implemented
- **Backend Models & DB (`modules/customs_clearance/model.py`, SQLite):**
  - أضيف لجدول `customs_clearance_records` الأعمدة: `inspection_type`, `inspection_yard`, `inspector_name`, `inspection_result`, `is_sample_drawn`, `sampling_date`, `sampling_record_no`, `sampled_regulatory_bodies`, `goeic_certificate_no`.
  - تنفيذ ترحيل قاعدة البيانات وإضافة الأعمدة لـ `sorour_logistics.db`.
- **Backend Schemas (`modules/customs_clearance/schemas.py`):**
  - إضافة مخطط `CustomsInspectionSamplingSubmit` مع التحقق من صحة المدخلات.
  - تحديث `CustomsClearanceCreate`, `CustomsClearanceUpdate`, و `CustomsClearanceResponse` بالأعمدة الجديدة.
- **Backend Service (`modules/customs_clearance/service.py`):**
  - إنجاز دالة `record_customs_inspection_sampling_service(db, payload, user)` متضمنة:
    1. التحقق من وجود ملف الشحنة وصلاحيته.
    2. التحقق من محضر سحب العينات وتوليده آلياً عند الحاجة.
    3. تحديث أو إنشاء سجل `CustomsClearanceRecord` وتعيين الحالة إلى `"Inspection & Sampling Recorded"`.
    4. مزامنة `ImportFile`: رفع نسبة الإنجاز إلى $\ge 88\%$, وتحديث المرحلة والإجراء التالي إلى `CL-02`.
    5. الإغلاق التلقائي لمهام CL-01 المعلقة.
    6. جدولة المهمة الذكية اللاحقة CL-02 (`FINAL_DUTY_ASSESSMENT`).
    7. بث إشعار `SystemNotification` بفئة `STAGE_PROGRESSION`.
    8. ترقية لوحة دورة الحياة وتمرير `STEP_14` و `STEP_15` نحو `STEP_17`.
- **Backend Router (`modules/customs_clearance/router.py`):**
  - إضافة نقطة النهاية `POST /api/v1/customs-clearance/record-inspection-sampling`.
- **Frontend Layer:**
  - `customs_clearance_model.dart`: تحديث النموذج والـ `fromJson` والـ `toJson` بكافة حقول المعاينة وسحب العينات.
  - `customs_clearance_provider.dart`: إضافة دالة `recordInspectionSampling(payload)` مع إبطال الكاش التفاعلي لـ `importFilesProvider` و `smartTasksProvider`.
  - `customs_inspection_sampling_dialog.dart`: إنشاء نافذة حوارية تفاعلية ثرية بنظام Material Desktop تحتوي على بطاقات تفاصيل الشحنة، قائمة ساحات الفحص بـ `SearchableDropdownField`، محدد نوع المعاينة، بطاقات نتيجة المعاينة، مفتاح سحب العينات مع مولد محاضر السحب، شرائح الجهات الرقابية GOEIC، ومؤشرات التحميل والتحقق الصارم من صحة النماذج.
  - `import_file_details_dialog.dart`: إضافة زر إجرائي سريع `الكشف والمعاينة (CL-01)` بشريط رأس النافذة.
  - `customs_clearance_screen.dart`: إضافة زر إجرائي مباشر `الكشف والمعاينة (CL-01)` بشريط أدوات الشاشة.
- **Registries:**
  - تحديث `checklist_tasks_dump.json` (البند #33 -> `"مكتملة ومختبرة بنسبة 100%"`).
  - تحديث `TASKS.md` بتحديد `CL-01` كـ `[x] done`.

### 🧪 Validation / Testing
- **Backend Unit Tests:**
  - `python -m pytest tests/unit/test_cl01_inspection_sampling.py -v` -> **3 passed**, 100% green.
  - Clearance full suite (`test_cs01_customs_broker_authorization.py`, `test_cs02_delivery_order_payment.py`, `test_cs03_customs_declaration_46.py`, `test_cl01_inspection_sampling.py`) -> **12 passed**, 100% green.
- **Frontend Automated Tests:**
  - `flutter test test/cs01_customs_broker_authorization_test.dart test/cs02_delivery_order_payment_test.dart test/cs03_customs_declaration_46_test.dart test/cl01_inspection_sampling_test.dart` -> **11 passed**, 100% green.
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/customs_clearance/ lib/features/import_files/ test/cl01_inspection_sampling_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- الانتقال إلى **المهمة التالية في المرحلة السابعة: التخليص والإفراج (Stage 7: Customs Clearance & Release)**:
  - **`CL-02:` `Final Duty & Tax Assessment`** — احتساب الرسوم والضرائب الجمركية النهائية ومطابقتها (البند #34 في قائمة المهام الرئيسية) [COMPLETED].

---

## 📅 Task: CL-02: Final Duty & Tax Assessment (احتساب الرسوم والضرائب الجمركية النهائية ومطابقتها)
- **Checklist Item:** #34 — Stage 7: Customs Clearance & Release / المرحلة السابعة: التخليص والإفراج
- **Status:** ✅ مكتملة ومختبرة بنسبة 100%

### 📝 Overview & Rationale
تنفيذ وتفعيل متطلب **CL-02** الخاص باحتساب ومطابقة الرسوم والضرائب الجمركية النهائية للشحنة وفقاً لمطالبة منظومة نافذة MTS:
1. تسجيل رقم وتاريخ المطالبة الجمركية بنظام نافذة (`nafeza_claim_number`, `nafeza_claim_date`).
2. تسجيل وتوثيق القيمة المقبولة جمركياً بالجنيه المصري (CIF Base Amount) وسعر الصرف الجمركي الرسمي المعلن.
3. تفصيل بنود الضرائب والرسوم الجمركية وفقاً لبند التعريفة الجمركية (HS Code):
   - ضريبة الوارد الجمركية (Import Duty).
   - ضريبة القيمة المضافة (VAT).
   - ضريبة الجدول (Schedule Tax).
   - رسم التنمية (Development Fee).
   - رسوم الخدمات الجمركية وأ.ت.ص (Customs Service Fees).
   - الأرباح التجارية والصناعية (WHT).
   - رسوم التحاليل والمعامل (Lab Fees).
4. الاحتساب التلقائي لإجمالي الرسوم والضرائب المستحقة (`total_duty_payable`, `actual_duty_total`).
5. محرك تحليل ومطابقة الانحراف (Variance Analysis Engine): مقارنة الرسوم الفعلية مع التقديرية المحسوبة بمرحلة المحاكاة الجمركية، واحتساب قيمة الانحراف (`duty_variance_amount`) ونسبة الانحراف (`duty_variance_percentage`) وتوثيق سبب الانحراف (`duty_variance_reason`).
6. رفع نسبة إنجاز الشحنة إلى $\ge 90\%$ وتحديث الإجراء التالي إلى `"تسجيل سداد الرسوم الجمركية بسداد (CL-03)"`.
7. الإغلاق التلقائي للمهام الذكية المعلقة الخاصة بـ CL-02 وجدولة المهمة الذكية اللاحقة CL-03 لفريق التخليص والمالية.
8. بث إشعار نظام مركزي `SystemNotification` لجميع الفرق بجاهزية المطالبة للسداد عبر سداد / E-Finance.
9. ترقية لوحة دورة الحياة وتمرير `STEP_15` إلى `STEP_16` و `STEP_17` (السداد الجمركي).

### 🛠️ Changes Implemented
- **Backend Models & DB (`modules/customs_clearance/model.py`, `modules/import_files/model.py`):**
  - جدول `customs_clearance_records`: أعمدة `nafeza_claim_number`, `nafeza_claim_date`, `cif_base_amount`, `customs_exchange_rate`, `import_duty_amount`, `vat_amount`, `schedule_tax_amount`, `development_fee_amount`, `customs_service_fees`, `wht_amount`, `lab_service_fees`, `total_duty_payable`, `actual_duty_total`, `duty_variance_amount`, `duty_variance_percentage`, `duty_variance_reason`, `assessment_status`, `nafeza_assessment_json`.
- **Backend Schemas (`modules/customs_clearance/schemas.py`):**
  - مخطط `FinalDutyAssessmentSubmit` و `FinalDutyAssessmentResponse` مع التحقق الصارم والحساب التلقائي للإجمالي.
  - تحديث مخططات `CustomsClearanceResponse`.
- **Backend Service (`modules/customs_clearance/service.py`):**
  - إنجاز دالة `assess_final_customs_duties_service(db, payload, user)` بأوركسترا تشغيلية متكاملة لـ CL-02.
- **Backend Router (`modules/customs_clearance/router.py`):**
  - إضافة نقطة النهاية `POST /api/v1/customs-clearance/assess-final-duties`.
- **Frontend Layer:**
  - `customs_clearance_model.dart`: إضافة حقول التثمين النهائي وتحليل الانحراف للموديل والـ JSON serialization.
  - `customs_clearance_provider.dart`: إضافة دالة `assessFinalDuties(payload)` مع إبطال الكاش التفاعلي لـ `importFilesProvider` و `smartTasksProvider`.
  - `final_duty_assessment_dialog.dart`: إنشاء نافذة حوارية تفاعلية ثرية بنظام Material Desktop تحتوي على بطاقات المطالبة الجمركية، تفصيل بنود الضرائب، بطاقة تحليل الانحراف التفاعلية (Variance Card)، مؤشرات التحميل والتحقق الصارم من صحة النماذج.
  - `import_file_details_dialog.dart`: إضافة زر إجرائي سريع `احتساب الرسوم (CL-02)` بشريط رأس النافذة.
  - `customs_clearance_screen.dart`: إضافة زر إجرائي مباشر `حساب الرسوم (CL-02)` بشريط أدوات الشاشة.
- **Registries:**
  - تحديث `checklist_tasks_dump.json` (البند #34 -> `"مكتملة ومختبرة بنسبة 100%"`).
  - تحديث `TASKS.md` بتحديد `CL-02` كـ `[x] done`.

### 🧪 Validation / Testing
- **Backend Unit Tests:**
  - `uv run pytest -o pythonpath=. tests/unit/test_cl02_final_duty_assessment.py -v` -> **3 passed**, 100% green.
  - Clearance full suite (`test_cs01_customs_broker_authorization.py`, `test_cs02_delivery_order_payment.py`, `test_cs03_customs_declaration_46.py`, `test_cl01_inspection_sampling.py`, `test_cl02_final_duty_assessment.py`) -> **15 passed**, 100% green.
- **Frontend Automated Tests:**
  - `flutter test test/cl02_final_duty_assessment_test.dart` -> **2 passed**, 100% green.
  - Full clearance frontend test suite (`cs01`, `cs02`, `cs03`, `cl01`, `cl02`) -> **13 passed**, 100% green.
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/customs_clearance/ lib/features/import_files/widgets/import_file_details_dialog.dart test/cl02_final_duty_assessment_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- الانتقال إلى **المهمة التالية في المرحلة السابعة: التخليص والإفراج (Stage 7: Customs Clearance & Release)**:
  - **`CL-03:` `Customs Duty Payment Receipt`** — تسجيل سداد الرسوم الجمركية عبر سداد / E-Finance وتوثيق إيصال السداد البنكي (البند #35 في قائمة المهام الرئيسية) [COMPLETED].

---

## 📅 Task: CL-03: Customs Duty Payment Receipt (تسجيل سداد الرسوم الجمركية عبر سداد / E-Finance)
- **Checklist Item:** #35 — Stage 7: Customs Clearance & Release / المرحلة السابعة: التخليص والإفراج
- **Status:** ✅ مكتملة ومختبرة بنسبة 100%

### 📝 Overview & Rationale
تنفيذ وتفعيل متطلب **CL-03** الخاص بتسجيل وتوثيق سداد الرسوم والضرائب الجمركية عبر منظومة سداد الإلكترونية (Sadad / E-Finance) وإيصالات السداد البنكي:
1. تسجيل رقم سداد المالي الإلكتروني (`sadad_number`) الصادر عن منظومة الفاتورة والجمارك مع دعم التوليد التلقائي للمعرفات المعتمدة.
2. تسجيل رقم إشعار / إيصال السداد البنكي (`bank_receipt_no` / `receipt_number`).
3. توثيق طريقة السداد (`payment_method`: E-Finance / Sadad, Bank Transfer, CBE Direct, Cashier Deposit) والبنك المسدد من خلاله (`bank_name`).
4. تسجيل المبلغ المسدد فعلياً وتاريخ السداد بالدقائق والثواني ومرجع الملف المرفق (`receipt_file_url`).
5. التزامن ثنائي الاتجاه الفوري مع ملف الشحنة `ImportFile`:
   - `customs_duty_paid_amount`: حفظ المبلغ المسدد.
   - `customs_duty_receipt_no`: رقم إيصال السداد.
   - `customs_duty_sadad_no`: رقم سداد.
   - `customs_duty_payment_date`: تاريخ السداد.
   - `customs_duty_payment_status`: تحديث الحالة إلى `"Paid"`.
6. ترقية نسبة إنجاز الشحنة التشغيلية إلى $\ge 92\%$ وتحديث الإجراء التالي إلى `"استلام إذن الإفراج النهائي (CL-04) - جاري إنهاء إجراءات الإفراج الجمركي الأخضر"`.
7. الإغلاق التلقائي للمهام الذكية المعلقة الخاصة بـ CL-03 وجدولة المهمة الذكية اللاحقة CL-04 لمأمور الجمرك والمخلص الجمركي.
8. بث إشعار نظام مركزي `SystemNotification` بتمام سداد الرسوم الجمركية بنجاح.
9. ترقية وتحديث لوحة دورة الحياة وتمرير المرحلة من `STEP_16` إلى `STEP_17` (إذن الإفراج النهائي).

### 🛠️ Changes Implemented
- **Database & Migration (`sorour_logistics.db`):**
  - جدول `customs_clearance_records`: إضافة أعمدة `sadad_number`, `payment_method`, `duty_paid_amount`, `receipt_file_url`.
  - جدول `import_files`: إضافة أعمدة `customs_duty_paid_amount`, `customs_duty_receipt_no`, `customs_duty_sadad_no`, `customs_duty_payment_date`, `customs_duty_payment_status`.
- **Backend Models & Schemas (`modules/customs_clearance/`, `modules/import_files/`):**
  - تحديث `modules/customs_clearance/model.py` و `modules/import_files/model.py`.
  - مخطط `CustomsDutyPaymentSubmit` و `CustomsClearanceResponse` و `ImportFileUpdate` في `schemas.py`.
- **Backend Service & Router (`modules/customs_clearance/service.py`, `router.py`):**
  - تنفيذ دالة `record_customs_duty_payment_service` مع كافة آليات الأوركسترا التشغيلية.
  - إضافة نقطة النهاية `POST /api/v1/customs-clearance/record-duty-payment`.
- **Frontend Layer:**
  - `customs_clearance_model.dart`: تضمين حقول `sadadNumber`, `paymentMethod`, `dutyPaidAmount`, `receiptFileUrl`.
  - `import_file_model.dart`: تضمين حقول `customsDutyPaidAmount`, `customsDutyReceiptNo`, `customsDutySadadNo`, `customsDutyPaymentDate`, `customsDutyPaymentStatus` في النموذج و `fromJson` و `toJson`.
  - `customs_clearance_provider.dart`: إضافة دالة `recordDutyPayment(payload)` مع إبطال الكاش التفاعلي لـ `importFilesProvider` و `smartTasksProvider`.
  - `customs_duty_payment_dialog.dart`: إنشاء نافذة حوارية احترافية ثرية بنظام Material Desktop Responsive Layout تشمل بطاقة الملخص المالي، حقول رقم سداد وإيصال السداد، المبلغ المسدد مع التحقق الصارم، طريقة السداد، اسم البنك، منتقي التاريخ، رابط المرفق، مؤشرات التحميل والمنع من الإرسال المتكرر.
  - `customs_clearance_screen.dart`: إضافة زر إجرائي بشريط الأدوات `سداد الرسوم (CL-03)` وربط أزرار الإجراءات داخل الجداول والبطاقات بالنافذة الجديدة مباشرة.
  - `import_file_details_dialog.dart`: إضافة زر إجرائي بشريط رأس تفاصيل الملف `سداد الرسوم (CL-03)`.
- **Registries:**
  - تحديث `TASKS.md` بتحديد `CL-03` كـ `[x] done`.
  - تحديث `checklist_tasks_dump.json` (البند #35 -> `"مكتملة ومختبرة بنسبة 100%"`).

### 🧪 Validation / Testing
- **Backend Unit Tests:**
  - `python -m pytest tests/unit/test_cl03_customs_duty_payment.py tests/unit/test_cl02_final_duty_assessment.py -v` -> **6 passed**, 100% green.
- **Frontend Automated Tests:**
  - `flutter test test/cl03_customs_duty_payment_test.dart` -> **3 passed**, 100% green.
  - Full Clearance Test Suite (`cl01`, `cl02`, `cl03`, `cs01`, `cs02`, `cs03`, `final_duty_payment_tab`) -> **24 passed**, 100% green.
- **Frontend Static Analysis:**
  - `flutter analyze lib/features/customs_clearance/widgets/customs_duty_payment_dialog.dart lib/features/customs_clearance/providers/customs_clearance_provider.dart lib/features/import_files/models/import_file_model.dart lib/features/customs_clearance/screens/customs_clearance_screen.dart test/cl03_customs_duty_payment_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- الانتقال إلى **المهمة التالية في المرحلة السابعة: التخليص والإفراج (Stage 7: Customs Clearance & Release)**:
  - **`CL-04:` `Final Customs Release Order`** — صدور أمر الإفراج الجمركي الأخضر وبدء إجراءات النقل للمخازن (البند #36 في قائمة المهام الرئيسية) [COMPLETED].

---

## 📅 Task: CL-04: Final Customs Release Order (صدور إذن الإفراج الجمركي الأخضر وبدء إجراءات النقل للمخازن)
- **Checklist Item:** #36 — Stage 7: Customs Clearance & Release / المرحلة السابعة: التخليص والإفراج
- **Status:** ✅ مكتملة ومختبرة بنسبة 100%

### 📝 Overview & Rationale
تنفيذ وتفعيل متطلب **CL-04** الخاص بصدور أمر وإذن الإفراج الجمركي الأخضر النهائي (Final Green Release Permit):
1. التحقق الصارم من استيفاء سداد الرسوم الجمركية (`Duties Paid` / `Paid & Verified` / `customs_duty_paid_amount > 0`) قبل السماح بإصدار الإذن.
2. فحص سريان صلاحية رقم ACID طبقاً للقاعدة المصرية الصارمة (DC-02 / ACID-002)، وحظر الإفراج في حال انتهاء صلاحية الـ ACID ما لم يكن مفرجاً عنه أو مجدداً.
3. تسجيل رقم إذن الإفراج الجمركي الأخضر الرسمي (`release_permit_no`) مع توفير زر التوليد الذكي.
4. توثيق تاريخ صدور الإفراج (`release_date`) واسم مأمور الجمرك المفرج (`release_officer_name`).
5. تصنيف نوع الإفراج الجمركي (`release_type`: نهائي وبات، تحت التحفظ، برسم السماح المؤقت، قطعي بعد استيفاء العينات).
6. تسجيل تصريح خروج بوابة الميناء (`gate_pass_number`) مع أداة التوليد الذكي، وتاريخ الخروج المتوقع من بوابة الميناء (`port_gate_out_date`).
7. تسجيل رسوم الأرضيات وغرامات التأخير إن وجدت بالميناء (`demurrage_storage_fees`).
8. تفعيل تصريح النقل والمغادرة الميداني (`dispatch_authorized: true`) وتاريخ التفويض (`dispatch_date`).
9. تسجيل تعليمات النقل والتعتيق لمخازن الشركة (`transport_instructions`) وتوجيهات السلامة الجمركية.
10. التزامن ثنائي الاتجاه التلقائي مع ملف الشحنة `ImportFile`:
    - تحديث `is_customs_released: true` وتاريخ الإفراج `customs_released_at`.
    - حفظ `customs_release_permit_no`, `customs_release_type`, `customs_release_officer`, `customs_gate_pass_no`.
    - ترقية نسبة إنجاز الشحنة إلى $\ge 95\%$.
    - تحديث الإجراء التالي: `"حجز شاحنات النقل الداخلي (TR-01) ومتابعة خروج الشحنة من بوابة الميناء (TR-02)"`.
    - انتقال الملف للمرحلة اللاحقة: `"Phase 8 - Inland Transport & Warehouse Delivery"`.
11. إغلاق المهام الذكية السابقة لـ CL-04 تلقائياً، وتوليد مهام المتابعة اللاحقة TR-01 و TR-02 لمسؤولي النقل واللوجستيات.
12. بث إشعار نظام مركزي `SystemNotification` للأطراف المعنية بصدور إذن الإفراج وتفويض حركة الشاحنات.
13. تقدم خطوة لوحة دورة الحياة من `STEP_17` إلى `STEP_18` مع العنوان المخصص: `"Customs Released (تم الإفراج الجمركي النهائي وبدء النقل)"`.

### 🛠️ Changes Implemented
- **Database & Migration (`sorour_logistics.db`):**
  - جدول `customs_clearance_records`: إضافة أعمدة `release_officer_name`, `release_type`, `release_document_url`, `gate_pass_number`, `transport_instructions`.
  - جدول `import_files`: إضافة أعمدة `customs_release_permit_no`, `customs_release_type`, `customs_release_officer`, `customs_gate_pass_no`.
- **Backend Models & Schemas (`modules/customs_clearance/`, `modules/import_files/`):**
  - تحديث النماذج في `modules/customs_clearance/model.py` و `modules/import_files/model.py`.
  - مخطط `CustomsFinalReleaseSubmit`, `CompleteReleaseSubmit`, `CustomsClearanceResponse`, `ImportFileUpdate` في `schemas.py`.
- **Backend Validators & Service (`modules/customs_clearance/validators.py`, `service.py`, `router.py`):**
  - تحديث التحقق من إذن الإفراج وقبول حالات السداد المعتمدة.
  - تنفيذ خدمة `issue_final_customs_release_service` المتكاملة تشغيلياً مع حظر انتهاء ACID وتوليد المهام والإشعارات.
  - إضافة نقطة النهاية `POST /api/v1/customs-clearance/issue-final-release`.
- **Frontend Layer:**
  - `customs_clearance_model.dart`: تضمين حقول CL-04 في البناء و `fromJson` و `toJson`.
  - `import_file_model.dart`: تضمين حقول الإفراج الجمركي في البناء و `fromJson` و `toJson`.
  - `customs_clearance_provider.dart`: إضافة دالة `issueFinalRelease(payload)` مع إبطال الكاش التفاعلي لـ `importFilesProvider` و `smartTasksProvider`.
  - `customs_final_release_dialog.dart`: نافذة حوارية احترافية خضراء (Emerald Green) متجاوبة لبيئة سطح المكتب بدون أي مشاكل تدفق (Zero RenderFlex Overflow)، وتتضمن ملخص شهادة 46 وسداد الرسوم، حقول إذن الإفراج، أزرار التوليد التلقائي لأرقام الإفراج وبطاقة البوابة، قائمة نوع الإفراج، مفتاح تفويض النقل، تعليمات التعتيق، ومؤشرات التحميل.
  - `customs_clearance_screen.dart`: إضافة زر إجرائي بشريط الأدوات `إذن الإفراج (CL-04)` وربط الدالة `_showFinalReleaseDialog` بالنافذة الجديدة.
  - `import_file_details_dialog.dart`: إضافة زر إجرائي بشريط رأس تفاصيل الملف `إذن الإفراج (CL-04)`.
- **Registries:**
  - تحديث `TASKS.md` بتحديد `CL-04` كـ `[x] done`.
  - تحديث `checklist_tasks_dump.json` (البند #36 -> `"مكتملة ومختبرة بنسبة 100%"`).

### 🧪 Validation / Testing
- **Backend Unit Tests:**
  - `py -m pytest tests/unit/test_cl04_customs_final_release.py -v` -> **4 passed**, 100% green.
  - Stage 7 backend suite (`cl01`, `cl02`, `cl03`, `cl04`) -> **13 passed**, 100% green.
- **Frontend Automated Tests:**
  - `flutter test test/cl04_customs_final_release_test.dart` -> **3 passed**, 100% green.
  - Stage 7 full clearance frontend suite -> **14 passed**, 100% green.
- **Frontend Static Analysis:**
  - `dart analyze lib/features/customs_clearance/ lib/features/import_files/` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- الانتقال إلى **المهمة التالية في المرحلة السابعة: التخليص والإفراج (Stage 7: Customs Clearance & Release)**:
  - **`CL-05:` `Clearance Fees & Port Invoices`** — تسجيل فواتير المخلص ومصاريف العتالة ونولون الميناء (البند #37 في قائمة المهام الرئيسية) [COMPLETED].

---

## 📅 Task: CL-05: Clearance Fees & Port Invoices (تسجيل فواتير المخلص ومصاريف العتالة ونولون الميناء)
- **Checklist Item:** #37 — Stage 7: Customs Clearance & Release / المرحلة السابعة: التخليص والإفراج
- **Status:** ✅ مكتملة ومختبرة بنسبة 100%
- **Stage 7 Status:** 🎉 **المرحلة السابعة مكتملة بنسبة 100% (8/8 مهام)**

### 📝 Overview & Rationale
تنفيذ وتفعيل متطلب **CL-05** الخاص بتسجيل فواتير ومصروفات المخلص الجمركي ونولون الميناء ومصاريف العتالة والتحاليل وترحيلها المباشر إلى محرك تكلفة الوصول (Landed Cost Engine):
1. تسجيل فواتير مقدمي الخدمات اللوجستية والمخلصين (Customs Broker Fees, Port Dues & Wharfage, Stevedoring & Handling, Inspection & Weighing, Laboratory & Testing Fees, Storage & Demurrage, Other Expenses).
2. دعم احتساب ضريبة القيمة المضافة (VAT 14%) وضريبة الخصم والأرباح التجارية (WHT) واحتساب صافي المبلغ المستحق (Net Payable).
3. تحديد قاعدة توزيع التكلفة على الأصناف (Allocation Rule: Equal, Value-Based CIF, Weight-Based Gross Weight, Volume-Based CBM).
4. الترحيل والمزامنة الفورية والتلقائية مع كشف حساب تكلفة الشحنة (`LandedCostSettlementRecord.expense_invoices`) للمرحلة التاسعة (Stage 9: Landed Cost & Closure).
5. إعادة احتساب التجميعيات وتحديث إجمالي مصاريف التخليص والميناء والعتالة في سجل التخليص `CustomsClearanceRecord` وملف الشحنة `ImportFile`.
6. الإغلاق التلقائي للمهام الذكية المعلقة لـ CL-05، وبث إشعار نظام مركزي `SystemNotification` للأطراف المعنية.

### 🛠️ Changes Implemented
- **Database & Migration (`sorour_logistics.db`):**
  - إنشاء جدول `clearance_expense_invoices` بكافة الحقول المالية والمحاسبية والمحددات الفريدة.
  - جدول `customs_clearance_records`: إضافة أعمدة `total_clearance_expenses_egp`, `total_port_expenses_egp`, `total_handling_expenses_egp`, `clearance_invoices_count`.
  - جدول `import_files`: إضافة أعمدة `total_clearance_expenses_egp`, `clearance_invoices_status`.
- **Backend Models & Schemas (`modules/customs_clearance/`, `modules/import_files/`):**
  - إنشاء نموذج `ClearanceExpenseInvoice` في `modules/customs_clearance/model.py`.
  - تحديث النماذج في `modules/import_files/model.py`.
  - إنشاء المخططات `ClearanceExpenseInvoiceCreate`, `ClearanceExpenseInvoiceUpdate`, `ClearanceExpenseInvoiceResponse`, `ClearanceInvoicesSummaryResponse` في `modules/customs_clearance/schemas.py`.
- **Backend Service & Routes (`modules/customs_clearance/service.py`, `router.py`):**
  - دوال `record_clearance_invoice_service`, `get_clearance_invoices_by_file_service`, `delete_clearance_invoice_service`, `_recalculate_clearance_expenses_aggregates`.
  - إضافة نقاط النهاية: `POST /api/v1/customs-clearance/invoices`, `GET /api/v1/customs-clearance/invoices/by-file/{import_file_id}`, `DELETE /api/v1/customs-clearance/invoices/{invoice_id}`.
- **Frontend Layer:**
  - `clearance_expense_invoice_model.dart`: نماذج `ClearanceExpenseInvoiceModel` و `ClearanceInvoicesSummaryModel`.
  - `customs_clearance_model.dart` و `import_file_model.dart`: تضمين حقول CL-05 التجميعية.
  - `customs_clearance_provider.dart`: إضافة دوال `fetchClearanceInvoices`, `createClearanceInvoice`, `deleteClearanceInvoice` مع إبطال الكاش التفاعلي لـ `importFilesProvider` و `smartTasksProvider`.
  - `clearance_expenses_dialog.dart`: نافذة حوارية احترافية سطح مكتب متجاوبة تشمل بطاقات الملخص المالي KPI، قائمة وبطاقات الفواتير المسجلة، نموذج إضافة فاتورة تفاعلي محمي بـ `Form` و `validator`، احتساب VAT و WHT التفاعلي، وحذف الفاتورة مع التحديث التلقائي.
  - `customs_clearance_screen.dart`: إضافة زر إجرائي بشريط الأدوات `فواتير التخليص (CL-05)`، وأزرار سريعة بكل بطاقة سجل `clearanceExpensesBtn_${record.clearanceCode}`.
  - `import_file_details_dialog.dart`: إضافة زر إجرائي بشريط رأس تفاصيل الملف `فواتير التخليص (CL-05)`.
- **Registries:**
  - تحديث `TASKS.md` بتحديد `CL-05` كـ `[x] done`.
  - تحديث `checklist_tasks_dump.json` (البند #37 -> `"مكتملة ومختبرة بنسبة 100%"`).

### 🧪 Validation / Testing
- **Backend Unit Tests:**
  - `py -m pytest tests/unit/test_cl05_clearance_fees_invoices.py -v` -> **4 passed**, 100% green.
  - Stage 7 complete backend suite (`cs01`, `cs02`, `cs03`, `cl01`, `cl02`, `cl03`, `cl04`, `cl05`) -> **26 passed**, 100% green.
- **Frontend Automated Tests:**
  - `flutter test test/cl05_clearance_invoices_test.dart` -> **4 passed**, 100% green.
  - Stage 7 complete frontend suite (`cs01`, `cs02`, `cs03`, `cl01`, `cl02`, `cl03`, `cl04`, `cl05`) -> **23 passed**, 100% green.
- **Frontend Static Analysis:**
  - `dart analyze lib/features/customs_clearance/ lib/features/import_files/ test/cl05_clearance_invoices_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- **Stage 7 (Customs Clearance & Release / التخليص والإفراج) is now 100% COMPLETE.**
- الانتقال الفوري إلى **المرحلة الثامنة: النقل والمخازن (Stage 8: Inland Transport & Warehouse Receiving)**:
  - **`TR-01:` `Inland Transport Coordination`** — تنسيق وحجز سيارات وسائقي النقل الداخلي للمخزن وتتبع زمن الخروج والوصول (البند #38 في قائمة المهام الرئيسية).

---

## 📝 [2026-09-18 14:22] - Completed Task: TR-01 — Inland Transport Coordination (تنسيق وحجز سيارات وسائقي النقل الداخلي للمخزن)

### 📌 Overview
- **Task Code:** `TR-01`
- **Stage:** المرحلة الثامنة: النقل والمخازن (Stage 8: Inland Transport & Warehouse Receiving)
- **Checklist Task #:** 38
- **Core Goal:** حجز سيارات وسائقي النقل الداخلي من ميناء الوصول للمخزن، تسجيل بوالص النقل الداخلي Waybill، تتبع زمن الخروج من بوابة الميناء Port Gate-Out، وتتبع زمن الوصول المتوقع والفعلي لرصيف تفريغ المخزن مع مزامنة نسبة إنجاز ملف الشحنة ($\ge 97\%$) وترحيل نولون النقل الداخلي لتكلفة البضاعة (Landed Cost Engine).

### 🎯 Key Accomplishments & Business Logic
1. **جدول الحجز الداخلي ومحددات البيانات (`inland_transport_bookings`):**
   - تم إنشاء جدول `inland_transport_bookings` بخصائص متكاملة: `transport_code` (TR-YYYY-XXXX تلقائي فريد)، `waybill_number`، `carrier_name`، `truck_plate_number`، `truck_type`، `driver_name`، `driver_phone`، `driver_national_id`، `container_numbers`، `pickup_port_location`، `destination_warehouse`، `planned_departure_at`، `actual_departure_at`، `expected_arrival_at`، `actual_arrival_at`، `transport_fare_egp`، `status`، `tracking_notes`.
2. **تحديث ومزامنة ملف الاستيراد (`import_files`):**
   - إضافة أعمدة النقل الداخلي: `inland_transport_status`, `inland_transport_booking_no`, `inland_carrier_name`, `inland_truck_plate_no`, `inland_driver_name`, `inland_driver_phone`, `inland_transport_cost_egp`, `inland_departure_date`, `inland_expected_arrival_date`, `inland_actual_arrival_date`.
   - تقدم مرحلة الملف تلقائياً إلى `Phase 8 - Inland Transport & Warehouse Receiving`، ورفع نسبة الإنجاز إلى $\ge 97\%$.
3. **تتبع دورة حياة الشاحنة والرحلة:**
   - مرحلة الحجز وتأكيد الشاحنة والسائق (`Booking Confirmed`).
   - خروج الشاحنة من بوابة الميناء (`In Transit (Gate-Out)`).
   - وصول الشاحنة لرصيف المخزن (`Arrived Warehouse`) مع رفع الإنجاز إلى 100% وإتاحة استلام الأصناف في المخزن.
4. **توليد المهام الذكية والإشعارات:**
   - إغلاق المهام السابقة لـ TR-01 تلقائياً.
   - إنشاء مهام ذكية لاحقة: TR-02 (Demurrage Radar) و TR-03 (Warehouse GRN).
   - إرسال إشعارات نظام فورية (`SystemNotification`).

### 🛠️ Changes Implemented
- **Database & Migration (`sorour_logistics.db`):**
   - إنشاء جدول `inland_transport_bookings` مع الفهارس.
   - تحديث جدول `import_files` بإضافة أعمدة النقل الداخلي.
- **Backend Module (`modules/inland_transport/`):**
   - `model.py`: كائن `InlandTransportBooking`.
   - `schemas.py`: `InlandTransportBookingCreate`, `InlandTransportBookingUpdate`, `InlandTransportBookingResponse`, `InlandTransportGateOutSubmit`, `InlandTransportArrivalSubmit`.
   - `repository.py`: استعلامات CRUD وتوليد الكود التسلسلي الآلي `TR-YYYY-XXXX`.
   - `validators.py`: فحص صحة بيانات الشاحنة، اللوحات، ورقم هاتف السائق والتواريخ.
   - `service.py`: دوال `create_inland_transport_booking_service`, `record_gate_out_service`, `record_warehouse_arrival_service`, `get_booking_by_file_service`.
   - `router.py`: نقاط النهاية `/api/v1/inland-transport/bookings`.
   - `main.py`: تسجيل الراوتر والنموذج.
- **Frontend Layer:**
   - `frontend/lib/features/inland_transport/models/inland_transport_model.dart`: نموذج Dart كامل.
   - `frontend/lib/features/inland_transport/providers/inland_transport_provider.dart`: `InlandTransportNotifier` مع إبطال الكاش لـ `importFilesProvider` و `smartTasksProvider`.
   - `frontend/lib/features/import_files/models/import_file_model.dart`: تحديث النموذج بالحقول الجديدة.
   - `frontend/lib/features/inland_transport/widgets/inland_transport_dialog.dart`: نافذة حوارية لسطح المكتب متجاوبة بنموذج متكامل، توليد بوليصة تلقائي، وبطاقات KPIs، وإجراءات Gate-Out و Warehouse Arrival.
   - `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart`: إضافة زر إجرائي `النقل الداخلي (TR-01)` مع مفتاح `Key('inlandTransportHeaderBtn')`.
   - `frontend/lib/features/warehouse_receiving/screens/goods_in_transit_screen.dart`: إضافة زر إجرائي `حجز وتنسيق النقل (TR-01)` مع مفتاح `Key('inlandTransportScreenBtn')`.
- **Registries:**
   - تحديث `TASKS.md` بتحديد `TR-01` كـ `[x] done`.
   - تحديث `checklist_tasks_dump.json` (البند #38 -> `"مكتملة ومختبرة بنسبة 100%"`).

### 🧪 Validation / Testing
- **Backend Unit Tests:**
   - `py -m pytest tests/unit/test_tr01_inland_transport_coordination.py tests/unit/test_warehouse_receiving.py -v` -> **7 passed**, 100% green.
- **Frontend Automated Tests:**
   - `flutter test test/tr01_inland_transport_coordination_test.dart` -> **4 passed**, 100% green.
- **Frontend Static Analysis:**
   - `dart analyze lib/ test/tr01_inland_transport_coordination_test.dart` -> **0 errors, 0 warnings! Clean!**

### 🏁 Next Steps
- الانتقال إلى **`TR-02:` `Demurrage & Detention Radar`** — رادار مراقبة فترات السماح وتفادي غرامات الأرضيات والتأخير (Checklist Task #39).

---

## 📝 [2026-09-18 14:35] - Completed Task: TR-02 — Demurrage & Detention Radar

### 📌 Overview
- **Task Code:** `TR-02`
- **Stage:** المرحلة 8: النقل الداخلي واستلام المخزن (Stage 8: Inland Transport & Warehouse Receiving)
- **Checklist Task:** #39 (line 133 in `TASKS.md`)
- **Features Covered:**
  - `TR-02:` `Demurrage & Detention Radar` — رادار مراقبة فترات السماح وتفادي غرامات الأرضيات والتأخير للأسطول والحاويات بالكامل.
  - محرك التقييم الرباعي اللوني الدقيق للحاويات:
    - 🟢 الأخضر (`SAFE` - `#27AE60`): داخل فترة السماح المعتمدة (أيام الملاحة $> 4$ وأيام الأرضيات $> 2$).
    - 🟡 الأصفر / البرتقالي (`WARNING` - `#E67E22`): تحذير وشيك (إنذار 72 ساعة / متبقي $\le 4$ أيام ملاحي أو $\le 2$ أيام أرضيات قبل احتساب الغرامات).
    - 🔴 الأحمر (`CRITICAL_OVERDUE` - `#C0392B`): تجاوز فترات السماح وسريان احتساب الغرامات اليومية (\$ USD للخط الملاحي و EGP للأرضيات).
    - ⚪ الرمادي (`RETURNED_SAFE` - `#7F8C8D`): تم إرجاع الحاوية فارغة للخط الملاحي وإغلاق دورتها بسلام.
  - إحصائيات KPIs المجمعة ومؤشر التعرض المالي التقديري الإجمالي.
  - بطاقة الرادار التفاعلية `ContainerDemurrageRadarCard` في واجهة الداش بورد التشغيلي `OperationalDashboardScreen`.
  - زر إجرائي مخصص في هيدر تفاصيل ملف الاستيراد `ImportFileDetailsDialog` بمفتاح `Key('demurrageRadarHeaderBtn')`.

### 🎯 Key Accomplishments & Business Logic
1. **محرك تصنيف وتجميع رادار الحاويات (`get_containers_radar_overview_service`):**
   - استعلام كافة جلسات تتبع الحاويات النشطة عبر جميع ملفات الاستيراد.
   - حساب فترات السماح المستهلكة والمتبقية للخط الملاحي (`demurrage`) وأرضيات الميناء (`storage`).
   - تصنيف كل حاوية بدقة وحساب إجمالي الغرامات التقديرية بالدولار والجنيه ومجموع التعرض المالي.
2. **نقطة نهاية API RESTful عالية الأداء:**
   - مسار: `GET /api/v1/demurrage-detention/radar-overview`.
   - استجابة متوافقة بنموذج `ContainerRadarOverviewResponse` وقائمة `ContainerRadarItem`.
3. **طبقة العرض والواجهة الأمامية في Flutter:**
   - نماذج البيانات: `ContainerRadarItemModel` و `ContainerRadarOverviewModel` في `demurrage_model.dart`.
   - مزود ريفر بود: `containerRadarOverviewProvider` و `fetchRadarOverview()` في `demurrage_provider.dart`.
   - مكون رادار تفاعلي `ContainerDemurrageRadarCard`:
     - شريط أزرار تحكم مع مفتاح `Key('refreshDemurrageRadarBtn')` وزر فتح شاشة الغرامات `Key('openDemurrageRadarBtn')`.
     - 4 شارات إحصائية ملونة حسب حالة الحاوية (🟢 آمن | 🟡 تحذير | 🔴 غرامات | ⚪ مرجع).
     - لافتة تحذير التعرض المالي عند وجود غرامات جارية.
     - شريط أفقي لبطاقات الحاويات مع مؤشرات تقدم مزدوجة لسماح الخط وسماح الميناء ورسائل تنبيهية باللغة العربية.
     - حماية كاملة من الـ RenderFlex Overflow على بيئات سطح المكتب والموبايل.
   - تضمين الرادار في شاشة العمليات المركزية `OperationalDashboardScreen`.
   - إضافة زر `رادار الغرامات (TR-02)` في نافذة تفاصيل ملف الاستيراد `ImportFileDetailsDialog`.

### 📁 Files Changed
- `modules/demurrage_detention/schemas.py`: إضافة `ContainerRadarItem` و `ContainerRadarOverviewResponse`.
- `modules/demurrage_detention/service.py`: تطبيق `get_containers_radar_overview_service`.
- `modules/demurrage_detention/router.py`: إضافة مسار `GET /radar-overview`.
- `frontend/lib/features/demurrage_detention/models/demurrage_model.dart`: إضافة `ContainerRadarItemModel` و `ContainerRadarOverviewModel`.
- `frontend/lib/features/demurrage_detention/providers/demurrage_provider.dart`: إضافة `containerRadarOverviewProvider` و `fetchRadarOverview`.
- `frontend/lib/features/operational_dashboard/widgets/container_demurrage_radar_card.dart` [NEW]: بطاقة الرادار المتكاملة.
- `frontend/lib/features/operational_dashboard/screens/operational_dashboard_screen.dart`: تضمين `ContainerDemurrageRadarCard`.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart`: إضافة زر الهيدر `demurrageRadarHeaderBtn`.
- `tests/unit/test_tr02_demurrage_detention_radar.py` [NEW]: اختبارات الباك إند للخدمة والراوتر.
- `frontend/test/tr02_demurrage_detention_radar_test.dart` [NEW]: اختبارات النموذج والويدجت.
- `TASKS.md`: تمييز TR-02 كـ `[x] done`.
- `checklist_tasks_dump.json`: تحديث البند #39 إلى `"مكتملة ومختبرة بنسبة 100%"`.

### 🧪 Validation / Testing
- **Backend Unit Tests:**
  - `py -m pytest tests/unit/test_tr02_demurrage_detention_radar.py tests/unit/test_tr01_inland_transport_coordination.py -v` -> **6 passed**, 100% green.
  - `py -m pytest tests/unit/test_demurrage_detention_engine.py tests/unit/test_demurrage_alerts.py tests/unit/test_tr02_demurrage_detention_radar.py -v` -> **18 passed**, 100% green.
- **Frontend Automated Tests:**
  - `flutter test test/tr02_demurrage_detention_radar_test.dart` -> **3 passed**, 100% green.
  - `flutter test test/tr01_inland_transport_coordination_test.dart test/tr02_demurrage_detention_radar_test.dart` -> **7 passed**, 100% green.
- **Frontend Static Analysis:**
  - `dart analyze lib/features/operational_dashboard/widgets/container_demurrage_radar_card.dart lib/features/demurrage_detention/models/demurrage_model.dart lib/features/demurrage_detention/providers/demurrage_provider.dart lib/features/import_files/widgets/import_file_details_dialog.dart test/tr02_demurrage_detention_radar_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- الانتقال إلى **`TR-03:` `Warehouse Goods Receiving Note (GRN)`** — [مكتمل بنسبة 100%]

---

## 📝 [2026-09-18 15:05] - Completed Task: TR-03 — Warehouse Goods Receiving Note (GRN)

### 📌 Overview
- **Task Code:** `TR-03` (Task #40 in Master Checklist)
- **Stage:** المرحلة 8: النقل والمخازن (Stage 8: Inland Transport & Warehouse Receiving)
- **Feature:** إصدار إذن استلام البضاعة في المخزن وتأكيد الوصول وإيقاف عدادات النقل والشحن (Warehouse Goods Receiving Note - GRN)
- **Status:** مكتملة ومختبرة بنسبة 100%

### 🎯 Key Accomplishments
1. **الربط المتكامل ومزامنة وصول الشحنات في المخزن (Backend Synchronization Engine):**
   - تحديث حالة النقل للشحنة في `ImportFile` إلى `ARRIVED` وتوثيق تاريخ ووقت الوصول الفعلي `inland_actual_arrival_date`.
   - إيقاف عدادات النقل الداخلي وتحديث حجوزات الشاحنات في `InlandTransportBooking` إلى `Arrived at Warehouse` مع تسجيل `actual_arrival_at`.
   - إيقاف احتساب أرضيات الميناء في تتبع الحاويات `DemurrageTracking` عند وصول البضاعة للمخزن بتسجيل `gate_out_date`.
   - دعم التعيين الحصري لمنطقة الحجر المؤقت `Quarantine Zone` عند وجود أصناف تحت التحليل المعملي أو الفحص الرقابي.
   - تقدم مسار دورة حياة الشحنة آلياً من `STEP_19` إلى `STEP_20` (Landed Cost Settlement) مع تمرير تفاصيل إذن الإضافة `custom_stage_title` و `custom_next_action`.
   - توليد مهمة متابعة ذكية تلقائية (Smart Task) للبدء في الفحص الفني وحصر العجز والتالف `TR-04`.
   - بث إشعار نظام مركزي (System Notification) بوصول البضاعة للمخزن وصدور إذن الإضافة.
   - إضافة مسار استعلام مخصص: `GET /api/v1/warehouse-receiving/by-file/{import_file_id}`.
2. **الواجهة الأمامية في Flutter (Frontend Layer):**
   - تحديث `WarehouseReceivingNotifier` للقيام بإبطال الكاش التلقائي (`ref.invalidate`) لـ `importFilesProvider` و `smartTasksProvider` و `goodsInTransitProvider`.
   - إضافة زر إجراء رئيسي في شريط أدوات ملف الاستيراد `ImportFileDetailsDialog` بمفتاح `Key('warehouseReceivingHeaderBtn')` للوصول المباشر لشاشة أذون الإضافة المخزنية.
   - توفير مفاتيح الفحص والتحكم الرئيسية `Key('createGrnBtn')` و `Key('searchAndCloneGrnBtn')`.
3. **الاختبارات الشاملة (100% Passing Tests):**
   - اختبارات الباك إند: `tests/unit/test_tr03_warehouse_goods_receiving_note.py` اختبرت إصدار إذن الإضافة ومزامنة النقل وإيقاف العدادات، وتسجيل العجز والتالف وحجز البضاعة في منطقة الحجر (2/2 Passed).
   - اختبارات الواجهة الأمامية: `frontend/test/tr03_warehouse_receiving_test.dart` اختبرت نماذج الـ GRN وتوازن الكميات والعجز والتالف ورندرة الشاشة مع الأزرار التفاعلية (3/3 Passed).

### 📁 Files Changed
- `modules/warehouse_receiving/service.py`: مزامنة مرحلة ملف الاستيراد وإيقاف عدادات النقل والحاويات والتقدم في دورة الحياة.
- `modules/warehouse_receiving/router.py`: إضافة endpoint للـ GRN عبر ملف الاستيراد.
- `frontend/lib/features/warehouse_receiving/providers/warehouse_receiving_provider.dart`: ربط الـ invalidation التفاعلي للـ providers ذات الصلة.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart`: إضافة زر الهيدر `warehouseReceivingHeaderBtn`.
- `tests/unit/test_tr03_warehouse_goods_receiving_note.py`: اختبارات الباك إند لـ TR-03.
- `frontend/test/tr03_warehouse_receiving_test.dart`: اختبارات الفرونت إند لـ TR-03.
- `TASKS.md`: تمييز TR-03 كـ `[x] done`.
- `checklist_tasks_dump.json`: تحديث البند #40 إلى `"مكتملة ومختبرة بنسبة 100%"`.

### 🧪 Validation / Testing
- **Backend Unit Tests:**
  - `py -m pytest tests/unit/test_tr03_warehouse_goods_receiving_note.py -v` -> **2 passed**, 100% green.
- **Frontend Automated Tests:**
  - `flutter test test/tr03_warehouse_receiving_test.dart` -> **3 passed**, 100% green.
- **Combined Regression:**
  - `py -m pytest tests/unit/test_tr01_inland_transport_coordination.py tests/unit/test_tr02_demurrage_detention_radar.py tests/unit/test_tr03_warehouse_goods_receiving_note.py -v` -> **8 passed**, 100% green.

### 🏁 Next Steps
- البدء في مهمة **`TR-04:` `Warehouse Inspection & Discrepancy`** — [مكتملة بنسبة 100%]

---

## 📝 [2026-09-18 15:15] - Completed Task: TR-04 — Warehouse Inspection & Discrepancy

### 📌 Overview
- **Task Code:** `TR-04` (Task #41 in Master Checklist)
- **Stage:** المرحلة 8: النقل والمخازن (Stage 8: Inland Transport & Warehouse Receiving)
- **Feature:** الفحص الفني ومحضر مطابقة العجز والتالف بالمخازن ومطالبات التأمين والمورد (Warehouse Technical Inspection & Discrepancy Protocol)
- **Status:** مكتملة ومختبرة بنسبة 100%

### 🎯 Key Accomplishments
1. **قاعدة البيانات ومحرك الفحص الفني (Backend Inspection & Discrepancy Engine):**
   - إضافة أعمدة بروتوكول الفحص الفني لجدول `warehouse_receiving_records`:
     `inspection_date`, `inspection_committee`, `inspection_protocol_number` (e.g. `INSP-2026-0001`), `inspection_verdict`, `root_cause`, `supplier_claim_filed`, `supplier_claim_ref`, `claim_amount_estimated`, `claim_currency`, `discrepancy_rate_percent`.
   - تنفيذ الترحيل الآمن لجدول SQLite `sorour_logistics.db` مع الحفاظ التام على البيانات الحالية.
   - تطوير خدمة `submit_warehouse_inspection_protocol_service`:
     - حساب نسب العجز والتالف بدقة `discrepancy_rate_percent = ((shortage + damaged) / invoiced) * 100`.
     - توليد الرقم التسلسلي الرسمي لمحضر الفحص الفني `INSP-{Year}-{ID}`.
     - تحديد القرار الفني التلقائي (`ACCEPTED_FULL` عند اكتمال وسلامة البضاعة، أو `ACCEPTED_WITH_DISCREPANCY`، أو `REJECTED_QUARANTINED`).
     - تصنيف السبب الجذري للتلفيات (سوء مناولة وتفريغ بالميناء، اهتزازات وسوء نقل بري، عيوب تغليف من المورد، رطوبة وتسريب بالحاوية، عجز مصنعي).
     - توثيق مطالبات التأمين البحري/البري وحساب التعويضات التقديرية.
     - توثيق طلب إشعار دائن (Credit Note) أو تعويض عيني من المورد الأجنبي.
     - تحديث مرحلة ملف الاستيراد `ImportFile` وتوجيه الإجراء التالي مباشرة نحو تكلفة الوصول `CLO-02` والتسوية الختامية `CLO-01`.
     - الإغلاق التلقائي للمهام الذكية المرتبطة `SmartTask` الخاصة بـ TR-04 وتدوين ملاحظات الاعتماد.
     - بث إشعار نظام مركزي (System Notification) لفريق الجودة والمخازن والمالية.
   - إضافة مسارات RESTful:
     - `POST /api/v1/warehouse-receiving/{record_id}/inspection-protocol`
     - `GET /api/v1/warehouse-receiving/{record_id}/inspection-protocol`
2. **الواجهة الأمامية في Flutter (Frontend Layer):**
   - تحديث نموذج `WarehouseReceivingModel` لدعم كافة حقول بروتوكول الفحص والنسب والقرارات.
   - إضافة دالة `submitInspectionProtocol` في `WarehouseReceivingNotifier`.
   - تطوير النافذة الحوارية المتكاملة `WarehouseInspectionDialog`:
     - شريط مؤشرات أداء KPI فوري (الفاتورة، المقبول، العجز، التالف، نسبة الفروقات %).
     - جدول استعراض ومطابقة بنود الفاتورة الفعلية بالألوان.
     - نموذج إدخال أعضاء لجنة الفحص والاستلام، وتحديد قرار الفحص الفني والسبب الجذري.
     - لوحة إدارة المطالبات (مطالبة التأمين + إشعار خصم المورد + قيمة التعويض التقديرية بالعملة + العزل بالحجر).
     - أزرار اعتماد المحضر `Key('certifyInspectionProtocolBtn')` وتصدير المحضر إلى Excel `Key('exportInspectionProtocolBtn')`.
   - إضافة زر إجراء `محضر الفحص (TR-04)` في شريط هيدر `ImportFileDetailsDialog` بمفتاح `Key('warehouseInspectionHeaderBtn')`.
   - إضافة زر إجراء مباشر في بطاقات جدول `WarehouseReceivingScreen` بمفتاح `Key('openInspectionProtocolBtn_{id}')`.
3. **الاختبارات الشاملة (100% Passing Tests):**
   - اختبارات الباك إند: `tests/unit/test_tr04_warehouse_inspection_discrepancy.py` اختبرت سيناريو الفحص مع العجز والتالف والمطالبات والإغلاق الآلي للمهام الذكية، وسيناريو الفحص السليم 100%، واختبار نقاط النهاية API (3/3 Passed).
   - اختبارات الواجهة الأمامية: `frontend/test/tr04_warehouse_inspection_test.dart` اختبرت النماذج وحسابات نسبة الفروقات ورندرة النافذة الحوارية مع الـ KPIs والأزرار (3/3 Passed).
   - التحليل الساكن: `dart analyze` حقق **0 issues** (خلو تام من الأخطاء والتحذيرات).

### 📁 Files Changed
- `modules/warehouse_receiving/model.py`: إضافة أعمدة الفحص والمطابقة والمطالبات.
- `modules/warehouse_receiving/schemas.py`: إضافة `WarehouseInspectionSubmit` و `InspectionSummaryResponse`.
- `modules/warehouse_receiving/service.py`: تطبيق `submit_warehouse_inspection_protocol_service` و `get_inspection_summary_service`.
- `modules/warehouse_receiving/router.py`: إضافة مسارات اعتماد واستعلام محضر الفحص.
- `frontend/lib/features/warehouse_receiving/models/warehouse_receiving_model.dart`: تحديث النموذج بالحقول والخصائص.
- `frontend/lib/features/warehouse_receiving/providers/warehouse_receiving_provider.dart`: إضافة `submitInspectionProtocol`.
- `frontend/lib/features/warehouse_receiving/widgets/warehouse_inspection_dialog.dart` [NEW]: النافذة الحوارية المتكاملة لمحضر الفحص.
- `frontend/lib/features/warehouse_receiving/screens/warehouse_receiving_screen.dart`: ربط زر محضر الفحص الفني.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart`: إضافة زر الهيدر `warehouseInspectionHeaderBtn`.
- `tests/unit/test_tr04_warehouse_inspection_discrepancy.py` [NEW]: اختبارات الباك إند لـ TR-04.
- `frontend/test/tr04_warehouse_inspection_test.dart` [NEW]: اختبارات الفرونت إند لـ TR-04.
- `TASKS.md`: تمييز TR-04 كـ `[x] done`.
- `checklist_tasks_dump.json`: تحديث البند #41 إلى `"مكتملة ومختبرة بنسبة 100%"`.

### 🧪 Validation / Testing
- **Backend TR-04 Suite:**
  - `py -m pytest tests/unit/test_tr04_warehouse_inspection_discrepancy.py -v` -> **3 passed**, 100% green.
- **Backend Stage 8 Regression Suite:**
  - `py -m pytest tests/unit/test_tr01_inland_transport_coordination.py tests/unit/test_tr02_demurrage_detention_radar.py tests/unit/test_tr03_warehouse_goods_receiving_note.py tests/unit/test_tr04_warehouse_inspection_discrepancy.py -v` -> **11 passed**, 100% green.
- **Frontend TR-04 Suite:**
  - `flutter test test/tr04_warehouse_inspection_test.dart` -> **3 passed**, 100% green.
- **Frontend Stage 8 Combined Tests:**
  - `flutter test test/tr01_inland_transport_coordination_test.dart test/tr02_demurrage_detention_radar_test.dart test/tr03_warehouse_receiving_test.dart test/tr04_warehouse_inspection_test.dart` -> **13 passed**, 100% green.
- **Static Analysis:**
  - `dart analyze lib/features/warehouse_receiving lib/features/import_files/widgets/import_file_details_dialog.dart test/tr04_warehouse_inspection_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- البدء في المهمة الأخيرة من المرحلة الثامنة: **`TR-05:` `Empty Container Return (EIR)`** — تأكيد إرجاع الحاويات الفارغة للخط الملاحي واستلام إيصال EIR وإغلاق ملف الخط (Checklist Task #42).

---

## 📝 [2026-09-18 16:00] - Completed Task: TR-05 — Empty Container Return (EIR) & Final Container Demurrage Cutoff

### 📌 Overview
- **Task Code:** `TR-05` (Checklist Item #42)
- **Stage:** المرحلة 8: النقل والمخازن (Stage 8: Inland Transport & Warehouse Receiving)
- **Feature:** تأكيد وتوثيق إرجاع الحاويات الفارغة للخط الملاحي وإيصال الاستلام (EIR) وإيقاف عدادات الغرامات بالكامل (Empty Container Return & Equipment Interchange Receipt)
- **Status:** مكتملة ومختبرة بنسبة 100%

### 🎯 Key Accomplishments
1. **محرك وقاعدة بيانات إرجاع الحاويات الفارغة (Backend EIR & Demurrage Cutoff Engine):**
   - إضافة أعمدة تسليم وإرجاع الحاويات لجدول `import_files`:
     `empty_containers_returned_at`, `empty_containers_return_status`, `empty_containers_eir_numbers`, `empty_containers_depot_name`.
   - تنفيذ الترحيل التلقائي الآمن لجدول SQLite `sorour_logistics.db`.
   - بناء خدمة الإرجاع المركزية `record_empty_container_return_service`:
     - استلام رقم إيصال استلام الحاوية (`EIR Number`) وتاريخ التسليم الفعلي وساحة/مستودع الخط الملاحي (`Carrier Depot`).
     - إيقاف عداد غرامات التأخير (`Carrier Detention`) في جدول `demurrage_trackings` فورياً وتحويل حالة الحاوية إلى `RETURNED_SAFE` (أو `Closed` عند خلوها من الغرامات).
     - تحديث مرحلة ملف الاستيراد `ImportFile` وتصعيد نسبة الإنجاز التراكمية إلى $\ge 98.0\%$ وترقيته إلى `Phase 9 - Financial Settlement & File Closure` (`STEP_20`).
     - تحديث حالة النقل الداخلي `inland_transport_status = 'COMPLETED'`.
     - الإغلاق التلقائي للمهام الذكية المعلقة الخاصة بـ TR-05.
     - توليد وتكليف المهمة الذكية التالية لفريق الحسابات والمالية `TSK-0901: تجميع وتسوية الفواتير الختامية وتكلفة الوصول (CLO-01)`.
     - بث إشعار نظام مركزي عالي الأولوية لجميع أطراف العملية.
   - توفير نقطة نهاية API RESTful:
     `POST /api/v1/demurrage-detention/empty-container-return`

2. **الواجهة الأمامية في Flutter (Desktop UI Layer):**
   - تحديث نموذج `ImportFileModel` بدعم كامل لكافة حقول إرجاع الحاويات الفارغة ومطابقتها مع JSON.
   - إضافة نماذج `EmptyContainerReturnSubmitModel` و `EmptyContainerReturnResponseModel`.
   - بناء نافذة حوارية تفاعلية متجاوبة عالية التباين `EmptyContainerReturnDialog`:
     - شريط تنبيه وإشعار أثر إرجاع الحاوية على إيقاف الغرامات وترقية الشحنة للمرحلة التاسعة.
     - اختيار ملف الشحنة عبر قائمة منسدلة ذكية تدعم البحث الحي `SearchableDropdownField`.
     - حقل إدخال رقم إيصال `EIR` مع زر توليد تسلسلي تلقائي `توليد جديد`.
     - اختيار ساحة ومستودع الخط الملاحي مع قائمة الساحات الشائعة (ساحات الإسكندرية والدخيلة ودمياط والسخنة والميناء الجاف).
     - بطاقات تفاعلية لحالة سلامة الحاوية (`سليمة ونظيفة - SOUND_CLEAN`، `تلفيات بسيطة - MINOR_DAMAGE`، `تتطلب إصلاح - REPAIR_REQUIRED`) مع إظهار حقول الملاحظات وقيمة غرامة الإصلاح التقديرية فور اختيار التلفيات.
     - حقول بيانات سائق شاحنة الإرجاع ورقم لوحة الشاحنة والملاحظات.
     - زر اعتماد وإثبات التسليم بمفتاح `Key('submitEmptyContainerReturnBtn')` مع مؤشر تحميل تفاعلي وحماية لمنع الضغط المتكرر.
   - ربط زر `إرجاع الحاويات (TR-05)` في شريط أدوات هيدر تفاصيل الشحنة `ImportFileDetailsDialog` بمفتاح `Key('emptyContainerReturnHeaderBtn')`.
   - ربط زر `إرجاع الحاويات (TR-05)` في شريط أدوات رادار الغرامات `DemurrageDetentionScreen` بمفتاح `Key('globalEmptyContainerReturnBtn')` وفي كل بطاقة حاوية بمفتاح `Key('emptyContainerReturnBtn_{trackingId}')`.

3. **الاختبارات الشاملة المعتمدة (100% Passing Tests):**
   - **اختبارات الباك إند (Pytest):** نجاح 4 من 4 اختبارات في `tests/unit/test_tr05_empty_container_return.py`، ونجاح كافة اختبارات المرحلة الثامنة كاملة (15 من 15 اختبار أخضر).
   - **اختبارات الواجهة الأمامية (Flutter Test):** نجاح 4 من 4 اختبارات في `frontend/test/tr05_empty_container_return_test.dart`، ونجاح كافة اختبارات الواجهة للمرحلة الثامنة (17 من 17 اختبار أخضر).
   - **التحليل الساكن (Dart Analyze):** خلو تام من أي أخطاء أو تحذيرات (`No issues found!`).

### 📁 Files Changed
- `modules/import_files/model.py`: إضافة أعمدة تسليم الحاويات الفارغة.
- `modules/import_files/schemas.py`: دعم حقول EIR في `ImportFileBase` و `ImportFileUpdate`.
- `modules/demurrage_detention/schemas.py`: إضافة `EmptyContainerReturnSubmit` و `EmptyContainerReturnResponse`.
- `modules/demurrage_detention/service.py`: تطبيق `record_empty_container_return_service`.
- `modules/demurrage_detention/router.py`: إضافة مسار `POST /api/v1/demurrage-detention/empty-container-return`.
- `frontend/lib/features/import_files/models/import_file_model.dart`: تحديث النموذج بالحقول والـ JSON serialization.
- `frontend/lib/features/demurrage_detention/models/demurrage_model.dart`: إضافة نماذج الطلب والاستجابة لـ EIR.
- `frontend/lib/features/demurrage_detention/providers/demurrage_provider.dart`: إضافة دالة `recordEmptyContainerReturn`.
- `frontend/lib/features/demurrage_detention/widgets/empty_container_return_dialog.dart` [NEW]: النافذة الحوارية المتكاملة لإثبات إرجاع الحاويات.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart`: إضافة زر الهيدر `emptyContainerReturnHeaderBtn`.
- `frontend/lib/features/demurrage_detention/screens/demurrage_detention_screen.dart`: ربط أزرار الإرجاع في شريط الأدوات وبطاقات الحاويات.
- `tests/unit/test_tr05_empty_container_return.py` [NEW]: اختبارات الباك إند لميزة TR-05.
- `frontend/test/tr05_empty_container_return_test.dart` [NEW]: اختبارات الفرونت إند لميزة TR-05.
- `TASKS.md`: تمييز TR-05 كـ `[x] done`.
- `checklist_tasks_dump.json`: تحديث البند #42 إلى `"مكتملة ومختبرة بنسبة 100%"`.

### 🧪 Validation / Testing
- **Backend TR-05 Suite:**
  - `python -m pytest tests/unit/test_tr05_empty_container_return.py -v` -> **4 passed**, 100% green.
- **Backend Stage 8 Full Suite (TR-01 to TR-05):**
  - `python -m pytest tests/unit/test_tr01_inland_transport_coordination.py tests/unit/test_tr02_demurrage_detention_radar.py tests/unit/test_tr03_warehouse_goods_receiving_note.py tests/unit/test_tr04_warehouse_inspection_discrepancy.py tests/unit/test_tr05_empty_container_return.py -v` -> **15 passed**, 100% green.
- **Frontend TR-05 Suite:**
  - `flutter test test/tr05_empty_container_return_test.dart` -> **4 passed**, 100% green.
- **Frontend Stage 8 Full Suite (TR-01 to TR-05):**
  - `flutter test test/tr01_inland_transport_coordination_test.dart test/tr02_demurrage_detention_radar_test.dart test/tr03_warehouse_receiving_test.dart test/tr04_warehouse_inspection_test.dart test/tr05_empty_container_return_test.dart` -> **17 passed**, 100% green.
- **Static Analysis:**
  - `dart analyze lib/features/demurrage_detention lib/features/import_files/widgets/import_file_details_dialog.dart test/tr05_empty_container_return_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- الانتقال رسميًا إلى المرحلة التاسعة والأخيرة من دورة الاستيراد:
  **`Stage 9: تكلفة الوصول والإغلاق (Landed Cost & File Closure)`**
  والبدء بالمهمة الأولى:
  **`CLO-01:` `Final Settlement Invoices Aggregation`** — تجميع وتسوية الفواتير الختامية لكافة الأطراف (Checklist Item #43).

---

## 📝 [2026-09-18 16:30] - Completed Task: CLO-01 — Final Settlement Invoices Aggregation & Multi-Party Reconciliation

### 📌 Overview
- **Task Code:** `CLO-01` (Checklist Item #43)
- **Stage:** المرحلة 9: تكلفة الوصول والإغلاق (Stage 9: Landed Cost & File Closure)
- **Feature:** تجميع وتسوية الفواتير الختامية للملف لكافة الأطراف والموردين ومقدمي الخدمات والضرائب (Final Settlement Invoices Aggregation)
- **Status:** مكتملة ومختبرة بنسبة 100%

### 🎯 Key Accomplishments
1. **محرك تجميع وتسوية الفواتير الختامية (Backend Invoices Aggregation Engine):**
   - إضافة أعمدة التسوية المالية لجدول `import_files`:
     `financial_settlement_status`, `financial_settlement_date`, `financial_settlement_invoices_count`, `financial_settlement_total_egp`.
   - تنفيذ الترحيل التلقائي الآمن لقاعدة بيانات SQLite `sorour_logistics.db`.
   - بناء محرك الفحص والتجميع الشامل `aggregate_shipment_invoices_service`:
     - فحص فواتير البضاعة التجارية (FOB Commercial Invoices).
     - فحص فواتير وتكاليف حجز الشحن والنولون البحري/الجوي (Freight Booking).
     - فحص وثائق التأمين البحري (Cargo Insurance).
     - فحص الرسوم والضرائب الجمركية ونموذج 46 ك.م (Customs Duties & Taxes).
     - فحص مصاريف أذون التسليم الملاحية (Delivery Order Fees).
     - فحص مصاريف وأتعاب التخليص الجمركي والجهات العارضة (Customs Clearance Expenses CL-05).
     - فحص فواتير النقل الداخلي والتعتيق (Inland Transport Invoices TR-01).
     - فحص غرامات التأخير والأرضيات (Demurrage & Detention Penalties TR-02/TR-05).
     - استخراج ملخص لكل طرف (Supplier, Shipping Line, Insurance Co, Customs Authority, Customs Broker, Transport Co) متضمناً الإجمالي، المدفوع، المتبقي، وضرائب الخصم والإضافة (WHT).
   - بناء خدمة الاعتماد والتسوية الختامية `confirm_invoices_settlement_service`:
     - تحديث حالة التسوية في `import_files` إلى `INVOICES_SETTLED`.
     - تصعيد نسبة تقدم الملف التراكمية إلى $\ge 98.5\%$.
     - إغلاق المهمة الذكية المعلقة `TSK-0901` تلقائياً.
     - تكليف وتوليد المهمة الذكية التالية `TSK-0902: احتساب تكلفة الوصول الفعلية والانحراف لكل وحدة (CLO-02)`.
     - بث إشعار نظام مركزي عالي الأولوية لجميع أطراف العملية.
   - توفير نقاط النهاية RESTful:
     - `GET /api/v1/financial-settlement/invoices-aggregation/{import_file_id}`
     - `POST /api/v1/financial-settlement/confirm-invoices-settlement`

2. **الواجهة الأمامية في Flutter (Desktop UI Layer):**
   - تحديث نموذج `ImportFileModel` بدعم كامل لكافة حقول التسوية المالية.
   - إنشاء نماذج البيانات المتكاملة `AggregatedInvoiceItemModel` و `InvoicesPartySummaryModel` و `InvoicesAggregationResponseModel`.
   - إضافة دوال `fetchInvoicesAggregation` و `confirmInvoicesSettlement` في `FinancialSettlementNotifier` مع إبطال الكاش التلقائي `_ref?.invalidate(importFilesProvider)`.
   - بناء نافذة حوارية تفاعلية متجاوبة عالية التباين `FinalSettlementInvoicesDialog`:
     - شريط KPI إحصائي (إجمالي الفواتير، المسدد، المتبقي، ضريبة الخصم WHT، عدد الفواتير).
     - مصفوفة بطاقات ملخص الأطراف مع شارات الألوان لكل فئة.
     - جدول بيانات تفاعلي أفقي عالي الدقة يضم كافة الفواتير مع شارات حالة السداد.
     - قسم اعتماد التسوية الختامية وتوقيع المسؤول مع مؤشر تحميل تفاعلي.
   - ربط زر `تسوية الفواتير (CLO-01)` في شريط هيدر `ImportFileDetailsDialog` بمفتاح `Key('finalSettlementInvoicesHeaderBtn')`.
   - ربط زر `تسوية الفواتير الختامية` في شاشة التسوية المالية `FinancialSettlementScreen` بمفتاح `Key('globalFinalSettlementInvoicesBtn')` وفي كل صف بمفتاح `Key('settlementInvoicesBtn_{code}')`.

3. **الاختبارات الشاملة المعتمدة (100% Passing Tests):**
   - **اختبارات الباك إند (Pytest):** نجاح 4 من 4 اختبارات في `tests/unit/test_clo01_invoices_aggregation.py`، ونجاح 8 من 8 اختبارات مالية شاملة بنسبة 100%.
   - **اختبارات الواجهة الأمامية (Flutter Test):** نجاح 5 من 5 اختبارات في `frontend/test/clo01_invoices_aggregation_test.dart`.
   - **التحليل الساكن (Dart Analyze):** خلو تام من أي أخطاء أو تحذيرات (`No issues found!`).

### 📁 Files Changed
- `modules/import_files/model.py`: إضافة أعمدة التسوية المالية.
- `modules/import_files/schemas.py`: دعم حقول التسوية في النماذج.
- `modules/financial_settlement/schemas.py`: إضافة نماذج تجميع وتسوية الفواتير.
- `modules/financial_settlement/service.py`: تطبيق `aggregate_shipment_invoices_service` و `confirm_invoices_settlement_service`.
- `modules/financial_settlement/router.py`: إضافة مسارات التجميع والاعتماد.
- `frontend/lib/features/import_files/models/import_file_model.dart`: تحديث النموذج بالحقول الجديدة.
- `frontend/lib/features/financial_settlement/models/invoices_aggregation_model.dart` [NEW]: نماذج تجميع الفواتير.
- `frontend/lib/features/financial_settlement/providers/financial_settlement_provider.dart`: إضافة دوال الاستعلام والاعتماد وإبطال الكاش.
- `frontend/lib/features/financial_settlement/widgets/final_settlement_invoices_dialog.dart` [NEW]: النافذة الحوارية لتجميع وتسوية الفواتير.
- `frontend/lib/features/financial_settlement/screens/financial_settlement_screen.dart`: ربط أزرار التسوية.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart`: إضافة زر الهيدر `finalSettlementInvoicesHeaderBtn`.
- `tests/unit/test_clo01_invoices_aggregation.py` [NEW]: اختبارات الباك إند لـ CLO-01.
- `frontend/test/clo01_invoices_aggregation_test.dart` [NEW]: اختبارات الفرونت إند لـ CLO-01.
- `TASKS.md`: تمييز CLO-01 كـ `[x] done`.
- `checklist_tasks_dump.json`: تحديث البند #43 إلى `"مكتملة ومختبرة بنسبة 100%"`.

### 🧪 Validation / Testing
- **Backend CLO-01 Suite:**
  - `python -m pytest tests/unit/test_clo01_invoices_aggregation.py tests/unit/test_financial_settlement.py -v` -> **8 passed**, 100% green.
- **Frontend CLO-01 Suite:**
  - `flutter test test/clo01_invoices_aggregation_test.dart` -> **5 passed**, 100% green.
- **Static Analysis:**
  - `dart analyze lib/features/financial_settlement lib/features/import_files/widgets/import_file_details_dialog.dart test/clo01_invoices_aggregation_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- البدء في المهمة الثانية من المرحلة التاسعة:
  **`CLO-02:` `Actual Landed Cost Calculation`** — محرك احتساب تكلفة الوصول الفعلية والانحراف لكل وحدة مقارنة بالتقديري (Checklist Item #44) [COMPLETED].

---

## 📅 Task: CLO-02: Actual Landed Cost Calculation Engine (محرك احتساب تكلفة الوصول الفعلية والانحراف لكل وحدة)
- **Checklist Item:** #44 — Stage 9: Landed Cost & File Closure / المرحلة التاسعة: تكلفة الوصول والإغلاق
- **Status:** ✅ مكتملة ومختبرة بنسبة 100%

### 📝 Overview & Rationale
تنفيذ وتفعيل متطلب **CLO-02** الخاص بمحرك احتساب تكلفة الوصول الفعلية الشاملة (Actual Landed Cost Engine) وتوزيع كافة المصاريف اللوجستية والجمركية المسددة على بنود الأصناف الفعلية مقارنة بأساس المحاكاة التقديرية (PL-08):
1. استخراج بنود الأصناف الفعلية مباشرة من أوامر الشراء المعتمدة للشحنة (`PurchaseOrder` و `POLineItem`) مع الحفاظ على قيم الـ FOB والكميات والأوزان والأحجام.
2. تجميع المصاريف الفعلية المسددة من كافة المصادر التشغيلية (فواتير خطوط الشحن، الإقرار الجمركي 46، إذن التسليم، رسوم ومصاريف التخليص الجمركي، فواتير النقل الداخلي، العمولات والمصاريف البنكية).
3. إتاحة قواعد توزيع مرنة للمصاريف المشتركة (Allocation Rules):
   - حسب القيمة (`Value-Based - FOB`).
   - حسب الوزن الإجمالي (`Weight-Based - Gross Weight`).
   - حسب الحجم بالمتر المكعب (`Volume-Based - CBM`).
   - بالتساوي بين بنود الأصناف (`Equal`).
4. احتساب مؤشرات تكلفة الوصول لكل وحدة بضاعة (`actual_unit_landed_cost_egp`)، ومقارنتها بالتكلفة التقديرية (`estimated_unit_landed_cost_egp`).
5. احتساب انحراف التكلفة الشاملة وانحراف كل صنف (`variance_egp` و `variance_pct`) مع تحديد حالة الانحراف (`FAVORABLE` وفر/توفير، `UNFAVORABLE` زيادة، `MATCHED` مطابق).
6. احتساب معامل التضخيم الفعلي للشحنة (`actual_markup_factor = actual_total_landed_cost_egp / total_fob_egp`).
7. اعتماد التكلفة الفعلية وترقية مرحلة الملف الاستيرادي:
   - تحديث حالة التسوية المالية للشحنة إلى `COST_ALLOCATED`.
   - تسجيل إجمالي التكلفة الفعلية، معامل التضخيم، ونسبة الانحراف وتاريخ الاحتساب في جدول `import_files`.
   - رفع نسبة إنجاز الشحنة إلى $\ge 99.0\%$.
   - إنشاء وحفظ سجل التسوية المعتمد في جدول `landed_cost_settlements`.
   - إغلاق المهمة الذكية المعلقة `TSK-0902`، وجدولة وإطلاق المهمة الذكية اللاحقة `TSK-0903: إصدار وتصدير التقرير والملف الشامل PDF/Excel (CLO-03)`.
   - بث إشعار نظام مركزي `SystemNotification` لجميع الإدارات المعنية.
   - ترقية لوحة دورة الحياة وتمرير `STEP_20` إلى `STEP_21`.

### 🛠️ Changes Implemented
1. **قاعدة البيانات والباك إند (Database & Backend Engine):**
   - إضافة 5 أعمدة جديدة في جدول `import_files`:
     - `actual_landed_cost_total_egp` (Float)
     - `actual_landed_cost_markup_factor` (Float)
     - `actual_landed_cost_variance_egp` (Float)
     - `actual_landed_cost_variance_pct` (Float)
     - `actual_landed_cost_calculated_at` (DateTime)
   - تطبيق `modules/financial_settlement/schemas.py`:
     - `ActualLandedCostCategoryBreakdown`
     - `ActualLandedCostItemLine`
     - `ActualLandedCostCalculationRequest`
     - `ActualLandedCostCalculationResponse`
     - `ApproveActualLandedCostRequest`
     - `ApproveActualLandedCostResponse`
   - تطبيق `modules/financial_settlement/service.py`:
     - `calculate_actual_landed_cost_service`
     - `approve_actual_landed_cost_service`
   - إضافة المسارات في `modules/financial_settlement/router.py`:
     - `POST /api/v1/financial-settlement/calculate-actual-landed-cost/{import_file_id}`
     - `POST /api/v1/financial-settlement/approve-actual-landed-cost`

2. **الواجهة الأمامية في Flutter (Desktop UI Layer):**
   - تحديث نموذج `ImportFileModel` بدعم كامل لكافة حقول التكلفة الفعلية والانحرافات.
   - إنشاء نماذج البيانات `ActualLandedCostCategoryBreakdownModel` و `ActualLandedCostItemLineModel` و `ActualLandedCostCalculationResponseModel` و `ApproveActualLandedCostResponseModel`.
   - إضافة دوال `calculateActualLandedCost` و `approveActualLandedCost` في `FinancialSettlementNotifier` مع إبطال الكاش التلقائي لـ `importFilesProvider`.
   - بناء نافذة حوارية تفاعلية ثرية متجاوبة `ActualLandedCostDialog`:
     - شريط KPI إحصائي (إجمالي البضاعة FOB، المصاريف الفعلية، تكلفة الوصول الشاملة، معامل التضخيم، الانحراف المالي الشامل).
     - شريط محدد قاعدة التوزيع (`Value-Based`, `Weight-Based`, `Volume-Based`, `Equal`) مع إعادة احتساب حية وتلقائية.
     - تبويب مزدوج (مقارنة بنود المصروفات والانحرافات جدولياً، وتكلفة وصول الأصناف للوحدة بالانحراف ومعامل الزيادة).
     - قسم اعتماد التكلفة وتوقيع المسؤول المالي ومؤشر تحميل لمنع التكرار.
   - ربط زر `التكلفة الفعلية (CLO-02)` في شريط هيدر `ImportFileDetailsDialog` بمفتاح `Key('actualLandedCostHeaderBtn')`.
   - ربط زر `التكلفة الفعلية (CLO-02)` في شاشة التسوية المالية `FinancialSettlementScreen` بمفتاح `Key('globalActualLandedCostBtn')` وفي كل صف بمفتاح `Key('actualLandedCostBtn_{code}')`.

3. **الاختبارات الشاملة المعتمدة (100% Passing Tests):**
   - **اختبارات الباك إند (Pytest):** نجاح 4 من 4 اختبارات في `tests/unit/test_clo02_actual_landed_cost.py`، ونجاح كافة اختبارات التسوية المالية (8 من 8).
   - **اختبارات الواجهة الأمامية (Flutter Test):** نجاح 6 من 6 اختبارات في `frontend/test/clo02_actual_landed_cost_test.dart`، ونجاح كافة اختبارات الـ CLO (11 من 11).
   - **التحليل الساكن (Dart Analyze):** خلو تام من أي أخطاء أو تحذيرات (`No issues found!`).

### 📁 Files Changed
- `modules/import_files/model.py`: إضافة أعمدة التكلفة الفعلية ومعامل التضخيم والانحراف.
- `modules/import_files/schemas.py`: دعم حقول التكلفة الفعلية في النماذج.
- `modules/financial_settlement/schemas.py`: إضافة نماذج احتساب واعتماد التكلفة الفعلية.
- `modules/financial_settlement/service.py`: محرك الاحتساب والاعتماد `calculate_actual_landed_cost_service` و `approve_actual_landed_cost_service`.
- `modules/financial_settlement/router.py`: إضافة مسارات احتساب واعتماد التكلفة الفعلية.
- `frontend/lib/features/import_files/models/import_file_model.dart`: تحديث النموذج بالحقول الجديدة.
- `frontend/lib/features/financial_settlement/models/actual_landed_cost_model.dart` [NEW]: نماذج التكلفة الفعلية والانحرافات.
- `frontend/lib/features/financial_settlement/providers/financial_settlement_provider.dart`: إضافة دوال الاحتساب والاعتماد.
- `frontend/lib/features/financial_settlement/widgets/actual_landed_cost_dialog.dart` [NEW]: النافذة الحوارية لاحتساب واعتماد التكلفة الفعلية.
- `frontend/lib/features/financial_settlement/screens/financial_settlement_screen.dart`: ربط أزرار التكلفة الفعلية.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart`: إضافة زر الهيدر `actualLandedCostHeaderBtn`.
- `tests/unit/test_clo02_actual_landed_cost.py` [NEW]: اختبارات الباك إند لـ CLO-02.
- `frontend/test/clo02_actual_landed_cost_test.dart` [NEW]: اختبارات الفرونت إند لـ CLO-02.
- `TASKS.md`: تمييز CLO-02 كـ `[x] done`.
- `checklist_tasks_dump.json`: تحديث البند #44 إلى `"مكتملة ومختبرة بنسبة 100%"`.

### 🧪 Validation / Testing
- **Backend CLO-02 Suite:**
  - `python -m pytest tests/unit/test_clo02_actual_landed_cost.py tests/unit/test_clo01_invoices_aggregation.py -v` -> **8 passed**, 100% green.
- **Frontend CLO-02 Suite:**
  - `flutter test test/clo01_invoices_aggregation_test.dart test/clo02_actual_landed_cost_test.dart` -> **11 passed**, 100% green.
- **Static Analysis:**
  - `dart analyze lib/features/financial_settlement lib/features/import_files test/clo02_actual_landed_cost_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- البدء في المهمة الثالثة من المرحلة التاسعة:
  **`CLO-03:` `Comprehensive Shipment Dossier Export`** — إصدار وتصدير التقرير والملف الشامل PDF/Excel (Checklist Item #45).

---

## 📅 Task: CLO-03: Comprehensive Shipment Dossier Export (إصدار وتصدير التقرير والملف الشامل PDF/Excel)
- **Checklist Item:** #45 — Stage 9: Landed Cost & File Closure / المرحلة التاسعة: تكلفة الوصول والإغلاق
- **Status:** ✅ مكتملة ومختبرة بنسبة 100%

### 📝 Overview & Rationale
تنفيذ وتفعيل متطلب **CLO-03** الخاص بإصدار وتصدير الملف والتقرير الاستيرادي الشامل (Comprehensive Shipment Dossier Export Engine) لحفظ وأرشفة وتصدير كافة بيانات وتفاصيل الشحنة عبر مراحلها العشر بالكامل:
1. تجميع بيانات المراحل العشر الاستيرادية (10 Phases) من كافة الموديلات المترابطة:
   - المرحلة 1: أوامر الشراء ومواصفات البضاعة (PO & Items).
   - المرحلة 2: التمويل المصرفي ونموذج 4 وسويفت التحويل (Bank Form 4 & SWIFT).
   - المرحلة 3: النافذة الواحدة والرقم التعريفي ACID (Nafeza & ACID Tracker).
   - المرحلة 4: الشحن الدولي وبوالص الشحن وتتبع الباخرة (Freight Booking & B/L).
   - المرحلة 5: التوثيق الرقمي وسلسلة الكتل CargoX (CargoX Blockchain Hub).
   - المرحلة 6: الإفراج الجمركي والشهادة 46 ك.م والمسار الجمركي والرسوم والضرائب (Customs Clearance 46).
   - المرحلة 7: النقل البري الداخلي وتتبع الشاحنات والسائقين (Inland Transport).
   - المرحلة 8: الفحص المخزني وإذن الإضافة GRN ومحضر العجز والتالف (Warehouse Receiving & GRN).
   - المرحلة 9: إعادة الحاويات الفارغة وإيصالات EIR وتفادي الغرامات (Empty Container Return).
   - المرحلة 10: التسوية المالية الشاملة وتكلفة الوصول الفعلية والانحراف لكل وحدة (Actual Landed Cost & Settlement).
2. إتاحة خيارات تصدير متعددة الصيغ وفقاً لـ `Mandatory Save File Location Dialog Rule`:
   - حزمة PDF الرسمية بتنسيق القاهرة (A4 Cairo Vector PDF) مع ملخص التكاليف والمطابقات.
   - حزمة Excel المتوافقة (CSV بترميز UTF-8 مع BOM) لدعم جداول البيانات دون أي تشويه للأحرف العربية.
   - تصدير بيانات TSV النصية المهيكلة.
   - نسخ تقرير الملف الشامل للحافظة (Clipboard Quick Dossier Copy).
   - تسمية الملفات وفق المعيار المعتمد: `Comprehensive Dossier - [Import File Code].[ext]`.
3. اعتماد التصدير وترقية الملف الاستيرادي:
   - تسجيل مسؤول وتوقيع التصدير (`dossier_exported_by`) وتاريخ التصدير (`dossier_exported_at`) في جدول `import_files`.
   - رفع نسبة إنجاز الشحنة إلى $\ge 99.5\%$.
   - تحديث المرحلة الحالية إلى `Stage 10: Import File Closure & Archival`.
   - تحديث الإجراء التالي إلى `الإغلاق الرسمي والأرشفة الرقمية للملف (CLO-04)`.
   - إغلاق المهمة الذكية المعلقة `TSK-0903`، وجدولة وإطلاق المهمة الذكية النهائية `TSK-0904: الإغلاق الرسمي والأرشفة الرقمية للملف (CLO-04)`.
   - بث إشعار نظام مركزي `SystemNotification` لفرق المالية والعمليات وسلاسل الإمداد.

### 🛠️ Changes Implemented
1. **قاعدة البيانات والباك إند (Database & Backend Engine):**
   - إضافة عمودين جديدين في جدول `import_files`:
     - `dossier_exported_at` (DateTime)
     - `dossier_exported_by` (String)
   - تطبيق `modules/file_closure/schemas.py`:
     - `DossierSectionSummary`
     - `ComprehensiveShipmentDossierResponse`
     - `DossierExportConfirmRequest`
     - `DossierExportConfirmResponse`
   - تطبيق `modules/file_closure/service.py`:
     - `get_comprehensive_shipment_dossier_service` (تجميع كامل لبيانات الـ 10 مراحل والمطابقات التلقائية).
     - `confirm_dossier_export_service` (اعتماد التصدير وترقية المرحلة إلى Stage 10 وإطلاق TSK-0904).
   - إضافة المسارات في `modules/file_closure/router.py`:
     - `GET /api/v1/file-closure/comprehensive-dossier/{import_file_id}`
     - `POST /api/v1/file-closure/confirm-dossier-export`

2. **الواجهة الأمامية في Flutter (Desktop UI Layer):**
   - تحديث نموذج `ImportFileModel` بدعم `dossierExportedAt` و `dossierExportedBy`.
   - إنشاء نماذج البيانات في `comprehensive_dossier_model.dart`:
     - `DossierSectionSummaryModel`
     - `ComprehensiveShipmentDossierModel`
     - `DossierExportConfirmRequestModel`
     - `DossierExportConfirmResponseModel`
   - إنشاء خدمة الباك إند `ComprehensiveDossierService` وموفرها `comprehensiveDossierServiceProvider`.
   - ترقية خدمة التصدير `ComprehensiveReportExportService` بإضافة التكلفة الفعلية (CLO-01 & CLO-02)، وإيصالات EIR، والامتثال التام لـ `FileSaveHelper` ونمط التسمية القياسي.
   - بناء نافذة حوارية تفاعلية متجاوبة لسطح المكتب `ComprehensiveDossierExportDialog`:
     - شريط KPI رباعي (قيمة البضاعة FOB، تكلفة الوصول الفعلية، الانحراف المالي، حالة الملف والجاهزية).
     - شريط أزرار التصدير (PDF رسمي، Excel، TSV، ونسخ للحافظة) مع مفاتيح الاختبار.
     - استعراض جاهزية المراحل العشر بشارات تفاعلية ملونة تبين حالة كل مرحلة.
     - نموذج الاعتماد والتصدير وإطلاق مهمة الأرشفة النهائية (CLO-04) مع مؤشر تحميل تفاعلي وحماية النموذج بـ Form Validator.
   - ربط زر `الملف الشامل (CLO-03)` في شريط هيدر `ImportFileDetailsDialog` بمفتاح `Key('comprehensiveReportHeaderBtn')`.
   - ربط زر `تصدير الملف الشامل (CLO-03)` في شريط أدوات شاشة التقرير الشامل (الشاشة 47) بمفتاح `Key('comprehensiveDossierModalBtn')`.

3. **الاختبارات الشاملة المعتمدة (100% Passing Tests):**
   - **اختبارات الباك إند (Pytest):** نجاح 5 من 5 اختبارات في `tests/unit/test_clo03_comprehensive_dossier_export.py`، ونجاح كافة اختبارات الـ CLO (13 من 13).
   - **اختبارات الواجهة الأمامية (Flutter Test):** نجاح 5 من 5 اختبارات في `frontend/test/clo03_comprehensive_dossier_export_test.dart`، ونجاح كافة اختبارات الـ CLO (16 من 16).
   - **التحليل الساكن (Dart Analyze):** خلو تام من أي أخطاء أو تحذيرات (`No issues found!`).

### 📁 Files Changed
- `modules/import_files/model.py`: إضافة أعمدة `dossier_exported_at` و `dossier_exported_by`.
- `modules/import_files/schemas.py`: إضافة حقول التصدير في النماذج.
- `modules/file_closure/schemas.py`: نماذج Pydantic لتجميع واعتماد الملف الشامل.
- `modules/file_closure/service.py`: دوال تجميع الملف الشامل `get_comprehensive_shipment_dossier_service` واعتماد التصدير `confirm_dossier_export_service`.
- `modules/file_closure/router.py`: مسارات GET و POST للملف الشامل.
- `frontend/lib/features/import_files/models/import_file_model.dart`: إضافة حقول `dossierExportedAt` و `dossierExportedBy`.
- `frontend/lib/features/comprehensive_report/models/comprehensive_dossier_model.dart` [NEW]: نماذج الملف الشامل والـ 10 مراحل وتأكيد التصدير.
- `frontend/lib/features/comprehensive_report/services/comprehensive_dossier_service.dart` [NEW]: خدمة الاتصال بالـ API.
- `frontend/lib/features/comprehensive_report/services/comprehensive_report_export_service.dart`: تحديث التصدير بالتكلفة الفعلية وإيصالات EIR ونمط التسمية الموحد.
- `frontend/lib/features/comprehensive_report/widgets/comprehensive_dossier_export_dialog.dart` [NEW]: النافذة الحوارية التفاعلية لتصدير الملف الشامل.
- `frontend/lib/features/comprehensive_report/screens/import_file_comprehensive_report_screen.dart`: ربط زر `comprehensiveDossierModalBtn`.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart`: إضافة زر الهيدر `comprehensiveReportHeaderBtn`.
- `tests/unit/test_clo03_comprehensive_dossier_export.py` [NEW]: اختبارات الباك إند الشاملة لـ CLO-03.
- `frontend/test/clo03_comprehensive_dossier_export_test.dart` [NEW]: اختبارات الفرونت إند الشاملة لـ CLO-03.
- `TASKS.md`: تمييز CLO-03 كـ `[x] done`.
- `checklist_tasks_dump.json`: تحديث البند #45 إلى `"مكتملة ومختبرة بنسبة 100%"`.

### 🧪 Validation / Testing
- **Backend CLO-03 Suite:**
  - `py -3.14 -m pytest tests/unit/test_clo03_comprehensive_dossier_export.py -v` -> **5 passed**, 100% green.
  - Combined CLO regression: `py -3.14 -m pytest tests/unit/test_clo01_invoices_aggregation.py tests/unit/test_clo02_actual_landed_cost.py tests/unit/test_clo03_comprehensive_dossier_export.py` -> **13 passed**, 100% green.
- **Frontend CLO-03 Suite:**
  - `flutter test test/clo03_comprehensive_dossier_export_test.dart` -> **5 passed**, 100% green.
  - Combined CLO regression: `flutter test test/clo01_invoices_aggregation_test.dart test/clo02_actual_landed_cost_test.dart test/clo03_comprehensive_dossier_export_test.dart` -> **16 passed**, 100% green.
- **Static Analysis:**
  - `dart analyze lib/features/comprehensive_report lib/features/import_files` -> **No issues found!** (0 errors, 0 warnings).

### 🏁 Next Steps
- البدء في المهمة الرابعة والأخيرة من المرحلة التاسعة:
  **`CLO-04:` `Official File Closure & Digital Archive`** — الإغلاق الرسمي والأرشفة الرقمية المكتملة للملف (Checklist Item #46).

---

## 📝 [2026-09-18 21:30] - Completed Task: CLO-04 — Official File Closure & Digital Archive (Checklist Item #46 - FINAL TASK)

### 📌 Overview
- **Task Code:** `CLO-04`
- **Checklist Item:** #46 (The 46th and Final Task of ImportFlow ERP Checklist — إتمام كامل بنود النظام بنسبة 100%)
- **Stage:** المرحلة 9: تكلفة الوصول والإغلاق (Stage 9/10: Landed Cost & File Closure)
- **Features Covered:**
  - `CLO-04:` `Official File Closure & Digital Archive` — الإغلاق الرسمي والأرشفة الرقمية المكتملة للملف
- **Description:**
  - إنجاز متكامل للإغلاق النهائي والأرشفة الرقمية للشحنة مع استيفاء بوابة التدقيق المسبقة المرتكزة على ركائز التدقيق الستة (6 Core Pillars).
  - تطوير مسار التدقيق المسبق المباشر `GET /api/v1/file-closure/closure-precheck/{import_file_id}` لفحص جاهزية الإغلاق:
    1. الإفراج الجمركي النهائي (Customs Final Release).
    2. استلام المستودع وإصدار إذن الإضافة (Warehouse GRN).
    3. احتساب واعتماد تسوية تكلفة الوصول الفعلية (Actual Landed Cost Settled).
    4. تصدير واعتماد الملف الشامل للرسالة (Comprehensive Shipment Dossier Export - CLO-03).
    5. تسليم الحاويات الفارغة للخط الملاحي وإيصالات EIR (Empty Containers Returned - TR-05).
    6. اكتمال المهام التشغيلية للملف (Operational Tasks Closed).
  - تطوير مسار إصدار شهادة الإغلاق والأرشفة الرقمية `POST /api/v1/file-closure/official-close`:
    - توليد كود الشهادة المعتمد `CLR-YYYY-XXXX`.
    - ترقية حالة ملف الاستيراد `ImportFile` إلى:
      - `status = "Closed"`
      - `current_stage = "Archived & Closed (Certificate: CLR-YYYY-XXXX)"`
      - `current_module = "Phase 10 - Import File Closure & Historical Archive"`
      - `progress_percent = 100.0`
      - `next_action = "File Archived - Read-Only Historical State"`
      - `closed_at = datetime.now(timezone.utc)`
    - الإغلاق الآلي للمهمة الذكية `TSK-0904` ("الإغلاق الرسمي والأرشفة الرقمية للملف (CLO-04)").
    - ترقية لوحة دورة حياة الشحنة (Lifecycle Board) إلى اكتمال الخطوة النهائية `STEP_21` بنسبة 100% بدون أي خطوات معلقة.
    - إرسال إشعار نظام مركزي `SystemNotification` بفئة `FILE_CLOSURE` موجه للجميع.
  - تكامل الواجهة الأمامية لسطح المكتب (Flutter Desktop):
    - إضافة نماذج `ClosurePrecheckResponseModel` و `OfficialClosureCertificateResponseModel`.
    - تحديث `file_closure_provider.dart` بدوال `fetchClosurePrecheck` و `officialCloseImportFile` مع التحديث الآلي لـ `importFilesProvider` و `fileClosureProvider`.
    - بناء النافذة الحوارية التفاعلية المعتمدة لسطح المكتب `OfficialFileClosureDialog`:
      - شريط استعراض ركائز التدقيق الستة بشارات تفاعلية خضراء وحمراء.
      - صندوق تنبيهات حمراء للأسباب المعيقة وصندوق تنبيهات برتقالية للملاحظات التدقيقية.
      - بطاقة معاينة كود الشهادة وتكلفة الوصول الفعلية مع زر تصدير الشهادة الرسمية `Key('exportClosureCertificateBtn')` الملتزم بقاعدة نافذة الحفظ الموحدة `FileSaveHelper`.
      - نموذج بيانات المدقق وخزينة الأرشيف الرقمي مع حماية الإدخال بـ Form Validator.
      - زر اعتماد الإغلاق الرسمي `Key('confirmOfficialClosureBtn')` بمؤشر تحميل تفاعلي وتعطيل آلي في حال وجود متطلبات معلقة.
    - ربط زر `الإغلاق الرسمي (CLO-04)` في شريط هيدر `ImportFileDetailsDialog` بمفتاح `Key('officialFileClosureHeaderBtn')`.

### 📁 Files Changed
- `modules/file_closure/schemas.py` — إضافة نماذج `ClosurePrecheckResponse` و `OfficialClosureCertificateResponse` ودعم `dossier_exported` و `empty_containers_returned` كحقول اختيارية في قائمة التحقق.
- `modules/file_closure/service.py` — إضافة دوال `get_closure_precheck_service` و `official_close_import_file_service`، وإصلاح معلمات إشعار النظام وتثبيت `next_action` و `current_stage` و `status = Closed`.
- `modules/file_closure/validators.py` — تحديث `CHECKLIST_STAGE_MAPPING` بدعم `dossier_exported` و `empty_containers_returned`.
- `modules/file_closure/router.py` — إضافة مسارات `GET /closure-precheck/{import_file_id}` و `POST /official-close`.
- `frontend/lib/features/file_closure/models/file_closure_model.dart` — إضافة `ClosurePrecheckResponseModel` و `OfficialClosureCertificateResponseModel` وتحديث `ClosureChecklistModel`.
- `frontend/lib/features/file_closure/providers/file_closure_provider.dart` — إضافة `fetchClosurePrecheck` و `officialCloseImportFile`.
- `frontend/lib/features/file_closure/widgets/official_file_closure_dialog.dart` [NEW] — نافذة الإغلاق الرسمي والأرشفة الرقمية المتوافقة مع معايير سطح المكتب.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart` — ربط زر الهيدر `officialFileClosureHeaderBtn`.
- `tests/unit/test_clo04_official_file_closure.py` [NEW] — 5 اختبارات باكد إند شاملة لـ CLO-04.
- `frontend/test/clo04_official_file_closure_test.dart` [NEW] — 4 اختبارات واجهة أمامية شاملة لـ CLO-04.
- `TASKS.md` — تمييز `CLO-04` كـ `[x] done` (اكتمال 46 من 46 مهمة بنسبة 100%).
- `checklist_tasks_dump.json` — تحديث البند #46 إلى `"مكتملة ومختبرة بنسبة 100%"`.

### 🧪 Validation / Testing
- **Backend Test Execution:**
  - `py -3.14 -m pytest tests/unit/test_clo04_official_file_closure.py -v` -> **5 passed** (100% green).
  - All File Closure Suite (CLO-01 to CLO-04 + file_closure): **23 passed** (100% green).
- **Frontend Test Execution:**
  - `flutter test test/clo04_official_file_closure_test.dart` -> **4 passed** (100% green).
  - All File Closure Suite: **23 passed** (100% green).
- **Static Analysis:**
  - `dart analyze lib/features/file_closure lib/features/import_files/widgets/import_file_details_dialog.dart test/clo04_official_file_closure_test.dart` -> **No issues found!** (0 errors, 0 warnings).

### 🏆 Milestone Achievement:
- تم الانتهاء بنجاح تام من كافة بنود الـ Checklist الـ 46 لنظام **ImportFlow ERP** بنسبة إنجاز **100%** واختبارات خضراء بالكامل في الباك إند والفرونت إند!

