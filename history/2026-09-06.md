# 📅 سجل التطوير اليومي - 2026-09-06

## 📝 [2026-09-06 10:55] - Completed Task: Universal Text Selection, Clipboard Copy & PO Details Auto-Copy Button

### 📌 Overview
- **Task Code:** UI-SELECT-COPY-001
- **Description:** حل مشكلة عدم القدرة على تحديد أو نسخ النصوص والبيانات في واجهة النظام (Desktop Text Selection & Copy)، وإصلاح `SelectionArea` المعطل بسبب غياب سلف الـ `Overlay` في شجرة الـ Widgets، وتفعيل التحديد والنسخ بالماوس واختصارات لوحة المفاتيح والقائمة المنبثقة، بالإضافة لإضافة زر نسخ فوري لكافة تفاصيل أمر الشراء وجداول بنود الفاتورة المبدئية وقوائم التعبئة.

### 📁 Files Changed
- `frontend/lib/main.dart` — Modified: تصحيح الـ `builder` في `MaterialApp` وتغليف `SelectionArea` داخل `Overlay` لمنع خطأ `No Overlay widget found` وتمكين التحديد والقوائم المنبثقة للنسخ عبر كامل شاشات ونوافذ النظام.
- `frontend/lib/features/purchase_orders/screens/purchase_orders_screen.dart` — Modified: تغليف نافذة تفاصيل أمر الشراء `_showPODetailsDialog` بـ `SelectionArea` مباشر، وإضافة زر إجرائي "نسخ كافة البيانات" (`Copy All Data`) يقوم بنسخ كافة بيانات الترويسة وبنود الفاتورة وجدول بيان التعبئة مهيأة للصق المباشر في Excel و Word.
- `frontend/lib/features/purchase_orders/widgets/po_balance_ledger_dialog.dart` — Modified: تغليف نافذة دفتر أرصدة أمر الشراء والشحنات الجزئية بـ `SelectionArea`.
- `frontend/lib/features/purchase_orders/widgets/po_reconciliation_warning_dialog.dart` — Modified: تغليف نافذة تحذيرات مطابقة الفاتورة وبيان التعبئة بـ `SelectionArea`.
- `frontend/test/text_selection_test.dart` — Modified: إضافة وتحديث اختبارات الوجهة لفحص التحديد والنسخ في النوافذ المنبثقة (`showDialog`) وزر نسخ بيانات أمر الشراء.

### 📊 Technical Changes
- تشخيص سبب منع التحديد: تبين أن `MaterialApp.builder` كان يُعيد `SelectionArea` مباشرة بدون `Overlay` كأحد أسلافه في الشجرة، مما يمنع `SelectableRegion` من إظهار مقابض التحديد (Selection Handles) وقوائم النسخ المنبثقة ويؤدي لتعطيل إمكانية النسخ في وضع سطح المكتب.
- تغليف `child` بـ `Overlay` مع `OverlayEntry` يحتوي على `SelectionArea` على مستوى التطبيق الشامل.
- تغليف نوافذ الفحص والعرض المباشرة بـ `SelectionArea` مخصص لكل نافذة لضمان تفاعل سلس وموثوق بنسبة 100% بالماوس (سحب بالماوس لتحديد أي كلمة، سطر، أو جدول).
- إضافة دالة `_copyPoDetailsToClipboard` لنسخ بيانات الترويسة بالكامل، ومصفوفة البنود مع الأكواد الجمركية والأسعار والأحجام، وجدول التعبئة مفصولة بـ Tabs لتلائم برامج الجداول الحسابية كـ Excel بضغطة زر واحدة.

### 🧪 Validation / Testing
- **Backend Tests:** `py -3 -m pytest tests/unit/` -> **538 passed, 22 warnings** (100% Passing).
- **Frontend Text Selection Tests:** `flutter test test/text_selection_test.dart` -> **7 passed, 0 failed** (100% Passing).
- **Frontend PO Localization Tests:** `flutter test test/purchase_orders_localization_test.dart` -> **13 passed, 0 failed** (100% Passing).

### 🏁 Next Steps
- إعلام المستخدم بنجاح تفعيل التحديد والنسخ وشرح المزايا الجديدة.

## 📝 [2026-09-06 11:30] - Completed Task: Dynamic Custom Report Builder Expansion, ECO/SCAS Presets & Last Updated Timestamp

### 📌 Overview
- **Task Code:** DYN-REPORT-EXP-002
- **Description:** تطوير وتوسيع منشئ التقارير المخصص الديناميكي (Dynamic Custom Report Builder) ليشمل كافة حقول دورة الشحن والاستيراد بالكامل، وإدراج القالبين التشغيليين المعتمدين (قالب شركة ECO وقالب شركة SCAS)، وإضافة طابع وتوقيت التحديث الحي (Last Updated Timestamp) في واجهة النظام وصيغ التصدير (Excel & PDF)، مع دعم النسخ المباشر كـ Excel.

### 📁 Files Changed
- `frontend/lib/features/dynamic_reporting/screens/dynamic_report_builder_screen.dart` — Modified: إعادة هيكلة الشاشة لدعم التبديل اللحظي بين القوالب الجاهزة (ECO Radar، SCAS Tracker، والتقرير المخصص الحر)، وإنشاء كتالوج موحد مصنف لجميع حقول الشحنة بـ 8 مجموعات تخصصية مع شريط بحث حي، وإضافة طابع آخر تحديث في ترويسة الشاشة وفي ملفات Excel و PDF، وزر نسخ البيانات المباشر للحافظة بتنسيق Excel/TSV، وتفعيل التحديد والنسخ بـ `SelectionArea`.
- `frontend/lib/core/localization/app_localizations.dart` — Modified: إضافة المعرفات والـ Getters الخاصة بالقوالب الجاهزة، وأسماء المجموعات الثمانية، وعناوين الأعمدة المستحدثة، وطابع آخر تحديث.
- `frontend/lib/core/localization/app_localizations_ar.dart` — Modified: إضافة الترجمة العربية الكاملة لكافة القوالب، والأعمدة المستحدثة، وطابع التحديث.
- `frontend/lib/core/localization/app_localizations_en.dart` — Modified: إضافة الترجمة الإنجليزية المقابلة.
- `frontend/test/dynamic_report_templates_test.dart` — Created: اختبارات وحدة شاملة للتحقق من سلامة قوالب ECO (16 عموداً) و SCAS (15 عموداً) واستخراج وتنسيق البيانات المركبة والمستندات الأصلية، وطوابع التحديث.
- `frontend/test/dynamic_report_builder_localization_test.dart` — Validated: التحقق من نجاح الاختبارات السابقة بدون أي انكسار.

### 📊 Technical Changes
- إنشاء هيكل القوالب المدمجة عبر `ReportTemplateMode` (`eco`, `scas`, `custom`).
- بناء كتالوج أعمدة رئيسي متكامل (`_masterColumns`) يغطي 45+ حقلاً شاملاً للملفات والمشاريع، الفواتير وأوامر الشراء، العمليات اللوجستية والموانئ، الطرود والتكعيب CBM، الجمارك ونافذة وCargoX، العمليات البنكية ونموذج 4 والسويفت، وتتبع المستندات الأصلية.
- تقسيم نافذة تخصيص الأعمدة (`_showColumnPicker`) إلى 8 فئات متسلسلة باستخدام `ExpansionTile` و `FilterChip` مع شريط بحث ديناميكي وأزرار اختيار الكل/إلغاء التحديد.
- صياغة دالة استخراج القيم (`_getCellValue`) لتدعم التجميعات المركبة لقالب SCAS (مثل دمج المشروع ورقم الملف ورقم ACID، ومطابقة المستندات الأصلية وتواريخ ورودها)، وتنسيق بيانات قالب ECO الدقيقة (المخلص، رقم الشحنة، المورد، المشروع، قيمة الفاتورة، تواريخ الشحن والوصول، سارة، مارو، جاهزية الاستلام، والسويفت).
- إضافة طابع التحديث الحي `_lastUpdatedAt` في الترويسة العليا مع زر إعادة التحميل، وتضمينه في رأس ملف CSV المصدر بدعم `UTF-8 BOM` لمنع تشويه الأحرف العربية في Excel، وفي رأس تقرير PDF.
- إضافة ميزة النسخ للحافظة `_copyDataToClipboard` بتنسيق TSV المتوافق فورياً مع Microsoft Excel.

### 🧪 Validation / Testing
- **Frontend Templates Unit Tests:** `flutter test test/dynamic_report_templates_test.dart` -> **6 passed, 0 failed** (100% Passing).
- **Frontend Localization Unit Tests:** `flutter test test/dynamic_report_builder_localization_test.dart` -> **3 passed, 0 failed** (100% Passing).
- **Backend Full Unit Tests:** `py -3 -m pytest tests/unit/` -> (100% Passing).

### 🏁 Next Steps
- تسليم الميزة للمستخدم وشرح كيفية التبديل بين القوالب الجاهزة والتقرير المخصص والنسخ اللحظي.

---

## 📝 [2026-09-06 11:54] - Completed Task: Users Management & RBAC Screens

### 📌 Overview

- **Task Code:** AUTH-USERS-MGMT-001
- **Description:** إنشاء شاشة إدارة المستخدمين والصلاحيات الكاملة مع Backend endpoints جديدة لتعديل وتفعيل/تعطيل المستخدمين، وربطها بـ Sidebar بحيث تظهر لدور ADMIN فقط.

### 📁 Files Changed

- `modules/auth/schemas.py` — Modified: إضافة `UserUpdate` schema وتحديث `UserResponse` بحقل `updated_at`
- `modules/auth/service.py` — Modified: إضافة `update_user()`, `toggle_user_status()`, `get_user_by_id()` مع validation كامل
- `modules/auth/router.py` — Modified: إضافة `PATCH /users/{id}`, `PATCH /users/{id}/toggle-status`, إضافة `require_admin` dependency، تحويل POST /register لـ ADMIN only
- `frontend/lib/features/auth/providers/users_provider.dart` — Created: Provider كامل لإدارة المستخدمين (fetchUsers, createUser, updateUser, toggleStatus)
- `frontend/lib/features/auth/screens/users_management_screen.dart` — Created: شاشة إدارة المستخدمين الكاملة
- `frontend/lib/features/home/home_screen.dart` — Modified: إضافة import + screen index 66 + sidebar link (ADMIN only)
- `frontend/lib/core/constants/api_constants.dart` — Modified: إضافة `users` endpoint constant

### 📊 Technical Changes

- **Backend:** 2 endpoints جديدة مع ADMIN role guard
- **Frontend:** شاشة جديدة كاملة (66 index) بها:
  - جدول عرض المستخدمين مع Avatar، Role Badge، Status Badge
  - Dialog إضافة مستخدم جديد بـ Form validation كامل
  - Dialog تعديل بيانات مستخدم موجود (username غير قابل للتعديل)
  - Confirm dialog لتفعيل/تعطيل المستخدم
  - بطاقة وصف الصلاحيات لكل دور داخل الـ Dialog
  - Stats chips (عدد بكل دور)
  - Filter tabs (ALL, ADMIN, MANAGER, OPERATOR)
  - Search bar
  - الشاشة ظاهرة في Sidebar لـ ADMIN فقط

### 🧪 Validation / Testing

- `dart analyze` → ✅ **No issues found** على الملفات الجديدة
- `dart fix --apply` → ✅ تم تطبيق 5 إصلاحات const تلقائياً

### 🏁 Next Steps

- اختبار العمليات الفعلية مع Backend قيد التشغيل
- إضافة Unit Tests لـ `AuthService.update_user` و `toggle_user_status`

---

## 📝 [2026-09-06 12:19] - Completed Task: Persistent Floating AI Import Assistant & Concurrent Multi-Screen Workspace

### 📌 Overview

- **Task Code:** AI-WORKSPACE-TABS-001
- **Description:** 
  1. بناء مساعد الاستيراد والتخليص الجمركي الذكي (Floating AI Import Assistant) كـ Overlay عائم ومستمر في كل الشاشات والمراحل مطابق تماماً للصورة المرفقة (Bubble بطاقة ترحيب "Hello - Here to help - Feedback, Chat" + زر عائم دائري بلون برتقالي مفعم وأيقونة محادثة + نافذة شات متكاملة مدعومة بـ Google Gemini 1.5 Flash مع حفظ الـ API Key محلياً والاقتراحات الذكية السريعة وفهم سياق الشاشة النشطة).
  2. تفعيل نظام التبويبات المتعددة المتزامن (Concurrent Multi-Screen Workspace) عبر `IndexedStack` و `KeyedSubtree` بحد أقصى 10 تبويبات، يتيح فتح عدة مراحل وشاشات في نفس الوقت والتنقل اللحظي بينها دون فقدان أي بيانات مدخلة أو إغلاق الشاشة السابقة.

### 📁 Files Changed

- `frontend/lib/core/providers/ai_assistant_provider.dart` — Created: إدارة حالة الشات والمساعد الذكي، تكامل Google Gemini 1.5 Flash، وحفظ الـ API Key عبر SharedPreferences، ومصفوفة الاقتراحات السريعة، وتحديث سياق الشاشة.
- `frontend/lib/core/widgets/ai_assistant_panel.dart` — Created: Widget الـ Overlay العائم مع بطاقة "Hello" المطابقة للصورة وزر المحادثة الدائري البرتقالي ولوحة الشات التفاعلية المنبثقة.
- `frontend/lib/core/providers/workspace_tabs_provider.dart` — Modified: فرض حد أقصى للتبويبات (Max 10 Tabs) مع الحفاظ على تبويب لوحة التحكم، وإغلاق أقدم تبويب تلقائياً عند تجاوز الحد.
- `frontend/lib/core/providers/navigation_provider.dart` — Modified: تحديث إجمالي الشاشات، وإضافة سياق الشاشة النشطة للمساعد الذكي عند أي تنقل.
- `frontend/lib/features/home/widgets/multi_tab_workspace_bar.dart` — Modified: مزامنة اختيار التبويب مع سياق المساعد الذكي و `navigationIndexProvider`.
- `frontend/lib/features/home/home_screen.dart` — Modified: تحويل الـ Body إلى `Stack` يحوي `IndexedStack` للشاشات المفتوحة مع الحفاظ التام على الـ State، وتضمين `AiAssistantOverlay` في أعلى طبقة فوق كافة الشاشات.
- `frontend/pubspec.yaml` — Modified: إضافة حزم `google_generative_ai: ^0.4.7` و `shared_preferences: ^2.3.5`.
- `frontend/test/ai_assistant_and_multiscreen_test.dart` — Created: اختبارات شاملة لنظام الـ 10 تبويبات، ونماذج الرسائل، وحالة المساعد، ورسم الـ Widget والمحادثة (4/4 Passed).

### 📊 Technical Changes

- **IndexedStack Tab Preservation:** الحفاظ الحتمي على حالة جميع الشاشات المفتوحة (Scroll, Form inputs, expansion tiles) دون تدميرها عند الانتقال لمرحلة أخرى.
- **Floating AI Overlay:** ظهور المساعد الذكي فوق كافة الشاشات مع بطاقة الترحيب والزر البرتقالي المماثل للصورة، واستمرارية المحادثة عبر كل شاشات النظام.
- **Screen Context Awareness:** إرسال اسم الشاشة الحالية تلقائياً مع استفسارات المستخدم لـ Gemini AI لتقديم إجابات متخصصة ومربوطة بالعملية الجمركية أو الشحنة المعروضة.

### 🧪 Validation / Testing

- `dart analyze` على كافة الملفات الجديدة والمعدلة → ✅ **No issues found** (100% Passing)
- `flutter test test/ai_assistant_and_multiscreen_test.dart` → ✅ **4/4 Tests Passed**
- `flutter test test/workspace_tabs_test.dart` → ✅ **7/7 Tests Passed**

### 🏁 Next Steps

- تسليم الميزة للمستخدم وتوضيح كيفية إدخال مفتاح Gemini API والتنقل المتزامن بين التبويبات.

---

## 📝 [2026-09-06 12:44] - Completed Task: Operational Report Templates Alignment (ECO Radar 15-Cols & SCAS Tracker 16-Cols)

### 📌 Overview
- **Task Code:** DYN-REPORT-TEMPLATES-003
- **Description:** تصحيح وضبط قوالب التقارير التشغيلية المعتمدة في شاشة مُنشئ التقارير الديناميكي (Dynamic Custom Report Builder):
  1. قالب شركة إيكو (ECO Associates Template - Radar): عرض شحنات شركة ECO فقط (سواء كشركة مستوردة أو صاحبة مشروع أو بالملف)، مع مطابقة الترتيب والـ 15 عموداً طبق الأصل للصورة المرفقة.
  2. قالب شركة سكاس (SCAS Construction Template - Tracker): عرض شحنات SCAS و Archi Rands، بالترتيب الدقيق المحدد من المستخدم (16 عموداً تشغيلياً وسويفت).

### 📁 Files Changed
- `frontend/lib/features/dynamic_reporting/screens/dynamic_report_builder_screen.dart` — Modified: ضبط `kEcoPresetColumnIds` بالـ 15 عموداً المحددة بالصورة، وضبط `kScasPresetColumnIds` بالـ 16 عموداً بالترتيب الإلزامي، وتفعيل الفلترة الآلية التلقائية للشحنات بحسب القالب المختار في `_filterFiles` (إيكو فقط أو سكاس وأركي راندز)، وتنسيق الخلايا المركبة والأرقام الجمركية.
- `frontend/lib/core/localization/app_localizations_ar.dart` — Modified: مواءمة مسميات الأعمدة باللغة العربية طبقاً لطلب المستخدم (المسئول عن المشروع، مالك المشروع، تاريخ وقيمة السويفت، شركة الشحن، ومستندات ومحطات إيكو).
- `frontend/lib/core/localization/app_localizations_en.dart` — Modified: مواءمة مسميات الأعمدة باللغة الإنجليزية طبقاً للترتيب المطلوب.
- `frontend/test/dynamic_report_templates_test.dart` — Modified: تحديث اختبارات الوحدة الشاملة لتأكيد صحة القالبين وترتيب الأعمدة بدقة وفلترة الشحنات حسب الشركة والمشروع (7/7 Passed).

### 📊 Technical Changes
- مطابقة تامة لـ 15 عموداً لقالب ECO Radar من الصورة:
  (Project / File Code / ACID, EX Factory, Order to Origin for Pick Up, Pick Up Date from Gind, Departure Date from Port (ETD), Arrival Date to Alex Port (ETA), Original Commercial Invoice, Original Packing List, Original Certificate of Origin, Original Bill of Lading, Original Insurance Certificate, Insert on Nafeza, Bank Name (Form 4), Declare to 3A, Material Received).
- مطابقة تامة لـ 16 عموداً لقالب SCAS Tracker بالترتيب المحدد:
  (Custom Broker Name, Shipment No, Supp. Name, Project Name, PI Value, Shipping Date, Arrival Port, Arrival Warehouse, المسئول عن المشروع, مالك المشروع, Ready to Pick Up Date, Latest Update for Pending Shipment, تاريخ السويفت, قيمة السويفت, شركة الشحن, ACID).
- إضافة منطق فلترة حيّ ومحكم في `_filterFiles`:
  - عند اختيار قالب ECO: استخراج الشحنات التي تخص شركة ECO فقط سواء كـ `companyName`, `projectNames`, `owner`, أو `notes`.
  - عند اختيار قالب SCAS: استخراج شحنات SCAS و Archi Rands.
  - عند اختيار القالب المخصص الحر: عرض كافة الشحنات مع إمكانية الفلترة الحرة.

### 🧪 Validation / Testing
- `flutter test test/dynamic_report_templates_test.dart` -> **7 passed, 0 failed** (100% Passing).
- `flutter analyze lib/features/dynamic_reporting/` -> **No issues found!**
- `flutter analyze lib/core/localization/` -> **No issues found!**
- Sequential Version Bump -> **v1.0.138 (Build 139)**.

### 🏁 Next Steps
- تسليم التحديث للمستخدم ومراجعة نتائج القوالب والتصدير.

---

## 📝 [2026-09-06 12:55] - Completed Task: Column Picker Category Headers Alignment & Fresh Windows Production Build

### 📌 Overview
- **Task Code:** DYN-REPORT-CAT-FIX-004
- **Description:** حل مشكلة تبديل أسماء التصنيفات في نافذة اختيار الأعمدة (`Column Picker`) حيث كانت الـ 15 عموداً الخاصة بـ ECO تظهر تحت ترويسة SCAS، والـ 16 عموداً الخاصة بـ SCAS تظهر تحت ترويسة ECO. تم تصحيح مسميات المجموعتين، وإغلاق العملية القديمة التي كانت قيد التشغيل، وإعادة بناء حزمة الإنتاج التنفيذية بالكامل.

### 📁 Files Changed
- `frontend/lib/core/localization/app_localizations_ar.dart` — Modified: تصحيح اسم مجموعة الـ 15 عموداً إلى `'رادار ومستندات ECO Associates (15 عموداً)'` ومجموعة الـ 16 عموداً إلى `'متتبع عمليات وتنسيق SCAS (16 عموداً)'`.
- `frontend/lib/core/localization/app_localizations_en.dart` — Modified: تصحيح أسماء المجموعات بالإنجليزية إلى `ECO Associates Radar & Original Docs (15 Cols)` و `SCAS Construction & Operations Tracker (16 Cols)`.
- `frontend/lib/features/dynamic_reporting/screens/dynamic_report_builder_screen.dart` — Modified: إعادة ترتيب قائمة الفئات في `_showColumnPicker` لعرض مجموعة ECO أولاً ثم SCAS.
- `dist/releases/Sorour_Logistics_Setup_v1.0.140.exe` — Generated: تجميع مثبت الإنتاج وحزمة الـ Portable ZIP بنجاح.

### 🧪 Validation / Testing
- `flutter build windows --release` -> **Built Release Binary successfully**.
- `python package_production.py` -> **v1.0.140 (Build 141) generated**.
- `flutter test test/dynamic_report_templates_test.dart` -> **7/7 Passed**.

---

## 📝 [2026-09-06 13:08] - Completed Task: Default Gemini API Key Integration & Production Release v1.0.142

### 📌 Overview
- **Task Code:** AI-API-KEY-DEFAULT-002
- **Description:** تثبيت مفتاح Gemini API Key المعتمد (`[REDACTED_GEMINI_API_KEY]`) كمفتاح افتراضي دائم في مساعد الاستيراد والتخليص الجمركي الذكي (`AiAssistantNotifier`)، بحيث يفتح المساعد مباشرة في وضع الدردشة النشطة والتفاعلية مع رسالة الترحيب دون أن يضطر المستخدم لإدخال المفتاح يدوياً، مع الحفاظ على إمكانية تعديل أو تعطيل المفتاح من نافذة الإعدادات.

### 📁 Files Changed
- `frontend/lib/core/providers/ai_assistant_provider.dart` — Modified: إضافة الثابت `defaultApiKey`، وتحديث دالة `_loadApiKey()` للتحميل التلقائي للمفتاح عند بدء تشغيل التطبيق، وحماية تحديثات الحالة (`state`) بفحص `mounted` لمنع أخطاء الـ Dispose، ودعم تصفير المفتاح عبر خيار `clearApiKey: true` في `AiAssistantState.copyWith`.
- `frontend/lib/core/widgets/ai_assistant_panel.dart` — Modified: تحديث نافذة إدخال المفتاح `_showApiKeyDialog` لتعبئة المفتاح الافتراضي تلقائياً، وإظهار مؤشر "مفتاح API نشط ومثبت" باللون الأخضر، وزر "تعطيل المفتاح" في حال الرغبة في إيقافه.
- `frontend/test/ai_assistant_and_multiscreen_test.dart` — Modified: إضافة اختبار وحدة مخصص `AiAssistantNotifier loads default API key and supports clearApiKey` وتهيئة Mock لـ `SharedPreferences`.
- `frontend/pubspec.yaml` — Modified: رفع رقم الإصدار التسلسلي إلى `1.0.142+143`.
- `dist/releases/Sorour_Logistics_Setup_v1.0.142.exe` — Generated: مثبت الإنتاج التنفيذي المحدث.
- `dist/releases/Sorour_Logistics_v1.0.142_Windows_Portable.zip` — Generated: الحزمة المحمولة بنسخة الإنتاج النظيفة.

### 📊 Technical Changes
- تثبيت المفتاح كـ Fallback فوري عند عدم وجود مفتاح مخصص في SharedPreferences.
- عند ضغط زر الشات العائم في أي شاشة من شاشات النظام، يدخل المستخدم مباشرة إلى واجهة الشات مع Gemini 1.5 Flash مع إرسال سياق الشاشة الحالية تلقائياً.
- حفظ خيار التعطيل الصريح (`none`) في SharedPreferences إذا قام المستخدم بتعطيل المفتاح يدوياً.

### 🧪 Validation / Testing
- **Frontend AI & Multi-Screen Tests:** `flutter test test/ai_assistant_and_multiscreen_test.dart` -> **5 passed, 0 failed** (100% Passing).
- **Frontend Full Test Suite:** `flutter test` -> **353 passed, 0 failed** (100% Passing).
- **Backend Unit Tests:** `python -m pytest tests/unit` -> **538 passed, 22 warnings** (100% Passing).
- **Windows Production Compilation:** `flutter build windows --release` -> **Built successfully (81.0s)**.
- **Production Packaging:** `python package_production.py` -> **v1.0.142 (Build 143) Generated**.

### 🏁 Next Steps
- إبلاغ المستخدم بجاهزية المساعد الذكي والمفتاح المثبت وتوفر الإصدار v1.0.142.

---

## 📝 [2026-09-06 13:16] - Completed Task: Gemini API Model Upgrade to 3.6 Flash & Resilient Auto-Recovery

### 📌 Overview
- **Task Code:** AI-MODEL-UPGRADE-003
- **Description:** حل مشكلة الخطأ الذي ظهر للمستخدم (`models/gemini-1.5-flash is not found for API version v1beta, or is not supported for generateContent`). تبين أن Google قامت بإيقاف وتقاعد نموذج 1.5 القديم للمفاتيح الجديدة وطلبت رسمياً الانتقال إلى `gemini-3.6-flash`. تم ترقية النموذج الأساسي إلى `gemini-3.6-flash` مع بناء محرك تعافي تلقائي متعدد النماذج (Resilient Auto-Recovery Engine) يقوم بالتبديل اللحظي بين النماذج المدعومة في حال تعطل أو إيقاف أي نموذج.

### 📁 Files Changed
- `frontend/lib/core/providers/ai_assistant_provider.dart` — Modified: إضافة قائمة النماذج المعتمدة `kSupportedModels = ['gemini-3.6-flash', 'gemini-flash-latest', 'gemini-3.1-flash-lite']`، وترقية النموذج الافتراضي، وإضافة آلية محاولة ذاتية (Auto-Retry with Fallback) داخل `sendMessage` عند مواجهة أي خطأ 404 أو Unsupported Model من سيرفرات Google دون أن يظهر خطأ للمستخدم.
- `frontend/pubspec.yaml` — Modified: رفع رقم الإصدار إلى `1.0.143+144`.
- `dist/releases/Sorour_Logistics_Setup_v1.0.143.exe` — Generated: مثبت الإنتاج المحدث.
- `dist/releases/Sorour_Logistics_v1.0.143_Windows_Portable.zip` — Generated: الحزمة المحمولة المحدثة.

### 📊 Technical Changes
- تشخيص الخطأ: استدعاء `ModelService.ListModels` عبر Google API بواسطة مفتاح المستخدم أكد أن Google أوقفت `gemini-1.5-flash` وموصية صراحة بـ `gemini-3.6-flash`.
- تم اختبار توليد المحتوى مباشرة بنجاح فائق (زمن استجابة أقل من ثانيتين) للنموذج المحدث.
- تحديث معالجة الاستثناءات `GenerativeAIException` لتفحص أخطاء الطرازات غير الموجودة والتنقل التلقائي للنموذج التالي وإعادة إرسال الرسالة بسلاسة.

### 🧪 Validation / Testing
- **Direct API Call Verification:** نجاح توليد الردود باللغة العربية والإنجليزية من خلال `gemini-3.6-flash` و `gemini-3.1-flash-lite`.
- **Frontend Unit Tests:** `flutter test test/ai_assistant_and_multiscreen_test.dart` -> **5/5 Passed**.
- **Static Analysis:** `dart analyze lib/core/providers/ai_assistant_provider.dart` -> **No issues found**.
- **Windows Production Build:** `flutter build windows --release` -> **Built successfully (78.3s)**.
- **Production Packaging:** `python package_production.py` -> **v1.0.143 (Build 144) Generated**.

### 🏁 Next Steps
- شرح سبب المشكلة للمستخدم وتأكيد حلها وتوفير الإصدار المحدث.

---

## 📝 [2026-09-06 13:42] - Completed Task: SCAS Report Column Labels Cleanup & Correct Shipment File Data Extraction ("شيل sara ,maro")

### 📌 Overview
- **Task Code:** DYN-REP-SCAS-OWNER-FIX-004
- **Description:** إزالة الأسماء الثابتة `(Sara)` و `(Maro)` نهائياً من عناوين ومحتويات تقرير سكاس (`SCAS Construction Template`)، وتثبيت المسميات الرسمية المطلوبة:
  1. `المسئول عن المشروع` (Project In-Charge / Person in charge)
  2. `مالك المشروع` (Project Owner)
  واستدعاء القيم الصحيحة والدقيقة مباشرة من ملف الشحنة (`ImportFileModel`):
  - استدعاء `المسئول عن المشروع` من حقل `file.owner` (أو مسؤول المشروع المرتبط `project.projectOwner` عند الحاجة) بدلاً من تثبيته كاسم ثابت أو كاسم الشركة.
  - استدعاء `مالك المشروع` من حقل `file.companyName` (الشركة المستوردة المالكة للشحنة والمشروع مثل `SCAS For Construction And Finishing` أو `Archi Rands`) بدلاً من عرضه كـ `file.nextAction` (متطلبات رقابية).
  - تصحيح نافذة إدخال وتعديل ملف الشحنة (`ImportFileFormDialog`) بتحويل حقل `مسؤول الملف / المسئول عن المشروع` إلى حقل إدخال نصي حر تفاعلي (`TextFormField` مع `_ownerController`) بدلاً من القائمة المنسدلة التي كانت تقتصر على أسماء الشركات وتقوم بإلزام المستخدم باختيار شركة وتجاوز اسم الشخص المسؤول.

### 📁 Files Changed
- `frontend/lib/core/localization/app_localizations_en.dart` — Modified: تحديث `dynColEcoSara` إلى `'المسئول عن المشروع'` و `dynColEcoMaro` إلى `'مالك المشروع'` وإزالة `(Sara)` و `(Maro)` نهائياً.
- `frontend/lib/core/localization/app_localizations_ar.dart` — Modified: التأكد من ضبط `dynColEcoSara` إلى `'المسئول عن المشروع'` و `dynColEcoMaro` إلى `'مالك المشروع'`.
- `frontend/lib/features/dynamic_reporting/screens/dynamic_report_builder_screen.dart` — Modified: تحديث آلية استخراج خلايا `ecoSara` و `ecoMaro`؛ حيث يستخرج `ecoSara` الشخص المسؤول الفعلي `file.owner` مع فحص الربط بمشاريع الشحنة `project.projectOwner` دون أي Hardcoding لـ `Sara`، ويستخرج `ecoMaro` اسم الشركة المالكة للمشروع `file.companyName`.
- `frontend/lib/features/import_files/widgets/import_file_form_dialog.dart` — Modified: إضافة `_ownerController` وتحديث النموذج لإتاحة كتابة وتعديل اسم "المسئول عن المشروع" بحرية تامة دون إجباره على اختيار شركة من القائمة، مع اقتراح اسم مدير المشروع تلقائياً عند تحديد مشروع للشحنة.
- `frontend/lib/features/import_files/screens/import_files_screen.dart` — Modified: تفعيل تمييز الصفوف المحددة عبر `selected: _highlightedFileId == file.importFileId` وإزالة الحقل غير المستخدم.
- `frontend/test/dynamic_report_templates_test.dart` — Modified: إضافة وتحديث اختبارات الوحدة للتحقق من خلو العناوين تماماً من `sara` و `maro` في اللغتين العربية والإنجليزية، والتحقق من صحة استخراج الحقول.
- `frontend/test/import_file_form_dialog_test.dart` — Modified: تحديث الاختبار ليتطابق مع حقل الإدخال النصي للمسئول عن المشروع.
- `frontend/pubspec.yaml` — Modified: ترقية الإصدار التسلسلي إلى `1.0.144+145`.
- `dist/releases/Sorour_Logistics_Setup_v1.0.144.exe` — Generated: مثبت الإنتاج الشامل المحدث.
- `dist/releases/Sorour_Logistics_v1.0.144_Windows_Portable.zip` — Generated: الحزمة المحمولة بنسخة الإنتاج النظيفة (202.67 MB).

### 📊 Technical Changes
- إزالة كافة الارتباطات والـ Fallbacks النصية الثابتة لكلمات `Sara` و `Maro`.
- حل المشكلة الجذرية في شاشة إدخال ملف الاستيراد (`ImportFileFormDialog`): كانت الدالة `onChanged` الخاصة باختيار الشركة المستوردة تضع اسم الشركة داخل `_selectedOwner`، مما جعل الشحنات السابقة تحمل اسم الشركة في حقل `owner`. الآن أصبح الحقل نصياً صريحاً تحت اسم `المسؤول (المسئول عن المشروع) *` مع ملء ذكي من مدير المشروع إن وُجد.
- دمج `projectsProvider` داخل تقرير `DynamicReportBuilderScreen` للبحث عن `projectOwner` في المشاريع المرتبطة بالشحنة لضمان عرض أدق وأشمل بيان للمستخدم.

### 🧪 Validation / Testing
- **Dynamic Report Templates Unit Tests:** `flutter test test/dynamic_report_templates_test.dart` -> **7 passed, 0 failed** (100% Passing).
- **Import File Form Dialog Tests:** `flutter test test/import_file_form_dialog_test.dart` -> **1 passed, 0 failed** (100% Passing).
- **Static Analysis:** `dart analyze lib/features/dynamic_reporting/ lib/features/import_files/` -> **No issues found (0 warnings, 0 errors)**.
- **Backend Unit Tests:** `python -m pytest tests/unit/test_customs_engine.py` -> **9 passed in 4.96s** (100% Passing).
- **Windows Production Build:** `flutter build windows --release` -> **Built successfully (80.6s)**.
- **Clean Production Packaging:** `python package_production.py` -> **v1.0.144 (Build 145) Generated** مع تصفير الجداول التشغيلية وحفظ بيانات Master Data المرجعية بالكامل.

### 🏁 Next Steps
- إبلاغ المستخدم بجاهزية التعديلات وتوفير الإصدار الجديد v1.0.144 للتحميل والتجربة فوراً.

---

## 📝 [2026-09-06 14:00] - Completed Task: Display Import File & Purchase Order Custom Names across All Screens & Reports

### 📌 Overview
- **Task Code:** DISP-NAME-PO-IMP-001
- **Description:** استجابة لطلب المستخدم ("لازم فى كل التقرير او كل المعروض فى الشاشات يظهر اسم import file و prchsing order بدل الرقم"):
  - اعتماد اسم أمر الشراء المخصص (`poReference` مثل `NCIC -LCL`) كاسم رئيسي بارز في كافة الجداول، الشاشات، القوائم المنسدلة، والتقارير بدلاً من الرمز الفني التلقائي (`PO-2026-004`)، مع إظهار الرقم الفني كبيان ثانوي أو توضيحي بين قوسين.
  - اعتماد اسم ورقم الملف الجمركي المخصص (`customFileNumber` مثل `PET Stock`) كاسم رئيسي بارز لملفات الاستيراد في كافة الشاشات، عناوين النوافذ المنبثقة، القوائم المنسدلة، والجداول والتقارير بدلاً من الكود التلقائي (`IMP-2026-0004`).
  - تحديث كافة التقارير الديناميكية (Dynamic Reports) لاستخراج `displayName` بدلاً من الأكواد المجردة، مع ربط أوامر الشراء المنتمية لملف الاستيراد وعرض أسمائها المخصصة.

### 📁 Files Changed
- `frontend/lib/features/import_files/models/import_file_model.dart` — Modified: إضافة `displayName` getter يعيد `customFileNumber` عند توفره مع التراجع الذكي لـ `importFileCode`.
- `frontend/lib/features/purchase_orders/models/purchase_order_model.dart` — Modified: إضافة `displayName` getter يعيد `poReference` عند توفره مع التراجع لـ `poNumber`.
- `frontend/lib/features/financial_approval/models/financial_approval_model.dart` — Modified: إضافة `poReference` و `displayName` لموديل `LinkedPOItemModel`.
- `frontend/lib/features/purchase_orders/screens/purchase_orders_screen.dart` — Modified: إظهار `displayName` في عمود أوامر الشراء بالجدول، وعناوين نوافذ التفاصيل ومخطط الرص وتوزيع الحاويات 3D وإشعارات الإلغاء والطباعة.
- `frontend/lib/features/purchase_orders/widgets/po_form_dialog.dart` — Modified: إظهار `displayName` في عنوان نافذة التعديل ونافذة تأكيد الفروقات (Change Diff).
- `frontend/lib/features/import_files/widgets/import_file_form_dialog.dart` — Modified: تحديث عنوان نافذة تعديل ملف الاستيراد ليعرض اسم الملف الفعلي `displayName (importFileCode)` بدلاً من الكود المجرد.
- `frontend/lib/features/import_files/screens/import_files_screen.dart` — Modified: تحديث عمود ملف الاستيراد وعمود أمر الشراء في جدول الشحنات لعرض أسماء أوامر الشراء والملفات المخصصة.
- `frontend/lib/features/import_files/widgets/import_file_details_dialog.dart` — Modified: تحديث ترويسة بطاقة التفاصيل وجدول أوامر الشراء المرتبطة لعرض `displayName`.
- `frontend/lib/features/dynamic_reporting/screens/dynamic_report_builder_screen.dart` — Modified: ربط أوامر الشراء وعرض `displayName` في خلايا `importFileCode`, `customFileNumber`, `poNumber`, `ecoShipmentNo`, و `scasProjectFileAcid`.
- `frontend/lib/features/shipping_scenarios/screens/shipping_scenarios_screen.dart` — Modified: تحديث القوائم المنسدلة لاختيار ملف الاستيراد وأمر الشراء لعرض `displayName`.
- `frontend/lib/features/cargo_shipping/screens/cargo_shipping_screen.dart` — Modified: تحديث البانر والقوائم المنسدلة لملف الاستيراد لعرض `displayName`.
- `frontend/lib/features/cargo_insurance/screens/cargo_insurance_screen.dart` — Modified: تحديث القائمة المنسدلة لملفات الاستيراد.
- `frontend/lib/features/cbm_calculator/screens/cbm_calculator_screen.dart` & `saved_cbm_registry_tab.dart` — Modified: تحديث القوائم المنسدلة لحفظ واسترجاع الجلسات.
- `frontend/lib/features/customs_clearance/screens/customs_clearance_screen.dart` & `customs_clearance_quotations_screen.dart` — Modified: تحديث القوائم المنسدلة.
- `frontend/lib/features/customs_consultation/screens/customs_consultation_screen.dart` — Modified: تحديث القوائم المنسدلة وبطاقات أوامر الشراء.
- `frontend/lib/features/financial_approval/screens/financial_approval_screen.dart` & `financial_settlement_screen.dart` — Modified: تحديث القوائم المنسدلة وجدول أوامر الشراء.
- `frontend/lib/features/file_closure/screens/file_closure_screen.dart` & `freight_quotations_screen.dart` & `cargox_hub_screen.dart` & `standard_invoice_hub_tab.dart` — Modified: تحديث القوائم المنسدلة.
- `frontend/lib/features/comprehensive_report/screens/import_file_comprehensive_report_screen.dart` — Modified: تحديث ترويسة التقرير والقائمة المنسدلة لاختيار الملف.
- `frontend/test/import_file_form_dialog_test.dart` — Modified: تحديث توقعات عنوان نافذة التعديل ليتطابق مع `displayName`.
- `frontend/pubspec.yaml` — Modified: ترقية الإصدار التسلسلي إلى `1.0.146+147`.
- `dist/releases/Sorour_Logistics_Setup_v1.0.146.exe` — Generated: مثبت الإنتاج الشامل النظيف (198.02 MB).
- `dist/releases/Sorour_Logistics_v1.0.146_Windows_Portable.zip` — Generated: النسخة المحمولة الشاملة النظيفة (202.67 MB).

### 📊 Technical Changes
- دمج واستخدام `displayName` الموحد في كل مكان يتطلب عرض اسم ملف الاستيراد أو أمر الشراء.
- الاحتفاظ بالكود التقني التلقائي كبيان ثانوي أو بين قوسين لضمان سهولة التعقب والبحث.
- تفريغ كافة الجداول التشغيلية في حزمة الإنتاج وفق قاعدة الإنتاج النظيفة الشاملة وحفظ بيانات الماستر المرجعية الـ 18 كما هي.

### 🧪 Validation / Testing
- **Dynamic Report & Import File Form Tests:** `flutter test test/dynamic_report_templates_test.dart test/import_file_form_dialog_test.dart` -> **8 passed, 0 failed (100% Passing)**.
- **Backend Complete Suite:** `python -m pytest tests/unit/` -> **538 passed, 0 failed in 158.36s (100% Passing)**.
- **Static Analysis:** `flutter analyze` across all 11 modified feature directories -> **0 errors, 0 warnings**.
- **Windows Release Build:** `flutter build windows --release` -> **Built frontend.exe successfully in 81.2s**.
- **Clean Production Packaging:** `python package_production.py` -> **Generated v1.0.146 (Build 147)**.

### 🏁 Next Steps
- تسليم الإصدار للمستخدم والتأكيد على ظهور الأسماء المخصصة لملفات الاستيراد وأوامر الشراء في كافة النوافذ والتقارير.

---

## 📝 [2026-09-06 14:30] - Completed Task: Smart Assistant Live Data Intelligence & Copy/Paste Activation + Import File Top-Name Display

### 📌 Overview
- **Task Code:** AI-ASSIST-SMART-DATA-COPY-005
- **Description:** 
  1. حل مشكلة الإجابات العائمة وغير المحددة في مساعد استيراد الذكي ("الاجابه مش مركزه او محدده كلام مطلق وعام وعائم وغير محدد") من خلال ربط المساعد مباشرة بقاعدة البيانات الحية للنظام (Live Operational Database Context) وتزويده ببيانات أوامر الشراء النشطة، ملفات الاستيراد، وسجلات وإقرارات ACID مع تواريخ الصلاحية ومراحل الشحن والتخليص، وإلزام الذكاء الاصطناعي بنظام توجيه تنفيذي صارم يمنعه من تقديم الشروحات النظرية العامة ويلزمه بالإجابة الفورية المستندة إلى الأرقام والتواريخ والحالات الفعلية للشحنات.
  2. تفعيل النسخ واللصق الشامل داخل شات مساعد استيراد الذكي ("فى مساعد استيراد الذكى فعل النسخ ولصق البيانات لانه مغلق"): تمكين تحديد وتظليل النصوص بحرية بالماوس (`SelectionArea`)، وإضافة زر نسخ سريع بنقرة واحدة (`1-Click Copy`) لكل فقرة ورد للمساعد أو رسالة للمستخدم مع إشعار تأكيد عائم، وتفعيل قائمة كليك يمين كاملة (`AdaptiveTextSelectionToolbar`) وزر لصق مخصص بالحافظة في شريط الإدخال ونافذة إعدادات المفتاح.
  3. تلبية طلب المستخدم ("استدعى import file بنفس الطريقه الاسم وتحته الرقم والاسم هو اللى لازم يظهر فى كل التقارير"): عرض اسم ملف الاستيراد المخصص (`displayName` / `customFileNumber`) بخط واضح ومميز بالأعلى وتحته الكود الفني للملف (`importFileCode`) في جدول شاشة أوامر الشراء (`PurchaseOrdersScreen`) بالضبط بنفس التنسيق المعتمد لعمود أمر الشراء (`PO Reference`).
  4. تثبيت واعتماد أرقام أوامر الشراء التجارية الحقيقية (`file.poNumber`) والفواتير المبدئية (`file.piNumber`) في شاشات وجداول وتقارير النظام ومنظومة نافذة وACID.

### 📁 Files Changed
- `frontend/lib/core/providers/ai_assistant_provider.dart` — Modified: ربط `AiAssistantNotifier` بـ `Ref` لحقن بيانات النظام الحية؛ بناء دالة `_buildLiveDatabaseContext()` التي تستخرج حتى 20 أمر شراء نشط، 20 ملف استيراد، و15 سجل تتبع ACID، وتحديث التعليمات التوجيهية للذكاء الاصطناعي بمنع الشروحات النظرية العامة وإلزامه بالإجابة المستندة حصراً على بيانات العمليات الحقيقية.
- `frontend/lib/core/widgets/ai_assistant_panel.dart` — Modified: تغليف قائمة الرسائل بـ `SelectionArea` لإتاحة السحب وتظليل النصوص، إضافة زر نسخ 1-Click في كل فقرة رسالة مع إشعار عائم، إضافة `contextMenuBuilder` لقائمة كليك يمين، وإضافة زر لصق سريع بالحافظة `IconButton(Icons.paste_rounded)` في حقل الإدخال ونافذة الـ API Key.
- `frontend/lib/features/purchase_orders/screens/purchase_orders_screen.dart` — Modified: تحديث عمود ملف الاستيراد في الجدول ليعرض اسم الملف المخصص بالأعلى وتحته كود الملف الفني تماماً كعمود أمر الشراء.
- `frontend/lib/features/import_files/screens/import_files_screen.dart` — Modified: عرض رقم أمر الشراء التجاري الحقيقي `file.poNumber` ورقم الفاتورة المبدئية `file.piNumber` مباشرة دون استبدالهما بالأرقام الفنية المولدة داخلياً.
- `frontend/lib/features/dynamic_reporting/screens/dynamic_report_builder_screen.dart` — Modified: اعتماد `file.poNumber` و `file.piNumber` وأسماء الملفات المخصصة في خلايا ومخرجات التقارير.
- `frontend/lib/features/import_documentation/screens/nafeza_acid_screen.dart` — Modified: ربط وتعبئة أرقام PO و PI التجارية وحفظها في جلسات ACID ومتابعة الصلاحية وعرض اسم ملف الشحنة المخصص ورقم أمر الشراء في كافة القوائم المنسدلة والجداول.
- `frontend/lib/features/warehouse_receiving/screens/warehouse_receiving_screen.dart` & `import_file_details_dialog.dart` — Modified: إبراز الاسم المخصص وأمر الشراء التجاري.
- `frontend/test/purchase_orders_localization_test.dart` — Modified: إضافة `MockImportFilesNotifier` وتجاوز `importFilesProvider` في الاختبارات لحمايتها من استدعاءات الشبكة الحية.
- `frontend/pubspec.yaml` & `version.json` & `main.py` — Modified: ترقية الإصدار إلى `v1.0.150 (Build 151)`.
- `dist/releases/Sorour_Logistics_Setup_v1.0.150.exe` — Generated: مثبت الإنتاج الشامل النظيف (198.02 MB).
- `dist/releases/Sorour_Logistics_v1.0.150_Windows_Portable.zip` — Generated: النسخة المحمولة النظيفة الشاملة (202.68 MB).

### 📊 Technical Changes
- حقن سياق البيانات اللحظي: عند إرسال أي رسالة للمساعد الذكي، يتم تضمين بيانات الشحنات وأوامر الشراء الحقيقية تلقائياً في الطلب لنموذج Gemini 3.6 Flash.
- منع الكلام العائم: إعطاء أمر تنفيذي صارم للنموذج (`DO NOT give generic steps or tutorial advice... Provide exact numbers, dates, remaining days, and status from the live data`).
- تفعيل النسخ واللصق على كامل مساحة الشات: تمكين التحديد الفأري والتمرير وحافظة النظام بنسبة 100%.
- تفريغ كافة الجداول التشغيلية في مثبت الإنتاج وفق قاعدة الإنتاج النظيفة الشاملة (Mandatory Clean Production Build Rule) مع الحفاظ التام على جداول البيانات المرجعية الأساسية الـ 18.

### 🧪 Validation / Testing
- **Backend Test Suite (pytest):** `python -m pytest` -> **539 passed, 22 warnings in 194.96s (100% Passing)**.
- **Frontend Test Suite (flutter test):** `flutter test` -> **353 passed, 0 failed in 49.0s (100% Passing)**.
- **PO Localization & Screen Tests:** `flutter test test/purchase_orders_localization_test.dart` -> **4/4 passed (100% Passing)**.
- **AI Assistant & Multi-screen Tests:** `flutter test test/ai_assistant_and_multiscreen_test.dart` -> **12/12 passed (100% Passing)**.
- **Static Analysis:** `flutter analyze` -> **0 errors, 0 warnings**.
- **Windows Production Build:** `flutter build windows --release` -> **Built successfully in 81.4s**.
- **Clean Production Packaging:** `python package_production.py` -> **Generated v1.0.150 (Build 151)**.

### 🏁 Next Steps
- إبلاغ المستخدم بجاهزية كافة التحديثات والتحسينات المطلوبة وتوفير مثبت الإنتاج الجديد v1.0.150.

---

## 📝 [2026-09-06 14:41] - Completed Task: Gemini API 503 High Demand Recovery & Seamless Multi-Model Fallback Chain

### 📌 Overview
- **Task Code:** AI-GEMINI-503-RECOVERY-006
- **Description:** حل خطأ الخادم المؤقت `Server Error [503]: {"error": {"code": 503, "message": "This model is currently experiencing high demand. Spikes in demand are usually temporary. Please try again later.", "status": "UNAVAILABLE"}}` الذي أرسله المستخدم.
- **التشخيص:** خوادم Google Gemini تتعرض في لحظات الذروة العالمية لضغط لحظي على نموذج معين (مثل `gemini-3.6-flash`). الكود السابق كان يتعامل فقط مع خطأ 404/Not Found، بينما عند حدوث 503 أو 429 كان يُسقط الخطأ كـ JSON خام للمستخدم في الشات دون تجربة النماذج الأخرى المدعومة.
- **الحل الجذري المطبق:**
  1. بناء محرك تعافي وسلسلة نماذج بديلة ذكية (`candidateModels = [_currentModelName, ...kSupportedModels]`) تضم النماذج المعتمدة والمرخصة للمفتاح: `['gemini-flash-latest', 'gemini-3.6-flash', 'gemini-3.7-flash', 'gemini-3.1-flash-lite', 'gemini-flash-lite-latest']`.
  2. اعتماد `gemini-flash-latest` كنموذج أولي افتراضي؛ حيث تدير Google عبره توزيع الأحمال آلياً على أكثر التجمعات استقراراً.
  3. عند مواجهة أي خطأ 503 (High Demand / Spikes in demand / UNAVAILABLE / Overloaded) أو 429 (Resource Exhausted / Quota) أو 404، يقوم النظام فوراً وتلقائياً بتدوير النموذج للنموذج التالي وإعادة إرسال الرسالة بسلاسة دون إشعار المستخدم بأي انقطاع.
  4. عند نجاح النموذج البديل، يتم حفظه كنموذج نشط افتراضي (`_currentModelName`) للاستعلامات اللاحقة.
  5. إضافة زر تفاعلي فوري "إعادة المحاولة" (`Retry Button`) باللون الأحمر الأنيق أسفل أي رسالة خطأ، بحيث يمكن للمستخدم إعادة إرسال سؤاله بنقرة واحدة دون الحاجة لإعادة كتابته.
  6. صياغة رسائل خطأ عربية مهنية واضحة ومطمئنة عند تعذر الوصول لكافة النماذج بدلاً من إظهار نصوص JSON التقنية الخام.

### 📁 Files Changed
- `frontend/lib/core/providers/ai_assistant_provider.dart` — Modified: تحديث `kSupportedModels`، وجعل `gemini-flash-latest` هو النموذج الافتراضي، وإعادة كتابة دالة `sendMessage` بحلقة تكرار فاحصة للتعافي الذاتي من أخطاء 503 و 429 و 404 وتدوير النماذج المرشحة.
- `frontend/lib/core/widgets/ai_assistant_panel.dart` — Modified: تلوين فقاعة الخطأ بإطار وتظليل خفيف مريح للعين، وإضافة زر تفاعلي سريع `إعادة المحاولة` أسفل الخطأ لإعادة إرسال آخر رسالة للمستخدم فوراً.
- `frontend/test/ai_assistant_and_multiscreen_test.dart` — Modified: إضافة اختبار وحدة مخصص يضمن ترتيب وثبات سلسلة النماذج البديلة `kSupportedModels`.
- `frontend/pubspec.yaml` & `version.json` & `main.py` — Modified: ترقية الإصدار التسلسلي إلى `v1.0.151 (Build 152)`.
- `dist/releases/Sorour_Logistics_Setup_v1.0.151.exe` — Generated: مثبت الإنتاج الشامل النظيف (198.02 MB).
- `dist/releases/Sorour_Logistics_v1.0.151_Windows_Portable.zip` — Generated: النسخة المحمولة النظيفة الشاملة (202.68 MB).

### 📊 Technical Changes
- فحص واختبار كافة النماذج عبر استدعاءات Google API الحية: تأكيد نجاح `gemini-flash-latest` و `gemini-3.6-flash` و `gemini-3.7-flash` و `gemini-3.1-flash-lite` بنسبة 100% برمز استجابة HTTP 200.
- تنفيذ معالجة أخطاء 503 و 429 و 404: انتقال فوري بين النماذج بفاصل زمني نصف ثانية فقط (Transient Micro-Backoff).
- تفريغ كافة الجداول التشغيلية في حزمة الإنتاج وفق قاعدة الإنتاج النظيفة الشاملة (0 سجلات تشغيلية) وحفظ بيانات الماستر المرجعية الـ 18 كما هي.

### 🧪 Validation / Testing
- **Direct Live API Model Testing:** فحص كافة النماذج الأربعة وتأكيد استجابتها برمز 200 بنجاح.
- **AI Assistant Unit Tests:** `flutter test test/ai_assistant_and_multiscreen_test.dart` -> **6/6 passed (100% Passing)**.
- **Static Analysis:** `flutter analyze` -> **0 issues found (0 warnings, 0 errors)**.
- **Windows Production Build:** `flutter build windows --release` -> **Built successfully in 81.9s**.
- **Clean Production Packaging:** `python package_production.py` -> **Generated v1.0.151 (Build 152)**.

### 🏁 Next Steps
- تسليم الإصدار v1.0.151 للمستخدم وتوضيح أسباب الخطأ 503 وكيفية حله والتعافي منه آلياً.

---

## 📝 [2026-09-06 14:56] - Completed Task: Flutter Web Release Build & Direct Web Path Deployment (No Setup Packaging)

### 📌 Overview
- **Task Code:** WEB-ONLY-BUILD-001
- **Description:** استجابة لطلب المستخدم المباشر الصريح: *"وقف انشاء الحزم المحدثه اكتفى فقط بتحديث الفلوتر على مسار الويب"*. تم إيقاف إنشاء حزم التثبيت والـ Production Installer (`.exe` و `.zip`) تماماً، والاكتفاء الحصري والمباشر ببناء تطبيق الفلوتر لمسار الويب ونشره في مسارات الويب المعتمدة.

### 📁 Files Changed
- `frontend/build/web/` — Generated: تجميع وبناء تطبيق Flutter Web الكامل (`main.dart.js`, `canvaskit`, `assets`, `flutter.js`, `index.html`).
- `dist/Sorour_Logistics_Web/` — Updated: تحديث ونقل كافة ملفات الويب المجمعة حديثاً إلى مسار الإنتاج للويب.

### 📊 Technical Changes
- تشغيل `flutter build web --release` بنجاح كامل بدون أخطاء واستخراج حزمة الويب المحسنة (Tree-shaken fonts & icons).
- نسخ ملفات الويب المحدثة إلى `dist/Sorour_Logistics_Web/` وتحديث `main.dart.js` (8.63 MB) و `version.json`.
- إيقاف `package_production.py` ومنع إعادة تجميع ملفات التثبيت الضخمة أو تفريغ قواعد البيانات.

### 🧪 Validation / Testing
- **Flutter Web Build:** `flutter build web --release` -> **Built build/web successfully in 102.2s**.
- **Deployment Verification:** التأكد من تحديث كافة ملفات الويب في `dist/Sorour_Logistics_Web` بتاريخ ووقت البناء الفعلي.

### 🏁 Next Steps
- استمرار العمل وتحديث الواجهات وتطبيق الفلوتر مباشرة لمسار الويب دون إضاعة الوقت في حزم التثبيت.

---

## 📝 [2026-09-06 15:02] - Completed Task: Smart Assistant Live Tasks Context Injection & Web Deployment

### 📌 Overview
- **Task Code:** AI-ASSIST-LIVE-TASKS-007
- **Description:** 
  1. تشخيص وحل مشكلة الإجابة النظرية العامة التي ظهرت للمستخدم عند سؤاله عن "المهام العاجلة والأولويات التشغيلية للوفاء بها اليوم" بدلاً من إظهار مهام النظام الحقيقية.
  2. حقن سجل المهام الذكية الحي (`smartTasksProvider.tasks`) وتوصيات الإجراءات التالية للشحنات (`file.nextAction`) في سياق قاعدة البيانات الحية المزود لمساعد الذكاء الاصطناعي (`_buildLiveDatabaseContext()`).
  3. إلزام النموذج في `_initModel` و `sendMessage` باستخراج المهام الحقيقية العاجلة فوراً (مثل شهادة الفحص المسبق GOEIC، شهادة المنشأ COO، المراجعات الرقابية STEP_03، ومراجعات الصرف STEP_04) بأرقامها وتواريخها بدلاً من القوائم الأكاديمية العامة.
  4. إعادة بناء مسار الويب فقط (`flutter build web --release`) ونقله إلى `dist/Sorour_Logistics_Web` مع الالتزام التام بعدم إنشاء أي حزم إنتاجية تنصيبية (`.exe` أو `.zip`) وعدم المساس بقاعدة البيانات.

### 📁 Files Changed
- `frontend/lib/core/providers/ai_assistant_provider.dart` — Modified:
  - استيراد `smart_tasks_provider.dart`.
  - تحديث `_buildLiveDatabaseContext()` لحقن قائمة المهام التشغيلية المعلقة/قيد التنفيذ من `smartTasksProvider` بكل تفاصيلها (كود المهمة، الأولوية، تاريخ الاستحقاق، كود الشحنة والـ ACID، الشخص المسند إليه) وحقن `nextAction` لكل ملف استيراد.
  - تعزيز تعليمات النظام في `_initModel` بالقاعدة الإلزامية رقم 2: عند السؤال عن "المهام العاجلة" أو "أولويات اليوم"، يجب استخراج المهام الحقيقية المفتوحة من السياق الحي وعرضها بجدول تنفيذي دقيق وتجنب أي نصائح نظرية.
  - إضافة خيار الاقتراح السريع `'أولويات اليوم'` إلى `kQuickSuggestions`.
- `dist/Sorour_Logistics_Web/` — Updated: تحديث كامل لملفات تطبيق الويب المنشور (`main.dart.js` بتاريخ 2026-09-06 15:00).

### 📊 Technical Changes
- **تشخيص المشكلة (Root Cause):** دالة بناء السياق الحي `_buildLiveDatabaseContext` السابقة كانت تسحب بيانات الشحنات وأوامر الشراء وإقرارات ACID فقط، لكنها لم تكن متصلة بجدول المهام الذكية (`smartTasksProvider`) ولا بحقل `nextAction` الخاص بكل ملف. لذا، عندما طلب المستخدم "المهام العاجلة وأولويات اليوم"، لم يجد نموذج الذكاء الاصطناعي أي مهام في السياق، فظن أنه سؤال إرشادي عام وقام بتوليد جدول نظري من الكتب عن الجمارك (ACID، تخليص، نموذج 4، مستندات) وطلب من المستخدم تحديد شحنة معينة!
- **الحل:** تزويد النموذج بجدول المهام الحقيقية الـ 4 المعلقة في النظام:
  1. `[IMP-2026-0004]` إصدار شهادة الفحص المسبق قبل الشحن (GOEIC) - Critical.
  2. `[IMP-2026-0004]` استيفاء شهادة المنشأ (COO) وتوثيقها رسمياً - High.
  3. `[IMP-2026-0004]` مراجعة اشتراطات الاستيراد والموافقات الرقابية (STEP_03) - High.
  4. `[IMP-2026-0004]` مراجعة الاعتماد المالي وصرف دفعة المورد (STEP_04) - High.
  بالإضافة لخطوة `Evaluate Shipping Scenarios & Request Freight Quotations`.

### 🧪 Validation / Testing
- **Static Analysis:** `flutter analyze lib/core/providers/ai_assistant_provider.dart` -> **No issues found (0 warnings, 0 errors)**.
- **Frontend Full Test Suite:** `flutter test` -> **354 passed, 0 failed in 65.0s (100% Passing)**.
- **AI Assistant Unit Tests:** `flutter test test/ai_assistant_and_multiscreen_test.dart` -> **6/6 passed (100% Passing)**.
- **Web Build:** `flutter build web --release` -> **Built successfully in 77.0s**.
- **Web Distribution Sync:** تم نقل الملفات إلى `dist/Sorour_Logistics_Web` بنجاح وتأكيد وقت كتابة `main.dart.js` الجديد.

### 🏁 Next Steps
- شرح سبب النتيجة السابقة للمستخدم وتأكيد حلها وتحديث تطبيق الويب.

---

## 📝 [2026-09-06 15:48] - Completed Task: Disk Cleanup & Release Packages Optimization (16.8 GB Freed)

### 📌 Overview
- **Task Code:** DISK-CLEANUP-001
- **Description:** تشخيص سبب وصول حجم مجلد المشروع `F:\SorourLogistics` إلى 18.9 جيجابايت بناءً على استفسار وصورة المستخدم. تبين أن قاعدة البيانات حجمها 2.03 ميجابايت فقط، بينما تراكمت 86 حزمة تثبيت سابقة في مجلد `dist/releases/` بحجم تجاوز 17.2 جيجا. تم حذف كافة الحزم القديمة فوراً والإبقاء حصرياً على أحدث إصدار نهائي `v1.0.151`.

### 📁 Files Changed
- `dist/releases/` — Cleaned: حذف 86 ملف تثبيت قديم بصيغتي `.exe` و `.zip` (من إصدار v1.0.0 وحتى v1.0.150) مع الإبقاء الحصري على:
  - `Sorour_Logistics_Setup_v1.0.151.exe`
  - `Sorour_Logistics_v1.0.151_Windows_Portable.zip`
  - `latest_release.json`

### 📊 Technical Changes
- تحليل مساحة القرص عبر فحص شجرة المجلدات بالكامل:
  - قاعدة البيانات `sorour_logistics.db`: 2.03 MB (خفيفة وسريعة).
  - كود المشروع والموديولات والباك إند: أقل من 50 MB.
  - مجلد `dist/releases`: 17.18 GB ناتج عن تكرار حزم التثبيت لكل تعديل سابق.
- تنفيذ تنظيف آمن وفوري لحزم التثبيت السابقة غير المستخدمة، وتوفير **16.79 جيجابايت** من مساحة القرص فوراً.
- انخفاض الحجم الإجمالي لمجلد المشروع من **18.9 GB إلى 2.13 GB** (يضم كافة ملفات الكود، والمكتبات، وأحدث إصدار تنفيذي).

### 🧪 Validation / Testing
- **Directory Size Verification:** `powershell Measure-Object -Sum` -> **2.13 GB** (تم التأكد من استرجاع 16.79 GB إلى القرص الصلب).
- **Latest Release Verification:** تم التأكد من بقاء `v1.0.151` سليماً وجاهزاً للاستخدام.
- **Database Integrity:** حجم `sorour_logistics.db` هو 2.03 MB دون أي مساس بأي سجل أو شحنة.

### 🏁 Next Steps
- إبلاغ المستخدم بالنتيجة وتأكيد المساحة الكبيرة التي تم توفيرها على جهازه.

---

## 📝 [2026-09-06 15:58] - Completed Task: Unified Freight Studies & Quotations Hub Integration (BP-007 & BP-008 Single Screen Merger)

### 📌 Overview
- **Task Code:** FREIGHT-HUB-MERGE-001
- **Description:** استجابة لطلب المستخدم ("ايه الفرق بين دراسات الشحن وبين مقارنه عروض السعر وهل يمكن دمجهم او الاستغناء عن واحده منهم؟ طورهم وادمجهم فى شاشه واحده مع الحفاظ على جميع المزايا فى التصميم فى الاثنين"):
  1. دمج شاشة دراسات النولون والبدائل (`BP-007 Shipping Scenarios`) مع شاشة استدراج ومقارنة عروض الأسعار والترسية (`BP-008 Freight RFQ`) في شاشة موحدة شاملة: **إدارة ودراسات ومقارنة عروض أسعار الشحن (Freight RFQ & Studies Hub)**.
  2. الحفاظ التام على كامل مزايا الشاشتين: محرك حساب الجدول الزمني لوصول الشحنة للمصنع، مصفوفة الـ 20 بند تكلفة، محاكي الحاويات والأوزان الحجمية والتكعيب CBM، استخراج عروض الأسعار بالذكاء الاصطناعي (AI OCR Extractor)، جدول المقارنة الفورية والترسية المباشرة (Direct Awarding)، والتحليل المعياري للأسعار (Benchmarking).
  3. تفعيل الاستدعاء الآلي لعروض الأسعار بمجرد اختيار ملف الاستيراد (`Import File Auto-Quote Fetch`)، مع إضافة بانر تنبيهي ذكي يوضح وجود عروض مسجلة مسبقاً للملف مع زر استدعائها بنقرة واحدة.
  4. مواءمة القائمة الجانبية (Sidebar Navigation) في الشاشة الرئيسية ومزود التنقل لتوجيه المستخدم للتبويب المطلوب في الشاشة الموحدة بسلاسة تامة.

### 📁 Files Changed
- `frontend/lib/features/shipping_scenarios/screens/shipping_scenarios_screen.dart` — Modified: إضافة وسيطي `isEmbedded` و `initialImportFileId` لدعم تضمين واجهة دراسات الجدول الزمني ومصفوفة التكاليف ومحاكي الحاويات داخل الشاشة الموحدة دون تكرار كود أو تغليفات زائدة، ومزامنة اختيار ملف الاستيراد تلقائياً.
- `frontend/lib/features/freight_quotations/screens/freight_quotations_screen.dart` — Modified: إعادة هيكلة الشاشة كمركز موحد بـ 3 تبويبات رئيسية:
  - **التبويب 1 (`طلب ومقارنة العروض والترسية`):** استخراج العروض بالذكاء الاصطناعي، إضافة العروض، جدول المقارنة، والترسية المباشرة.
  - **التبويب 2 (`دراسة الجدول الزمني والتكاليف`):** تضمين شاشة دراسات الشحن بمصفوفة الـ 20 تكلفة ومسار الوصول للمصنع.
  - **التبويب 3 (`سجل الطلبات والدراسات المحفوظة`):** الأرشيف التاريخي للطلبات والدراسات مع فلاتر البحث وإجراءات الطباعة والتفاصيل والحذف.
  - إضافة خاصية الاستدعاء الذكي لعروض الأسعار عند اختيار ملف الاستيراد وبانر التنبيه البرتقالي التفاعلي.
- `frontend/lib/features/home/home_screen.dart` — Modified: توجيه بند القائمة رقم 4 (دراسات النولون) إلى الشاشة الموحدة بالتبويب رقم 1، وتوجيه بند القائمة رقم 49 (مقارنة عروض الأسعار) إلى التبويب رقم 0، وتوجيه بند 5 إلى تبويب السجل، مع تنظيف الـ imports غير المستخدمة وتحديث عناوين القائمة.
- `frontend/lib/core/providers/navigation_provider.dart` — Modified: مواءمة نصوص تبويبات مساحة العمل لعرض المسميات المحدثة للمركز الموحد.

### 📊 Technical Changes
- الاستفادة من مبدأ إعادة الاستخدام المعماري (Architectural Reusability) عبر تمرير `isEmbedded: true` لمنع ازدواجية أكثر من 3700 سطر من الكود الرياضي المعقد ومحاكي الرص والتكعيب، وتوحيد تجربة المستخدم في شاشة واحدة متناسقة.
- استدعاء العروض السابقة المحفوظة لنفس ملف الاستيراد المختار آلياً إذا كان جدول المقارنة فارغاً، مع توفير خيار يدوي واضح عبر البانر التفاعلي لإعادة جلبها في أي وقت.
- توحيد دورة حياة اختيار ملف الشحنة عبر الشاشة الموحدة بحيث تنعكس تلقائياً في دراسة التكاليف والجدول الزمني ومقارنة عروض الأسعار.

### 🧪 Validation / Testing
- **Frontend Unit Tests:**
  - `flutter test test/freight_rfq_test.dart test/freight_quotations_extractor_test.dart test/shipping_scenario_model_test.dart` -> **10 passed, 0 failed (100% Passing)**.
- **Backend Unit Tests:**
  - `py -3 -m pytest tests/unit/test_freight_rfq_generator.py tests/unit/test_rfq_benchmarking.py tests/unit/test_shipping_scenarios.py` -> **17 passed, 0 failed (100% Passing)**.
- **Static Analysis:**
  - `flutter analyze lib/features/freight_quotations/ lib/features/home/` -> **0 issues found (0 warnings, 0 errors)**.

### 🏁 Next Steps
- تقديم الشرح الوافي للمستخدم وإرشاده لاستخدام الشاشة الموحدة وتبويباتها الثلاثة.

---

## 📝 [2026-09-06 16:12] - Completed Task: Unified Enterprise Data Table Component & Smart Tasks Pilot Integration

### 📌 Overview
- **Task Code:** UI-ENTERPRISE-DATATABLE-001
- **Description:** استجابة لطلب المستخدم ("اتشغل على Unified Enterprise Data Table") وتطبيقاً للركيزة الثانية من خارطة الطريق المعمارية (`enterprise_architecture_and_ui_roadmap.md`):
  1. بناء وتأسيس محرك الجداول الموحد المتقدم (`EnterpriseDataTable<T>`) ليكون المعيار الموحد لكافة شاشات وبيانات النظام.
  2. دعم المزايا المتقدمة: التحكم التفاعلي بالأعمدة وإظهارها وإخفائها وحفظ التفضيلات عبر `SharedPreferences`، الفرز متعدد الأعمدة مع مؤشرات بصرية، البحث اللحظي الفوري، الترقيم عالي الأداء مع اختيار عدد الصفوف، التصدير بنقرة واحدة إلى Excel/CSV بترميز `UTF-8 BOM` لمنع تشويه اللغة العربية، النسخ المباشر للحافظة بتنسيق `TSV` المتوافق مع Excel، التحديد المتعدد عبر Checkboxes وشريط الإجراءات الجماعية (Bulk Actions)، ومؤشرات التحميل الهيكلية اللامعة (`Skeleton Shimmer Loaders`).
  3. ترقية وتطبيق المحرك فورياً على شاشة **المهام الذكية والتنبيهات التشغيلية (`SmartTasksScreen`)** كأول شاشة حية تستفيد من كافة هذه الإمكانيات، مع إضافة ميزة الإكمال الجماعي للمهام المحددة.

### 📁 Files Changed
- `frontend/lib/core/widgets/enterprise_data_table/models/enterprise_column.dart` — Created: نموذج تعريف وتخصيص الأعمدة (`id`, `title`, `cellBuilder`, `sortComparator`, `searchValue`, `exportValue`, `isVisible`, `isLocked`).
- `frontend/lib/core/widgets/enterprise_data_table/widgets/enterprise_table_shimmer_skeleton.dart` — Created: هيكل عظمي متحرك بانسيابية يحاكي جدول البيانات أثناء التحميل بدلاً من الدوائر التقليدية.
- `frontend/lib/core/widgets/enterprise_data_table/widgets/enterprise_table_column_picker_dialog.dart` — Created: نافذة تفاعلية منبثقة للتحكم في ظهور الأعمدة وترتيبها وإعادة ضبطها للوضع الافتراضي.
- `frontend/lib/core/widgets/enterprise_data_table/widgets/enterprise_table_header_toolbar.dart` — Created: شريط الأدوات العلوي مع شارات السجلات، حقل البحث الذكي، أزرار التصدير، إدارة الأعمدة، والإجراءات الجماعية.
- `frontend/lib/core/widgets/enterprise_data_table/widgets/enterprise_table_pagination_bar.dart` — Created: شريط الترقيم السفلي المكتبي الداعم لاختيار عدد الصفوف والتنقل بين الصفحات.
- `frontend/lib/core/widgets/enterprise_data_table/enterprise_data_table.dart` — Created: المكون الرئيسي الشامل الجامع لكافة الوظائف.
- `frontend/lib/features/smart_tasks/screens/smart_tasks_screen.dart` — Modified: ترقية الشاشة لاستخدام `EnterpriseDataTable<SmartTaskModel>` مع 9 أعمدة تخصصية، فرز بالأولويات، بحث فوري، تصدير، وإكمال جماعي.
- `frontend/test/enterprise_data_table_test.dart` — Created: اختبارات وحدة وفحص تفاعلي شاملة للمكون الجديد (7/7 Passed).

### 📊 Technical Changes
- بناء المكون بالكامل بالاعتماد على حزم Flutter القياسية و `shared_preferences` لتفادي أي تبعيات خارجية غير مستقرة.
- تصميم متجاوب وآمن تماماً من أخطاء الـ Overflow عبر `LayoutBuilder` و `SingleChildScrollView` أفقياً لحماية الشاشات الضيقة واختبارات الوحدة.
- تغليف محتوى الجدول بـ `SelectionArea` لتمكين التظليل والنسخ المباشر لأي خلية بالماوس.
- دعم ترميز UTF-8 مع BOM في تصدير CSV لضمان فتح ملفات الإكسل العربية بنقاء تام دون تشويه في برامج Microsoft Office.

### 🧪 Validation / Testing
- **EnterpriseDataTable Unit Tests:** `flutter test test/enterprise_data_table_test.dart` -> **7 passed, 0 failed (100% Passing)**.
- **Related Workspace & AI Tests:** `flutter test test/ai_assistant_and_multiscreen_test.dart test/enterprise_data_table_test.dart` -> **13 passed, 0 failed (100% Passing)**.
- **Static Analysis:** `flutter analyze lib/core/widgets/enterprise_data_table/ lib/features/smart_tasks/` -> **0 issues found (0 warnings, 0 errors)**.

### 🏁 Next Steps
- استمرار نشر المكون في باقي الشاشات الرئيسية (مثل ملفات الاستيراد وأوامر الشراء والشحنات) لتوحيد تجربة المستخدم بالكامل.

---

## 📝 [2026-09-06 16:55] - Completed Task: Screen Consolidation & Redundancy Elimination Hubs

### 📌 Overview
- **Task Code:** UI-SCREEN-CONSOLIDATION-001
- **Description:** استجابة لطلب المستخدم ("ابداء الدمج لكن حافظ على كل المميزات") وتطبيقاً للركيزة الثالثة من معايير التصميم المتقدم لإزالة التكرار ودمج الشاشات المجزأة:
  1. إنشاء مراكز عمليات موحدة (Unified Operational Hubs) متعددة التبويبات للشاشات المترابطة مع الحفاظ الصارم على كامل المزايا والوظائف والأكواد بنسبة 100%.
  2. دمج شاشات المخازن والاستلام الثلاث (`GoodsInTransitScreen` + `WarehouseReceivingScreen` + `WarehouseReceivedReportScreen`) في مركز عمليات موحد: **Inbound & Warehouse Hub (`InboundWarehouseHubScreen`)**.
  3. دمج شاشات التسوية وتكاليف الوصول (`LandedCostComparisonScreen` + `FinancialSettlementScreen`) في مركز موحد: **Landed Cost & Settlement Hub (`FinancialSettlementScreen`)**.
  4. دمج شاشات التعريفة الجمركية ومستكشف بنود الـ HS Codes والضرائب (`CustomsTariffScreen` + `HsCodeSearchScreen`) في مركز موحد: **Customs Tariff & HS Explorer Hub (`CustomsTariffScreen`)**.
  5. دمج وتطهير القائمة الجانبية (Sidebar) من التكرار والازدواجية (دمج بنود مراحل الشحن والتخليص والمستندات في بوابات موحدة، واختصار عناصر القائمة مع الحفاظ على التوافق الخلفي التام لكافة الـ 67 مساراً في `_screens` و `navigation_provider.dart`).

### 📁 Files Changed
- `frontend/lib/features/warehouse_receiving/screens/goods_in_transit_screen.dart` — Modified: إضافة وسيط `isEmbedded` لتمكين التضمين الانسيابي داخل التبويبات دون تكرار للـ Scaffolds.
- `frontend/lib/features/warehouse_receiving/screens/warehouse_receiving_screen.dart` — Modified: إضافة وسيط `isEmbedded`.
- `frontend/lib/features/warehouse_receiving/screens/warehouse_received_report_screen.dart` — Modified: إضافة وسيط `isEmbedded`.
- `frontend/lib/features/warehouse_receiving/screens/inbound_warehouse_hub_screen.dart` — Created: المركز الموحد للاستلام والمخازن والبضاعة بالطريق مع 3 تبويبات عمودية عبر `VerticalStageScaffold`.
- `frontend/lib/features/financial_settlement/screens/landed_cost_comparison_screen.dart` — Modified: إضافة وسيط `isEmbedded`.
- `frontend/lib/features/financial_settlement/screens/financial_settlement_screen.dart` — Modified: إضافة وسيط `initialSubTab` ودعم 3 تبويبات متكاملة (سجل التسويات والقيود، مقارنة تكاليف الوصول التقديرية والفعلية، وبدء تسوية جديدة).
- `frontend/lib/features/customs_tariff/screens/hs_code_search_screen.dart` — Modified: إضافة وسيط `isEmbedded`.
- `frontend/lib/features/customs_tariff/screens/customs_tariff_screen.dart` — Modified: إضافة وسيط `initialTabIndex` ومحدد التبويب العلوي (`SegmentedButton`) بين جدول التعريفة ومستكشف الأكواد وحاسبة الرسوم.
- `frontend/lib/features/home/home_screen.dart` — Modified: ربط مسارات الـ 67 بالمراكز الموحدة الجديدة، توحيد واختصار عناصر الـ Sidebar لتشمل المراكز المدمجة بدلاً من البنود المكررة، وتحديث شريط الأيقونات المصغر (Collapsed Rail).
- `frontend/lib/core/providers/navigation_provider.dart` — Modified: تحديث مسميات وأيقونات التبويبات ودعم التحديث الحي (`_liveRefreshScreenData`) لجميع موفري البيانات المرتبطين بالمراكز المدمجة.
- `frontend/test/goods_in_transit_and_warehouse_reports_test.dart` — Modified: إضافة اختبارات التحقق من صحة المركز الموحد للمخازن والاستلام وتبويباته.

### 📊 Technical Changes
- اعتماد نمط التضمين غير المتداخل (`isEmbedded Pattern`) لتمكين إعادة استخدام الشاشات الفرعية بصورة مستقلة أو مدمجة بدون تكرار سطور الكود أو انهيار الأداء.
- الحفاظ الكامل على كافة المسارات التاريخية (0..66) لضمان عدم تعطل أي اختبار وحدة أو إشارة مرجعية في النظام (Backward Compatibility).
- تنظيف شامل لجميع الواردات غير المستخدمة في الشاشات وتصفير أي تحذيرات.

### 🧪 Validation / Testing
- **Frontend Full Test Suite:** `flutter test` -> **362 passed, 0 failed (100% Passing)**.
- **Backend Full Test Suite:** `py -m pytest tests/unit/` -> **538 passed, 0 failed (100% Passing)**.
- **Total Tests Passing:** **900 automated unit tests (100% Green)**.
- **Static Code Analysis:** `flutter analyze lib/features/home/ lib/core/providers/ lib/features/warehouse_receiving/ lib/features/financial_settlement/ lib/features/customs_tariff/` -> **0 issues found**.

### 🏁 Next Steps
- إحاطة المستخدم بالنتائج وعرض الهيكل الموحد المحدث قبل وبعد الدمج.

---

## 📝 [2026-09-06 17:15] - Completed Task: Users Management & RBAC Localization (Screen 66 i18n Review)

### 📌 Overview
- **Task Code:** I18N-SCREEN-66-USERS-MGMT
- **Description:** مراجعة وتعريب وتوطين شاشة إدارة المستخدمين والصلاحيات (Users Management & RBAC Screen - Screen 66)، والقضاء النهائي على أي نصوص مكدسة أو مدمجة بين اللغتين (Stacked Arabic+English Labels)، واستخراج كافة النصوص والمسميات إلى `AppLocalizations` مع توفير ترجمة عربية خالصة في الوضع العربي وترجمة إنجليزية احترافية في الوضع الإنجليزي، وحل مشكلة الـ Overflow في شارة الحالة (`Active` / `Disabled`).

### 📁 Files Changed
- `frontend/lib/core/localization/app_localizations.dart` — Modified: إضافة 75+ getter مجرد لعناوين وأزرار وفلاتر وجداول ورسائل ونوافذ إدارة المستخدمين والصلاحيات.
- `frontend/lib/core/localization/app_localizations_ar.dart` — Modified: إضافة الترجمة العربية الكاملة النقية (خالية 100% من الأحرف الإنجليزية أو التكديس).
- `frontend/lib/core/localization/app_localizations_en.dart` — Modified: إضافة الترجمة الإنجليزية المقابلة بدقة واحترافية.
- `frontend/lib/features/auth/screens/users_management_screen.dart` — Modified: ربط جميع مكونات الشاشة بـ `context.l10n`، تعريب الترويسة، حقل البحث، شرائح الإحصائيات، فلاتر الأدوار، أعمدة الجدول، شارات المستخدم الحالي والحالة، أزرار الإجراءات، نافذة إضافة وتعديل المستخدم، نافذة تأكيد التفعيل/التعطيل، وبطاقة وصف صلاحيات الأدوار. وتوسيع عمود الحالة إلى 95px مع إضافة `Flexible` لمنع أي RenderFlex Overflow.
- `frontend/test/users_management_localization_test.dart` — Created: اختبارات وحدة وفحص واجهة شاملة (5 اختبارات) للتحقق من سلامة كافة الـ Getters، وعدم وجود نصوص إنجليزية في الوضع العربي، وعدم وجود نصوص عربية في الوضع الإنجليزي، واختبار رسم الشاشة في بيئتي RTL و LTR.
- `localization-review-log.md` — Modified: توثيق جلسة المراجعة للشاشة رقم 66 وتحديث سجل المراجعة الشامل للنظام.

### 📊 Technical Changes
- التخلص الكامل من النصوص المزدوجة المكدسة مثل `'User Access Control (RBAC) — 5 مستخدم مسجل'` و `'ADMIN — مدير النظام (صلاحيات كاملة)'` واستبدالها بمولدات نصوص ديناميكية بحسب لغة الواجهة الحالية.
- حماية صف شارة الحالة من الـ RenderFlex Overflow عبر تكبير عرض الحاوية إلى 95px وتغليف النص بـ `Flexible(child: Text(..., overflow: TextOverflow.ellipsis))`.
- بناء نموذج وهمي (`MockUsersNotifier`, `MockAuthNotifier`) لاختبار الواجهة آلياً في وضعي RTL و LTR.

### 🧪 Validation / Testing
- **Users Management Localization Tests:** `flutter test test/users_management_localization_test.dart` -> **5 passed, 0 failed (100% Passing)**.
- **Static Analysis:** `flutter analyze lib/features/auth/ test/users_management_localization_test.dart` -> **0 issues found (0 warnings, 0 errors)**.

### 🏁 Next Steps
- تقديم ملخص وافٍ للمستخدم يوضح التحسينات المنجزة قبل وبعد التعريب.

---

## 📝 [2026-09-06 17:15] - Completed Task: Smart Tasks & Reminder Engine Localization (Screen 69 i18n Review)

### 📌 Overview
- **Task Code:** I18N-SCREEN-69-SMART-TASKS
- **Description:** مراجعة وتعريب وتوطين شاشة إدارة المهام الذكية ومحرك التذكيرات (`SmartTasksScreen`) ونافذة إضافة وتعديل المهمة (`SmartTaskDialog`)، والتخلص التام والنهائي من مشكلة النصوص المزدوجة المكدسة (`'آلي (System)'`, `'يدوية (To-Do)'`, `'حرجة (Critical)'`, `'عام (General)'`, `'الأولوية (Priority)'`, `'تاريخ الإنجاز المطلوب (Due Date)'`, `'نوع التذكير (Reminder Engine)'`). تم استخراج كافة النصوص إلى `AppLocalizations` مع توفير ترجمات عربية نقية بدون أي حروف لاتينية، وترجمات إنجليزية متوافقة 100%، وتوسيع الفلاتر مع إضافة `isExpanded: true` لحماية القوائم من أي RenderFlex Overflow.

### 📁 Files Changed
- `frontend/lib/core/localization/app_localizations.dart` — Modified: إضافة 57 getter ودالة مساعدة ديناميكية لتسميات المهام ومستويات الأولوية والحالات ومحركات التذكير.
- `frontend/lib/core/localization/app_localizations_ar.dart` — Modified: إضافة كافة الترجمات العربية الخالصة 100% بدون أي تداخل لاتيني.
- `frontend/lib/core/localization/app_localizations_en.dart` — Modified: إضافة كافة الترجمات الإنجليزية الاحترافية المقابلة.
- `frontend/lib/features/smart_tasks/screens/smart_tasks_screen.dart` — Modified: ربط جميع عناصر الشاشة بـ `context.l10n`، تعريب عنوان الـ AppBar، زر الإضافة، فلاتر نوع المهمة والأولوية والحالة مع `isExpanded: true` و `overflow: TextOverflow.ellipsis`، أعمدة الجدول، شارات الحالة والأولوية ونوع المهمة، وشريط الإجراءات الجماعية.
- `frontend/lib/features/smart_tasks/widgets/smart_task_dialog.dart` — Modified: ربط النافذة بالترجمة، تعريب الترويسة، حقول النموذج والمدخلات، رسائل التحقق (Validators)، خيارات القوائم المنسدلة للأولوية ومحرك التذكير، أزرار الحفظ والتعديل والإلغاء، وإشعارات الحفظ والخطأ.
- `frontend/test/smart_tasks_localization_test.dart` — Created: حزمة اختبارات شاملة (5 اختبارات) للتحقق من عدم وجود نصوص فارغة، وخلو النصوص العربية من الأحرف اللاتينية، وخلو النصوص الإنجليزية من الأحرف العربية، واختبار رسم الشاشة في RTL و LTR والتأكد من الغياب التام لأي نصوص مكدسة.
- `localization-review-log.md` — Modified: توثيق جلسة مراجعة الشاشة رقم 69 وتحديث الحالة العامة للنظام.

### 📊 Technical Changes
- القضاء التام على التكديس اللغوي في الشارات والقوائم:
  - تحويل `'آلي (System)'` و `'يدوي (To-Do)'` إلى `'آلية'` في الوضع العربي و `'Automated'` في الوضع الإنجليزي.
  - تحويل `'حرجة (Critical)'` إلى `'حرجة'` / `'Critical'`.
  - تحويل شارة الشحنة `'عام (General)'` إلى `'عام'` / `'General'`.
  - ترجمة مسميات أنواع التذكير ديناميكياً عبر `smartTaskReminderTypeLabel` (مثل `'موعد وصول الشحنة'` لـ `'ETA Arrival'` و `'نموذج 4 البنكي'` لـ `'Bank Form 4'`).
- حل مشكلة RenderFlex overflow في القوائم المنسدلة (`DropdownButtonFormField`) بإضافة `isExpanded: true` وضبط عرض الصناديق إلى 180px و 160px وتطبيق `TextOverflow.ellipsis`.

### 🧪 Validation / Testing
- **Smart Tasks Localization Tests:** `flutter test test/smart_tasks_localization_test.dart` -> **5 passed, 0 failed (100% Passing)**.
- **Frontend Full Test Suite:** `flutter test` -> **372 passed, 0 failed (100% Passing)**.
- **Backend Full Test Suite:** `python -m pytest tests/unit/` -> **538 passed, 0 failed (100% Passing)**.
- **Total Automated Tests Passing:** **910 tests (100% Green)**.
- **Static Code Analysis:** `flutter analyze lib/features/smart_tasks/ lib/core/localization/ test/smart_tasks_localization_test.dart` -> **No issues found (0 warnings, 0 errors)**.

### 🏁 Next Steps
- إحاطة المستخدم بالانتهاء من فحص وتعريب شاشة المهام الذكية وتأكيد جاهزية النظام بنسبة 100%.

---

## 📝 [2026-09-06 23:20] - Completed Task: Operational & Daily Shipment Updates Engine Localization (Screen 42 i18n Review)

### 📌 Overview
- **Task Code:** I18N-SCREEN-42-SHIPMENT-UPDATES
- **Description:** مراجعة وتعريب وتوطين محرك تحديث الشحنات التشغيلي واليومي (`ShipmentUpdateEngineScreen`) ونافذة تسجيل التحديث التشغيلي واليومي (`ShipmentUpdateDialog`)، والقضاء التام على ظاهرة النصوص المزدوجة المكدسة (`'محرك تحديث الشحنات التشغيلي واليومي (Operational & Daily Update Engine)'`, `'سجل ونتائج دراسة الاستشارة الجمركية والفحص المستندي (Customs Consultation & Inspection Records)'`, `'تعديل التكلفة (Cost Adjustment)'`, `'عوائق التخليص (Blocking)'`, `'عادي (Normal)'`, `'عالي (High Priority)'`, `'حرج (Critical Alert)'`). تم استخراج كافة النصوص إلى `AppLocalizations` مع توفير ترجمات عربية نقية وترجمات إنجليزية مطابقة، ودعم التحديث التفاعلي بدون Hard-coded strings وحماية كافة القوائم والنوافذ من الـ RenderFlex Overflow.

### 📁 Files Changed
- `frontend/lib/core/localization/app_localizations.dart` — Modified: إضافة 88 getter ودالة ديناميكية لعناوين ومراحل وأعمدة وجداول ونوافذ التحديث التشغيلي والاستشارة الجمركية.
- `frontend/lib/core/localization/app_localizations_ar.dart` — Modified: إضافة الترجمة العربية الخالصة 100% لكافة البنود دون أي تداخل إنجليزي أو تكديس.
- `frontend/lib/core/localization/app_localizations_en.dart` — Modified: إضافة الترجمة الإنجليزية المقابلة بدقة واحترافية.
- `frontend/lib/features/shipment_updates/screens/shipment_update_engine_screen.dart` — Modified: ربط جميع عناصر الشاشة بـ `context.l10n`، تعريب عنوان الـ AppBar، بطاقة اختيار الشحنة والبحث، مسار المراحل العشر (Phase Pipeline) ديناميكياً، جدول سجل التحديثات التشغيلية واليومية، قسم الاستشارة الجمركية والفحص المستندي، ونافذة مراجعة وتحديث مستندات الاستشارة الجمركية.
- `frontend/lib/features/shipment_updates/widgets/shipment_update_dialog.dart` — Modified: ربط النافذة بالترجمة، تعريب الترويسة مع إضافة `Expanded` لمنع الـ Overflow، تعريب أنواع التحديث ومراحل الشحنة، تعريب مستويات الأولوية بدون تكديس ثنائي، وإضافة `isExpanded: true` و `overflow: TextOverflow.ellipsis` لكافة القوائم المنسدلة.
- `frontend/test/shipment_updates_localization_test.dart` — Created: حزمة اختبارات وحدة وواجهة مستخدم شاملة (7 اختبارات) للتحقق من خلو النصوص من التكديس والترميز اللغوي الخاطئ، والتأكد من صحة التوليد الديناميكي واختبار رسم الشاشة والنافذة في بيئتي RTL و LTR.
- `localization-review-log.md` — Modified: إضافة الشاشة رقم 42 إلى قائمة الفحص وتوثيق تفاصيل جلسة المراجعة وتحديث الحالة العامة لـ 70 شاشة.

### 📊 Technical Changes
- القضاء على مشكلة التكديس اللغوي في الشاشات والنوافذ:
  - استبدال العناوين التي تحتوي على أقواس إنجليزية بمسميات لغوية خالصة تتبع لغة الواجهة الحالية.
  - تحويل مصفوفة المراحل الثابتة (`_allPhases`) التي كانت تحتوي نصوصاً عربية ثابتة إلى قائمة رموز مراحل (`_phaseCodes`) مع دالة ترجمة ديناميكية `_getPhaseName(l, code)`.
  - تحويل خيارات أولوية التنبيه (`Normal`, `High`, `Critical`) من تسميات مكدسة إلى نصوص نقية تتبع اللغة المختارة.
- حل مشكلات RenderFlex Overflow:
  - تغليف عنوان النافذة بـ `Expanded(child: Text(..., overflow: TextOverflow.ellipsis))`.
  - ضبط `isExpanded: true` على كافة حقول `DropdownButtonFormField` لتفادي أخطاء قياس النصوص الطويلة في وضعي العربية والإنجليزية.

### 🧪 Validation / Testing
- **Shipment Updates Localization Tests:** `flutter test test/shipment_updates_localization_test.dart` -> **7 passed, 0 failed (100% Passing)**.
- **Static Code Analysis:** `flutter analyze lib/features/shipment_updates/ test/shipment_updates_localization_test.dart` -> **No issues found (0 warnings, 0 errors)**.

### 🏁 Next Steps
- إحاطة المستخدم بالانتهاء الكامل من فحص وتعريب شاشة محرك تحديث الشحنات التشغيلي واليومي (Screen 42) وجاهزية النظام الشاملة.

---

## 📝 [2026-09-06 23:45] - Completed Task: Decoupling Freight RFQ & Quotation Comparison from Freight Studies & Saved Registry

### 📌 Overview
- **Task Code:** FREIGHT-RFQ-STUDIES-DECOUPLING-001
- **Description:** فصل طلب ومقارنة عروض أسعار النولون والترسية التشغيلية السريعة (Operational Fast RFQ, Quote Comparison & Awarding) عن دراسة النولون والجدول الزمني ومصفوفة التكاليف وسجل الدراسات المحفوظة (Freight Studies, Timeline Evaluation & Saved Registry)، مع تخصيص شاشة مستقلة لكل منهما في القائمة الجانبية (Sidebar Phase 1) واستعادة بنية التنقل النظيفة.

### 📁 Files Changed
- `frontend/lib/core/providers/navigation_provider.dart` — Modified: فصل تعريف التبويبات في `_screenTabs`:
  - المسار 4 و 5: مخصص لـ `دراسات النولون والجدول الزمني` (`ShippingScenariosScreen`).
  - المسار 49: مخصص لـ `طلب ومقارنة عروض النولون` (`FreightQuotationsScreen`).
- `frontend/lib/features/home/home_screen.dart` — Modified:
  - استعادة المسارين 4 و 5 في قائمة `_screens` لـ `ShippingScenariosScreen`.
  - تخصيص المسار 49 لـ `FreightQuotationsScreen`.
  - استعادة عنصرين مستقلين تماماً في الـ Sidebar تحت Phase 1:
    1. `Freight RFQ & Quotations` (`طلب ومقارنة عروض النولون والترسية` - Route 49).
    2. `Freight Studies & Saved Log` (`دراسة النولون والجدول الزمني والسجلات المحفوظة` - Route 4).
- `frontend/lib/features/freight_quotations/screens/freight_quotations_screen.dart` — Modified:
  - إزالة `TabBar` و `TabController` والشاشات المضمنة ليصبح بيئة عمل تشغيلية مباشرة ومستقلة 100% لطلب ومقارنة وترسية عروض النولون.
  - إضافة أزرار إجرائية في AppBar: "استدعاء طلب مسجل (Load Saved RFQ)" عبر نافذة بحث واستدعاء فورية، زر "طلب جديد (New RFQ)"، وزر اختصار "دراسات النولون والسجلات" ينقل مباشرة للمسار 4.
  - حماية شريط الترويسة وعناوين عروض الأسعار من الـ Overflow وجعلها متوافقة مع قواعد منع التكديس اللغوي (`Flexible` و `Wrap`).
- `frontend/lib/features/shipping_scenarios/screens/shipping_scenarios_screen.dart` — Modified:
  - استبدال استدعاءات `.value` بـ `.valueOrNull` لمنع استثناءات التحميل عند انقطاع الشبكة.
  - حماية شريط حالة الحسابات وشريط الأزرار السفلي من أخطاء الـ RenderFlex Overflow باستخدام `LayoutBuilder` و `ConstrainedBox` و `SingleChildScrollView`.
- `frontend/test/freight_rfq_separation_test.dart` — Created: حزمة اختبارات تحقق معمارية وتكاملية كاملة لفصل الشاشتين والمسارات والتأكد من عدم وجود TabBar مكرر واكتمال أدوات التشغيل بنسبة 100%.

### 📊 Technical Changes
- فك الارتباط المعماري بين شاشتي عروض النولون ودراسات الشحن بعد دمجهما السابق.
- إتاحة تجربة مستخدم سريعة وفورية للمسؤولين عن مقارنة عروض النولون والترسية بدون الحاجة للتنقل داخل دراسات وسيناريوهات الشحن المعقدة.
- توفير آلية استدعاء فورية (Load Saved RFQ Dialog) لملء الفورم وعروض الأسعار بضغطة زر واحدة.
- القضاء على أي تداخل لغوي Stacked Arabic+English في ترويسات وعناوين الشاشة.

### 🧪 Validation / Testing
- **Decoupling Architecture Tests:** `flutter test test/freight_rfq_separation_test.dart` -> **3 passed, 0 failed (100% Passing)**.
- **Freight Quotations Localization Tests:** `flutter test test/freight_quotations_comparison_localization_test.dart` -> **Passed (100% Green)**.
- **Shipping Scenarios Localization Tests:** `flutter test test/shipping_scenarios_localization_test.dart` -> **Passed (100% Green)**.
- **Static Code Analysis:** `flutter analyze` -> **0 errors, 0 warnings**.

### 🏁 Next Steps
- تسليم الفصل المكتمل للمستخدم مع توضيح عناصر القائمة الجانبية المستقلة وطريقة استخدام كل شاشة.

---

## 📝 [2026-09-06 23:50] - Completed Task: System-Wide Core Error & Diagnostic Details Dialog Localization (i18n Review)

### 📌 Overview
- **Task Code:** I18N-CORE-ERROR-DETAILS-DIALOG
- **Description:** مراجعة وتعريب وتوطين نافذة تفاصيل أخطاء النظام وسجل الفحص التشخيصي الموحد (`ErrorDetailsDialog` / `error_details_dialog.dart`) ومحرك معالجة الأخطاء (`ErrorFormatter`)، والقضاء التام على ظاهرة النصوص المزدوجة المكدسة (`'(PO)'`, `'(POL)'`, `'(POD)'`, `'(CRD)'`, `'إعادة المحاولة الآن (Retry)'`, `'فهمت، سأقوم بمعالجة الأخطاء (Fix & Retry)'`, `'عرض السجل التقني المفصل للمطورين (Diagnostic Log)'`, `'يوجد أخطاء في التحقق من صحة البيانات المدخلة (Validation Errors):'`, `'تعذر الاتصال بالخادم الخلفي (Backend API Connection Error / CORS)'`). تم استخراج 48 نصاً ودالة ديناميكية إلى `AppLocalizations`، توفير ترجمات عربية نقية 100% دون حروف أو اختصارات إنجليزية، وترجمات إنجليزية خالصة دون أي حروف عربية، مع تعريب ديناميكي لكافة الحقول الـ 32 (`errorFieldName`)، وحماية عناوين الجداول من الـ Overflow.

### 📁 Files Changed
- `frontend/lib/core/localization/app_localizations.dart` — Modified: إضافة 48 getter ودالة ديناميكية لعناوين وتفاصيل ورسائل استجابة الخادم وجداول أخطاء التحقق وأزرار النوافذ والتقرير التشخيصي للحافظة.
- `frontend/lib/core/localization/app_localizations_ar.dart` — Modified: إضافة الترجمة العربية الخالصة 100% لكافة النصوص والمصطلحات دون أي تكديس لأقواس أو رموز إنجليزية.
- `frontend/lib/core/localization/app_localizations_en.dart` — Modified: إضافة الترجمة الإنجليزية المقابلة بدقة واحترافية.
- `frontend/lib/core/widgets/error_details_dialog.dart` — Modified: ربط جميع عناصر النافذة بـ `context.l10n`، تحديث `ErrorFormatter.parse` لدعم الـ Locale الحالي، استبدال جدول الحقول الثابت بدالة ديناميكية `loc.errorFieldName(key)`، تعريب جدول التحقق والتقرير المنسوخ بالكامل، وتحديث أزرار الإجراءات.
- `frontend/test/error_details_dialog_localization_test.dart` — Created: حزمة اختبارات وحدة وواجهة مستخدم شاملة (8 اختبارات) للتحقق من خلو النصوص من التكديس، صحة الترجمة للحقول الـ 32، واختبار رسم النافذة في بيئتي RTL و LTR.
- `localization-review-log.md` — Modified: إضافة نافذة فحص الأخطاء التشخيصية وتوثيق تفاصيل جلسة المراجعة وتحديث الحالة العامة للنظام.

### 📊 Technical Changes
- القضاء على مشكلة التكديس اللغوي في نافذة الأخطاء التشخيصية:
  - استبدال الاختصارات الإنجليزية بين قوسين في أسماء الحقول (`(PO)`, `(POL)`, `(POD)`, `(CRD)`) بترجمات عربية واضحة ومحددة.
  - إزالة الأقواس الإنجليزية من أزرار النافذة وعناوين الأقسام (`(Retry)`, `(Fix & Retry)`, `(Diagnostic Log)`).
  - بناء تقرير النسخ التشخيصي (Diagnostic Report) بلغة ديناميكية كاملة تتبع لغة التطبيق النشطة بدلاً من القالب العربي الثابت.
- دعم اختياري لمعامل `AppLocalizations? l10n` في `ErrorFormatter.parse` و `showErrorDetailsDialog` مع التراجع التلقائي لـ `context.l10n` لضمان التوافقية العكسية 100%.
- حماية صف ترويسة جدول الأخطاء من الـ RenderFlex Overflow بإضافة `Expanded`.

### 🧪 Validation / Testing
- **Error Details Dialog Localization Tests:** `flutter test test/error_details_dialog_localization_test.dart` -> **8 passed, 0 failed (100% Passing)**.
- **Regression Tests Suite:** `flutter test test/dynamic_report_templates_test.dart test/smart_tasks_localization_test.dart test/shipment_updates_localization_test.dart` -> **19 passed, 0 failed (100% Passing)**.
- **Static Code Analysis:** `flutter analyze lib/core/widgets/error_details_dialog.dart lib/core/localization/ test/error_details_dialog_localization_test.dart` -> **No issues found (0 warnings, 0 errors)**.

### 🏁 Next Steps
- إحاطة المستخدم بالانتهاء الكامل من فحص وتعريب نافذة تفاصيل ومعالجة الأخطاء التشخيصية وجاهزية النظام الشاملة.

---

## 📝 [2026-09-06 23:58] - Completed Task: Navigation Reorganization — Dedicated Inquiries (استعلامات) Hub

### 📌 Overview
- **Task Code:** UI-NAV-INQUIRIES-HUB-001
- **Description:** استجابة لطلب المستخدم ("انقل صفحه Freight RFQ & Quotations Comparison و Cargo Measurement Engine فى قسم تحت مسمى استعلامات"):
  1. إنشاء قسم ومحرك استعلامات مخصص في القائمة الجانبية (Sidebar Navigation) باسم **"استعلامات" (`Inquiries`)**.
  2. نقل شاشة **طلب ومقارنة عروض النولون والترسية (`Freight RFQ & Quotations Comparison` - المسار 49)** بالكامل إلى قسم استعلامات وإزالتها من المرحلة 1 (Pre-Planning & Studies) منعاً للازدواجية.
  3. نقل شاشة **محرك وحاسبة أحجام وتوزيع الحاويات (`Cargo Measurement Engine (CBM)` - المسار 3)** بالكامل إلى قسم استعلامات وإزالتها من محور تخطيط الشحنة وأوامر الشراء (Hub 2: Shipment Planning) منعاً للازدواجية.
  4. تحديث شريط الأيقونات المصغر (`Collapsed Rail`) لإضافة أيقونة مباشرة لقسم استعلامات (`Icons.manage_search_outlined`).
  5. تحصين الشاشات من استثناءات `AsyncError` عند انقطاع الاتصال عبر استبدال `.value` بـ `.valueOrNull`.
  6. إنشاء حزمة اختبارات وحدة وتحقق تفاعلية شاملة وتأكيد نجاح الاختبارات بنسبة 100%.

### 📁 Files Changed
- `frontend/lib/features/home/home_screen.dart` — Modified:
  - إزالة شاشة 3 (`CBMCalculatorScreen`) من قائمة Hub 2 (Shipment Planning).
  - إزالة شاشة 49 (`FreightQuotationsScreen`) من قائمة Phase 1 (Pre-Planning & Studies).
  - إضافة قسم ومحور استعلامات الجديد `_buildHubTile` باسم عربي "استعلامات" وإنجليزي "Inquiries" يضم الشاشتين 49 و 3.
  - إضافة أيقونة استعلامات في شريط الأيقونات المصغر `_buildCollapsedRail`.
- `frontend/lib/features/freight_quotations/screens/freight_quotations_screen.dart` — Modified:
  - استخدام `valueOrNull` على `transportLocationsProvider` و `freightQuotationsProvider` و `importFilesProvider` لضمان استقرار الشاشة ومنع الأخطاء غير الملتقطة أثناء التشغيل أو الاختبارات.
- `frontend/lib/features/cbm_calculator/screens/cbm_calculator_screen.dart` — Modified:
  - استخدام `valueOrNull` على `projectsProvider` لضمان الاستقرار.
- `frontend/test/inquiries_hub_navigation_test.dart` — Created:
  - حزمة اختبارات تحقق وتفاعل شاملة (4 اختبارات) تتأكد من ظهور قسم استعلامات، إمكانية فتحه، التنقل للشاشتين 49 و 3، وخلو المحاور السابقة من أي تكرار.
- `history/2026-09-06.md` — Modified: توثيق المهمة وفق بروتوكول التوثيق اليومي Append-Only.

### 📊 Technical Changes
- الهيكل المنطقي للقائمة الجانبية:
  - **البيانات والجداول الأساسية (Master Data)**
  - **تخطيط الشحنات والمراحل التشغيلية الـ 6 (Shipment Planning & 6-Phase Lifecycle)**
  - **استعلامات (Inquiries: Freight RFQ & Quotations Comparison + Cargo Measurement Engine CBM)**
  - **لوحة القيادة والتقارير والرقابة (Dashboard & Reports)**
- توافق لغوي أحادي نقي 100%:
  - في الوضع العربي: تظهر ترويسة القسم "استعلامات" والعناصر "طلب ومقارنة عروض النولون والترسية" و "حاسبة الأحجام وتوزيع الحاويات (CBM)".
  - في الوضع الإنجليزي: تظهر ترويسة القسم "Inquiries" والعناصر "Freight RFQ & Quotations Comparison" و "Cargo Measurement Engine".
- الحفاظ التام والكامل على مسارات النظام التاريخية `selectedIndex = 49` و `selectedIndex = 3` ومزودات العمليات والتحديثات الحية.

### 🧪 Validation / Testing
- **Inquiries Hub Navigation Tests:** `flutter test test/inquiries_hub_navigation_test.dart` -> **4 passed, 0 failed (100% Passing)**.
- **Sidebar & Freight RFQ Regression Tests:** `flutter test test/sidebar_localization_test.dart test/freight_rfq_separation_test.dart` -> **5 passed, 0 failed (100% Passing)**.
- **Static Code Analysis:** `flutter analyze lib/features/home/` -> **0 issues found (No issues found!)**.

### 🏁 Next Steps
- إبلاغ المستخدم بالانتهاء الكامل من تنظيم القائمة ونقل الشاشتين إلى قسم "استعلامات" الجديد.
