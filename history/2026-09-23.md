## 📝 [2026-09-23 19:30] - Completed Task: إصلاح إقلاع الباك إند + إعداد أتمتة Claude Code

### 📌 Overview

- **Task Code:** DEVOPS-CLAUDE-001
- **Description:** الباك إند كان مش بيقلع على Python 3.12 (نفس نسخة الـ CI) بسبب أسماء مش معرّفة، وده كان بيوقف كل الـ test suite في مرحلة الـ collection (44 خطأ، صفر تست بيشتغل). اتصلح، واتضاف إعداد Claude Code كامل (hooks و skills و subagents و MCP) عشان يمنع تكرار النوع ده من الأخطاء.

### 📁 Files Changed

- `modules/customs_consultation/service.py` — Modified (إضافة `Dict` للـ import)
- `modules/financial_approval/repository.py` — Modified (إضافة `from typing import Optional`)
- `modules/financial_approval/service.py` — Modified (import `PaymentRequestResponse`)
- `modules/import_files/service.py` — Modified (import `CloneImportFileRequest`, `CloseShipmentSubmit`, `ReopenShipmentSubmit`)
- `modules/import_documentation/service.py` — Modified (import `CustomsDeclarationDraft`)
- `modules/cargox/excel_invoice_service.py` — Modified (import `datetime`, `timezone`)
- `main.py` — Modified (إضافة `import json` المستخدم في قراءة `version.json`)
- `.gitignore` — Modified (تجاهل `.claude/worktrees/` و `.claude/settings.local.json`)
- `CLAUDE.md` — Created
- `.mcp.json` — Created (GitHub + context7)
- `.claude/settings.json` — Created (hooks)
- `.claude/hooks/smoke_import.py` — Created
- `.claude/hooks/protect_files.py` — Created
- `.claude/agents/endpoint-auth-auditor.md` — Created
- `.claude/agents/customs-rules-reviewer.md` — Created
- `.claude/skills/release-check/SKILL.md` — Created
- `.claude/skills/release-check/check_release.py` — Created
- `.claude/skills/{acid-expiry-tracker,cbm-calculator,customs-landed-cost-engine,erp-module-generator,history-logger,screen-audit,smart-nafeza-parser}/SKILL.md` — Created (نسخة مطابقة من `.agents/skills/`)

### 📊 Technical Changes

- **سبب العطل:** على Python 3.12 الـ annotations بتتقيّم وقت تعريف الدالة، فأي `Dict` أو `Optional` أو schema مش متعمله import بيوقع تحميل الموديول، وبالتالي `main.py` كله. أول خطأ دخل في `508bc12` بتاريخ 2026-09-08، والتاني في `1133c0c` بتاريخ 2026-09-20، يعني قبل إصدار v2.0.2 بـ 10 دقايق.
- **طريقة الحصر:** فحص بـ `pyflakes` على `modules/` و `database/` و `utils/` و `main.py`. الحالات اللي فيها annotation نصية (زي `"CargoXStandardInvoiceReviewSession"`) مع import جوه الدالة مش أخطاء، واتسابت زي ما هي.
- **Hook `smoke_import` (PostToolUse):** بعد أي تعديل على ملف `.py` في الباك إند، بيستورد الموديول اللي اتعدّل بس، على قاعدة بيانات مؤقتة (~3.5 ثانية). لو فشل، بيرجع exit 2 فالخطأ يتصلح في ساعته. مش بيلمس `sorour_logistics.db`.
- **Hook `protect_files` (PreToolUse):** بيطبّق قاعدة Append-Only على `history/*.md` (بيمنع `Write` فوق ملف موجود ويسمح بـ `Edit`)، وبيمنع أي تعديل على `.env` وملفات `*.db`.
- **Skill `release-check`:** بتتأكد إن رقم الإصدار واحد في `version.json` و `pubspec.yaml` و `main.py` و `.iss`، وإن `installer_url` و `installer_filename` بيشاوروا على نفس الإصدار، ووجود `installer_sha256` (تحذير بس)، وإن `import main` بيقلع. وبعدها بتشغّل pytest و flutter test.
- **الـ Subagents:** `endpoint-auth-auditor` بيحصر الـ endpoints اللي من غير مصادقة. `customs-rules-reviewer` بيراجع الحسابات الجمركية على قواعد AGENTS.md (7.2 و 7.3).

### 🧪 Validation / Testing

- `python -c "import main"` على Python 3.12: ✅ بيقلع (كان بيطلع `NameError`).
- **Backend pytest:** 909 تست، **903 نجحوا**، 5 فشلوا، 1 اتخطى، في 428 ثانية. قبل الإصلاح: 44 خطأ collection وصفر تست اشتغل.
- **الـ 5 اللي فشلوا معتمدين على بيئة الجهاز، ومالهمش علاقة بالتعديلات دي:**
  - `test_arabic_ocr_engine` (2): محرك الـ OCR مش متثبت، وفي تست منهم النص العربي راجع معكوس.
  - `test_md09_ports_tariff_exchange_audit` (2): بيتوقعوا قاعدة بيانات فيها بيانات مرجعية (100+ ميناء و 50+ بند تعريفة).
  - `test_version_and_updates_api::test_uat_environment_staging_integrity`: بيتوقع ملف `sorour_logistics_uat.db` موجود على الجهاز.
- **Flutter tests:** متشغلوش لأن مفيش أي تعديل على كود Dart.
- **الـ hooks:** اتجربت بحالات تعدّي وحالات تتمنع (history و `.env` و `.db` وموديول بايظ وملف Dart). كلها رجعت النتيجة المتوقعة.
- **`release-check`:** نجحت على الكود الحالي، ومسكت `installer_url` البايت في اختبارين سلبيين.

### ⚠️ ملاحظات مكتشفة

- بعد `import main`، سطر ملخص pytest الأخير (`N passed in Xs`) مش بيظهر في الكونسول، مع إن exit code والـ JUnit XML بيطلعوا صح. عشان كده الحكم على النتيجة لازم يكون بـ exit code أو `--junitxml`، ومش بقراءة آخر الكونسول. وده احتمال كبير سبب إن كوميتات قبل كده قالت إن التستات خضرا من غير ما تكون شغالة.
- التستات بتكتب في مجلدات جوه المشروع (`dist/` و `logs/` و `backups/`)، فهي مش معزولة عن البيئة (not hermetic).

### 🏁 Next Steps

- إضافة مصادقة عامة لكل الـ routers: 6 بس من 50 عليهم dependency مصادقة.
- إضافة `installer_sha256` في `version.json` من الـ CI، والتحقق منه في `auto_updater_service.dart` قبل التشغيل.
- تشغيل `endpoint-auth-auditor` للحصول على قائمة الـ endpoints المكشوفة مرتبة حسب الخطورة.
- ضبط `GITHUB_PERSONAL_ACCESS_TOKEN` كمتغير بيئة عشان GitHub MCP يشتغل.
