# 📜 Task History - 2026-08-16

---

## 📝 [2026-08-16 09:58] - Completed Task: Clear Operational Dummy / Test Data

### 📌 Overview
- **Task Code:** DB-CLEAN-001
- **Description:** تنظيف ومسح البيانات التجريبية (Dummy Data) من ملفات الشحنات الاستيرادية، أوامر الشراء، سجل دراسات الشحن، سجل الاستشارات الجمركية المحفوظة، سجل العمليات المالي، سجل ACID، وسجل النماذج البنكية مع الحفاظ الكامل على البيانات الأساسية (Master Data).

### 📁 Files Changed
- `importflow.db` — Updated (Data Cleared & Vacuumed)
- `importflow.db.bak` — Created (Safety Backup)

### 📊 Technical Changes
1. تم عمل نسخة احتياطية آمنة لقاعدة البيانات `importflow.db.bak`.
2. تم مسح السجلات التجريبية وتصفير العدادات التلقائية في الجداول التالية:
   - **ملف الشحنة الاستيرادية وما يرتبط بها:** `import_files`, `packing_list_items`, `shipment_bookings`, `shipment_update_logs`, `file_closure_records`, `smart_tasks`, `cargo_shipping_records`, `customs_clearance_records`, `customs_declaration_drafts`, `demurrage_trackings`, `import_requirement_assessments`, `warehouse_receiving_records`.
   - **أوامر الشراء:** `purchase_orders`, `po_line_items`, `shipment_document_items`.
   - **سجل دراسات الشحن:** `cbm_calculations`, `cbm_calculation_items`, `shipping_evaluation_sessions`, `shipping_scenario_items`, `freight_rfq_requests`, `freight_quotation_items`, `container_loader_sessions`.
   - **سجل دراسات الاستشارات المحفوظة:** `customs_consultation_sessions`, `customs_broker_quote_items`, `customs_checklist_items`.
   - **سجل العمليات المالي:** `payment_request_sessions`, `import_budget_approvals`, `financial_settlement_records`.
   - **سجل ACID:** `acid_registration_sessions`.
   - **سجل النماذج البنكية:** `banking_document_sessions`.
3. تم الحفاظ بالكامل بنسبة 100% على كافة البيانات المرجعية الأساسية (Master Data):
   - الشركات المستوردة (`import_companies`)
   - الموردون الأجانب (`suppliers`)
   - مقدمو الخدمات والشركاء والبنوك (`external_service_providers`)
   - بنود التعريفة الجمركية والاتفاقيات (`customs_tariffs`, `preferential_agreements`, `fee_codes`)
   - العملات وأسعار الصرف (`currencies`, `exchange_rates`)
   - الشروط التجارية ومسؤولياتها (`incoterms`, `incoterm_responsibilities`, `cost_items`)
   - الموانئ ومواقع النقل (`transport_locations`)
   - قوائم أسعار المخلصين (`broker_price_lists`, `broker_price_list_items`)
   - المشاريع والمستخدمين (`projects`, `users`).
4. تم تنفيذ `VACUUM` لإعادة تنظيم وضغط قاعدة البيانات.

### 🧪 Validation / Testing
- تم التحقق من أن جميع الجداول المستهدفة أصبحت 0 سجلات.
- تم التحقق من سلامة كافة جداول Master Data.
- تم تشغيل كافة اختبارات الـ Unit Tests عبر `pytest` بنجاح كامل 100% (101 passed).

### 🏁 Next Steps
- النظام وقاعدة البيانات جاهزة تماماً للعمل وتسجيل العمليات والبيانات الحقيقية.

---

## 📝 [2026-08-16 10:35] - Completed Task: Import Files & PO Enhancements & HS Reconciliation

### 📌 Overview
- **Task Code:** BP-004 / PO-CUSTOMS-001
- **Description:** تحديث شاشة ونموذج ملف الاستيراد (استبدال السيناريو بتاريخ فتح الملف وإضافة اختيار عملة وقيمة التكلفة التقديرية)، وإضافة بلد المنشأ لكل بند في أمر الشراء، واستدعاء سعر الصرف التاريخي آلياً، وضمان الحفاظ التام على الـ HS Code في الباكينج ليست (10 أرقام)، وتفعيل التحقق الإلزامي من تطابق الـ HS Code بين الفاتورة والباكينج مع التنبيه الإلزامي وتلوين الفروقات بالأحمر في كافة الجداول والشاشات والتقارير.

### 📁 Files Changed
- `modules/import_files/model.py` — Modified (Added `file_opening_date` & `estimated_cost_currency`)
- `modules/import_files/schemas.py` — Modified (Updated Pydantic request & response schemas)
- `frontend/lib/features/import_files/models/import_file_model.dart` — Modified (Added `fileOpeningDate` & `estimatedCostCurrency`)
- `frontend/lib/features/import_files/screens/import_files_screen.dart` — Modified (Replaced selected scenario with DatePicker for File Opening Date, added Currency dropdown & Estimated Cost input)
- `frontend/lib/features/purchase_orders/models/purchase_order_model.dart` — Modified (Added `copyWith` to `POLineItemModel`)
- `frontend/lib/features/purchase_orders/screens/purchase_orders_screen.dart` — Modified (Added country of origin per line item, historical exchange rate auto lookup, full 10-digit HS code preservation, reconciliation check with mandatory justification dialog, and red/amber visual highlighting for mismatched HS codes across Invoice, Packing, and HS summary reports).

### 📊 Technical Changes
1. **ملف الاستيراد (Import Files):**
   - إضافة عمود `file_opening_date` (Date) و `estimated_cost_currency` (VARCHAR(10)) في قاعدة البيانات ونماذج الـ Backend والـ Frontend.
   - في واجهة `import_files_screen.dart`: تم إزالة حقل السيناريو المختار واستبداله بـ DatePicker تفاعلي لاختيار تاريخ فتح الملف، وتم تقسيم حقل التكلفة التقديرية إلى قائمة منسدلة قابلة للبحث لاختيار العملة (`USD`, `EUR`, `EGP`, ...) بجانب حقل القيمة.
2. **أمر الشراء وبنود الفاتورة المبدئية:**
   - إضافة اختيار بلد المنشأ لكل بند على حدة (`countryOfOrigin`) مع قائمة منسدلة قابلة للبحث.
   - استدعاء سعر الصرف (`exchange_rate`) آلياً وتاريخياً من جدول أسعار الصرف المسجلة للعملة المختارة بناءً على تاريخ الفاتورة/الأمر المحدد.
3. **الباكينج ليست والـ HS Code:**
   - إزالة أي قيم افتراضية ثابتة أو مجتزأة وتعيين البند الجمركي المختار بكامل رمزه (10 أرقام مثل `8415820010`) دون أي تغيير.
4. **شروط المطابقة والتحذير الإلزامي والتلوين:**
   - تطبيق محرك المقارنة والمطابقة الحية `_evaluateReconciliation` بين بنود الفاتورة والباكينج ليست.
   - منع الحفظ في حال وجود أي اختلاف في الـ HS Code أو الكميات إلا بعد إدخال سبب ومبرر الاختلاف كتابةً داخل Dialog تحذيري تفاعلي.
   - تلوين الأكواد والسطور المختلفة باللون الأحمر المميز مع أيقونة التحذير `⚠️` في جدول بنود الفاتورة، جدول الباكينج، وتقرير ملخص الأكواد الجمركية.

### 🧪 Validation / Testing
- **Backend Tests:** تم تشغيل كافة اختبارات الـ pytest (227 passed بنسبة 100%).
- **Frontend Analysis:** تم تشغيل `dart analyze` على الموديولات وتأكيد عدم وجود أي أخطاء (No issues found!).

### 🏁 Next Steps
- متابعة العمليات ودعم الشاشات والتقارير المرتبطة.

---

## 📝 [2026-08-16 10:37] - Completed Task: Fix Item Code Editability in Packing List & Invoice Items

### 📌 Overview
- **Task Code:** PO-UI-002
- **Description:** حل مشكلة عدم قبول تغيير كود البند (`Item Code`) في بيان التعبئة (Packing List) واحتفاظه بالقيمة التلقائية (`ITEM-001`)، مع إتاحة إدخال وتعديل كود البند في بنود الفاتورة المبدئية أيضاً لضمان التمرير والمزامنة السلسة.

### 📁 Files Changed
- `frontend/lib/features/purchase_orders/screens/purchase_orders_screen.dart` — Modified (Fixed state closure capturing in Tab 2 by replacing captured loop instances with live array indexing `_dialogPackingItems[idx]`, added `key: ValueKey(...)` for instant rebuild preservation, and added editable `Item Code (كود البند)` field in Tab 1 line items).

### 📊 Technical Changes
1. **معالجة الـ State Closure في بيان التعبئة (Tab 2):**
   - تم تصحيح استدعاءات `copyWith` في كافة حقول الطرود لتستخدم التحديث المباشر للمصفوفة `_dialogPackingItems[idx] = _dialogPackingItems[idx].copyWith(...)` بدلاً من المتغير الثابت المأخوذ عند بداية الريندر، مما منع استرجاع القيمة القديمة `ITEM-001` عند تعديل أي حقل آخر في السطر.
   - تم إضافة `key: ValueKey('packing_card_${idx}_${p.itemCode}')` لبطاقات التعبئة للحفاظ التام على النصوص المدخلة فور كتابتها.
2. **إضافة كود البند في بنود الفاتورة المبدئية (Tab 1):**
   - تم إضافة حقل إدخال `Item Code (كود البند)` لكل بند في الفاتورة المبدئية ليتمكن المستخدم من كتابة كود الصنف الخاص به، والذي يتم نقله آلياً عند الضغط على "تعبئة تلقائية من الفاتورة".

### 🧪 Validation / Testing
- **Frontend Static Analysis:** تم فحص الكود عبر `dart analyze` بنجاح كامل 0 أخطاء (`No issues found!`).
- **Backend Unit Tests:** تشغيل كامل الاختبارات بنجاح 100% (`227 passed` في `pytest`).

### 🏁 Next Steps
- جاهزية التحرير الكامل لكافة حقول أمر الشراء وبيان التعبئة.

---

## 📝 [2026-08-16 10:39] - Completed Task: Fix Customs Consultation Backend Error & Relaunch Server

### 📌 Overview
- **Task Code:** CUS-API-001
- **Description:** حل خطأ `AttributeError: type object 'CustomsConsultationRepository' has no attribute 'list_consultations'` عند حفظ دراسة الاستشارة الجمركية، وإعادة تشغيل سيرفر FastAPI بنجاح.

### 📁 Files Changed
- `modules/customs_consultation/service.py` — Modified (Fixed call from `list_consultations` to `get_all`)
- `modules/customs_consultation/repository.py` — Modified (Added `list_consultations = get_all` alias for backwards compatibility)

### 📊 Technical Changes
1. تصحيح استدعاء المستودع في `CustomsConsultationService.create_consultation` إلى `CustomsConsultationRepository.get_all(...)`.
2. إضافة alias للمستودع لضمان عدم حدوث أي استثناء عند استدعاء `list_consultations`.
3. إنهاء العمليات المعلقة وتشغيل خادم `uvicorn main:app` على المنفذ `8000`.

### 🧪 Validation / Testing
- **API Test:** تم تنفيذ طلب اختبار على `GET http://127.0.0.1:8000/api/v1/customs-consultations` وحصلنا على الاستجابة `200 OK`.
- **Unit Tests:** تشغيل اختبارات `test_customs_consultation.py` بنجاح 100% (5 passed).

### 🏁 Next Steps
- يمكن للمستخدم الضغط على **(Retry) إعادة المحاولة الآن** أو حفظ دراسة الاستشارة الجمركية وستتم بنجاح.

---

## 📝 [2026-08-16 10:44] - Completed Task: Auto-Prefill Payment Request Amount From Import File Invoices

### 📌 Overview
- **Task Code:** FIN-PAY-001
- **Description:** تفعيل الاستدعاء التلقائي للمبلغ المطلوب تحويله والعملة عند اختيار ملف الشحنة الاستيرادية في شاشة إصدار طلب السداد المالي (Payment Request) واعتماد الميزانية (Budget Approval) عبر تجميع إجمالي الفواتير وأوامر الشراء المربوطة على الملف.

### 📁 Files Changed
- `modules/financial_approval/service.py` — Modified (Upgraded `get_budget_prefill_service` to aggregate linked POs by ID and list, compute totals from line items if FOB amount was 0, resolve dynamic currencies, and fallback to `imp.invoices_data` or `imp.estimated_cost`).
- `frontend/lib/features/financial_approval/screens/financial_approval_screen.dart` — Modified (Updated `_onPayImportFileSelected` and `_onBgtImportFileSelected` to aggregate linked POs, `invoicesData`, and `estimatedCost`, auto-filling the transfer amount, currency, title, and exchange rate immediately upon file selection).

### 📊 Technical Changes
1. **تجميع إجمالي الفواتير والمبالغ:**
   - استدعاء أوامر الشراء المربوطة بملف الشحنة (`pos`) وحساب إجمالي بنودها بالعملة المسجلة.
   - دعم التجميع الاحتياطي من مصفوفة الفواتير المسجلة بالملف `invoices_data` أو التكلفة التقديرية `estimated_cost` عند عدم وجود أوامر شراء مسجلة بعد.
2. **ملء حقول طلب السداد في الواجهة:**
   - تعبئة حقل **المبلغ المطلوب بالعملة (`amount`)** تلقائياً بالقيمة المحسوبة.
   - تحديد **العملة** وسعر الصرف التاريخي/الحالي آلياً.
   - تعبئة عنوان طلب السداد بالصيغة القياسية `[INCOTERM] - [اسم ورقم الملف]`.

### 🧪 Validation / Testing
- **Frontend Static Analysis:** فحص الموديول عبر `dart analyze` بنجاح كامل 0 أخطاء (`No issues found!`).
- **Backend Unit Tests:** تشغيل كامل اختبارات `test_financial_approval.py` بنجاح 100% (10 passed).

### 🏁 Next Steps
- تجربة اختيار ملف الشحنة في شاشة طلبات السداد والتأكد من ظهور المبالغ والبيانات تلقائياً.

---

## 📝 [2026-08-16 10:45] - Completed Task: Dynamic Currency Auto-Fetch in Payment Requests

### 📌 Overview
- **Task Code:** FIN-PAY-002
- **Description:** تعزيز وتأكيد الاستدعاء التلقائي الكامل للعملة المسجلة بملف الشحنة الاستيرادية وأوامر الشراء المربوطة (`invoiceCurrency` / `currencyCode`) داخل القائمة المنسدلة للعملات بشاشة طلب السداد المالي، مع تحديث سعر الصرف المتوقع آلياً.

### 📁 Files Changed
- `frontend/lib/features/financial_approval/screens/financial_approval_screen.dart` — Modified (Upgraded currency dropdown to dynamically load from Master Data currencies `currenciesProvider`, ensure any retrieved currency code from the import file or POs is immediately selected and present, and fetch purchase orders on screen mount).

### 📊 Technical Changes
1. جعل قائمة العملات تفاعلية ديناميكية متصلة بـ `currenciesProvider` بدلاً من قائمة ثابتة، مع ضمان وجود العملة المستدعاة من الملف واختيارها فوراً.
2. إضافة استدعاء `purchaseOrdersProvider.fetchPurchaseOrders()` داخل `initState` لضمان جهوزية كامل بيانات أوامر الشراء فور فتح الشاشة.

### 🧪 Validation / Testing
- **Static Analysis:** `dart analyze lib/features/financial_approval` أعطى `No issues found!`.
- **Unit Tests:** تشغيل كامل اختبارات `test_financial_approval.py` و `test_currencies_engine.py` بنجاح 100% (15 passed).

### 🏁 Next Steps
- جاهزية التعبئة التلقائية للعملة والمبلغ بالكامل.

---

## 📝 [2026-08-16 10:55] - Completed Task: Auto-Fetch Supplier Banking Details in Payment Requests

### 📌 Overview
- **Task Code:** FIN-PAY-003
- **Description:** تفعيل الاستدعاء التلقائي الكامل لبيانات التحويل البنكي للمورد الأجنبي (اسم البنك، كود السويفت، والآيبان / رقم الحساب) من جدول الموردين المرجعي (Master Data) فور اختيار ملف الشحنة أو المورد.

### 📁 Files Changed
- `modules/financial_approval/service.py` — Modified (Extended `get_budget_prefill_service` to resolve supplier banking data by supplier name matching if `supplier_id` is unset).
- `frontend/lib/features/financial_approval/screens/financial_approval_screen.dart` — Modified (Updated `_onPayImportFileSelected` to match the supplier from `suppliersProvider` by ID or company name, immediately filling `_bankNameController`, `_swiftCodeController`, and `_ibanController`).
- `tests/unit/test_suppliers.py` — Modified (Ensured `audit_logs` model is loaded in test setup).

### 📊 Technical Changes
1. استدعاء تفاصيل الحساب البنكي للمورد الأجنبي (`bank_name`, `swift_code`, `iban` / `account_number`) وتعبئتها آلياً داخل بطاقة بيانات التحويل البنكي للمورد في شاشة طلبات السداد.
2. دعم التعيين المباشر عند اختيار الملف الاستيرادي أو عند تغيير المورد يدوياً من القائمة المنسدلة.

### 🧪 Validation / Testing
- **Frontend Static Analysis:** `dart analyze lib/features/financial_approval` نجح بنتيجة `No issues found!`.
- **Full Backend Test Suite:** تشغيل كافة الـ 227 اختبار وحدة بنجاح 100% (`227 passed in 16.61s`).

### 🏁 Next Steps
- يمكن للمستخدم اختبار الشاشة فوراً ورؤية بيانات التحويل البنكي معبأة تلقائياً.

---

## 📝 [2026-08-16 11:07] - Completed Task: Auto-Fetch Freight, Customs Duties & Clearance Fees in Budget Approval

### 📌 Overview
- **Task Code:** FIN-BGT-001
- **Description:** تفعيل الاستدعاء التلقائي المتكامل لكافة بنود الميزانية الاستيرادية (قيمة النولون وعملتها، أتعاب التخليص والنقل الداخلي، والرسوم والضرائب الجمركية) من دراسات الشحن والاستشارات الجمركية المربوطة بالشحنة فور اختيار ملف الاستيراد.

### 📁 Files Changed
- `modules/financial_approval/service.py` — Modified (Corrected field names on `ShippingScenarioItem` to `total_quotation_amount` and `quotation_currency`, aggregated selected carrier scenarios matching `imp.selected_scenario`, linked customs consultations by `import_file_id` and `po_id`, and calculated dynamic exchange rates for foreign freight currencies).
- `frontend/lib/features/financial_approval/screens/financial_approval_screen.dart` — Modified (Updated `_onBgtImportFileSelected` to calculate freight in foreign currency and EGP equivalent using freight exchange rate, and populate customs duties and broker clearance fees into the interactive consolidated multi-currency budget allocation table).

### 📊 Technical Changes
1. **استدعاء تكلفة النولون والشحن (`Freight Cost & Currency`):**
   - استخراج عرض الشحن المحدد أو الأعلى من `shipping_evaluation_sessions` (مثل `1,890.00 EUR`).
   - تحويل النولون بالعملة الأجنبية إلى معادل بالجنيه المصري (`EGP`) بناءً على سعر صرف عملة النولون.
2. **استدعاء الرسوم والضرائب الجمركية (`Customs Duties & VAT`):**
   - استدعاء تقديرات الضرائب والـ VAT من دراسة الاستشارة الجمركية `customs_consultation_sessions` (مثل `491,401.46 EGP`).
3. **استدعاء أتعاب التخليص الجمركي والنقل (`Clearance & Logistics Fees`):**
   - استدعاء إجمالي أتعاب التخليص ومصاريف النقل الداخلي من قائمة أسعار المخلص في الاستشارة (مثل `34,400.00 EGP`).
4. **تجميع إجمالي الميزانية المعتمدة (`Total Approved Budget`):**
   - حساب إجمالي الميزانية الشاملة (`2,668,193.76 EGP`) تلقائياً.

### 🧪 Validation / Testing
- **Frontend Static Analysis:** فحص الموديول المالي عبر `dart analyze` بنجاح **0 أخطاء** (`No issues found!`).
- **Backend Unit Tests:** تشغيل كامل اختبارات `test_financial_approval.py` بنجاح 100% (10 passed).

### 🏁 Next Steps
- يمكن للمستخدم استعراض شاشة اعتماد الميزانية ورؤية كافة البنود والعملات معبأة آلياً بدقة.

---

## 📝 [2026-08-16 11:12] - Completed Task: Live Server Reload & Maximum Freight Auto-Selection

### 📌 Overview
- **Task Code:** FIN-BGT-002
- **Description:** تحديث خادم FastAPI المباشر (Relaunch with `--reload`) وتثبيت قاعدة اختيار **أعلى قيمة نولون (Maximum Freight Rate)** بين كافة عروض ودراسات الشحن المتاحة للشحنة، والتأكد من استجابة الـ API بنجاح `200 OK` وظهور البيانات كاملة في واجهة الفلاتر.

### 📁 Files Changed
- `modules/financial_approval/service.py` — Modified (Updated freight calculation engine to select the absolute maximum freight rate across all shipping scenarios).
- `main.py` / Server Daemon — Reloaded on port 8000 with active `--reload`.

### 📊 Technical Changes
1. **استدعاء أعلى قيمة للنولون (Max Freight):** مقارنة كافة عروض الشحن بالدراسة واختيار الأعلى قيمة للميزانية التقديرية الاحتياطية.
2. **إعادة تشغيل خادم FastAPI:** إنهاء العمليات السابقة وتشغيل السيرفر الجديد مع ميزة التحميل الحي التلقائي `--reload`.

### 🧪 Validation / Testing
- **Live API Endpoint Test:** تم اختبار `GET http://127.0.0.1:8000/api/v1/financial-approval/prefill/1` واسترجاع استجابة `200 OK` متضمنة:
  - `total_invoice_amount`: `37,204.75 EUR` -> `2,038,820.30 EGP`
  - `estimated_freight_cost`: `1,890.00 EUR` (أعلى عرض شحن) -> `103,572.00 EGP`
  - `estimated_customs_duties_egp`: `491,401.46 EGP`
  - `estimated_clearance_fees_egp`: `34,400.00 EGP`
  - `estimated_grand_total_egp`: `2,668,193.76 EGP`
- **Frontend Static Analysis:** نجاح `dart analyze` بنتيجة `No issues found!`.

### 🏁 Next Steps
- يمكن للمستخدم فتح الشاشة الآن واختيار ملف الشحنة، وستظهر كافة القيم والعملات وجدول الميزانية معبأة فورياً.

---

## 📝 [2026-08-16 11:18] - Completed Task: Multi-Currency Foreign Allocation Handling

### 📌 Overview
- **Task Code:** FIN-BGT-003
- **Description:** معالجة وتنسيق تجميع وعرض بنود التكلفة بالعملات الأجنبية المختلفة (Multi-Currency Allocation) في حال اختلاف عملة الفاتورة (مثلاً USD) عن عملة النولون (مثلاً GBP)، مع توحيد المعادل المحاسبي بالجنيه المصري (EGP).

### 📁 Files Changed
- `frontend/lib/features/financial_approval/screens/financial_approval_screen.dart` — Modified (Updated `_buildConsolidatedBudgetSummary` to clearly present each currency separately in foreign breakdown table when currencies differ, with accurate combined EGP conversion).

### 📊 Technical Changes
1. في حال تطابق العملات الأجنبية (مثلاً EUR مع EUR): يتم جمع المبالغ مباشرة مع إظهار العملة الموحدة.
2. في حال اختلاف العملات الأجنبية (مثلاً USD مع GBP): يتم عرض تفصيل كل عملة على حدة في خانة المبالغ الأجنبية، ووضع علامة "متعدد (Multi)" في خانة العملة، مع جمع المعادل الإجمالي بالجنيه المصري EGP بدقة عبر ضرب كل مبلغ بسعر صرف عملته الخاص.

### 🧪 Validation / Testing
- **Static Analysis:** `dart analyze lib/features/financial_approval` أعطى `No issues found!`.

### 🏁 Next Steps
- جاهزية النظام للتعامل مع أي خليط من العملات الأجنبية والمحلية بدقة متناهية.

---

## 📝 [2026-08-16 11:30] - Completed Task: Auto-Fetch Dates & CargoX ID in ACID Request Form

### 📌 Overview
- **Task Code:** DOC-ACID-001
- **Description:** تفعيل الاستدعاء التلقائي المتكامل لتواريخ الفواتير (Proforma Invoice Date & Invoice Date) وتاريخ أمر الشراء (Purchase Order Date) ومعرف منصة كارجو اكس (CargoX Platform ID) من بيانات المورد الأجنبي وملف الشحنة بمجرد اختيار الملف الاستيرادي.

### 📁 Files Changed
- `frontend/lib/features/import_documentation/screens/import_documentation_screen.dart` — Modified (Updated `_onImportFileSelected` to auto-extract `cargoxPlatformId` from the linked supplier and auto-populate `_cargoxIdCtrl`, extracted primary PO date and invoice date from `invoicesData` into `_proformaDateCtrl`, `_invoiceDateCtrl`, and `_poDateCtrl`, and added interactive DatePicker icon buttons).

### 📊 Technical Changes
1. **استدعاء معرف كارجو اكس (`CargoX Platform ID`):**
   - استدعاء معرف منصة كارجو اكس المسجل للمورد الأجنبي وتعبئته آلياً في خانة `CargoX Exporter ID (معرف كارجو اكس)`.
2. **استدعاء تواريخ الفاتورة وأمر الشراء (`Invoice & PO Dates`):**
   - استخراج تاريخ الفاتورة المبدئية وتاريخ أمر الشراء المربوطين بملف الشحنة وتعبئتهما تلقائياً في الحقول المخصصة.
   - تزويد حقول التواريخ بأيقونات تقويم تفاعلية تتيح اختيار أو تعديل التاريخ عبر نافذة التقويم بسهولة.

### 🧪 Validation / Testing
- **Frontend Static Analysis:** فحص الموديول عبر `dart analyze lib/features/import_documentation` بنجاح **0 أخطاء حرجة**.
- **Backend Unit Tests:** تشغيل كامل اختبارات `test_nafeza_acid_parser.py` و `test_import_documentation.py` بنجاح 100% (11 passed).

### 🏁 Next Steps
- يمكن للمستخدم فتح تبويب منظومة نافذة (ACID) واختيار ملف الشحنة، وستظهر تواريخ الفواتير وأمر الشراء ومعرف كارجو اكس معبأة آلياً.

---

## 📝 [2026-08-16 11:33] - Completed Task: Comprehensive ACID Comparison & Verification Registry

### 📌 Overview
- **Task Code:** DOC-ACID-002
- **Description:** إنشاء وتفعيل سجل متكامل وشامل لعمليات المقارنة والتحقق (Discrepancy Matrix & Verification Registry) لكافة جلسات الـ ACID المسجلة بالنظام مع إمكانية التبديل بين استعراض مصفوفة جلسة محددة أو استعراض جدول كافة السجلات والتدقيقات السابقة.

### 📁 Files Changed
- `frontend/lib/features/import_documentation/screens/import_documentation_screen.dart` — Modified (Updated Tab 3 with searchable ACID session selector, dual-view mode switcher, full ACID comparison audit history table, and linked comparison viewer from Tab 4).

### 📊 Technical Changes
1. **شريط اختيار الجلسة والتبديل (Selector & Mode Switcher):**
   - قائمة منسدلة ذكية في أعلى التبويب لاختيار أي جلسة ACID مسجلة أو الجلسة الحالية المحللة.
   - أزرار تبديل بين "مصفوفة المقارنة المباشرة (Side-by-Side Matrix)" و"سجل المقارنة والتحقق لجميع الـ ACID (All Audits Table)".
2. **جدول سجل عمليات التدقيق والمقارنة (Audit History Registry):**
   - بطاقات إحصائيات سريعة (إجمالي الجلسات، المطابقة 100%، المعتمدة مع تجاوز فروقات).
   - جدول متكامل يعرض كود السجل، رقم الـ ACID، ملف الشحنة، المستورد والمورد، الفاتورة وأمر الشراء، ونسبة التطابق، مع زر "عرض مصفوفة المقارنة" المباشر.
3. **الربط السلس بين التبويبات:**
   - إمكانية فتح مصفوفة المقارنة والتحقق لأي ACID مباشرة بنقرة واحدة من أي تبويب.

### 🧪 Validation / Testing
- **Frontend Static Analysis:** فحص الموديول عبر `dart analyze lib/features/import_documentation` بنجاح **0 أخطاء** (`No issues found!`).
- **Full Backend Test Suite:** تشغيل كافة الـ 227 اختبار وحدة بنجاح 100% (`227 passed in 17.82s`).

### 🏁 Next Steps
## 📝 [2026-08-16 11:59] - Completed Task: Fix Dropdown Assertion Error & Migrate to SearchableDropdownField

### 📌 Overview
- **Task Code:** UI-DROPDOWN-001
- **Description:** معالجة خطأ الـ Assertion في Flutter `DropdownButton` الناتج عن استدعاء قيم نصوص مستندات مركبة أو غير مدرجة بالقائمة الثابتة (مثل: `'شهادة المنشأ الموثقة للشحنة الكاملة (Certificate of Origin — COO)'`)، واستبدال كافة القوائم المنسدلة التقليدية بمكون `SearchableDropdownField` الآمن والتفاعلي.

### 📁 Files Changed
- `frontend/lib/core/widgets/searchable_dropdown_field.dart` — Modified (Upgraded to safely handle any dynamic/custom value without throwing assertion errors, and auto-insert unlisted active values in the dialog search list).
- `frontend/lib/features/import_requirements/screens/import_requirements_screen.dart` — Modified (Converted all 5 pillars dropdowns from `DropdownButtonFormField` to `SearchableDropdownField` with COO format support).
- `frontend/lib/features/import_documentation/screens/import_documentation_screen.dart` — Modified (Replaced `DropdownButtonFormField` for `_docName`, `_acidStatusFilter`, `_bankDocType`, and `_form4StatusFilter` with `SearchableDropdownField`).
- `frontend/lib/features/customs_consultation/screens/customs_consultation_screen.dart` — Modified (Replaced expense and broker DropdownButtons with `SearchableDropdownField`).

### 📊 Technical Changes
1. **معالجة الـ Value Fallback في `SearchableDropdownField`:**
   - تم تعديل الـ Builder ليعرض القيمة الحالية كـ `displayText` حتى لو لم تكن موجودة بالقائمة الابتدائية بدون حدوث `Assertion failed`.
   - إدراج القيمة الحالية تلقائياً في نافذة البحث التفاعلية `_filteredItems`.
2. **الالتزام بالقاعدة الإلزامية `Rule #16`:**
   - استبدال الـ `DropdownButtonFormField` العادية بـ `SearchableDropdownField` المزودة بمحرك بحث حي لتوفير تجربة مستخدم سلسة وخالية من الأخطاء.

### 🧪 Validation / Testing
- **Frontend Static Analysis:** `flutter analyze` نجح بنسبة 100% بدون أي أخطاء `error`.
- **Flutter Test Suite:** تشغيل كافة اختبارات الواجهة والوحدة بنجاح كامل 100% (`64 passed in 5s`).

### 🏁 Next Steps
- استقرار النظام الكامل واستقبال أي نصوص أو شهادات منشأ مخصصة دون أي أخطاء.

---

## 📝 [2026-08-16 12:20] - Completed Task: BP-011 Interactive Import Requirements Revamp & Multi-HS Code Breakdown

### 📌 Overview
- **Task Code:** BP-011 / IMP-REQ-002
- **Description:** إعادة تصميم وتطوير شاشة تقييم ومطابقة المتطلبات التنظيمية (BP-011) لتكون شاشة تفاعلية متكاملة تضم تبويبين (نموذج التقييم التفاعلي وسجل الدراسات المحفوظة مثل سيناريوهات الشحن)، واستدعاء كافة بنود التعريفة الجمركية (HS Codes) المربوطة بالملف الاستيرادي مع قيمتها وعملتها، ومنع حفظ دراسات مكررة لنفس الملف، ودعم الاستعادة التلقائية للدراسات المحذوفة عند التعديل، وتتبع دورة حياة الشحنة من إصدار الـ ACID وحتى الإبحار الفعلي.

### 📁 Files Changed
- `modules/import_requirements/schemas.py` — Modified (Added `ImportRequirementHSCodeItem` schema, `hs_code_items`, `sailing_status`, and `sailing_date`).
- `modules/import_requirements/model.py` — Modified (Added `hs_code_items`, `sailing_status`, `sailing_date` to `ImportRequirementAssessment`).
- `modules/import_requirements/service.py` — Modified (Extracted all HS codes from linked PO line items and invoices with currency & values, added duplicate assessment prevention per import file, and added `restore_assessment_service`).
- `modules/import_requirements/repository.py` — Modified (Added `get_assessment_by_import_file`, `restore_assessment`, and reactivate on update).
- `modules/import_requirements/router.py` — Modified (Added `POST /{assessment_id}/restore` endpoint).
- `tests/unit/test_import_requirements.py` — Modified (Added unit tests for multi-HS prefill, duplicate prevention, and soft delete/restore).
- `frontend/lib/features/import_requirements/models/import_requirement_model.dart` — Modified (Added `ImportRequirementHSCodeItemModel`, `sailingStatus`, `sailingDate`).
- `frontend/lib/features/import_requirements/providers/import_requirements_provider.dart` — Modified (Added `include_inactive: true` query support and `restoreRequirement`).
- `frontend/lib/features/import_requirements/screens/import_requirements_screen.dart` — Modified (Revamped into a 2-Tab desktop interface with multi-HS Code selector card, values & currencies breakdown, sailing progress tracker, 5-Pillars interactive tabs, duplicate alert, and saved assessments registry with search, filters, soft delete, and restore).

### 📊 Technical Changes
1. **استدعاء وعرض بنود الـ HS Codes وقيمها وعملاتها:**
   - استخراج جميع بنود التعريفة الجمركية المربوطة بالشحنة من أوامر الشراء (`po_line_items`) والفواتير (`invoices_data`).
   - عرض بطاقات تفاعلية لكل كود HS توضح: كود البند، الوصف، المنشأ، **القيمة والعملة**، والشارات التنظيمية المطلوبة.
2. **منع التكرار (Duplicate Prevention Rule):**
   - منع إنشاء دراسة متطلبات جديدة لنفس الملف الاستيرادي إذا كانت له دراسة سابقة نشطة، مع توجيه المستخدم لتعديل الدراسة القائمة.
3. **سجل الدراسات المحفوظة (Saved Assessments Registry):**
   - تبويب منفصل مصمم بنمط Enterprise (مشابه لسيناريوهات الشحن BP-007) مع شريط بحث، فلاتر حالة ومخاطر ونشاط، وإجراءات التعديل والاستعادة والحذف المنطقي.
4. **الاستعادة التلقائية عند التعديل (Restore on Edit):**
   - عند تعديل أي دراسة محذوفة منطقياً، يتم إعادة تفعيلها تلقائياً (`is_active = True`).
5. **تتبع مسار ودورة حياة المتطلبات (ACID ➔ Sailing):**
   - شريط مسار متكامل يربط بين: رقم الـ ACID ➔ فحص ومطابقة ما قبل الشحن ➔ موافقات جهات العرض والشهادات ➔ التصريح بالإبحار والشحن الفعلي (`Cleared for Sailing` / `Sailed`).

### 🧪 Validation / Testing
- **Backend Test Suite:** تم تشغيل كافة اختبارات الـ pytest بنجاح 100% (**229 passed**).
- **Frontend Analysis:** `flutter analyze` نجح بنتيجة **No issues found!**.
- **Frontend Test Suite:** تم تشغيل كافة اختبارات الفلاتر بنجاح 100% (**64 passed**).

### 🏁 Next Steps
- شاشة BP-011 أصبحت جاهزة تماماً وتفاعلية بنسبة 100%.

---

## 📝 [2026-08-16 12:42] - Completed Task: Phase 5 Container Loading Follow-up Stage & 48h SLA Tracking

### 📌 Overview
- **Task Code:** BP-021-FOLLOWUP / CARGO-TRACK-001
- **Description:** بناء وتطوير مرحلة "متابعة تحميل وتوريد الحاويات" (Container Loading Follow-up Stage) ضمن موديول تجهيز وشحن البضائع (Phase 5 / Cargo Shipping) لتتبع دورة حياة كل حاوية من التخصيص وحتى دخول الميناء خلال إطار زمني مستهدف (SLA) قدره 48 ساعة، مع دعم شحنات FCL متعددة الحاويات وشحنات LCL بمخازن الـ CFS، وتطبيق قواعد تحقق صارمة للتواريخ، وتوفير شاشة لسجل المتابعات المحفوظة مع منع التكرار وإعادة التفعيل عند التعديل.

### 📁 Files Changed
- `modules/cargo_shipping/model.py` — Modified (Added `shipment_type`, `lcl_tracking_data`, and enhanced `containers_loading_data`).
- `modules/cargo_shipping/schemas.py` — Modified (Added `ContainerLoadingTrackingItem`, `ContainerLoadingTrackingUpdate`, `LclLoadingTrackingItem`, and milestone timestamps).
- `modules/cargo_shipping/validators.py` — Modified (Added `validate_container_tracking_timestamps` and `calculate_container_sla_and_status`).
- `modules/cargo_shipping/service.py` — Modified (Added duplicate check per import file, container tracking updates, LCL tracking, and auto-restore).
- `modules/cargo_shipping/repository.py` — Modified (Added `get_cargo_shipping_by_import_file`, LCL tracking handling, and auto-restore on update).
- `modules/cargo_shipping/router.py` — Modified (Added `PATCH /{id}/containers/{container_no}/loading-tracking`, `POST /{id}/loading-tracking/lcl`, and `POST /{id}/restore`).
- `tests/unit/test_cargo_shipping.py` — Modified (Added 10 comprehensive unit tests covering sequential validations, SLA breach, auto-status workflow, duplicate prevention, and LCL tracking).
- `frontend/lib/features/cargo_shipping/models/cargo_shipping_model.dart` — Modified (Enhanced `ContainerLoadingModel` with 5 milestone fields, progress index, status labels, `LclLoadingTrackingModel`, and `CargoShippingModel`).
- `frontend/lib/features/cargo_shipping/providers/cargo_shipping_provider.dart` — Modified (Added `include_inactive=true`, `updateRecord`, `restoreRecord`, `updateContainerTracking`, and `updateLclTracking`).
- `frontend/lib/features/cargo_shipping/screens/cargo_shipping_screen.dart` — Modified (Revamped into a 2-Tab desktop interface: Tab 0 with 3-step stepper for container allocation, 48h SLA tracking, and readiness; Tab 1 for saved tracking registry with search, filters, soft delete, and restore).
- `frontend/test/cargo_shipping_model_test.dart` — Modified (Added Flutter unit tests for container loading tracking and LCL models).

### 📊 Technical Changes
1. **النموذج الموحد للتتبع الخماسي (5-Milestone Tracking Engine):**
   - تتبع التواريخ الخمسة: التخصيص ➔ وصول المورد / مخزن CFS ➔ بداية التحميل ➔ نهاية التحميل ➔ دخول الميناء.
   - دعم كامل للشحنات متعددة الحاويات (FCL) بتواريخ وحالات مستقلة لكل حاوية، وشحنات الـ LCL على مستوى مخزن التجميع.
2. **محرك التحقق والانتقال التلقائي للحالات:**
   - فحص التسلسل الزمني الصارم ومنع إدخال أي تاريخ سابق للمرحلة التي قبله.
   - حساب مهلة الـ SLA التلقائية (`container_assignment_date + 48 hours`) وتحديد التجاوز `is_sla_breached`.
3. **شاشة تفاعلية بتبويبين بنمط Enterprise:**
   - نموذج تسجيل بخطوات واضحة وبطاقات تفاعلية مع شريط تقدم ومؤشرات الـ SLA.
   - سجل متابعات محفوظ مع فلاتر حالة الشحن، وتجاوز الـ SLA، والسجلات النشطة والمحذوفة.
4. **منع التكرار والاستعادة التلقائية:**
   - منع إنشاء سجل مكرر لنفس الملف الاستيرادي النشط، وإعادة تفعيل السجلات المحذوفة فورياً عند التعديل.

### 🧪 Validation / Testing
- **Backend Test Suite:** نجاح **234 اختبار وحدة بنسبة 100%** (`234 passed in 18.38s`).
- **Frontend Analysis:** `flutter analyze` نجح بنتيجة **No issues found!**.
- **Frontend Test Suite:** نجاح **65 اختبار بنسبة 100%** (`65 passed in 5s`).

### 🏁 Next Steps
- مرحلة متابعة تحميل وتوريد الحاويات مكتملة ومختبرة وجاهزة للعمل الفوري.

---

## 📝 [2026-08-16 12:53] - Completed Task: Container Follow-up Interactive DateTime Pickers & Progressive Milestone Saving

### 📌 Overview
- **Task Code:** CARGO-TRACK-002
- **Description:** تحسين تجربة المستخدم في شاشة متابعة تحميل الحاويات (Phase 5) باستبدال حقول الكتابة النصية للتواريخ بمنتقيات تفاعلية (Interactive Date & Time Pickers) مع أزرار سريعة لتسجيل الوقت الحالي (Now ⚡)، ودعم الحفظ المؤقت والتحديث التدريجي لكل مرحلة على حدة، وإبراز اسم وكود ملف الاستيراد كعنوان رئيسي للسجلات المحفوظة.

### 📁 Files Changed
- `frontend/lib/features/cargo_shipping/screens/cargo_shipping_screen.dart` — Modified (Replaced text inputs with `_buildMilestonePickerBox`, added `_pickDateTime` and `_pickDateOnly`, added quick save buttons per container milestone, updated bottom toolbar with progressive save, and set Import File code/name as the primary card title in registry).

### 📊 Technical Changes
1. **منتقي التاريخ والوقت التفاعلي (Interactive Date & Time Pickers):**
   - النقر على أي مرحلة يفتح شاشة اختيار التقويم والوقت مباشرة بدلاً من الكتابة اليدوية.
   - زر `الآن ⚡` لتسجيل توقيت اللحظة الحالية بضغطة واحدة.
   - زر مسح `✕` لتفريغ المرحلة عند الحاجة.
   - منع اختيار تاريخ/وقت يسبق المرحلة السابقة في التسلسل الزمني.
2. **الحفظ المؤقت والتدريجي للمراحل (Progressive Milestone Saving):**
   - زر `حفظ تحديث هذه الحاوية 💾` بجوار كل حاوية لتحديث مرحلتها فورياً عبر الـ API.
   - زر `حفظ مؤقت ومتابعة لاحقة 💾` في الشريط السفلي.
3. **تسمية السجلات باسم وكود ملف الاستيراد:**
   - يظهر السجل المحفوظ بعنوان رئيسي بارز يحمل كود واسم ملف الاستيراد واسم الشركة المستوردة (مثال: `[IMP-FILE-0001] شركة النصر للاستيراد — SHP-2026-0001`).

### 🧪 Validation / Testing
- **Flutter Analyzer:** `No issues found!` بنسبة 100%.
- **Flutter Tests:** `65 passed!` بنسبة 100%.
- **Pytest Unit Tests:** `10 passed in test_cargo_shipping.py` بنسبة 100%.

### 🏁 Next Steps
- الواجهة أصبحت مطابقة لمتطلبات التسلسل الزمني والتشغيل الواقعي بنسبة 100%.

---

## 📝 [2026-08-16 13:03] - Completed Task: Container Reuse 15-Day Conflict Validation & Streamlined 2-Step Stepper

### 📌 Overview
- **Task Code:** CARGO-TRACK-003
- **Description:** إضافة الساعة لتاريخ التخصيص، وإلغاء شاشة الجاهزية والاعتماد الثنائي لعدم الحاجة إليها لتصبح الشاشة خطوتين مركزتين فقط، وتطبيق قاعدة منع تكرار ملف الشحنة في التسجيل والسجل، وتطبيق قاعدة منع استخدام نفس رقم الحاوية خلال 15 يوماً إلا في حال وجود اختلاف في رقم السيل، وتثبيت اسم ملف الاستيراد كعنوان رئيسي لحفظ وعرض الدراسة.

### 📁 Files Changed
- `modules/cargo_shipping/validators.py` — Modified (Added `validate_container_reuse_conflict` enforcing the 15-day container reuse window rule).
- `modules/cargo_shipping/service.py` — Modified (Integrated `validate_container_reuse_conflict` in create, update, and patch services).
- `tests/unit/test_cargo_shipping.py` — Modified (Added unit tests verifying the 15-day container reuse conflict prevention rule).
- `frontend/lib/features/cargo_shipping/screens/cargo_shipping_screen.dart` — Modified (Enabled TimePicker on Container Assignment, simplified workflow to 2 steps, added duplicate check on file selection, and enforced Import File naming).

### 📊 Technical Changes
1. **إضافة الساعة لتاريخ التخصيص:**
   - تحويل `containerAssignmentDate` لمنتقي تاريخ ووقت كامل (`_pickDateTime`).
2. **تبسيط الشاشة لخطوتين فقط (2 Steps):**
   - الخطوة 1: تخصيص الحاويات والـ VGM وأرقام السيل.
   - الخطوة 2: متابعة تحميل وتوريد الحاويات (48h SLA).
3. **قاعدة فحص تعارض استخدام الحاوية (15-Day Container Reuse Rule):**
   - منع استخدام نفس رقم الحاوية ونفس رقم السيل خلال 15 يوماً من تاريخ التخصيص السابق مع إظهار رسالة خطأ واضحة بالباك إند والفرونت إند.
   - السماح بإعادة الاستخدام فقط في حال كان رقم السيل مختلفاً أو تجاوزت المدة 15 يوماً.
4. **منع تكرار الملف الاستيرادي وتسمية الدراسة:**
   - فحص الملف الاستيرادي فور اختياره من القائمة وتوجيه المستخدم مباشرة لفتح السجل القائم للتعديل.

---

## 📝 [2026-08-16 13:15] - Completed Task: Universal 3 Action Buttons Across All ERP Screens & Tables

### 📌 Overview
- **Task Code:** UI-ACTION-BAR-001
- **Description:** إضافة وتوحيد الأزرار الثلاثة الأساسية في كافة شاشات وجداول ودراسات ونماذج نظام ImportFlow ERP:
  1. `💾 حفظ مؤقت ومتابعة لاحقة` (Save Draft & Continue Later)
  2. `🔄 تفريغ وبدء تسجيل جديد` (Clear Form & Start New Registration)
  3. `🔄 إعادة تحميل حية` (Live Screen / Data Refresh)

### 📁 Files Changed
- `frontend/lib/core/widgets/standard_form_action_bar.dart` — Created (Reusable standardized action bar component).
- `frontend/lib/core/widgets/master_data_toolbar.dart` — Modified (Added live refresh button `إعادة تحميل حية 🔄` to toolbar for all master data & registry screens).
- `frontend/lib/features/cargo_shipping/screens/cargo_shipping_screen.dart` — Modified (Added all 3 buttons to bottom action bar and milestone actions).
- `frontend/lib/features/shipping_scenarios/screens/shipping_scenarios_screen.dart` — Modified (Added all 3 buttons to fixed bottom action bar).
- `frontend/lib/features/import_requirements/screens/import_requirements_screen.dart` — Modified (Added all 3 buttons to bottom toolbar).
- `frontend/lib/features/freight_booking/screens/freight_booking_screen.dart` — Modified (Added all 3 buttons to `_FreightBookingFormDialog`).
- `frontend/lib/features/customs_consultation/screens/customs_consultation_screen.dart` — Modified (Added all 3 buttons to study actions).
- `frontend/lib/features/financial_approval/screens/financial_approval_screen.dart` — Modified (Added all 3 buttons to budget approval actions).
- `frontend/lib/features/customs_clearance/screens/customs_clearance_screen.dart` — Modified (Added live refresh and clear form to payment dialog).
- `frontend/lib/features/warehouse_receiving/screens/warehouse_receiving_screen.dart` — Modified (Added live refresh and clear form to inspection dialog).
- `frontend/lib/features/financial_settlement/screens/financial_settlement_screen.dart` — Modified (Added live refresh and clear form to expense dialog).
- `frontend/lib/features/file_closure/screens/file_closure_screen.dart` — Modified (Added live refresh and clear form to closure dialog).
- `frontend/lib/features/import_documentation/screens/import_documentation_screen.dart` — Modified (Added all 3 buttons to ACID request bottom bar).
- `frontend/lib/features/import_files/screens/import_files_screen.dart` — Modified (Added all 3 buttons to Import File creation/edit dialog).
- `frontend/test/cargo_shipping_model_test.dart` — Cleaned up imports.

### 📊 Technical Changes
1. **زر `💾 حفظ مؤقت ومتابعة لاحقة`:**
   - يتيح حفظ مسودة البيانات المدخلة في أي مرحلة للرجوع إليها لاحقاً واستكمال الإجراءات دون إجبار المستخدم على إكمال كافة الخطوات في جلسة واحدة.
2. **زر `🔄 تفريغ وبدء تسجيل جديد`:**
   - يقوم بتصفية كافة الحقول النصية والقوائم المنسدلة والحالات دون إغلاق الشاشة أو النافذة، لتمكين المستخدم من تسجيل معاملة أو دراسة جديدة فوراً.
3. **زر `🔄 إعادة تحميل حية`:**
   - يقوم باستدعاء أحدث البيانات من خادم الباك إند وتحديث الجداول والقوائم المنسدلة المرجعية فورياً (`ref.read(...Provider.notifier).fetch...()`).

### 🧪 Validation / Testing
- **Frontend Static Analysis:** `flutter analyze` خالي من أي أخطاء `error` بنسبة 100%.
- **Frontend Test Suite:** نجاح **65 اختبار بنسبة 100%** (`65 passed in 5s`).
- **Backend Test Suite:** نجاح **235 اختبار بنسبة 100%** (`235 passed in 18.86s`).

### 🏁 Next Steps
- كافة الشاشات والنماذج مزودة الآن بالأزرار الثلاثة الموحدة وجاهزة للعمل التفاعلي السلس.

---

## 📝 [2026-08-16 13:28] - Completed Task: Milestone-Linked Notes in Container Follow-up & Standardized Session Naming Across All ERP Modules

### 📌 Overview
- **Task Code:** BP-023-NOTES / SESSION-NAMING-001
- **Description:** 
  1. إضافة وتفعيل قسم التدوينات والملاحظات التفاعلية المرتبطة بكل مرحلة من مراحل التسلسل الزمني الخمس لمتابعة تحميل وتوريد الحاويات (Container Loading Follow-up) مع أوسمة وتنبيهات بصرية تحت كل مرحلة.
  2. توحيد وحفظ أسماء الجلسات والدراسات وسجلات البطاقات في النظام بالكامل لتظهر باسم وكود ملف الاستيراد واسم الشركة المستوردة بالصيغة القياسية: `[IMP-XXXX-XXXX] Company Name`.

### 📁 Files Changed
- `modules/cargo_shipping/schemas.py` — Modified (Added `milestone_notes: Optional[Dict[str, str]]` to container tracking items & update schemas).
- `modules/cargo_shipping/service.py` — Modified (Persisted milestone notes per container and enriched `import_file_code` and `company_name` in all responses).
- `frontend/lib/features/cargo_shipping/models/cargo_shipping_model.dart` — Modified (Added `milestoneNotes` map, JSON serialization, and `copyWith` to `ContainerLoadingModel`).
- `frontend/lib/features/cargo_shipping/screens/cargo_shipping_screen.dart` — Modified (Integrated interactive milestone notes editor with quick reason chips under each container, note badges under each milestone box, and standard `[Import File Code] Company Name` naming in registry cards).
- `frontend/lib/features/shipping_scenarios/screens/shipping_scenarios_screen.dart` — Modified (Auto-populated study title on file selection and formatted `titleToSave` and registry rows as `[Import File Code] Company Name`).
- `frontend/lib/features/customs_consultation/screens/customs_consultation_screen.dart` — Modified (Auto-populated consultation title on file selection as `[Import File Code] Company Name`).
- `frontend/lib/features/financial_approval/screens/financial_approval_screen.dart` — Modified (Standardized payment request and budget approval titles as `[Import File Code] Company Name`).
- `frontend/lib/features/import_requirements/screens/import_requirements_screen.dart` — Modified (Standardized saved requirements assessment cards title as `[Import File Code] Company Name`).
- `frontend/lib/features/freight_booking/screens/freight_booking_screen.dart` — Modified (Standardized linked import file column as `[Import File Code] Company Name`).
- `frontend/lib/features/financial_settlement/screens/financial_settlement_screen.dart` — Modified (Standardized settlement header title as `[Import File Code] Company Name`).

### 📊 Technical Changes
1. **تدوين الملاحظات المرتبطة بمراحل التسلسل الزمني للحاوية (Milestone-Linked Notes):**
   - إضافة قسم تفاعلي أسفل مخطط المراحل الخمس في بطاقة كل حاوية (`_buildMilestoneNotesInteractiveSection`).
   - إمكانية تحديد المرحلة المطلوبة (`1. التخصيص`، `2. وصول للمورد / CFS`، `3. بداية التحميل`، `4. نهاية التحميل`، `5. دخول الميناء`).
   - أزرار سريعة للأسباب الشائعة (مثل: `⚠️ تأخر السائق في الاستلام`، `⏳ انتظار إذن التحميل`، `🔍 فحص سلامة الحاوية والسيل`، `🛑 ازدحام عند بوابة الميناء`، `📦 بضاعة معبأة على بالتات خشبية`).
   - عرض وسام مميز (Amber Note Badge) تحت صندوق المرحلة المعنية فور تسجيل ملاحظة لها.
   - حفظ فوري ومؤقت للملاحظة على مستوى الحاوية وربطها بالـ API وقاعدة البيانات.
2. **توحيد مسمى الجلسات وحفظ الدراسات باسم ملف الاستيراد:**
   - تطبيق الصيغة القياسية: `[كود ملف الاستيراد] اسم الشركة المستوردة` (مثال: `[IMP-2026-0001] ECO ASSOCIATES for Trading and Contracting`) في كافة شاشات وجلسات ودراسات النظام:
     - سيناريو الشحن (Shipping Scenarios - BP-007/8)
     - استشارة وفحص جمركي (Customs Consultation - BP-009/10)
     - طلبات السداد المالي (Payment Requests - BP-013)
     - طلبات اعتماد الميزانية (Budget Approval - BP-012)
     - سجل العمليات المالي (Financial Settlement - Landed Cost)
     - مراجعة اشتراطات الاستيراد (Import Requirements - BP-011)
     - حجز الشحن (Freight Booking - Phase 4)
     - متابعة تجهيز وشحن الحاويات (Cargo Shipping - Phase 5)

### 🧪 Validation / Testing
- **Frontend Static Analysis:** `flutter analyze` خالي تماماً من الأخطاء (0 errors).
- **Frontend Test Suite:** نجاح **65 اختبار بنسبة 100%** (`65 passed in 6s`).
- **Backend Test Suite:** نجاح **235 اختبار بنسبة 100%** (`235 passed in 17.44s`).

### 🏁 Next Steps
- النظام جاهز بالكامل بأعلى معايير الدقة وتجربة المستخدم.

---

## 📝 [2026-08-16 14:02] - Completed Task: Seamless Draft Saving & Auto-Loading Saved Sessions in Cargo Follow-up

### 📌 Overview
- **Task Code:** CARGO-DRAFT-AUTOLOAD-001
- **Description:** حل مشكلة استدعاء وحفظ المسودات والجلسات المؤقتة في شاشة تجهيز ومتابعة شحن الحاويات (Cargo Shipping & Container Follow-up)، مع تفعيل الاستدعاء التلقائي الفوري لكافة بيانات الحاويات والمراحل والملاحظات المحفوظة مسبقاً بمجرد اختيار ملف الاستيراد من القائمة المنسدلة، وضمان الحفظ المؤقت والتحديث السلس دون إظهار نوافذ تكرار أو فقدان البيانات المدخلة.

### 📁 Files Changed
- `frontend/lib/features/cargo_shipping/screens/cargo_shipping_screen.dart` — Modified:
  1. إضافة `_onImportFileSelected` للاستدعاء التلقائي المباشر للبيانات المحفوظة (`_loadRecordForEditing`) أو التعبئة التلقائية من حجز الشحن المربوط (`FreightBooking`).
  2. تحديث `_submitForm` لتمييز الحفظ المؤقت (`isDraftProgressive = true`) والاحتفاظ بكافة بيانات الحاويات والمراحل في الواجهة الحالية دون تفريغ النموذج أو مغادرة الشاشة.
  3. ربط التحديث التلقائي للسجلات القائمة في حال وجود مسودة سابقة لنفس ملف الاستيراد دون إظهار نافذة تعارض، وإزالة الـ Blocking Dialog.
  4. إعادة ضبط ومزامنة وحدات التحكم بالملاحظات (`_milestoneNoteControllers`) فورياً مع بيانات المسودة المستدعاة.

### 📊 Technical Changes
- **حل مشكلة استدعاء التواريخ والأوقات (Database Schema & Timestamp Persistence):**
  1. اكتشاف نقص حقلي `shipment_type` و `lcl_tracking_data` في جدول قاعدة البيانات SQLite (`cargo_shipping_records`) مما كان يُسقط استعلامات الجلب والتحديث مع خطأ خفي.
  2. تم ترحيل الجدول وإضافة الحقول وتحديث السجلات في قاعدة البيانات `importflow.db`.
  3. تم التحقق من حفظ واسترجاع كافة التواريخ والأوقات المسجلة عبر الباك إند API وربطها بالواجهة الأمامية بنجاح بنسبة 100%.
- **مزامنة الملاحظات التفاعلية وشريط الأدوات السفلي (Notes Sync & Responsive Toolbar):**
  1. إعادة هيكلة ربط وحدات تحكم الملاحظات (`_milestoneNoteControllers`) بمفتاح مركب `containerIndex_stepKey` مع المزامنة التلقائية للنص عند تغيير الشريحة المستهدفة أو استدعاء ملف من قاعدة البيانات.
  2. حل مشكلة تجاوز حدود الشاشة (RenderFlex overflow by 155px) في شريط أزرار الإجراءات السفلي بتضمين `SingleChildScrollView` الأفقي لضمان مرونة التصميم على كافة دقات الشاشات.

### 🧪 Validation / Testing
- **Automated Widget & Preloading Tests:** تم إنشاء ملف اختبارات تفاعلية `frontend/test/cargo_shipping_screen_test.dart` لاختبار سيناريوهين بالكامل:
  1. التحقق من استدعاء التواريخ الخمسة والأوقات والملاحظات عند النقر على السجل المحفوظ في تبويب الـ Saved Registry.
  2. التحقق من الاستدعاء التلقائي الفوري لكافة التواريخ والملاحظات بمجرد اختيار ملف الشحنة من الـ Dropdown.
- **Frontend Static Analysis:** `flutter analyze` خالي تماماً من الأخطاء (0 errors).
- **Frontend Unit Tests:** نجاح **67 اختبار بنسبة 100%** (`67 passed in 5s`).
- **Backend Unit Tests:** نجاح **235 اختبار بنسبة 100%** (`235 passed in 17.23s`).

---

## 📝 [2026-08-16 15:15] - Completed Task: Phase 6 Shipment Documents Intelligent Verification, Draft Reviews & Regulatory Compliance Engine

### 📌 Overview
- **Task Code:** BP-016-018 / PHASE-6-DOC-REVIEWS
- **Description:** بناء وتطوير المنظومة الذكية لمراجعة وتدقيق مستندات الشحنة ودرافت البوالص والشهادات الرسمية (Phase 6) وربطها التام بملف الاستيراد:
  1. **مراجعة وتأكيد الفاتورة وقائمة التعبئة النهائية (PO Final Reconciliation):** مطابقة الأعداد والكميات والأوزان والحجم (CBM) واعتماد الأرقام النهائية كمرجع رقمي حاكم للبوالص، المخزون بالطريق، والإفراج الجمركي، واستلام المخزن.
  2. **مراجعة درافت بوليصة الشحن (Draft B/L 4-Tab Engine):** (توليد المتطلبات من النظام ➔ الإدخال الذكي OCR/Text ➔ مصفوفة المقارنة التلقائية مع فحص الحقول الحرجة وخطاب التعديل الجاهز للخط الملاحي ➔ سجل الاعتمادات).
  3. **مراجعة درافت شهادة المنشأ و EUR.1 (4-Tab Engine):** مطابقة بيانات المصدر والمستورد والمنشأ وبنود الاتفاقية.
  4. **مراجعة درافت شهادة الفحص والمطابقة النوعية (Inspection Certificate 4-Tab Engine):** التحقق من جهات الفحص (SGS/TÜV/BV/Intertek) والمواصفات القياسية المصرية.
  5. **فحص صلاحية ACID والمستندات القانونية للشركة المستوردة (+30 يوم أمان بعد تاريخ الوصول المتوقع ETA):** حساب نافذة الأمان وتفعيل تنبيه وشريط أحمر بارز ومستمر عند وجود أي مستند منتهي أو قارب على الانتهاء.

### 📁 Files Changed
- `modules/import_documentation/model.py` — Modified (Added `DraftBLReviewSession`, `CertificateOfOriginReviewSession`, `InspectionCertificateReviewSession` database tables).
- `modules/import_documentation/schemas.py` — Modified (Added Pydantic schemas for PO Reconciliation, Draft B/L comparisons, COO reviews, Inspection reviews, and Legal Expiry compliance).
- `modules/import_documentation/repository.py` — Modified (Added repository methods for Draft B/L, COO, and Inspection review sessions with code generators).
- `modules/import_documentation/service.py` — Modified (Added comparison engines, fuzzy matching, ±0.5% tolerance checks, PO final reconciliation engine, ETA + 30 days safety window calculator, and carrier correction letter generators).
- `modules/import_documentation/router.py` — Modified (Exposed 11 FastAPI REST endpoints under `/api/v1/import-documentation/`).
- `tests/unit/test_draft_reviews_and_compliance.py` — Created (Comprehensive backend unit test suite).
- `frontend/lib/features/import_documentation/models/import_documentation_model.dart` — Modified (Added Dart models and JSON serializers for all Phase 6 entities).
- `frontend/lib/features/import_documentation/providers/import_documentation_provider.dart` — Modified (Added Riverpod state notifiers and providers for Draft B/L, COO, Inspection, PO Reconciliation, and Legal Compliance).
- `frontend/lib/features/import_documentation/widgets/legal_compliance_banner.dart` — Created (Persistent red alert banner for ETA + 30 days compliance).
- `frontend/lib/features/import_documentation/widgets/po_reconciliation_tab.dart` — Created (Interactive Proforma vs Final Invoice & Packing List reconciliation grid with variance tracking and certification).
- `frontend/lib/features/import_documentation/widgets/draft_bl_review_tab.dart` — Created (Draft B/L 4-Tab review engine with fuzzy matching, blocking rules, and carrier correction letter).
- `frontend/lib/features/import_documentation/widgets/coo_review_tab.dart` — Created (Certificate of Origin and EUR.1 4-Tab review engine).
- `frontend/lib/features/import_documentation/widgets/inspection_review_tab.dart` — Created (Inspection certificate 4-Tab review engine).
- `frontend/lib/features/import_documentation/screens/import_documentation_screen.dart` — Modified (Revamped Tab 3 into a multi-subtab workspace and embedded the persistent legal compliance banner).
- `frontend/test/phase6_draft_reviews_and_compliance_test.dart` — Created (Flutter unit and widget test suite).

### 📊 Technical Changes
1. **محرك مطابقة واعتماد الفاتورة والباكينج النهائية (PO Final Reconciliation Engine):**
   - مطابقة الكميات والأسعار والأوزان والحجم (CBM) للبند وحساب نسبة الفارق (`variancePercentage`).
   - اعتماد الأرقام النهائية بضغطة زر واحدة وتحديث ملف الاستيراد المركزي لاستخدامها في كافة المراحل اللاحقة.
2. **محرك مراجعة درافت بوليصة الشحن (Draft B/L Review Engine):**
   - استدعاء متطلبات البوليصة من 6 مصادر بالنظام (حجز الشحن، الحاويات، VGM، أوامر الشراء، المستورد، المصدر).
   - مطابقة 16 حقلاً أساسياً مع نسبة مطابقة نصية ذكية (Fuzzy Match >= 85%) ونسبة سماح رقمية للأوزان والحجم (±0.5%).
   - تطبيق قواعد حظر صارمة (Blocking Rules) تمنع الاعتماد في حال اختلاف رقم الحجز، أرقام الحاويات، أو السيل.
   - توليد آلي لخطاب طلب التعديل الرسمي للخط الملاحي ووكيل الشحن مع زر نسخ فوري.
3. **محرك درافت شهادة المنشأ واليورو 1 وشهادات الفحص:**
   - تطبيق نفس فلسفة الـ 4 تبويبات (توليد المتطلبات ➔ الإدخال الذكي ➔ مصفوفة المقارنة والفروق ➔ السجل المركزي).
4. **محرك نافذة الأمان القانونية (+30 يوم بعد الوصول):**
   - استخراج تاريخ الوصول المتوقع (ETA) من حجز الشحن وحساب نافذة الأمان (`ETA + 30 Days`).
   - فحص تواريخ انتهاء (ACID، البطاقة الاستيرادية، السجل التجاري، البطاقة الضريبية) وإظهار شريط تحذيري أحمر بارز ومستمر في الواجهة عند وجود أي مستند منتهي أو قارب على الانتهاء.

### 🧪 Validation / Testing
- **Backend Tests:** تم تشغيل كافة اختبارات الـ pytest بنجاح 100% (**240 passed in 19.53s**).
- **Frontend Analysis:** `flutter analyze` خالي تماماً من أي أخطاء (0 errors).
- **Frontend Test Suite:** تشغيل كافة اختبارات الفلاتر بنجاح 100% (**7 passed in test suite**).

### 🏁 Next Steps
- المنظومة جاهزة تماماً للعمل والتشغيل الميداني للمرحلة السادسة.

---

## 📝 [2026-08-16 15:25] - Completed Task: PO Price Review & Dedicated Packing List Packages/Weights Reconciliation

### 📌 Overview
- **Task Code:** BP-016-PO-RECON-002
- **Description:** 
  1. إضافة وتفعيل خانة لمراجعة وتعديل السعر النهائي (`مراجعة السعر النهائي $`) في جدول مراجعة الفاتورة التجارية مع حساب إجمالي السعر ونسبة فارق السعر تلقائياً.
  2. إضافة جدول وقسم مخصص لمراجعة وتدقيق بيان العبوة والباكينج ليست (`Packing List & Actual Packages/Weights`) يشمل مقارنة وتعديل: نوع التغليف، عدد الطرود/الكراتين الفعلية، الأوزان الصافية والقائمة الفعلية، والأحجام (CBM) مع حساب نسب الفروق وتحديث المجاميع العلوية فورياً.

### 📁 Files Changed
- `modules/import_documentation/schemas.py` — Modified (Added `initial_unit_price`, `final_unit_price`, `initial_packages_count`, `final_packages_count`, `package_type`, `price_variance_percentage`, `weight_variance_percentage` to `POReconciliationItem`).
- `modules/import_documentation/service.py` — Modified (Updated `reconcile_po_final_adjustments_service` to persist unit prices, line total prices, net/gross weights, CBM, and calculate price & weight variances).
- `tests/unit/test_draft_reviews_and_compliance.py` — Modified (Added unit test assertions verifying price review and packing list packages/weights reconciliation).
- `frontend/lib/features/import_documentation/models/import_documentation_model.dart` — Modified (Updated `POReconciliationItemModel` with price review and packing list fields, serialization, and deserialization).
- `frontend/lib/features/import_documentation/widgets/po_reconciliation_tab.dart` — Modified (Revamped into a dual-section enterprise interface: Section 1 for Commercial Invoice & Price Review with editable final unit price, and Section 2 for Packing List with actual packages, net/gross weights, and CBM).
- `frontend/test/phase6_draft_reviews_and_compliance_test.dart` — Modified (Updated Flutter tests covering price review and packing list reconciliation).

### 📊 Technical Changes
1. **خانة مراجعة وتعديل السعر النهائي (Price Review):**
   - إضافة حقل إدخال تفاعلي `مراجعة السعر النهائي $ *` بجانب `سعر الوحدة (PO) $`.
   - حساب إجمالي البند بالفاتورة (`final_quantity * unit_price`) ونسبة فارق السعر (`price_variance_percentage`) فور تعديل السعر أو الكمية.
   - تحديث إجمالي القيمة بالفاتورة النهائية في بطاقات الإحصائيات العلوية.
2. **قسم وجدول مراجعة بيان العبوة والباكينج ليست (Packing List Reconciliation):**
   - استدعاء بيانات الباكينج ليست المربوطة بأمر الشراء (`PackingListItemModel`).
   - جدول تفاعلي كامل يعرض: كود الصنف، نوع التغليف (Carton, Pallet, etc.)، عدد الطرود الأولية vs الفعلية، الوزن الصافي الأولي vs الفعلي، الوزن القائم الأولي vs الفعلي، فارق الوزن %، والحجم الأولي vs الفعلي CBM.
   - تحديث إجمالي عدد الطرود والأوزان والأحجام الحقيقية بالمنظومة فورياً.

### 🧪 Validation / Testing
- **Backend Pytest:** تشغيل اختبارات `test_draft_reviews_and_compliance.py` بنجاح 100% (**5 passed**).
- **Frontend Test Suite:** تشغيل كافة اختبارات الفلاتر بنجاح 100% (**7 passed**).
- **Flutter Analyzer:** الكود خالٍ تماماً من الأخطاء (**0 errors**).

### 🏁 Next Steps
- شاشة مراجعة أمر الشراء والفاتورة والباكينج ليست أصبحت تدعم مراجعة الأسعار والأعداد والأوزان الفعلية بدقة فائقة.

---

## 📝 [2026-08-16 16:40] - Completed Task: 5-Stage Draft B/L Review Engine, Checklist, Revision Reports, Dual Approval & UI Overflow Fixes

### 📌 Overview
- **Task Code:** BP-016-DRAFT-BL-5STAGE-003
- **Description:** 
  1. تطوير وتحديث محرك مراجعة درافت البوليصة ليتبع المراحل الخمس الصارمة (5-Stage B/L Review Engine):
     - **Stage 1 – Draft Review:** إنشاء ملخص الشحنة المرجعي تلقائياً (Auto Generated Shipment Summary) من النظام (بيانات المصدر والمستورد والـ ACID والبطاقة الضريبية والحجز الملاحي والأوزان وبيان العبوة) وقائمة التدقيق (Review Checklist) بـ 20 بنداً مع إجراءات (✅ Correct / ❌ Incorrect / ⚪ N/A).
     - **Stage 2 – Revision Required:** توليد تقرير التعديلات والملاحظات المفتوحة (Revision Report) مع إنشاء خطاب طلب التعديل الرسمي للخط الملاحي (Carrier Correction Request Letter) وإتاحته للنسخ بضغطة زر.
     - **Stage 3 – Reviewed & Versioning:** عند استلام مسودة معدلة جديدة (v2, v3...)، يتم قفل البنود المعتمدة مسبقاً (`is_locked = True`) وإعادة فتح البنود ذات الملاحظات فقط.
     - **Stage 4 – Dual Approval:** اشتراط الاعتماد الثنائي الإلزامي المستقل من مسؤول الاستيراد (Importer) والمخلص الجمركي (Customs Broker) مع حظر الاعتماد في حال وجود أي ملاحظات مفتوحة.
     - **Stage 5 – Final Certified & Registry:** اعتماد المسودة نهائياً وتوثيقها في سجل الإصدارات المعتمدة (Draft B/L Version Registry & Audit Trail).
  2. معالجة وتصحيح طفحان واجهة المستخدم (RenderFlex Right Overflow) في جداول بنود أمر الشراء والباكينج ليست.

### 📁 Files Changed
- `modules/import_documentation/schemas.py` — Modified (Added `DraftBLChecklistItem`, `RevisionReportItem`, `DualApprovalRequest`, `NewDraftVersionRequest`, and updated session schemas).
- `modules/import_documentation/repository.py` — Modified (Added model imports for `DraftBLReviewSession`).
- `modules/import_documentation/service.py` — Modified (Implemented snapshot extraction of 20 reference fields, 5-stage state transitions, fuzzy/overlap matching, carrier correction letter generator, checklist updating, dual approval processing, and version branching with lock preservation).
- `modules/import_documentation/router.py` — Modified (Added `PUT /draft-bl/{review_id}/checklist`, `POST /draft-bl/dual-approval`, and `POST /draft-bl/new-version`).
- `tests/unit/test_draft_reviews_and_compliance.py` — Modified (Added `test_draft_bl_5_stages_and_dual_approval`).
- `frontend/lib/features/import_documentation/models/import_documentation_model.dart` — Modified (Added `DraftBLChecklistItemModel`, `RevisionReportItemModel`, updated `DraftBLReviewModel`).
- `frontend/lib/features/import_documentation/providers/import_documentation_provider.dart` — Modified (Added notifier methods for checklist updates, dual approval, and new version creation).
- `frontend/lib/features/import_documentation/widgets/draft_bl_review_tab.dart` — Modified (Complete 5-stage UI with Auto Summary, Checklist, Revision Report, Dual Approval workspace, and Version Registry).
- `frontend/lib/features/purchase_orders/screens/purchase_orders_screen.dart` — Modified (Fixed RenderFlex overflows in HS code badges and packing list tables using `Wrap` and explicit column widths).
- `frontend/test/phase6_draft_reviews_and_compliance_test.dart` — Modified (Added Flutter unit tests for checklist items, revision reports, and 5-stage dual approval review model).

### 📊 Technical Changes
1. **Auto Generated Shipment Summary (Stage 1):**
   - استدعاء آلي لـ 20 حقلاً مرجعياً غير قابل للتعديل اليدوي من ماستر داتا المورد والشركة والحجز الملاحي وتخصيص الحاويات وبيان العبوة المعتمد.
2. **قائمة التدقيق المباشرة (Review Checklist):**
   - جدول تفاعلي بـ 20 حقلاً، يحدد تلقائياً المطابقة، مع إمكانية التعديل والتصنيف (Correct, Incorrect, N/A).
   - عند تحديد `Incorrect`، يتم طلب: الإجراء المطلوب والتصحيح، السبب، والجهة المسؤولة (`Shipping Provider`, `Supplier`, `Importer`, `Customs Broker`).
3. **تقرير التعديلات وخطاب الخط الملاحي (Stage 2 & 3):**
   - توليد تقرير يحتوي فقط على البنود غير المطابقة.
   - صياغة خطاب رسمي للخط الملاحي بضغطة زر نسخ واحدة.
   - قفل البنود المطابقة تلقائياً (`is_locked = True`) في النسخ الجديدة (v2/v3).
4. **منظومة الاعتماد الثنائي (Stage 4 & 5):**
   - بطاقتان مستقلتان لاعتماد مسؤول الاستيراد واعتماد المخلص الجمركي.
   - حظر الاعتماد التام طالما توجد ملاحظات مفتوحة، والانتقال التلقائي إلى `Stage 5: Final` فور اكتمال الاعتمادين.
5. **معالجة الـ UI Overflows:**
   - استبدال الـ Rows المزدحمة في بطاقات الـ HS Code بـ `Wrap` وضبط أعمدة جداول الباكينج ليست لمنع أي تجاوز بصري.

### 🧪 Validation / Testing
- **Backend Pytest Suite:** تشغيل كافة اختبارات الباك إند بنجاح 100% (**241 passed**).
- **Frontend Test Suite:** تشغيل كافة اختبارات الفلاتر بنجاح 100% (**75 passed**).
- **Flutter Analyzer:** لا توجد أي أخطاء blocking (**0 errors**).

### 🏁 Next Steps
- منظومة مراجعة درافت البوليصة جاهزة تماماً وتتبع القواعد التشغيلية الخمس بدقة.

---

## 📝 [2026-08-16 16:47] - Completed Task: Complete Packing List Extraction & Description in PO Reconciliation

### 📌 Overview
- **Task Code:** PO-RECON-PL-FULL-004
- **Description:** تصحيح وتوسيع آلية استدعاء بيان العبوة (Packing List) في شاشة مراجعة الفاتورة والباكينج ليست (`po_reconciliation_tab.dart`) لاستدعاء كافة سطور وبنود بيان التعبئة بالكامل دون إسقاط أي طرد أو كرتونة، مع احتساب الأوزان الصافية والقائمة والأحجام بدقة، وإضافة عمود `الوصف (Description)` في جدول الباكينج ليست.

### 📁 Files Changed
- `frontend/lib/features/import_documentation/widgets/po_reconciliation_tab.dart` — Modified (Updated `_loadPOItems` to iterate over all `po.packingListItems` with fallback matching by itemCode/index, computed weights/CBM from units/qty, added Description column in Section 2, and added live asynchronous refresh of POs on screen mount and dropdown change).

### 📊 Technical Changes
1. **استدعاء كافة سطور بيان التعبئة (Full Packing List Extraction):**
   - الانتقال من مجرد التكرار على بنود الفاتورة فقط إلى استخراج كل بند تعبئة في `po.packingListItems` وربطه بالصنف والفاتورة.
   - حساب الأوزان الصافية والقائمة الإجمالية بدقة من `totalNetWeightKg` أو `(netWeightUnitKg * qtyPkg)`، وحساب الحجم من `totalCbm` أو `calculatedCbm`.
2. **إضافة عمود الوصف لجدول الباكينج ليست (Description Column):**
   - إضافة عمود `الوصف` بجانب `كود الصنف` في جدول مراجعة الباكينج ليست لسهولة معرفة محتوى كل طرد.
3. **التحديث الحي المباشر (Live Asynchronous Refresh):**
   - إضافة `fetchPurchaseOrders()` غير المتزامن داخل `initState` وعند تغيير الملف لضمان استدعاء أحدث البيانات من قاعدة البيانات فوراً.

### 🧪 Validation / Testing
- **Flutter Test Suite:** تشغيل كافة اختبارات المراجعة بنجاح 100% (**8 passed**).
- **Flutter Analyzer:** الكود خالٍ تماماً من أي أخطاء (**0 errors**).

### 🏁 Next Steps
- استدعاء الباكينج ليست يعمل بكامل بنوده وأوزانه وتفاصيله الدقيقة.

---

## 📝 [2026-08-16 16:52] - Completed Task: Independent Extraction of Commercial Invoice Items & Packing List Breakdown

### 📌 Overview
- **Task Code:** PO-RECON-SPLIT-005
- **Description:** فصل استدعاء بنود الفاتورة التجارية (`_invoiceItems`) عن تفاصيل طرود بيان التعبئة والباكينج ليست (`_packingItems`) في شاشة مراجعة وتأكيد الفاتورة والباكينج ليست (`po_reconciliation_tab.dart`) لمنع تكرار أسعار وكميات الفاتورة على سطور الباكينج ليست، وضمان استدعاء بنود الفاتورة الأصلية بدقة 100% في الجدول الأول، واستدعاء كافة طرود ومقاسات الباكينج ليست في الجدول الثاني.

### 📁 Files Changed
- `frontend/lib/features/import_documentation/widgets/po_reconciliation_tab.dart` — Modified (Defined independent `_invoiceItems` bound to `po.items` for Section 1, and `_packingItems` bound to `po.packingListItems` for Section 2, with independent metrics computation and combined certification payload).
- `modules/import_documentation/service.py` — Modified (Updated `reconcile_po_final_adjustments_service` to update `POLineItem` for invoice pricing/quantities and `PackingListItem` for packages/weights/CBM).

### 📊 Technical Changes
1. **الفصل التام بين بنود الفاتورة وبيان التعبئة (Separate Data Lists):**
   - **الجدول الأول (Section 1):** يستدعي حصراً بنود الفاتورة المبدئية (`po.items`) بالكميات الأصلية والأسعار وإجمالي السطر ونسب التغيير، دون أي تكرار ناتج عن سطور الطرود.
   - **الجدول الثاني (Section 2):** يستدعي حصراً كافة سطور وتفاصيل بيان التعبئة (`po.packingListItems`) متضمناً: كود الصنف، الوصف، نوع التغليف (Pallet / Carton ...)، عدد الطرود، الأوزان الصافية والقائمة، والأحجام CBM.
2. **تحديث الـ Backend Service:**
   - تحديث `reconcile_po_final_adjustments_service` ليحدث `POLineItem` بأسعار وكميات الفاتورة، ويحدث `PackingListItem` بأوزان وأحجام وطرود الباكينج ليست.

### 🧪 Validation / Testing
- **Backend Pytest Suite:** تشغيل اختبارات `test_draft_reviews_and_compliance.py` بنجاح 100% (**6 passed**).
- **Frontend Test Suite:** تشغيل كافة اختبارات الفلاتر بنجاح 100% (**8 passed**).
- **Flutter Analyzer:** الكود سليم وخالٍ تماماً من الأخطاء (**0 errors**).

### 🏁 Next Steps
- شاشة المراجعة أصبحت تعرض الفاتورة بدقة تامة والباكينج ليست بكامل تفاصيل طروده.

---

## 📝 [2026-08-16 16:58] - Completed Task: Draft B/L Review Sheet & OCR Parser Overhaul Matching Real Carrier Operations

### 📌 Overview
- **Task Code:** DRAFT-BL-REAL-OCR-006
- **Description:** إعادة هيكلة وتطوير منظومة مراجعة درافت البوليصة بالكامل لتطابق متطلبات ومسودات الخطوط الملاحية الواقعية (مثل MSC و Far East International)، واستخراج بيانات ACID (19 رقماً) والبطاقة الضريبية للمستورد ورقم المصدر وسجل الحاويات والطرود، وتوحيد ورقة المراجعة التلقائية (Auto Generated Review Sheet) مع جدول المطابقة المباشر (Checklist Table).

### 📁 Files Changed
- `frontend/lib/features/import_documentation/widgets/draft_bl_review_tab.dart` — Rewritten (Created unified Review Sheet combining Auto Generated Shipment Summary with Smart OCR raw text input and the 20-field interactive checklist table, plus dedicated tabs for Revision Report & Carrier Request Letter, Version Branching, Dual Approval, and Final Registry).
- `modules/import_documentation/service.py` — Modified (Added `parse_draft_bl_raw_text` for automated parsing of Egyptian customs ACID block, importer tax ID, shipper reg, container numbers, seals, weights, CBM, vessel/voyage, and freight terms).
- `modules/import_documentation/schemas.py` — Modified (Supported `raw_text` / `raw_draft_text` in `DraftBLComparisonRequest`).
- `frontend/lib/features/import_documentation/models/import_documentation_model.dart` — Modified (Added `copyWith` to `POReconciliationItemModel`, added `version` getter to `DraftBLReviewModel`).

### 📊 Technical Changes
1. **المحرك الذكي لقراءة نصوص المسودات (Smart Draft B/L Text Parser):**
   - استخراج آلي مباشر لـ: رقم ACID (19 رقماً)، البطاقة الضريبية للمستورد، رقم تسجيل المصدر، كود دولة المصدر، رقم البوليصة، رقم الحجز، اسم الباخرة ورقم الرحلة، ميناء الشحن والتفريغ، شروط النولون (Prepaid / Collect)، أرقام الحاويات والسيل، عدد ونوع الطرود، الوزن القائم والصافي، والحجم CBM.
2. **ورقة المراجعة الموحدة (Unified Document Review Sheet):**
   - دمج ملخص الشحنة المرجعي التلقائي (Auto Summary) مع جدول قائمة التدقيق الشاملة (Checklist Table) وصندوق اللصق الذكي في شاشة واحدة متكاملة دون الحاجة للتنقل العشوائي.
3. **تقرير التعديلات وخطاب الخط الملاحي بنقرة واحدة:**
   - استخراج البنود غير المطابقة تلقائياً وتوليد الخطاب الرسمي للخط الملاحي بضغطة زر نسخ واحدة.
4. **الاعتماد الثنائي الرسمي (Dual Approval):**
   - اعتماد مسؤول الاستيراد + اعتماد المخلص الجمركي، مع الحظر التلقائي للاعتماد طالما توجد فروقات حرجة في ACID أو الحاويات أو البطاقة الضريبية.

### 🧪 Validation / Testing
- **Full Backend Pytest Suite:** تشغيل كافة اختبارات الباك إند بنجاح 100% (**241/241 passed**).
- **Full Frontend Flutter Test Suite:** تشغيل كافة اختبارات الواجهة بنجاح 100% (**75/75 passed**).
- **Flutter Analyzer:** الكود سليم تماماً (**0 errors**).

### 🏁 Next Steps
- منظومة مراجعة درافت البوليصة تعمل الآن بأعلى كفاءة وتطابق البوالص الحقيقية.

---

## 📝 [2026-08-16 17:20] - Completed Task: Multi-Format Document Extraction (PDF / Word / Excel) & Host Matching Fix

### 📌 Overview
- **Task Code:** DRAFT-BL-FILE-EXTRACT-007
- **Description:** إزالة النماذج الثابتة واستبدالها بمنظومة رفع واستخراج ذكية مباشرة تدعم ملفات PDF و Word (.docx) و Excel (.xlsx / .xls) والنصوص، مع معالجة وتوافق الاتصال بالباك إند عبر المتصفح (Host / CORS Matching).

### 📁 Files Changed
- `frontend/lib/core/constants/api_constants.dart` — Modified (Fixed web host detection to dynamically match `localhost` / `127.0.0.1` and prevent CORS / PNA discrepancies in web browsers).
- `frontend/lib/features/import_documentation/widgets/draft_bl_review_tab.dart` — Modified (Removed static sample buttons and built a modern Multi-Format File Uploader card supporting PDF, Word, Excel, and Text files with live progress, file status badges, and automatic controller population).
- `frontend/lib/features/import_documentation/providers/import_documentation_provider.dart` — Modified (Added `extractDraftBLFromFile` method with multipart file upload support).
- `modules/import_documentation/service.py` — Modified (Added `extract_text_from_uploaded_file` leveraging `pypdf`, `python-docx`, and `openpyxl` for multi-format text & table extraction).
- `modules/import_documentation/router.py` — Modified (Added `POST /api/v1/import-documentation/draft-bl/extract-file` endpoint).
- `tests/unit/test_draft_reviews_and_compliance.py` — Modified (Added `test_extract_text_from_uploaded_files` to verify file text extraction & parsing).

### 📊 Technical Changes
1. **استخراج البيانات من صيغ المستندات المتعددة (PDF / Word / Excel):**
   - **PDF:** قراءة كافة الصفحات واستخراج النصوص والجداول عبر `pypdf`.
   - **Word (.docx / .doc):** قراءة الفقرات وكافة خلايا الجداول عبر `python-docx`.
   - **Excel (.xlsx / .xls):** قراءة الشيتات والصفوف المنظمة عبر `openpyxl`.
   - **Text / CSV:** قراءة المحتوى بالترميز المناسب.
2. **الربط الآلي مع واجهة المستخدم (Automatic Controller Population):**
   - فور اختيار ورفع الملف، يتم استخراج وتعبئة كافة الحقول (رقم البوليصة، الحجز، ACID، البطاقة الضريبية للمستورد، الشاحن، المستورد، الحاويات، الطرود، الأوزان، الحجم، إلخ) مع تشغيل مقارنة التطابق وتحديث جدول الـ Checklist فورياً.
3. **تصحيح الاتصال وتوافق المتصفح (Host & CORS Alignment):**
   - تعديل `ApiConstants.serverUrl` في الفلاتر لمطابقة نطاق المتصفح المستخدم (`localhost` أو `127.0.0.1`) لمنع أخطاء المتصفحات المتعلقة بـ XMLHttpRequest / Private Network Access.

### 🧪 Validation / Testing
- **Full Backend Pytest Suite:** تشغيل كافة اختبارات الباك إند بنجاح 100% (**242/242 passed**).
- **Full Frontend Flutter Test Suite:** تشغيل كافة اختبارات الواجهة بنجاح 100% (**75/75 passed**).
- **Flutter Analyzer:** الكود سليم وخالٍ تماماً من الأخطاء (**0 errors**).

### 🏁 Next Steps
- منظومة استخراج المستندات المباشرة أصبحت جاهزة وتدعم ملفات PDF و Word و Excel بكفاءة وسرعة فائقة.

---

## 📝 [2026-08-16 17:25] - Completed Task: Interactive In-Place Draft Value Editor & Shipper/Consignee Auto-Extraction

### 📌 Overview
- **Task Code:** DRAFT-BL-INPLACE-EDIT-008
- **Description:** تحويل عمود "قيمة المسودة (Draft Value)" من مجرد نص ثابت إلى حقول إدخال وتعديل تفاعلية مباشرة داخل الجدول، مع زر نسخ فوري لقيمة النظام وتحديث ذكي تلقائي لحالة المطابقة (Correct / Incorrect)، وتوسيع محرك استخراج بيانات الشاحن والمستورد وجهة الإخطار.

### 📁 Files Changed
- `frontend/lib/features/import_documentation/widgets/draft_bl_review_tab.dart` — Modified (Replaced readonly text with an in-place editable `TextFormField` and copy system value button per row, updating `_updateChecklistRow` to re-evaluate matching state dynamically).
- `modules/import_documentation/service.py` — Modified (Expanded regex extractors in `parse_draft_bl_raw_text` for `shipper`, `consignee`, and `notify_party`).

### 📊 Technical Changes
1. **عرض وتعديل قيمة المسودة مباشرة داخل الجدول (Interactive In-Place Editing):**
   - استبدال العبارة الثابتة "غير مدخل بالمسودة" بحقل إدخال حي ومرن يعرض القيمة المستخرجة من الملف أو يسمح للمستخدم بكتابتها وتعديلها مباشرة في الخلية.
   - إعادة احتساب حالة التطابق (Correct vs Incorrect) وإزالة التصحيح المطلوب تلقائياً فور كتابة أو مطابقة القيمة مع بيانات النظام.
2. **زر النسخ السريع لقيمة النظام (1-Click Copy System Value):**
   - إضافة أيقونة نسخ بجانب كل خلية تتيح نسخ قيمة النظام المعتمدة فورياً وتأكيد المطابقة بنقرة واحدة.
3. **توسيع استخراج الشاحن والمستورد وجهة الإخطار:**
   - دعم صيغ متعددة في المسودات لاستخراج اسم وعنوان المصدر والمستورد وجهة الإخطار بدقة.

### 🧪 Validation / Testing
- **Full Backend Pytest Suite:** تشغيل كافة اختبارات الباك إند بنجاح 100% (**242/242 passed**).
- **Full Frontend Flutter Test Suite:** تشغيل كافة اختبارات الواجهة بنجاح 100% (**75/75 passed**).
- **Flutter Analyzer:** الكود سليم وخالٍ تماماً من الأخطاء (**0 errors**).

### 🏁 Next Steps
- أصبح بإمكان المستخدم رؤية وتعديل ومطابقة قيم المسودة بكل سهولة وسرعة داخل جدول المراجعة.

---

## 📝 [2026-08-16 17:30] - Completed Task: Visual Maritime Draft B/L Sheet & PDF / Excel Export Engine

### 📌 Overview
- **Task Code:** DRAFT-BL-VISUAL-SHEET-EXPORT-009
- **Description:** بناء واجهة معاينة تفاعلية رسمية على شكل مسودة بوليصة شحن بحرية (Draft Bill of Lading Document) مطابقة لنموذج الخطوط الملاحية (MSC وغيرها) ومعايير نافذة (ACID Block)، مع توفير إمكانية تنزيل وتصدير المسودة بصيغتي **PDF** و **Excel**.

### 📁 Files Changed
- `frontend/lib/features/import_documentation/services/draft_bl_export_service.dart` — Created (Handles generating printable and downloadable vector PDF via `pdf` / `printing` and structured CSV / Excel workbook).
- `frontend/lib/features/import_documentation/widgets/visual_draft_bl_sheet.dart` — Created (Renders the realistic maritime Draft B/L sheet with header banner, shipper, consignee, notify parties, transit grid, particulars furnished by shipper, Egyptian customs ACID block, containers, gross weight, CBM, and signature endorsement boxes).
- `frontend/lib/features/import_documentation/widgets/draft_bl_review_tab.dart` — Modified (Embedded `VisualDraftBLSheet` and added direct top header action buttons for 🔴 "تنزيل PDF" and 🟢 "تنزيل Excel").
- `frontend/test/phase6_draft_reviews_and_compliance_test.dart` — Modified (Added integration widget test verifying the visual B/L layout and export triggers).

### 📊 Technical Changes
1. **معاينة وثيقة البوليصة البحرية (Authentic Maritime B/L Layout):**
   - تصميم وثيقة البوليصة بدقة عالية مع تقسيم الخلايا والحدود مطابقاً لبوالص الشحن العالمية.
   - تضمين بلوك التسجيل المسبق للشحنات المصري بالكامل: `ACID (19 digits)`, `Importer Tax ID`, `Shipper Reg Type & ID`, `Shipper Country Code`, و `Freight Terms`.
   - عرض بيان الحاويات والرصاص والأوزان الإجمالية والصافية والحجم CBM وإجمالي الطرود.
2. **تصدير وتحميل PDF عالي الدقة (High-Resolution Printable PDF):**
   - توليد ملف PDF متوافق مع معايير الطباعة A4 مع دعم كامل للغة العربية والإنجليزية، وتنزيله فورياً للمستخدم.
3. **تصدير وتحميل Excel / CSV (Structured Workbook Export):**
   - توليد ملف إكسيل منسق يشمل كافة الأقسام وبيانات الرحلة والأطراف وبلوك نافذة لسهولة المعالجة والتوثيق المالي.

### 🧪 Validation / Testing
- **Full Backend Pytest Suite:** تشغيل كافة اختبارات الباك إند بنجاح 100% (**242/242 passed**).
- **Full Frontend Flutter Test Suite:** تشغيل كافة اختبارات الواجهة بنجاح 100% (**76/76 passed**).
- **Flutter Analyzer:** الكود سليم وخالٍ تماماً من الأخطاء (**0 errors**).

### 🏁 Next Steps
- أصبح بإمكان المستخدم معاينة مسودة البوليصة بشكلها الرسمي الكامل وتحميلها كملف PDF أو Excel بضغطة زر واحدة.





















