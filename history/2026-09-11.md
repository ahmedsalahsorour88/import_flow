# 📅 سجل التطوير اليومي — 2026-09-11

---

## 📝 [2026-09-11 15:30] - Completed Task: Screen 68: Smart Shipment Inquiry, History & Cloning (KB-INQ-013)

### 📌 Overview
- **Task Code:** Screen 68 / KB-INQ-013 (`ShipmentInquiryScreen`, Route Index 68)
- **Description:** مراجعة وتحديث الشاشة رقم 68 الشاملة للاستعلام الذكي عن الشحنات وسجل أسعار النولون التاريخية والاستنساخ الذكي للشحنات المتكررة (`ShipmentInquiryScreen`) طبقاً للمحاور الثلاثة الإلزامية:
  1. **المحور الأول (Task A - Localization):** تعريب كامل وشامل للواجهة بدون أي تراكم لغوي (Language Stacking) أو شرطات مائلة ثنائية اللغة (`/`)، مع الالتزام الصارم بخلو النصوص العربية بنسبة 100% من أي حروف إنجليزية أو لاتينية `[a-zA-Z]` واستخدام `context.l10n` حصرياً، ودعم التبديل اللحظي بين العربية والإنجليزية، وتوجيه النصوص ديناميكياً (RTL / LTR).
  2. **المحور الثاني (Task B - Copy Data Enablement):** تغليف جسم الشاشة كاملاً بـ `SelectionArea` لدعم التحديد الحر بالماوس على سطح المكتب، وإضافة شارات نسخ تفاعلية سريعة لكود ملف الشحنة `importFileCode` مع إشعارات منبثقة ملائمة، وإتاحة نسخ نصوص الحقول التفاعلية في لوحة التصفية (HS Code, POL, POD, Carrier) وفي نافذة الاستنساخ الذكي (5 حقول إدخال)، وتغليف كافة خلايا جدول النتائج في `CopyableTableCell` مع قائمة خيارات الزر الأيمن للماوس (نسخ القيمة / نسخ السطر بالكامل)، وإضافة زر إجراءات نسخ ملخص السطر الفوري المباشر بنقرة واحدة (`Icons.copy_rounded`).
  3. **المحور الثالث (Task C - Linked Outputs):** تدشين خدمة تصدير مركزية مستقلة `ShipmentInquiryExportService` تدعم 4 قنوات إخراج قياسية:
    1. **تصدير جدول TSV:** تصدير مباشر لبيانات الشحنات المستعلم عنها بترميز UTF-8 BOM (`\uFEFF`) لسهولة اللصق الفوري في جداول البيانات.
    2. **تصدير ملف CSV/Excel:** تصدير غير مدمج مع علامات تنصيص قياسية RFC 4180 وترميز UTF-8 BOM.
    3. **طباعة وحفظ تقرير متجهي رسمي Vector A4 Landscape PDF:** تقرير تشخيصي متكامل بخطوط Cairo العربية الرسمية يتضمن مؤشرات إجمالي الشحنات ومتوسط النولون وإجمالي التكاليف، مع جدول بيانات الشحنات التفصيلي وتذييل التقرير وتوجيه ديناميكي RTL/LTR.
    4. **نسخ ملف الحافظة الشامل (Dossier Clipboard Copy):** نسخ فوري نصي متكامل يلخص بنقرة واحدة إحصائيات البحث وسجلات الشحنات لمشاركتها عبر البريد أو واتساب أو المنصات التشغيلية.

### 📁 Files Changed
- `frontend/lib/core/localization/app_localizations.dart` — Modified (Added 62+ typed getters for Screen 68 subtitles, tooltips, validation messages, localized Incoterms, shipping modes, currencies, PDF branding, and dossier headers)
- `frontend/lib/core/localization/app_localizations_ar.dart` — Modified (Pure Arabic translations strictly devoid of Latin letters `[a-zA-Z]`, zero bilingual slashes)
- `frontend/lib/core/localization/app_localizations_en.dart` — Modified (Professional English translations)
- `frontend/lib/features/shipment_inquiry/services/shipment_inquiry_export_service.dart` — Modified (Updated export service for TSV with UTF-8 BOM, RFC-4180 CSV, Vector A4 PDF with Cairo font, and plain-text clipboard dossier copy)
- `frontend/lib/features/shipment_inquiry/screens/shipment_inquiry_screen.dart` — Modified (Wrapped body in SelectionArea, localized subtitle, tooltips, error states, KPI units, TSV button, dossier tooltip, table headers, matching count badge, and empty state; wrapped all DataTable cells in CopyableTableCell with comprehensive TSV rowSummary and right-click context menu; clickable copy badge on importFileCode; quick-copy row summary action button in actions column)
- `frontend/lib/features/shipment_inquiry/widgets/shipment_inquiry_filter_panel.dart` — Modified (Added copy suffix buttons to HS Code, POL, POD, and Carrier fields; replaced bilingual lists with localized getters)
- `frontend/lib/features/shipment_inquiry/widgets/smart_clone_shipment_dialog.dart` — Modified (Wrapped in SelectionArea; localized all banners, titles, input labels, hints, validation rules, and toasts; added copy icons to summary tiles; added copy suffix buttons to all 5 input fields)
- `frontend/test/shipment_inquiry_localization_test.dart` — Created (Unit tests asserting non-empty strings, 0 Latin characters in Arabic, and 0 slashes, 6/6 passed 100%)
- `frontend/test/shipment_inquiry_export_service_test.dart` — Created (Unit tests for TSV UTF-8 BOM, CSV RFC-4180, formatted text dossier, model mapping, 5/5 passed 100%)
- `frontend/test/shipment_inquiry_screen_widget_test.dart` — Created (Widget tests verifying SelectionArea, 4-action export toolbar, CopyableTableCell, copy badges, row copy action, AR/EN mode, empty state, 3/3 passed 100%)
- `frontend/test/perf/screen_68_shipment_inquiry_perf_test.dart` — Created (Benchmark diagnostics: Nav-IN First Frame: 375ms | Settled: 519ms | Nav-OUT: 45ms, 1/1 passed 100%)
- `screen-fixes-review-log.md` — Modified (Logged Screen 68 completion and updated review progress table)
- `history/2026-09-11.md` — Created (Documented task completion log following append-only protocol)

### 📊 Technical Changes
- **Task A (Localization / i18n):**
  - استبدال كافة النصوص الثابتة والرسائل الإجرائية بألفاظ مستخرجة من `context.l10n`.
  - التحقق عبر فحص التعبيرات المنتظمة `RegExp(r'[a-zA-Z]')` من خلو النصوص العربية الخاصة بالشاشة 68 بنسبة 100% من أي أحرف لاتينية، مع انعدام الشرطات المائلة الثنائية (` / `).
  - دعم التبديل اللحظي بين اللغتين مع توجيه النصوص ديناميكياً (RTL في العربية، LTR في الإنجليزية).
- **Task B (Copy Data Enablement):**
  - تغليف ودجات الشاشة بالكامل بـ `SelectionArea` لضمان قابلية التحديد الحر بالماوس على سطح المكتب.
  - إضافة شارة نسخ تفاعلية مزودة بأيقونة نسخ وتلميح `CopyHelper.copy` لكود ملف الشحنة.
  - إضافة أزرار نسخ لاحقة داخل حقول لوحة التصفية (HS Code, POL, POD, Carrier).
  - إضافة أزرار نسخ لاحقة لكافة حقول نافذة الاستنساخ الذكي (5 حقول إدخال) مع نسخ قيم بطاقات الملخص.
  - تغليف كافة خلايا جدول النتائج في `CopyableTableCell` مع قائمة خيارات النسخ بالزر الأيمن (نسخ القيمة / نسخ السطر).
  - إضافة زر مخصص لنسخ ملخص السطر بالكامل (`Icons.copy_rounded`) في عمود الإجراءات.
- **Task C (Linked Outputs):**
  - بناء خدمة `ShipmentInquiryExportService` المركزية لتصدير ملفات TSV بترميز UTF-8 BOM، وملفات CSV القياسية بترميز UTF-8 BOM وقواعد RFC-4180، وملفات Vector A4 PDF المتجهة بخط Cairo، وتوليد حافظة نصية متكاملة لنسخها في الحافظة.

### 🧪 Validation / Testing
- `flutter test test/shipment_inquiry_localization_test.dart` — **6/6 tests passed (100% green)** ✅
- `flutter test test/shipment_inquiry_export_service_test.dart` — **5/5 tests passed (100% green)** ✅
- `flutter test test/shipment_inquiry_screen_widget_test.dart` — **3/3 tests passed (100% green)** ✅
- `flutter test test/perf/screen_68_shipment_inquiry_perf_test.dart` — **1/1 benchmark passed (Nav-IN First Frame: 375ms | Settled: 519ms | Nav-OUT: 45ms)** ✅
- `flutter test test/shipment_inquiry_test.dart` — **4/4 existing tests passed (100% green)** ✅
- **Combined Test Suite:** **19/19 tests passed (100% green)** ✅
- **Full Codebase Analyze (`flutter analyze lib/`):** **0 issues found! (100% clean, 19.1s)** ✅

### 🏁 Next Steps
- استكمال مراجعة أي شاشات أو موديولات جديدة قد تضاف إلى النظام، حيث تم إنجاز كافة الشاشات المسجلة في النظام (من شاشة تسجيل الدخول والشاشات 0 حتى 68) بنسبة 100%.

---

## 📝 [2026-09-11 15:45] - Completed Task: Screen 0: Operational Workspace Dashboard

### 📌 Overview
- **Task Code:** Screen 0 / Operational Workspace Dashboard (`OperationalDashboardScreen`, Route Index 0)
- **Description:** تطبيق إعدادات اللغة (Task A)، وتمكين نسخ البيانات (Task B)، وتفعيل مخرجات التصدير الأربعة الموحدة (Task C) على الشاشة الرئيسية للعمليات التشغيلية (`OperationalDashboardScreen`) وفقاً للقواعد الصارمة:
  1. **المحور الأول (Task A - Localization):** تعريب كامل وشامل لواجهة لوحة مساحة العمليات بدون أي تراكم لغوي (Stacked Text) أو شرطات مائلة ثنائية (`/`). تم تنقية كافة أسماء المراحل التشغيلية والمسارات وبطاقات المهام من أي حروف لاتينية `[a-zA-Z]` في الوضع العربي (استبدال `ACID`، `CargoX`، `STEP_01`، `P2:` بتسميات عربية أصيلة ومعتمدة)، مع بقاء الوضع الإنجليزي باللغة الإنجليزية النقية، وتوجيه النصوص ديناميكياً (RTL / LTR).
  2. **المحور الثاني (Task B - Copy Data Enablement):** تغليف `CustomScrollView` بالكامل بـ `SelectionArea` لدعم التحديد والسحب الحر على أجهزة سطح المكتب، وإضافة زر نسخ صريح تفاعلي مع تلميح منبثق في رأس كل بطاقة شحنة لنسخ ملخص الشحنة كاملاً (`importFileCode | companyName -> supplierName | currentStage`) بضغطة واحدة، مع تغليف كافة تفاصيل الشحنات والمهام الذكية المرتبطة بـ `CopyableText`.
  3. **المحور الثالث (Task C - Linked Outputs):** تدشين خدمة تصدير مركزية متخصصة `OperationalDashboardExportService` وإضافة شريط تصدير استجابي في ترويسة نتائج الشحنات يتيح:
    1. **تصدير TSV:** ملف جدولي بترميز UTF-8 BOM (`\uFEFF`) لسهولة اللصق الفوري في الإكسيل.
    2. **تصدير CSV:** ملف إكسيل بترميز UTF-8 BOM وقواعد RFC-4180 بدون دمج خلايا.
    3. **طباعة تقرير متجهي رسمي Vector A4 Landscape PDF:** تقرير معتمد بخطوط Cairo الرسمية يتضمن الترويسة والعلامة الرسمية، ملخص الفلاتر، إحصائيات الشحنات، وجدول العمليات الكامل وتذييل أرقام الصفحات.
    4. **نسخ ملف الحافظة الشامل (Dossier Clipboard Copy):** تلخيص الشحنات ومطابقة الفلاتر بنصوص منسقة ورموز تعبيرية جاهزة للمشاركة الفورية.

### 📁 Files Changed
- `frontend/lib/features/operational_dashboard/screens/operational_dashboard_screen.dart` — Modified (Root SelectionArea wrapping, 4-action export toolbar, explicit copy IconButton on shipment cards, zero-Latin Arabic lifecycle phases, localized next step cards and pathways, clean stage name formatting)
- `frontend/lib/features/operational_dashboard/services/operational_dashboard_export_service.dart` — Created (Unified export service for TSV BOM, CSV RFC-4180, Cairo Vector Landscape PDF, and plain text clipboard dossier)
- `frontend/lib/core/localization/app_localizations.dart` — Modified (Removed duplicate definitions)
- `frontend/lib/core/localization/app_localizations_ar.dart` — Modified (Cleaned export getters to guarantee 0 Latin characters, removed duplicates)
- `frontend/lib/core/localization/app_localizations_en.dart` — Modified (Professional English export getters, removed duplicates)
- `frontend/test/operational_dashboard_localization_test.dart` — Created (Strict regex assertions ensuring 0 Latin characters and 0 bilingual slashes across all Screen 0 Arabic strings, 2/2 passed 100%)
- `frontend/test/operational_dashboard_export_service_test.dart` — Created (Unit tests for TSV, CSV, Dossier formatting, and model mapping, 5/5 passed 100%)
- `frontend/test/operational_dashboard_test.dart` — Modified (Adjusted virtualized scroll drag offset for export toolbar, 6/6 passed 100%)
- `frontend/test/perf/screen_0_operational_dashboard_perf_test.dart` — Verified (1/1 benchmark passed 100%)
- `screen-fixes-review-log.md` — Modified (Logged Screen 0 review completion)
- `history/2026-09-11.md` — Modified (Appended task completion entry)

### 📊 Technical Changes
- **Task A (Localization / i18n):**
  - استخراج كافة النصوص الثابتة وتوجيهها عبر `context.l10n`.
  - تنقية قاموس المراحل `_lifecyclePhases` ودالة `_formatStageName` ودالة `_cleanTaskTitle` من أي أحرف أو بادئات لاتينية (`P1:`, `P2:`, `STEP_01`, `ACID`, `CargoX`) عند تفعيل اللغة العربية.
  - تعريب أزرار إلغاء الفلاتر وعناوين البطاقات والخطوات التشغيلية بنسبة 100%.
- **Task B (Copy Data Enablement):**
  - تفعيل `SelectionArea` على الحاوية التمريرية الرئيسية `CustomScrollView` بالكامل.
  - تزويد كل بطاقة شحنة بزر نسخ فوري مخصص (`Icons.copy_rounded`) مع تلميح `CopyHelper.copy`.
  - تمكين نسخ أرقام الشحنات والمهام والتواريخ عبر ودجات `CopyableText`.
- **Task C (Linked Outputs):**
  - إنشاء `OperationalDashboardExportService` مع معالجة وتطهير الفواصل وخلايا الجداول.
  - دعم قنوات التصدير الأربعة باللغة النشطة للمستخدم مع تضمين UTF-8 BOM وتنسيق PDF متجهي فاخر.

### 🧪 Validation / Testing
- `flutter test test/operational_dashboard_localization_test.dart` — **2/2 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_export_service_test.dart` — **5/5 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_test.dart` — **6/6 tests passed (100% green)** ✅
- `flutter test test/perf/screen_0_operational_dashboard_perf_test.dart` — **1/1 benchmark passed (100% green)** ✅
- **Combined Test Suite:** **14/14 tests passed (100% green)** ✅
- **Full Codebase Analyze (`flutter analyze lib/`):** **0 issues found! (100% clean across entire project, 14.3s)** ✅

### 🏁 Next Steps
- استمرار متابعة أي متطلبات أو ميزات جديدة يطلبها المستخدم في النظام اللوجستي.

---

## 📝 [2026-09-11 16:00] - Completed Task: Screen 0 Operational Dashboard Task D (Replace Internal Codes with Human-Readable Names)

### 📌 Overview

- **Task Code:** `SCR-000-TASK-D`
- **Description:** تطبيق استبدال الأكواد البرمجية والتشغيلية الداخلية بأسماء بشرية واضحة ومفهومة (Task D) بالتكامل التام مع المهام السابقة (Localization A, Copy Data B, Linked Outputs C) لشاشة **Operational Workspace Dashboard** (شاشة 0).

### 📁 Files Changed

- `frontend/lib/core/services/display_name_resolver.dart` — Created (Single central service for resolving internal shipment codes `IMP-YYYY-NNNN` to human-readable commercial names, step codes `STEP_01`..`STEP_21` to plain language Arabic/English operation names, phases 1..10 to clean titles, and cleaning task titles by replacing embedded codes with resolved operation names)
- `frontend/lib/features/operational_dashboard/screens/operational_dashboard_screen.dart` — Modified (Integrated `DisplayNameResolver`: primary shipment card title displays commercial name prominently while preserving internal code in a muted secondary badge; task titles cleaned of raw codes and `(STEP_XX)` replaced with resolved operation names; daily checkin log cards resolve shipment titles and phase names; dialogs pass human-readable shipment titles)
- `frontend/lib/features/operational_dashboard/services/operational_dashboard_export_service.dart` — Modified (Integrated `DisplayNameResolver`: `OperationalDashboardReportRow` includes `shipmentName` and `shipmentTitle`; resolved `currentPhase`, `operationalStep`, and `nextAction`; added Column 1 `operationalTsvHeaderShipmentName` before `importFileCode` in TSV, CSV, and PDF tables; dossier clipboard exports resolved human-readable titles)
- `frontend/lib/core/localization/app_localizations.dart` — Modified (Added `operationalTsvHeaderShipmentName`)
- `frontend/lib/core/localization/app_localizations_ar.dart` — Modified (Added `operationalTsvHeaderShipmentName`: 'اسم الشحنة')
- `frontend/lib/core/localization/app_localizations_en.dart` — Modified (Added `operationalTsvHeaderShipmentName`: 'Shipment Name')
- `frontend/test/display_name_resolver_test.dart` — Created (Unit test suite for DisplayNameResolver: all 21 steps in AR/EN, phases 1-10, shipment name resolution, task title cleaning, 10/10 passed 100%)
- `frontend/test/operational_dashboard_export_service_test.dart` — Modified (Updated unit tests for new `shipmentName` and `shipmentTitle` columns and resolved operations, 5/5 passed 100%)
- `frontend/test/operational_dashboard_localization_test.dart` — Modified (Included `operationalTsvHeaderShipmentName` in strict zero-Latin assertions, 2/2 passed 100%)
- `frontend/test/operational_dashboard_test.dart` — Verified (6/6 passed 100%)
- `frontend/test/perf/screen_0_operational_dashboard_perf_test.dart` — Verified (1/1 passed 100%)
- `screen-fixes-review-log.md` — Modified (Updated Screen 0 session log with Task D details)

### 📊 Technical Changes

- **Shipment code → Human-Readable name:**
  - عرض الاسم التجاري المخصص للشحنة (`customFileNumber` أو ما يعادله) كعنوان رئيسي بارز عريض في بطاقة الشحنة.
  - الإبقاء على الكود الداخلي (`IMP-YYYY-NNNN`) كحاوية ثانوية هادئة `AppTheme.cobalt.withOpacity(0.1)` للحفاظ على التتبع المرجعي.
  - زر النسخ ينسخ الاسم التجاري مقترناً بالكود الداخلي `[Commercial Name] ([importFileCode])`.
- **Step / Operation codes → Clean Operation names:**
  - تحويل كافة أكواد الخطوات الـ 21 (`STEP_01` إلى `STEP_21`) إلى أسماء عمليات حقيقية بلغة واضحة خالية من الأكواد (مثل `STEP_07` ➔ "تخصيص وتوزيع الحاويات والبضائع" / "Container Allocation").
- **Phase names:**
  - تحويل أكواد ومسميات المراحل من 1 إلى 10 إلى مسميات رسمية كاملة خالية من الاختصارات اللاتينية في الواجهة العربية.
- **Linked Smart Tasks:**
  - تنظيف عناوين المهام من بادئات الأكواد `[IMP-XXXX-XXXX]` واستبدال وسوم `(STEP_XX)` باسم العملية الحقيقي المحسوب.
- **Linked Outputs (TSV, CSV/Excel, Vector PDF, Dossier):**
  - إضافة عمود رئيسي "اسم الشحنة" (`shipmentName`) إلى جانب كود الملف في كافة تقارير الـ TSV و CSV و PDF والـ Dossier.
  - إظهار الخطوات الحالية والمرحلة والإجراء القادم بالأسماء التشغيلية الحقيقية بدلاً من الأكواد الرمزية.
- **Preserved Regulatory Identifiers:**
  - الحفاظ التام على أرقام التسجيل الجمركي والرقابي الرسمية: رقم الـ ACID (19 رقماً)، وإقرار 46 ك.م، ونموذج 4، وبنود التعريفة HS Code.

### 🧪 Validation / Testing

- `flutter test test/display_name_resolver_test.dart` — **10/10 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_localization_test.dart` — **2/2 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_export_service_test.dart` — **5/5 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_test.dart` — **6/6 tests passed (100% green)** ✅
- `flutter test test/perf/screen_0_operational_dashboard_perf_test.dart` — **1/1 benchmark passed (100% green)** ✅
- **Combined Test Suite:** **24/24 tests passed (100% green)** ✅
- **Full Codebase Analyze (`flutter analyze`):** **0 issues found! (100% clean across all affected files and lib/, 22.3s)** ✅

### 🏁 Next Steps

- الاستعداد لتطبيق متطلبات Task D على بقية الشاشات اللوجستية حسب التوجيهات.

---

## 📝 [2026-09-11 16:30] - Completed Task: Fix Live Logistics Radar Infinite Loading & Complete Screen 0 Bilingual Alignment

### 📌 Overview
- **Task Code:** `BUG-RADAR-001` & `SCR-000-BILINGUAL-FIX`
- **Description:** معالجة المشاكل المذكورة في لقطات الشاشة المرفوعة من المستخدم ("لا يعمل"):
  1. **إصلاح تجمد وتحميل رادار العمليات اللوجستية الحي (`Live Logistics Radar`):**
     - كان `livePollingProvider` يبتلع أخطاء الشبكة والاستثناءات بصمت (`catch (_)`) دون استدعاء `controller.addError(e, st)`. ولأن `StreamProvider` يبدأ في حالة `loading`، فإن عدم إرسال أي قيمة أو خطأ كان يؤدي لبقاء الواجهة في شاشة بيضاء بسبينر دائري أزرق لا نهائي.
     - تم تحويل الـ `StreamController` إلى `broadcast`، وإضافة معالجة صريحة للخطأ تُبلغ Riverpod فوراً بحالة الـ `error` مع زر إعادة المحاولة (`Retry`) بدلاً من التجمد الدائم.
     - جعل تفكيك حقل `missingDocuments` في `LiveLogisticsTrackingItemModel` آمناً ضد أي أنواع غير متوقعة، مع إضافة `LiveLogisticsSummaryModel.empty()`.
  2. **تعريب ودعم كامل للغتين وإصلاح اتجاهات النصوص (BiDi Safe) في `ShipmentMilestoneTracker`:**
     - كان المكون يحتوي نصوصاً عربية ثابتة 100%، مما أدى عند تحويل لغة التطبيق للإنجليزية إلى خلط لغوي وظهور نصوص عربية معكوسة بعلامات ترقيم مقلوبة (`: مخطط تتبع... PET Stock` و `%نسبة الإنجاز: 50`).
     - تم تحويل كافة نصوص ومسميات المراحل الـ 10 إلى مصفوفات ثنائية اللغة (`name_ar` و `name_en`)، مع تعريب عناوين الشحنة واسم المستورد والمورد ونسبة الإنجاز ديناميكياً بحسب لغة الواجهة الحالية بدون أي انعكاس أو تشوه في الترقيم.
  3. **إزالة أكواد العمليات الداخلية من مسار الخطوات الثلاثي (3-Way Pathway):**
     - إزالة الأكواد الداخلية `(STEP_01)` و `(STEP_03)` و `(خطوة 01)` و `(خطوة 03)` من ملفات الترجمة الإنجليزية والعربية واستبدالها بمسميات العمليات الإجرائية الواضحة طبقاً لـ Task D.
     - معالجة تكرار اسم الخطوة الحالية والتالية (`Current: Container Allocation -> Next Step: Container Allocation`)؛ حيث أضيفت دالة `_formatActionName` و `DisplayNameResolver.resolveActionTitle` لفك تشفير الإجراء التشغيلي الفعلي التالي بدقة (مثل `VGM Verification & Shipping Drafts Review`).
  4. **معالجة بطاقة الخطوة التالية التشغيلية (`_buildNextStepCard`):**
     - إضافة فرع مخصص للخطوة `STEP_07` (تخصيص الحاويات وتدقيق أوزان VGM ومسودات الشحن) والمرحلة الثالثة، لضمان عدم السقوط في القيم الافتراضية العامة.
  5. **تنقية وتوطين مهام الشحنة في قائمة الـ TO-DO:**
     - تنقية عناوين المهام من الشرطات الزائدة `—` في البداية، وإزالة تكرار الأقواس.
     - تقديم صياغة إنجليزية نقية للمهام التشغيلية المألوفة عند استخدام اللغة الإنجليزية، وصياغة عربية واضحة خالية من الأكواد عند استخدام اللغة العربية.

### 📁 Files Changed
- `frontend/lib/features/lifecycle_board/providers/live_polling_provider.dart` — Modified (Handled initial fetch errors via `controller.addError(e, st)`, broadcast stream controller, prevented infinite loading hangs)
- `frontend/lib/features/lifecycle_board/models/lifecycle_board_model.dart` — Modified (Safe missingDocuments list casting, added `LiveLogisticsSummaryModel.empty()`)
- `frontend/lib/features/import_files/widgets/shipment_milestone_tracker.dart` — Modified (Full bilingual Arabic/English support, 10 localized phases, zero BiDi text reversal)
- `frontend/lib/core/services/display_name_resolver.dart` — Modified (Added `resolveActionTitle`, enhanced `cleanTaskTitle` to strip leading dashes and provide full clean English task names)
- `frontend/lib/features/operational_dashboard/screens/operational_dashboard_screen.dart` — Modified (Integrated `_formatActionName`, handled STEP_07/Phase 3 in `_buildNextStepCard`, eliminated repeated step names in 3-way pathway)
- `frontend/lib/core/localization/app_localizations_en.dart` — Modified (Cleaned internal codes from `pathwayPrevFreightStudies` and `pathwayNextImportReqs`)
- `frontend/lib/core/localization/app_localizations_ar.dart` — Modified (Cleaned internal codes from `pathwayPrevFreightStudies` and `pathwayNextImportReqs`)
- `frontend/test/display_name_resolver_test.dart` — Modified (Added tests for leading dash cleanup, English task titles, and action title resolution, 12/12 passed)
- `screen-fixes-review-log.md` — Modified (Updated Screen 0 & Lifecycle Board logs)
- `history/2026-09-11.md` — Modified (Appended task completion entry)

### 📊 Technical Changes
- منع الحالات الميتة للـ StreamProvider على الويب وسطح المكتب.
- توحيد قواميس مسميات المراحل والمهام في `DisplayNameResolver` لدعم العربية والإنجليزية في كل من الشاشة والتقارير.
- معالجة خوارزمية Unicode BiDi لمنع ارتباك علامات الترقيم عند اقتران نصوص عربية بأسماء شركات إنجليزية.

### 🧪 Validation / Testing
- `flutter test test/display_name_resolver_test.dart` — **12/12 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_localization_test.dart` — **2/2 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_export_service_test.dart` — **5/5 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_test.dart` — **6/6 tests passed (100% green)** ✅
- `flutter test test/perf/screen_0_operational_dashboard_perf_test.dart` — **1/1 benchmark passed (100% green)** ✅
- `flutter test test/lifecycle_board_enhanced_test.dart test/lifecycle_board_localization_test.dart test/lifecycle_board_test.dart` — **6/6 tests passed (100% green)** ✅
- `flutter test test/shipment_lifecycle_navigator_widget_test.dart test/shipment_stage_lifecycle_test.dart test/perf/screen_48_lifecycle_board_perf_test.dart` — **13/13 tests passed (100% green)** ✅
- **All Screen 0 & Screen 48 Tests Combined:** **45/45 tests passed (100% green)** ✅
- **Static Analysis (`flutter analyze` on modified files):** **0 issues found! (100% clean)** ✅

### 🏁 Next Steps
- استكمال أي متطلبات تفاعلية إضافية على شاشات النظام وفق بروتوكول التوثيق.

---

## 📝 [2026-09-11 16:35] - Completed Task: Task E — Actionable Summary & Alert Cards (Screen 0)

### 📌 Overview
- **Task Code:** Task E (Screen 0: Operational Workspace Dashboard)
- **Description:** تحويل كافة بطاقات المؤشرات والتنبيهات (9 بطاقات) في واجهة مساحة العمليات إلى بطاقات تفاعلية قابلة للنقر تفتح نافذة تفاصيل منبثقة موحدة (`DashboardCardDrillDownDialog`)، مع ضمان تطابق رياضي 100% بين الرقم المعروض على البطاقة وقائمة السجلات، والإجابة الدقيقة عن الثلاثية الإلزامية: ماذا / من المسؤول / الموعد النهائي (What / Who / By When)، وتوثيق فجوات البيانات صراحة في الواجهة، وتفعيل النسخ الشامل والفردي، وحالات الفراغ الصفرية.

### 📁 Files Changed
- `frontend/lib/features/operational_dashboard/widgets/dashboard_card_drilldown_dialog.dart` — Created (`DashboardCardType`, `DrillDownItem`, `DashboardDrillDownHelper`, `DashboardCardDrillDownDialog`)
- `frontend/lib/features/operational_dashboard/screens/operational_dashboard_screen.dart` — Modified (Upgraded `_buildKpiCardsBar` to bind card count strictly to `records.length`, added interactive `InkWell` and `Tooltip` and `open_in_new` affordance icon to `_buildKpiCard`, wrapped in `Expanded` to prevent overflow)
- `frontend/lib/core/localization/app_localizations.dart` — Modified (Added 21+ Task E getters)
- `frontend/lib/core/localization/app_localizations_ar.dart` — Modified (Pure Arabic Task E translations with 0 Latin characters)
- `frontend/lib/core/localization/app_localizations_en.dart` — Modified (Pure English Task E translations)
- `frontend/test/dashboard_card_drilldown_test.dart` — Created (Comprehensive test suite: 100% count equality, What/Who/When validation, DisplayNameResolver Task D, Data gap disclosure, Empty states, Search filter, 7/7 passed)
- `screen-fixes-review-log.md` — Modified (Detailed Task E log with card-by-card audit)
- `history/2026-09-11.md` — Modified (Appended task completion log)

### 📊 Technical Changes
- **حل المشكلة الأساسية:** كل بطاقة كانت تعرض فقط رقماً مجرداً مع سطر وصفي بدون أي وسيلة لمعرفة أي شحنات/مهام تخص، وما المطلوب تحديداً، ومن المسؤول، ومتى الموعد النهائي.
- **التطابق الرياضي الحتمي 100%:** استخراج الرقم المعروض على واجهة كل بطاقة مباشرة من `records.length` المولدة من نفس الدالة المركزية `DashboardDrillDownHelper.getRecords(type, ...)` التي تغذي نافذة التفاصيل، مما يجعل أي اختلاف بين العدد المعروض وعدد السجلات مستحيلاً رياضياً.
- **تغطية البطاقات الـ 9 بالكامل:**
  1. `Today's Tasks`: مهام اليوم المستحقة وغير المكتملة.
  2. `Pending Tasks`: المهام المعلقة وقيد التنفيذ مع حساب أيام التعليق.
  3. `Upcoming Shipments`: الشحنات النشطة في خط الأنابيب مع المرحلة والخطوة التالية.
  4. `Arriving This Week`: الشحنات ذات موعد الوصول القريب أو في مرحلة الإبحار والتخليص.
  5. `ETA Changes`: الشحنات التي تم تسجيل واعتماد موعد وصول ETA لها.
  6. `Waiting For Payment`: الشحنات بانتظار الاعتماد المالي وصرف الدفعة (المرحلة 2).
  7. `Waiting For Form 4`: الشحنات بانتظار استخراج وتأكيد نموذج 4 البنكي.
  8. `Pending Requirements`: الشحنات التي ينقصها رقم ACID أو إقرار 46 ك.م.
  9. `High Priority Alerts`: الشحنات ذات الأولوية الحرجة والعالية.
- **شفافية فجوات البيانات (Data Gaps Disclosure):** إظهار تنبيهات واضحة صريحة بأي بيانات غير مسجلة في قاعدة البيانات (مثل عدم تسجيل وقت الاستحقاق بالساعة والدقيقة، أو عدم تتبع سجل تعديلات ETA التاريخي)، دون إخفائها أو اختلاق بيانات وهمية.
- **التكامل مع Task D و Task B و Task A:** استخدام `DisplayNameResolver` للأسماء التجارية للشحنات، توفير أزرار نسخ فردية ونسخ جماعي لكافة السجلات، والترجمة الثنائية الأحادية بدون أي خلط.

### 🧪 Validation / Testing
- `flutter test test/dashboard_card_drilldown_test.dart` — **7/7 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_test.dart` — **6/6 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_localization_test.dart` — **2/2 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_export_service_test.dart` — **5/5 tests passed (100% green)** ✅
- `flutter test test/display_name_resolver_test.dart` — **12/12 tests passed (100% green)** ✅
- `flutter test test/perf/screen_0_operational_dashboard_perf_test.dart` — **1/1 benchmark passed (100% green)** ✅
- **All Screen 0 Tests Combined:** **33/33 tests passed (100% green)** ✅
- **Full Project Static Analysis (`flutter analyze lib/`):** **0 issues found! (100% clean across entire project)** ✅

### 🏁 Next Steps
- استكمال أي تحسينات موجهة على واجهات التطبيق وفق البروتوكول اليومي.

---

## 📝 [2026-09-11 17:00] - Completed Task: Task D — Full Human-Readable Names Resolution & Zero Duplication on Screen 0 Drilldown

### 📌 Overview
- **Task Code:** `TASK-D-SCREEN-0-DRILLDOWN`
- **Description:** تطبيق استبدال الأكواد الداخلية بأسماء بشرية واضحة ومترجمة (`Task D`) على لوحة العمليات التشغيلية (`OperationalDashboardScreen`) ونافذة تفاصيل البطاقات المنبثقة (`DashboardCardDrillDownDialog`)، والقضاء على التكرار المزدوج لأكواد الشحنة (`🚚 PET Stock (IMP-2026-0004) (IMP-2026-0004)`):
  1. **تنقية عناوين المهام التشغيلية في بطاقات التفاصيل:**
     - استبدال العناوين الخام مثل `[IMP-2026-0004] (STEP_03) مراجعة اشتراطات الاستيراد والموافقات الرقابية` بالعناوين الواضحة المعربة الخالية من كود الشحنة وكود الخطوة `(STEP_XX)`: `مراجعة اشتراطات الاستيراد والموافقات الرقابية` بالعربية، أو `Review Import Requirements & Regulatory Approvals` بالإنجليزية عبر `DisplayNameResolver.cleanTaskTitle`.
  2. **القضاء التام على التكرار المزدوج لأكواد الشحنة في الشارات والنسخ:**
     - إنشاء دالة التنسيق الموحدة `DashboardCardDrillDownDialog.formatBadgeShipment(item)` التي تتحقق رياضياً من عدم تكرار الكود، وتنتج `PET Stock (IMP-2026-0004)` أو `IMP-2026-0003` (عند عدم وجود اسم تجاري)، وتمنع منعاً باتاً ظهور `(IMP-2026-0004) (IMP-2026-0004)`.
     - تطبيقها على شارة بطاقة السجل داخل `_buildRecordCard`، وفي نصوص النسخ الفردي والجماعي للحافظة (`_copySingleRecord` و `_copyAllRecords`).
  3. **تعريب وتوطين مسميات أنواع وحالات المهام التشغيلية:**
     - إضافة `DisplayNameResolver.resolveTaskType` لترجمة نوع المهمة (`System Generated` ➔ `توليد آلي من النظام` بالعربية، و `System Generated` بالإنجليزية).
     - إضافة `DisplayNameResolver.resolveTaskStatus` لترجمة حالة المهمة (`Pending` ➔ `معلقة`، `In Progress` ➔ `قيد التنفيذ` بالعربية، و `Pending` بالإنجليزية).
  4. **تنقية الإجراء والمطلوب (What / Description):**
     - إضافة `DisplayNameResolver.resolveTaskDescription` لفك رموز الشحنات المعقدة في نصوص الملاحظات والمهام واستبدال الأكواد بالأسماء التجارية للشحنة وتقديم ترجمة إنجليزية احترافية في الوضع الإنجليزي.
  5. **منع تسرب الحروف اللاتينية في الواجهة العربية لمتتبع المراحل:**
     - استبدال بادئة المرحلة الثابتة `'P${phase['num']}'` في `ShipmentMilestoneTracker` بـ `isArabic ? 'م${phase['num']}' : 'P${phase['num']}'` (مثل `م1`, `م2` بدلاً من `P1`, `P2`).
  6. **التحديث الحي للغة وتنقية رسائل التنبيهات:**
     - ربط `_buildLinkedTasksSection` و `_buildDailyCheckinsCard` بـ `ref.watch(localeProvider)` لتحديث الواجهة فوراً عند تغيير اللغة.
     - تنقية عنوان المهمة في إشعار النجاح (`SnackBar`) عند إكمال المهمة.
     - تعريب تصنيفات المتابعات اليومية (`resolveUpdateCategory`).

### 📁 Files Changed
- `frontend/lib/core/services/display_name_resolver.dart` — Modified (Added `resolveShipmentNameByCode`, `resolveTaskType`, `resolveTaskStatus`, `resolveTaskDescription`, `resolveUpdateCategory`, refined `cleanTaskTitle` and action resolvers)
- `frontend/lib/features/operational_dashboard/widgets/dashboard_card_drilldown_dialog.dart` — Modified (Implemented `formatBadgeShipment`, integrated `cleanTaskTitle`, `resolveTaskType`, `resolveTaskStatus`, `resolveShipmentNameByCode`, updated `_buildRecordCard`, `_copyAllRecords`, `_copySingleRecord`)
- `frontend/lib/features/operational_dashboard/screens/operational_dashboard_screen.dart` — Modified (Reactive `ref.watch(localeProvider)` in linked tasks and checkins cards, clean task title in completion snackbar, localized update category)
- `frontend/lib/features/import_files/widgets/shipment_milestone_tracker.dart` — Modified (Localized phase chip prefix `'م'` in Arabic mode, `'P'` in English mode)
- `frontend/test/display_name_resolver_test.dart` — Modified (Added 6 comprehensive unit tests covering all new Task D resolver functions and user screenshot edge case, 18/18 passed)
- `frontend/test/dashboard_card_drilldown_test.dart` — Modified (Added tests for Task D human-readable titles, duplicate code prevention in badges, and localized statuses/types, 9/9 passed)
- `screen-fixes-review-log.md` — Modified (Updated Screen 0 Task D review and audit log)
- `history/2026-09-11.md` — Modified (Appended daily task record)

### 📊 Technical Changes
- الالتزام الصارم بمبدأ عدم التكرار (DRY) عبر تفويض كافة عمليات الاستبدال اللغوية إلى `DisplayNameResolver`.
- الحفاظ على كود الشحنة كمعرف ثانوي بين قوسين فقط عند وجود اسم تجاري واضح، وعرضه منفرداً بدون أقواس مكررة عند غياب الاسم التجاري.
- صيانة المعرفات الجمركية الحساسة (ACID, Form 4, 46 KM, HS Code) دون أي تحريف أو مساس.

### 🧪 Validation / Testing
- `flutter test test/display_name_resolver_test.dart` — **18/18 tests passed (100% green)** ✅
- `flutter test test/dashboard_card_drilldown_test.dart` — **9/9 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_test.dart` — **6/6 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_localization_test.dart` — **6/6 tests passed (100% green)** ✅
- `flutter test test/operational_dashboard_export_service_test.dart` — **1/1 tests passed (100% green)** ✅
- **Combined Screen 0 & Resolver Test Suite:** **40/40 tests passed (100% green)** ✅
- **Static Analysis (`flutter analyze` on all modified files):** **0 issues found! (100% clean)** ✅

### 🏁 Next Steps
- تقديم النتائج والتحسينات المكتملة للمستخدم وفق المعايير والبروتوكول المعمول به.
